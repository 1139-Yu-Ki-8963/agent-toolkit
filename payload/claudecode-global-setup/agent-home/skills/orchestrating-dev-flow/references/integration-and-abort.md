# 中断手順（詳細）

ユーザーが「やめたい」「中断して」と言った場合、以下のクリーンアップを実行してからフローを終了する。

| 進行状況 | クリーンアップ対象 |
|---|---|
| Phase 1（調査 + ルート判定）まで | なし。即終了 |
| Phase 2（worktree 作成）以降 | worktree 削除 + ブランチ削除 + ポート kill |
| Phase 6（TDD サイクル）以降 | 上記 + 未 push のコミットは worktree 内に残したまま削除（復元不可を警告） |
| Phase 9（PR 作成）以降 | 上記 + PR をクローズ |

中断前に AskUserQuestion で「作業中の変更を破棄してよいですか？」と確認する。
