---
doc_id: screen-<画面ID>
type: screen-detail-design
status: draft
target_screen: <画面名>
scenarios:
  - name: "<この表示状態の名前 例: 2025年レポート>"
    path: "<パス>"
    query: { <キー>: <値> }
    path_params: { <名>: <値> }
    ready:
      selector: "<描画完了を示す要素セレクタ>"
      not: "<残っていたら未描画とみなすセレクタ。既定は syncing の loading_selectors>"
    assert:
      tables: ["<内容を突合するテーブルの要素セレクタ>"]
      texts: ["<特定データ値の要素セレクタ>"]
    mask: ["<L3 でマスクする非決定領域のセレクタ>"]
# query/path_params/mask は該当なしなら省略可。ready/assert は描画到達・内容一致の判定に使うため記入推奨
# 後方互換: 旧 route: <パス> も受理（scenarios:[{path:<パス>}] 相当に自動正規化）
design_md: ./DESIGN.md
unit_test_sheet: ./単体テスト観点表.md
integration_test_sheet: ./結合テスト観点表.md
common_spec: ../_共通/共通設計書.md
common_spec_version: <vX.Y>
messages: ../_共通/メッセージ定義書.md
updated: <YYYY-MM-DD>
---

# <画面名> 画面詳細設計書

## 関連資料（正の宣言）

| 正の種類 | ファイル | 本書との役割分担 |
|---|---|---|
| スタイル・数値 | `./DESIGN.md` | 数値・色・間隔の正。本書はキー名参照のみ |
| 共通仕様 | `../_共通/共通設計書.md` | システム共通仕様の正。本書は準拠状況と画面固有制約のみ |
| 文言 | `../_共通/メッセージ定義書.md` | 文言の正。本書はメッセージ ID で参照 |
| テスト観点 | `./単体テスト観点表.md` / `./結合テスト観点表.md` | テスト観点・受け入れ条件の正。本書に含めない |

## 章マップ

| 役割キー | § | 章名 | 区分 | 正 | 実装後の扱い | 保守方式 |
|---|---|---|---|---|---|---|
| 画面概要 | §1 | 画面概要 | 必須 | 本書 | 維持 | 手 |
| 機能一覧 | §2 | 機能一覧 | 必須 | 本書 | 維持 | 手 |
| 画面構造 | §3 | 画面構造 | 必須 | 本書（§3.6 の数値は DESIGN.md） | 維持 | 生成 |
| 業務ルール | §4 | 業務ルール | 条件付き | 本書 | 維持 | 手 |
| 状態管理 | §5 | 状態管理 | 必須 | 本書 | 維持 | 生成 |
| データフロー | §6 | データフロー | 必須 | 本書 | 維持 | 生成 |
| API通信 | §7 | API 通信仕様 | 条件付き | API 定義書（本書は画面固有の使い方のみ） | 維持 | 生成 |
| イベント処理 | §8 | イベント処理 | 必須 | 本書 | 維持 | 生成 |
| 領域別仕様 | §9 | 領域別仕様 | 必須 | 本書 | 維持 | 混在 |
| 定数設定値 | §10 | 定数・設定値 | 条件付き | 設計時は本書 → 実装後は実装コード | 実装ファイルのパス参照に置換 | 混在 |
| エラーハンドリング | §11 | エラーハンドリング | 必須 | 本書 | 維持 | 混在 |
| 画面遷移 | §12 | 画面遷移仕様 | 必須 | 本書 | 維持 | 生成 |
| 非機能要件 | §13 | 非機能要件 | 必須 | 共通設計書（画面固有制約のみ本書） | 維持 | 手 |
| 共通準拠 | §14 | 共通仕様への準拠 | 必須 | 共通設計書（本書は逸脱のみ） | 維持 | 手 |
| 実装契約 | §15 | 実装契約 | 必須 | 設計時は本書 → 実装後はコード（§15.6 は DESIGN.md） | 構造の骨子のみ維持 | 生成 |
| 要確認事項 | §16 | 要確認事項一覧 | 必須 | 本書 | approved の条件は 0 件 | 手 |

保守方式: 手 = 人が保守する正 / 生成 = 納品時にコードからリバース生成 / 混在 = 生成した骨格に業務的意味を手で重ねる

---

## §1 画面概要

### 1.1 基本情報

<!-- 画面名・目的・遷移元/遷移先を 1 行ずつ記入する -->

| 項目 | 値 |
|---|---|
| 画面名 | `<画面名>` |
| 目的 | `<この画面で実現すること 1 文>` |
| ルート | `<パス>` |
| 遷移元 | `<画面名>` |
| 遷移先 | `<画面名>`（`<操作>` 時） |

### 1.2 業務上の位置づけ

<!-- この画面が業務フロー上のどの段階に位置するかを 1〜2 文で記述する -->

`<位置づけ>`

---

## §2 機能一覧

### 2.1 機能一覧表

<!-- この画面で実装する機能を列挙する。優先度は MoSCoW 等で表現する -->

| キー | 機能名 | 概要 | 優先度 |
|---|---|---|---|
| <機能名-要約> | `<機能名>` | `<概要>` | 必須 |

### 2.2 機能間の関係

<!-- 依存・排他などの関係がある場合のみ記述。なければ「該当なし（独立機能のみ）」 -->

`<制約または「該当なし（独立機能のみのため）」>`

---

## §3 画面構造

### 3.1 コンポーネント配置

<!-- 親子関係をインデントで表現する -->

```
<画面ルートコンポーネント>
├─ <ヘッダ領域コンポーネント>
├─ <メイン領域コンポーネント>
│   ├─ <サブコンポーネント A>
│   └─ <サブコンポーネント B>
└─ <フッタ領域コンポーネント>
```

### 3.2 DOM 配置順序

<!-- スクリーンリーダー読み上げ順序を考慮した DOM 順を列挙する -->

1. `<要素名>` — `<目的>`
2. `<要素名>` — `<目的>`

### 3.3 レイアウト制約

| 制約 | 値 |
|---|---|
| レイアウト方式 | `<Grid / Flex / etc>` |
| 最小幅 | `<値>` |
| 最大幅 | `<値>` |

### 3.4 メニューバー設定（条件付き：メニューバーを持つ画面のみ）

`<設定内容または「該当なし（メニューバーが存在しない画面のため）」>`

### 3.5 設定オブジェクト構造（条件付き：設定オブジェクトを持つ場合のみ）

`<構造定義または「該当なし（設定オブジェクトを持たない画面のため）」>`

### 3.6 スタイル適用パターン

<!-- 数値の正は DESIGN.md。パターン名とキー名のみ参照し数値は持たない -->

| 領域 | 適用パターン | 参照先 |
|---|---|---|
| `<領域名>` | `<パターン名>` | `DESIGN.md > colors.<キー>` |

### 3.7 ボタン一覧

| ボタン名 | 種別 | 位置 | 活性条件 |
|---|---|---|---|
| `<ボタン名>` | 主要 / 補助 / 危険 | `<位置>` | `<条件または「常時」>` |

---

## §4 業務ルール（条件付き：業務モード・権限制御がある画面のみ）

<!-- 業務モード・権限制御がない画面は「該当なし（理由）」と記入して見出しを残す -->

### 4.1 業務モードと切替条件

| モード | 切替条件 | UI の変化 |
|---|---|---|
| `<モード名>` | `<条件>` | `<変化内容>` |

### 4.2 業務判断ポイント

`<ユーザーや業務ルールが画面の挙動を分岐させるポイント>`

### 4.3 業務フロー内の前後画面との関係

`<前の画面から何を受け取り、次の画面に何を渡すか>`

### 4.4 権限別の UI 制御

<!-- ロール × ボタン活性 / 項目表示の対応表 -->

| ロール | `<ボタン名>` | `<項目名>` |
|---|---|---|
| `<ロール名>` | 表示 / 非表示 | 表示 / 非表示 |

---

## §5 状態管理

### 5.1 URL パラメータ

| パラメータ名 | 型 | 必須 | 用途 |
|---|---|---|---|
| `<パラメータ>` | `<型>` | ○ / △ | `<用途>` |

### 5.2 セッション・ストレージ参照

| キー名 | 種別 | 用途 |
|---|---|---|
| `<キー>` | session / local | `<用途>` |

### 5.3 メイン画面の状態変数

| 変数名 | 型 | 初期値 | 役割 |
|---|---|---|---|
| `<変数名>` | `<型>` | `<初期値>` | `<役割>` |

### 5.4 サブコンポーネントの状態変数

| コンポーネント | 変数名 | 型 | 役割 |
|---|---|---|---|
| `<コンポーネント名>` | `<変数名>` | `<型>` | `<役割>` |

### 5.5 派生値（計算値）

| 派生値名 | 算出ロジック概要 | 依存する状態変数 |
|---|---|---|
| `<派生値名>` | `<算出ロジック概要>` | `<変数名>` |

### 5.6 画面専用ストア（永続化）

`<ストア定義または「該当なし（画面固有の永続化なし）」>`

### 5.7 共有ストア参照

| ストア / コンテキスト | 参照キー | 用途 |
|---|---|---|
| `<ストア名>` | `<キー>` | `<用途>` |

### 5.8 検索条件更新関数

<!-- 検索フォームがある画面のみ記述 -->

`<関数名・引数・更新内容の概要または「該当なし」>`

### 5.9 初期値・デフォルト値一覧

<!-- 業務定数（ドロップダウン選択肢等）は §10 が持つ。本節は変数の初期値のみ記述する -->

| 変数名 | 初期値 | 根拠 |
|---|---|---|
| `<変数名>` | `<値>` | `<根拠>` |

---

## §6 データフロー

### 6.1 初期表示シーケンス

1. URL パラメータ取得
2. ストア初期化
3. API 呼び出し（§7 参照）
4. データ変換（§6.5 参照）
5. 画面状態を ready に更新

### 6.2 依存連鎖

`<A の変化が B を再取得するなどの連鎖、または「該当なし（独立したデータ取得のみ）」>`

### 6.3 ユーザー操作確定時の処理フロー

1. バリデーション（§11 参照）
2. 確認ダイアログ表示（該当時）
3. API 呼び出し
4. 成功時: 遷移または状態更新
5. 失敗時: エラー表示（§11 参照）

### 6.4 データ更新トリガーの分類

| トリガー | 種別 | 処理概要 |
|---|---|---|
| `<操作名>` | ユーザー操作 / 自動 | `<処理概要>` |

### 6.5 データ変換（API レスポンス → 画面表示用データ）

| 変換名 | 入力型 | 出力型 | 変換概要 |
|---|---|---|---|
| `<変換名>` | `<API 型>` | `<画面用型>` | `<変換内容>` |

### 6.6 画面状態の遷移

| 状態 | 遷移条件 | UI の表示 |
|---|---|---|
| loading | 初期表示・再取得中 | ローディング表示 |
| empty | データ 0 件 | 空状態メッセージ |
| error | API エラー | エラーメッセージ（§11 参照） |
| ready | データ取得成功 | メインコンテンツ |

---

## §7 API 通信仕様（条件付き：API を呼ぶ画面のみ）

<!-- 契約の正は API 定義書。本書は画面からの使い方（呼び出し条件・エラー処理）のみを記述する -->

### 7.1 API 一覧

| No | API 名 | メソッド | エンドポイント | 呼び出し契機 |
|---|---|---|---|---|
| 7.2 | `<API 名>` | `<メソッド>` | `<エンドポイント>` | `<契機>` |

### 7.2 `<API 名>`

| 項目 | 内容 |
|---|---|
| 概要 | `<概要>` |
| エンドポイント | `<メソッド> <パス>` |
| 呼び出し契機 | `<操作または初期表示>` |

**リクエスト**

```typescript
{ <フィールド名>: <型>; }
```

**レスポンス**

```typescript
{ <フィールド名>: <型>; }
```

**エラーと画面側の扱い**

| ステータス | 画面の挙動 | メッセージ ID |
|---|---|---|
| 400 | バリデーションエラー表示 | `<MSG-ID>` |
| 401 | ログイン画面へ遷移 | — |
| 500 | システムエラーメッセージ表示 | `<MSG-ID>` |

---

## §8 イベント処理

### 8.1 メイン画面イベント

| イベント | 発火要素 | 処理概要 |
|---|---|---|
| `<イベント名>` | `<要素>` | `<処理概要>` |

### 8.2 サブコンポーネントイベント

| コンポーネント | イベント | 処理概要 |
|---|---|---|
| `<コンポーネント名>` | `<イベント名>` | `<処理概要>` |

### 8.3 複数ステップ処理の詳細

`<確認→実行など複数ステップを踏む処理の記述、または「該当なし」>`

### 8.4 キーボード操作

| キー | 発火条件 | 挙動 |
|---|---|---|
| Tab | フォーカス可能要素 | `<Tab 順の方針>` |
| Enter | `<要素>` | `<挙動>` |

---

## §9 領域別仕様

<!-- 領域ごとに ### 9.n <領域名> を追加する。雛形は 9.1 の 1 領域分 -->

### 9.1 `<領域名>`

#### 9.1.1 概要

`<この領域の目的 1 文>`

#### 9.1.2 配置

`<親コンポーネント内のどこに配置されるか>`

#### 9.1.3 項目定義

| 項目名 | 型 | 必須 | 書式 | 状態別編集可否 |
|---|---|---|---|---|
| `<項目名>` | `<型>` | ○ / △ / × | `<書式>` | ready: 可 / loading: 不可 |

#### 9.1.4 イベント・挙動

| イベント | 挙動 |
|---|---|
| `<イベント名>` | `<挙動>` |

#### 9.1.5 領域固有の制約

`<この領域特有の制約または「該当なし」>`

---

## §10 定数・設定値（条件付き：画面固有の定数がある場合のみ）

<!-- 設計時は本書が契約。実装後は実装ファイルのパス参照に置換する -->

### 10.1 文字列定数

| 定数名 | 値 | 用途 |
|---|---|---|
| `<定数名>` | `"<値>"` | `<用途>` |

### 10.2 選択肢定義

| 定数名 | 選択肢 | 用途 |
|---|---|---|
| `<定数名>` | `<値 1>` / `<値 2>` | `<用途>` |

### 10.3 列定義・表示設定

`<テーブル・グリッドがある場合の列定義または「該当なし」>`

### 10.4 コード変換テーブル

| コード | 表示ラベル |
|---|---|
| `<コード>` | `<ラベル>` |

### 10.5 区分定数

| 定数名 | 値一覧 |
|---|---|
| `<定数名>` | `<値 1>` / `<値 2>` |

---

## §11 エラーハンドリング

### 11.1 ユーザー入力チェック

| チェック対象 | 条件 | エラー種別 |
|---|---|---|
| `<項目名>` | `<チェック内容>` | 必須 / 形式 / 範囲 |

### 11.2 エラーメッセージ・警告

<!-- 文言の正はメッセージ定義書。本書はメッセージ ID で参照する -->

| タイミング | メッセージ ID | 表示位置 |
|---|---|---|
| `<タイミング>` | `<MSG-ID>` | インライン / トースト |

### 11.3 システムエラー

| エラー種別 | 画面の挙動 | メッセージ ID |
|---|---|---|
| ネットワーク断 | エラーバナー表示・リトライ促進 | `<MSG-ID>` |
| 認証切れ | ログイン画面へ遷移 | — |
| 予期しないエラー | エラーメッセージ表示 | `<MSG-ID>` |

---

## §12 画面遷移仕様

<!-- §1.1 との役割分担: §1.1 は概要（遷移元・先の一覧）、本章は詳細（方式・パラメータ） -->

### 12.1 遷移先一覧

| 遷移先画面 | 遷移方式 | 条件 | 渡すパラメータ |
|---|---|---|---|
| `<画面名>` | push / replace / back | `<条件>` | `<パラメータ>` |

### 12.2 遷移方式の方針

`<push / replace / back の使い分け方針>`

---

## §13 非機能要件

<!-- 共通内容は共通設計書が正。本章は画面固有の制約のみ記述する -->

### 13.1 表示件数・データ量制限

| 対象 | 上限 | 超過時の挙動 |
|---|---|---|
| `<リスト名>` | `<N>` 件 | `<挙動>` |

### 13.2 パフォーマンス目安

| 操作 | 目標応答時間 |
|---|---|
| 初期表示 | `<N>` 秒以内 |
| `<操作名>` | `<N>` 秒以内 |

### 13.3 ローディング・処理中表示

<!-- 挙動詳細は §6・§11 参照。本節はローディング UI 仕様のみ -->

| 操作 | ローディング表示方式 |
|---|---|
| 初期表示 | スケルトン / スピナー（選択） |
| `<操作名>` | ボタン無効化 / オーバーレイ（選択） |

### 13.4 タイムアウト・リトライ方針

<!-- 挙動は §11、方針・設定値は本節 -->

| 項目 | 値 | 根拠 |
|---|---|---|
| タイムアウト時間 | `<N>` 秒 | `<根拠>` |
| リトライ回数 | `<N>` 回 | `<根拠>` |

### 13.5 ブラウザ・画面解像度の動作保証範囲

`<共通設計書との差分、または「共通設計書準拠（差分なし）」>`

---

## §14 共通仕様への準拠

<!-- 準拠バージョンは frontmatter の common_spec_version が正。画面横断レビューは本章同士の比較で行う -->

### 14.1 共通コンポーネント利用一覧

| コンポーネント名 | 利用箇所 | 独自実装の場合の理由 |
|---|---|---|
| `<コンポーネント名>` | `<利用箇所>` | — |

### 14.2 共通ソート・表示順ルールの適用

| 対象リスト | 適用ルール | 逸脱理由 |
|---|---|---|
| `<リスト名>` | `<ルール名>` | — |

### 14.3 用語・ラベルの統一

`<共通設計書の用語集との差分、または「共通設計書準拠（差分なし）」>`

### 14.4 日付・数値・コード値の表示フォーマット

| 値の種類 | フォーマット | 空値時 |
|---|---|---|
| 日付 | `<形式>` | `—` |
| 数値 | `<形式>` | `—` |
| コード値 | `<形式>` | `—` |

### 14.5 共通メッセージの利用

| 用途 | メッセージ ID |
|---|---|
| `<用途>` | `<MSG-ID>` |

---

## §15 実装契約

<!-- 本章は実装開始時点の契約。マージ後はコードが正で、本章は構造の骨子のみ維持する -->

### 15.1 ファイル分割と export 一覧

| ファイルパス | export 名 | 種別 |
|---|---|---|
| <ファイルパス> | <export 名> | コンポーネント / フック / ユーティリティ |

### 15.2 型定義

```typescript
// 実装後は実装ファイルが正
type <型名> = {
  <フィールド名>: <型>;
};
```

### 15.3 依存（import）一覧

| モジュール | import 内容 | 種別 |
|---|---|---|
| `<モジュール名>` | `<import 名>` | 内部 / 外部ライブラリ |

### 15.4 別画面コンポーネントとの型合わせ

`<他画面との型共有がある場合の記述、または「該当なし（画面間型共有なし）」>`

### 15.5 スコープ分類

| 対象 | スコープ | 理由 |
|---|---|---|
| `<状態変数名>` | コンポーネントローカル / コンテキスト / グローバル | `<理由>` |

### 15.6 スタイル定数一覧

<!-- 数値の正は DESIGN.md。定数名とキー名のみ参照し数値は持たない -->

| 定数名 | 用途 | 参照先 |
|---|---|---|
| `<定数名>` | `<用途>` | `DESIGN.md > <キー>` |

### 15.7 state 遷移シーケンス

<!-- §6.6 の状態遷移を実装観点で補足する -->

```
初期化 → loading → ready
                 ↘ error（リトライ可）
       → loading → empty（0 件時）
```

---

## §16 要確認事項一覧

<!-- status を approved にする条件は本章 0 件 -->

| キー | 起票日 | 内容 | 暫定扱いにしている § | 解消条件 |
|---|---|---|---|---|
| <確認事項-要約> | `<YYYY-MM-DD>` | `<確認事項>` | §`<番号>` | `<解消条件>` |
