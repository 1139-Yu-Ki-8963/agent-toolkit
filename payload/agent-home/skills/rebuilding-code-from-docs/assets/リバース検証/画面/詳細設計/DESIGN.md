---
doc_id: design-<画面名>
type: design
status: draft   # draft | traced（値が全て既存コードからの抽出値に置き換わった状態）
target_screen: <画面名>   # ./画面詳細設計書.md の target_screen と常に同値にする
source_design: ./画面詳細設計書.md
updated: <YYYY-MM-DD>
colors:
  primary: "<実測値>"        # 既存コードの実装値をそのまま転記する（あるべき値ではない）
  surface: "<実測値>"        # 画面背景・カード背景
  on-surface: "<実測値>"     # 本文テキスト
  error: "<実測値>"          # エラー表示
typography:
  heading: "<実測値>"  # 画面タイトル・セクション見出し
  body: "<実測値>"     # 本文・データ表示
  label: "<実測値>"    # 入力ラベル・テーブルヘッダ
components:
  # §3（./画面詳細設計書.md §3 画面構造）で定義した領域名と同名のキーで記述する
rounded: "<実測値>"
spacing: "<実測値>"
---

# <画面名> デザイン仕様（DESIGN.md・リバース版）

本書はレガシー画面の実装から抽出したスタイル・数値の記録である。ただし複数画面共通のデザイン値は `../../プロジェクト共通/DESIGN.md` が上位の正であり、本書は「画面固有の差分の実測記録」のみを持つ（`プロジェクト共通/DESIGN.md` と同じ値を重複して書かない）。往復検証の「正は既存挙動の忠実再現であり、あるべき仕様ではない」という原則に従い、デザイン改善案ではなく **既存コードの実測値** のみを記載する。画面状態の一覧の定義権は `./画面詳細設計書.md §5` にある。

## Overview

> `target_screen` は `./画面詳細設計書.md` の同フィールドと常に同値にすること。一方を変更したら他方も変更する。

<この画面の実装済みスタイルの特徴を 1〜2 文で書く。独自実装か共通コンポーネント準拠かの観点で記述する>

## Colors

<!-- 既存コードの CSS・スタイルファイル・computed style から実測した値をそのまま転記する -->
<!-- frontmatter の colors セクションも実測値に置き換える -->

| トークン名 | 用途 | 実測値の抽出元 |
|---|---|---|
| primary | 主要アクション・フォーカス表示 | `<ファイルパス・クラス名等>` |
| surface | 画面背景・カード背景 | `<ファイルパス・クラス名等>` |
| on-surface | 本文テキスト | `<ファイルパス・クラス名等>` |
| error | エラーメッセージ・バリデーション失敗 | `<ファイルパス・クラス名等>` |

## Typography

<!-- 既存コードのフォント指定を実測して転記する -->

| トークン名 | 用途 | 実測値の抽出元 |
|---|---|---|
| heading | 画面タイトル・セクション見出し | `<ファイルパス・クラス名等>` |
| body | 本文・データ表示 | `<ファイルパス・クラス名等>` |
| label | 入力ラベル・テーブルヘッダ | `<ファイルパス・クラス名等>` |

## Components

<!-- §3（./画面詳細設計書.md §3 画面構造）で定義した領域名と同名の項目で記述する -->

### <領域名>

<この領域の実装済みレイアウト・間隔・サイズを実測して書く>

## 画面状態の視覚仕様

<!-- 状態一覧の定義権は ./画面詳細設計書.md §5 にある。本節はその各状態の「実装済みの見え方」のみを担う -->

| 状態 | 実装済みの見せ方 | アニメーション・トランジション |
|---|---|---|
| loading | `<実装済みのスケルトン・スピナー等>` | `<実装済みの遷移方法>` |
| empty | `<実装済みの空状態表示>` | — |
| error | `<実装済みのエラー表示>` | — |
| ready | `<実装済みの通常表示>` | — |

## レスポンシブ仕様

<!-- 既存コードのブレークポイント定義を実測して転記する -->

| ブレークポイント | 実装済みのレイアウト変更点 |
|---|---|
| `<ブレークポイント名>` | `<実測した変更内容>` |

## アクセシビリティ

<!-- §11（./画面詳細設計書.md §11 エラーハンドリング）等との整合を実測ベースで確認する -->

| 観点 | 実装済みの状態 |
|---|---|
| フォーカス管理 | `<実測したフォーカス移動順序・トラップの有無>` |
| スクリーンリーダー対応 | `<実測した aria-label・role 等の有無>` |
| カラーコントラスト | `<実測値。基準未達なら要確認事項§16へ>` |
