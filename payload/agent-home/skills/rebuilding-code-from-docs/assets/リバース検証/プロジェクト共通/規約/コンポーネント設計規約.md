---
doc_id: component-design
type: component-design
status: draft
updated: <YYYY-MM-DD>
---

# コンポーネント設計規約（リバース版）

本書は既存コードから抽出したコンポーネントの設計パターンの記録である。

## コンポーネント分類

| 分類 | 特徴 | 例 |
|---|---|---|
| ページコンポーネント | `<実測: ルーティングと紐づく最上位>` | `<実例>` |
| コンテナコンポーネント | `<実測: データ取得・状態管理を担う>` | `<実例>` |
| プレゼンテーションコンポーネント | `<実測: 表示のみ>` | `<実例>` |

## Props 設計パターン

| パターン | 実測内容 |
|---|---|
| Props の渡し方 | `<個別 props / オブジェクト / spread 等>` |
| デフォルト値 | `<defaultProps / デフォルト引数 / ?? 等>` |
| children の使い方 | `<実測パターン>` |

## 状態管理パターン

| パターン | 実測内容 |
|---|---|
| ローカル状態 | `<useState / useReducer 等>` |
| グローバル状態 | `<Context / Redux / Zustand 等>` |
| サーバー状態 | `<SWR / React Query / fetch 等>` |

## エラーハンドリングパターン

| パターン | 実測内容 |
|---|---|
| Error Boundary | `<使用有無・配置場所>` |
| try-catch | `<使用パターン>` |
| エラー表示 | `<トースト / インライン / ダイアログ等>` |
