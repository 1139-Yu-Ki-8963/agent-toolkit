---
# API詳細設計書テンプレート（unit_kind=api・1エンドポイント1枚）
api_key: APIKEY
api_id: APIID
method: METHOD
path: PATH
feature_key: FEATUREKEY
source_ref: SOURCEREF
unit_kind: api
# chapter_map: 章の役割キーと §番号の対応（生成器と検査が章を特定するために使う機械向けの対応表。本文には表示しない）。
chapter_map:
  - role: API概要
    section: 1
  - role: APIリクエスト
    section: 2
  - role: APIレスポンス
    section: 3
  - role: API処理フロー
    section: 4
  - role: APIロジック
    section: 5
  - role: API疑似コード
    section: 6
  - role: APIデータアクセス
    section: 7
  - role: APIデータ定義
    section: 8
  - role: API業務ルール
    section: 9
  - role: APIエラー
    section: 10
  - role: API非機能要件
    section: 11
  - role: API実装契約
    section: 12
  - role: API関連資料
    section: 13
---

# <API名> API詳細設計書

<!-- INTRODUCTION_GUIDANCE: 冒頭案内は delivery-payload/references/設計書様式.md §9 の記入規則に従う。読み手が各節で何を見て何をするかを示す。複数節では「節｜内容｜読み手へのお願い」の3列表を置き、本文の節ごとに1行を対応づける。作る側の判断の理由・保管方法・作業の経緯・文書作成の方針・件数の内訳は書かない。 生成する本文の自由記述は敬体（です・ます）で書く。 -->

本書は 1 つのエンドポイントの実装契約を記します。情報源は API 一覧の拡張マニフェストと実装コードの読解です。

実装から確定できない事項は推測で埋めず、空欄のままとする。

## §1 API概要

> 区分: 仕様

### 1.1 基本情報

| 項目 | 値 |
|---|---|
| APIキー | APIKEY |
| API ID | APIID |
| メソッド | METHOD |
| パス | PATH |
| 所属する機能キー | FEATUREKEY |
| 認証の要否 | 要 / 不要 |
| 確からしさ | high / medium / low |
| 判定方法 | （マニフェストの検出手法） |

### 1.2 業務上の役割

## §2 リクエスト

> 区分: 仕様

### 2.1 パスパラメータ

| 名前 | 型 | 必須 | 有効な範囲 | NULL許容 | 初期値 | 桁と精度 | 制約 |
|---|---|---|---|---|---|---|---|

### 2.2 クエリパラメータ

| 名前 | 型 | 必須 | 有効な範囲 | NULL許容 | 既定値 | 桁と精度 | 制約 |
|---|---|---|---|---|---|---|---|

### 2.3 ヘッダ

| 名前 | 必須 | 有効な範囲 | NULL許容 | 初期値 | 桁と精度 | 用途 |
|---|---|---|---|---|---|---|

### 2.4 ボディ

| 項目 | 型 | 必須 | 有効な範囲 | NULL許容 | 初期値 | 桁と精度 | 制約 |
|---|---|---|---|---|---|---|---|

## §3 レスポンス

> 区分: 仕様

### 3.1 正常系の構造

<!-- 本表がレスポンス（戻り値）の構造を表す。項目列を戻り値のフィールド名として扱う -->

| 項目 | 型 | 常に存在 | 有効な範囲 | NULL許容 | 初期値 | 桁と精度 | 意味 |
|---|---|---|---|---|---|---|---|

### 3.2 ステータスコード一覧

| コード | 発生条件 | 本文の形 |
|---|---|---|

### 3.3 空・境界の扱い

| 状況 | 返す内容 |
|---|---|

## §4 処理フロー

> 区分: 仕様

### 4.1 処理順序

| 順 | 処理 | 呼び出す関数 |
|---|---|---|

### 4.2 内部呼び出し

| 呼び出し先 | 引数 | 戻り値 |
|---|---|---|

### 4.3 外部呼び出し

| 呼び出し先 | 契機 | 失敗時の扱い |
|---|---|---|

## §5 ロジック

> 区分: 仕様

### 5.1 分岐

| キー | 条件 | 真の場合 | 偽の場合 |
|---|---|---|---|

### 5.2 繰り返し

| キー | 対象 | 継続条件 | 打ち切りの条件 |
|---|---|---|---|

### 5.3 判定表

| キー | 入力の組み合わせ | 結果 |
|---|---|---|

## §6 疑似コード

> 区分: 今の作り（作り直す際: 決めておく）

<!-- 分岐と繰り返しの入れ子だけを日本語で書く。書き方は実装契約定義「疑似コード」の 7 点（関数名はそのまま・もし〜ならば・分岐ごとに空行・条件と本体を改行して字下げ・繰り返しの終わりを明示・字下げは全角の空白 2 つ・変数名は書かず役割で述べる）に従う。関数名は §12 実装契約（12.1 関数と役割・
     12.2 関数単位の契約）が定めるものを使ってよい。実装言語の記法（メソッド呼び出しの
     矢印・ハッシュの区切り等）と実装の変数名は使わない。変数の役割を述べる必要がある
     場合は役割を表す日本語（実行コンテキストの利用者情報 の類）を用いる。
     実際のコードや、対象コードのファイル名・行番号を記載しない。参照先は本書の節番号または関数単位の契約に限定する。 -->

`PSEUDOCODE`

## §7 データアクセス

> 区分: 今の作り（作り直す際: 決めておく）

### 7.1 参照テーブル

| テーブル | 取得条件 |
|---|---|

### 7.2 更新テーブル

| テーブル | 操作 | 更新条件 |
|---|---|---|

### 7.3 トランザクション境界

| 範囲 | 確定の契機 | 巻き戻しの条件 |
|---|---|---|

## §8 データ定義

> 区分: 今の作り（作り直す際: 決めておく）

### 8.1 内部データ構造

実装コードで組み立てる構造（ローカル変数、辞書・配列、DTO 等）を 1 構造につき 1 行で記録する。リクエスト・レスポンスの構造と、§12.2 の引数・戻り値としてだけ現れる構造は重複させない。

| 名前 | 型 | 構成要素 | 組み立てる箇所 | 用途 | 有効な範囲 | 寿命 |
|---|---|---|---|---|---|---|

## §9 業務ルールとバリデーション

> 区分: 仕様

### 9.1 入力検査

| 対象 | 検査内容 | 違反時の応答 |
|---|---|---|

### 9.2 業務判断

| キー | 判断内容 | 判断材料 |
|---|---|---|

### 9.3 権限

| 利用者区分 | 可否 | 判定材料 |
|---|---|---|

## §10 エラー

> 区分: 仕様

### 10.1 エラー一覧

| キー | 発生条件 | 応答コード | 利用者に見える結果 |
|---|---|---|---|

### 10.2 メッセージ定義との対応

| メッセージID | 本文 | 使う箇所 |
|---|---|---|

## §11 非機能

> 区分: 仕様

### 11.1 件数と量の制限

| 対象 | 上限 |
|---|---|

### 11.2 タイムアウトと再試行

| 対象 | 方針 |
|---|---|

### 11.3 冪等性

| 観点 | 性質 |
|---|---|

## §12 実装契約

> 区分: 今の作り（作り直す際: 決めておく）

### 12.1 関数と役割

| 関数 | 役割 |
|---|---|

### 12.2 関数単位の契約

対象は、当該 API のエントリーポイント、ならびに実装読解で追跡した 2 段以内の同一プロジェクト内の関数です。公開・非公開を問わず、定義を確認した関数をすべて 1 関数につき 1 行で記録します。外部ライブラリ、フレームワーク、および定義を確認できない呼び出し先は対象外とし、その旨を本節に注記する。 複数の設計単位から呼ばれる共通基盤の関数（事前関門・共通受付・応答整形など）は本表に載せず、プロジェクト共通の基盤設計書の「共通関数の契約」の節を正とし、§12.4 依存へ関数名と参照だけを書きます。

| 関数 | 公開範囲 | 引数 | 戻り値 | 前提条件 | 副作用 | 例外・エラー |
|---|---|---|---|---|---|---|

### 12.3 型定義

| 型名 | フィールド | 型 |
|---|---|---|

### 12.4 依存

共通基盤の関数は 1 関数につき 1 行で、依存先に「基盤設計 共通関数の契約 <関数名>」、用途に本 API が渡す値と受け取る値の使い方を書きます。契約（引数・戻り値・順序）は再掲しません。

| 依存先 | 用途 |
|---|---|

### 12.5 設計判断とその理由

実装で選ばれている方式と、その選択理由を 1 つの判断につき 1 行で記録する。
「記述区分」は、実装のコードコメントが現在の実装と理由を直接結び付けている場合だけ `観測（コードコメント）` とする。
コメントの存在を観測したことを表す区分であり、コメントの主張が現在も妥当であることを保証する区分ではない。
実装構造から理由を導いた場合は `推定（実装構造）` とし、確からしさを `medium` または `low` で記録する。

TODO、過去の経緯、却下済みの案、別の処理を説明するコメントは観測の根拠にしない。
選ばなかった選択肢または不採用理由を実装から読み取れない場合は `不明（実装に記述なし）` とし、補わない。
設計判断と選択理由のいずれも観測や推定ができない場合は行を作らず、その旨を本節に注記する。
実装コードの位置は本文にも別資料にも記録しない。判断キーから本書内の契約を参照できる状態にする。

| 判断キー | 設計判断 | 選択理由 | 記述区分 | 判断材料 | 選ばなかった選択肢 | 不採用理由 | 確からしさ |
|---|---|---|---|---|---|---|---|

## §13 関連資料

> 区分: 仕様

### 13.1 観測の出どころ

本文の実装仕様を読み取った実装箇所を記録する。ファイルと行はこの表だけに書き、本文へ括弧書きで埋め込まない。

| 対応する節・項目 | ファイル | 行 |
|---|---|---|

関連する資料を次の表に記録します。

| 資料 | パス | 本書との関係 |
|---|---|---|

