---
doc_id: screen-<画面ID>
type: screen-detail-design
status: draft
target_screen: <画面名>
source_repo: SOURCEREPOPATH
source_ref: SOURCECOMMIT
source_encoding: SOURCEENCODING
source_line_ending: SOURCELINEENDING
screen_type: SCREENTYPE  # single-page（画面内で状態を遷移させる型）/ server-rendered（要求ごとにサーバーが描画する型）
scenarios:
  - name: "SCENARIONAME"
    path: "ROUTEPATH"
    query: { QUERYKEY: QUERYVALUE }
    # ↑ query は YAML マップ形式が正。"school-id=1&..." のような文字列連結は誤り
    path_params: { PARAMNAME: PARAMVALUE }
    ready:
      selector: "READYSELECTOR"
      not: "NOTREADYSELECTOR"
    assert:
      tables: ["TABLESELECTOR"]
      texts: ["TEXTSELECTOR"]
    mask: ["MASKSELECTOR"]
# query/path_params/mask は該当なしなら省略可。ready/assert は描画到達・内容一致の判定に使うため記入推奨
# 後方互換: 旧 route: `ROUTEPATH` も受理（scenarios:[{path:`ROUTEPATH`}] 相当に自動正規化）
design_md: ./DESIGN.md
common_design_md: ../../プロジェクト共通/DESIGN.md
unit_test_design: ../テスト設計/画面単体テスト設計書.md
behavior_test_design: ../テスト設計/画面テスト設計書.md
operation_test_spec: ../テスト設計/操作シナリオ仕様書.md  # 画面固有。無ければ L5（操作シーケンス突合）はスキップされる
common_spec: ../../プロジェクト共通/共通設計書.md
common_spec_version: COMMONSPECVERSION
messages: ../../プロジェクト共通/メッセージ定義書.md
api_spec: ../../プロジェクト共通/API定義書.md  # 章マップの「正」に外部文書を指定する章がある場合は必須
updated: <YYYY-MM-DD>
---

# <画面名> 画面詳細設計書

## §1 画面概要

### 1.1 基本情報

| 項目 | 値 |
|---|---|
| 画面名 | `<画面名>` |
| 目的 | `SCREENPURPOSE` |
| ルート | `ROUTEPATH` |
| 遷移元 | `<画面名>` |
| 遷移先 | `<画面名>`（`TRANSITIONACTION` 時） |

### 1.2 業務上の位置づけ

<!-- この画面が業務フロー上のどの段階に位置するかを 1〜2 文で記述する -->

`BUSINESSPOSITION`

---

## §2 機能一覧

### 2.1 機能一覧表

<!-- この画面で実装する機能を列挙する。優先度は MoSCoW 等で表現する -->

| キー | 機能名 | 概要 | 優先度 |
|---|---|---|---|
| `FUNCTIONKEY` | `FUNCTIONNAME` | `FUNCTIONSUMMARY` | 必須 |

### 2.2 機能間の関係

<!-- 依存・排他などの関係がある場合のみ記述。なければ章冒頭の非該当項目行へ理由付きで集約する -->

`FUNCTIONCONSTRAINT`

---

## §3 画面構造

### 画面キャプチャ

<!-- 画像実体が取得できた場合は、この placeholder を ![元コードの画面](./original.png) に置き換える。 -->
<div class="screen-capture-placeholder" role="img" aria-label="画面キャプチャは未取得です。後続の画面確認で追加します。" style="box-sizing:border-box;display:flex;align-items:center;justify-content:center;min-height:180px;padding:24px;color:#64748b;background:#f8fafc;border:2px dashed #94a3b8;border-radius:0;text-align:center;">画面キャプチャは未取得です。後続の画面確認で追加します。</div>

### 部品構成（コンポーネント構造）

<!-- コンポーネント名（コード識別子）で入れ子構造を示す。
     基本設計書の部品構成とは異なり、実装のコンポーネント階層を反映する。 -->

```
┌─ <RootComponent> ──────────────────┐
│ ┌─ <ChildComponent> ────────────┐ │
│ │ <SubComponent>                 │ │
│ └────────────────────────────────┘ │
└────────────────────────────────────┘
```

### 3.1 コンポーネント配置

<!-- 親子関係をインデントで表現する -->

注意: facts の位置関係語（「外側」「内側」「〜の中に」）はネスト（親子）構造を意味する。兄弟配置（同じ階層に横並び）とは区別すること。ツリー図の同一インデント = 兄弟、インデント深化 = 親子ネスト。

```
<画面ルートコンポーネント>
├─ <ヘッダ領域コンポーネント>
├─ <メイン領域コンポーネント>
│   ├─ <サブコンポーネント A>
│   └─ <サブコンポーネント B>
└─ <フッタ領域コンポーネント>
```

### 3.2 DOM 配置順序

<!-- スクリーンリーダー読み上げ順序を考慮した DOM 順を列挙する -->

1. `ELEMENTNAME` — `ELEMENTPURPOSE`
2. `ELEMENTNAME` — `ELEMENTPURPOSE`

### 3.3 レイアウト制約

| 制約 | 値 |
|---|---|
| レイアウト方式 | `LAYOUTMETHOD` |
| 最小幅 | `MINWIDTH` |
| 最大幅 | `MAXWIDTH` |

### 3.4 メニューバー設定（条件付き：メニューバーを持つ画面のみ）

`MENUBARCONFIG`

### 3.5 設定オブジェクト構造（条件付き：設定オブジェクトを持つ場合のみ）

`LAYOUTCONFIG`

### 3.6 スタイル適用パターン

<!-- 数値の正は DESIGN.md。パターン名とキー名のみ参照し数値は持たない -->

| 領域 | 適用パターン | 参照先 |
|---|---|---|
| `AREANAME` | `PATTERNNAME` | `DESIGN.md > colors.COLORKEY` |

### 3.7 ボタン一覧

| ボタン名 | 種別 | 位置 | 活性条件 |
|---|---|---|---|
| `BUTTONNAME` | 主要 / 補助 / 危険 | `BUTTONPOSITION` | `DISPLAYCONDITION` |

### 3.8 テンプレート委譲（条件付き：描画をテンプレートエンジンへ委譲する画面のみ）

| 項目 | 値 |
|---|---|
| テンプレート名の解決規則 | `TEMPLATENAMERULE` |
| 解決先パス一覧 | `TEMPLATEPATHLIST` |
| 受け渡しデータキー | `TEMPLATEDATAKEYS` |

---

## §4 業務ルール（条件付き：業務モード・権限制御がある画面のみ）

<!-- 業務モード・権限制御がない画面は下位見出しと空表を削除し、章単位の全項目非該当書式を使う -->

### 4.1 業務モードと切替条件

| モード | 切替条件 | UI の変化 |
|---|---|---|
| `MODENAME` | `MODECONDITION` | `MODECHANGE` |

### 4.2 業務判断ポイント

`BEHAVIORBRANCH`

### 4.3 業務フロー内の前後画面との関係

`SCREENDATAFLOW`

### 4.4 権限別の UI 制御

<!-- ロール × ボタン活性 / 項目表示の対応表 -->

| ロール | `BUTTONNAME` | `FIELDNAME` |
|---|---|---|
| `ROLENAME` | 表示 / 非表示 | 表示 / 非表示 |

---

## §5 状態管理

### 5.1 URL パラメータ

| パラメータ名 | 型 | 必須 | 用途 |
|---|---|---|---|
| `PARAMETER` | `TYPE` | ○ / △ | `PURPOSE` |

### 5.2 セッション・ストレージ参照

| キー名 | 種別 | 用途 |
|---|---|---|
| `STORAGEKEY` | session / local | `PURPOSE` |

### 5.3 メイン画面の状態変数

| 変数名 | 型 | 初期値 | 役割 |
|---|---|---|---|
| `VARIABLENAME` | `TYPE` | `INITIALVALUE` | `ROLE` |

### 5.4 サブコンポーネントの状態変数

| コンポーネント | 変数名 | 型 | 役割 |
|---|---|---|---|
| `COMPONENTNAME` | `VARIABLENAME` | `TYPE` | `ROLE` |

### 5.5 派生値（計算値）

| 派生値名 | 算出ロジック概要 | 依存する状態変数 |
|---|---|---|
| `DERIVEDNAME` | `CALCULATION` | `SOURCENAME` |

### 5.6 画面専用ストア（永続化）

`STOREDEFINITION`

### 5.7 共有ストア参照

| ストア / コンテキスト | 参照キー | 用途 |
|---|---|---|
| `STORENAME` | `STOREKEY` | `PURPOSE` |

### 5.8 検索条件更新関数

<!-- 検索フォームがある画面のみ記述 -->

`SEARCHINITIALIZER`

### 5.9 初期値・デフォルト値一覧

<!-- 業務定数（ドロップダウン選択肢等）は §13 が持つ。本節は変数の初期値のみ記述する -->

| 変数名 | 初期値 | 参照先 |
|---|---|---|
| `VARIABLENAME` | `VALUE` | `EVIDENCE` |

---

## §6 データフロー

### 6.1 初期表示シーケンス

<!-- 以下は screen_type: single-page の既定手順。frontmatter の screen_type が server-rendered の場合は、
     要求受信から応答生成までの手順へ全面的に書き換える。§6.6 を非該当に集約した画面内状態の語
     （loading・ready・empty・error）を、この節に書き残してはならない -->

1. URL パラメータ取得
2. ストア初期化
3. API 呼び出し（§9 参照）
4. データ変換（§6.5 参照）
5. 画面状態を ready に更新

#### 6.1.1 起動時初期化（URL パラメータ → ストア書き戻し）

<!-- URL/クエリパラメータをストアへ復元する処理。なければ章冒頭の非該当項目行へ理由付きで集約する -->
`URLRESTORATION`

### 6.2 依存連鎖

`FETCHDEPENDENCY`

### 6.3 ユーザー操作確定時の処理フロー

<!-- 以下は screen_type: single-page の既定手順。server-rendered で利用者入力を受け付けない画面の場合は、
     手順を削除し、章冒頭の非該当項目行へ理由付きで集約する -->

1. バリデーション（§14 参照）
2. 確認ダイアログ表示（該当時）
3. API 呼び出し
4. 成功時: 遷移または状態更新
5. 失敗時: エラー表示（§14 参照）

### 6.4 データ更新トリガーの分類

| トリガー | 種別 | 処理概要 |
|---|---|---|
| `ACTIONNAME` | ユーザー操作 / 自動 | `PROCESSSUMMARY` |

### 6.5 データ変換（API レスポンス → 画面表示用データ）

| 変換名 | 入力型 | 出力型 | 変換概要 |
|---|---|---|---|
| `CONVERSIONNAME` | `APITYPE` | `SCREENTYPE` | `CONVERSIONDETAIL` |

### 6.6 画面状態の遷移

<!-- 以下は screen_type: single-page の既定の状態一覧。server-rendered で画面内の状態遷移を持たない場合は、
     表を削除し、章冒頭の非該当項目行へ理由付きで集約する。
     ここで削除した状態の語を §6.1・§16.7 に書き残してはならない -->

| 状態 | 遷移条件 | UI の表示 |
|---|---|---|
| loading | 初期表示・再取得中 | ローディング表示 |
| empty | データ 0 件 | 空状態メッセージ |
| error | API エラー | エラーメッセージ（§14 参照） |
| ready | データ取得成功 | メインコンテンツ |

### 6.7 戻り値

<!-- この画面のデータ取得・更新処理が呼び出し元（状態管理・ストア）へ返す値を記録する。区分は 戻り値 のみを記す -->

| 区分 | 名前 | 型 | 意味 | 参照先 |
|---|---|---|---|---|
| 戻り値 | `RETURNFIELD` | `RETURNTYPE` | `RETURNMEANING` | `EVIDENCE` |

---

## §7 ロジック

### 7.1 分岐

| キー | 条件 | 真の場合 | 偽の場合 |
|---|---|---|---|

### 7.2 繰り返し

| キー | 対象 | 継続条件 | 打ち切りの条件 |
|---|---|---|---|

### 7.3 判定表

| キー | 入力の組み合わせ | 結果 |
|---|---|---|

---

## §8 疑似コード

<!-- 分岐と繰り返しの入れ子だけを日本語で書く。コンポーネント名・関数名は §18 実装契約
     （18.1 ファイル分割と export 一覧）が定めるものを使ってよい。実装言語の記法
     （メソッド呼び出しの矢印・ハッシュの区切り等）と実装の変数名は使わない。変数の役割を
     述べる必要がある場合は役割を表す日本語（実行コンテキストの利用者情報 の類）を用いる。
     実際のコードを貼ることを禁じる。原本の行を指したい場合は §18 実装契約の
     「18.1 ファイル分割と export 一覧」から辿らせる。 -->

`PSEUDOCODE`

---

## §9 API 通信仕様（条件付き：API を呼ぶ画面のみ）

<!-- 契約の正は API 定義書。本書は画面からの使い方（呼び出し条件・エラー処理）のみを記述する -->

### 9.1 API 一覧

| No | API 名 | メソッド | エンドポイント | 呼び出し契機 |
|---|---|---|---|---|
| 7.2 | `APINAME` | `METHOD` | `ENDPOINT` | `TRIGGER` |

### 9.2 `APINAME`

| 項目 | 内容 |
|---|---|
| 概要 | `APISUMMARY` |
| エンドポイント | `METHOD ROUTEPATH` |
| 呼び出し契機 | `CALLTRIGGER` |

**リクエスト**

```typescript
{ <フィールド名>: <型>; }
```

**レスポンス**

```typescript
{ <フィールド名>: <型>; }
```

**エラーと画面側の扱い**

| ステータス | 画面の挙動 | メッセージ ID |
|---|---|---|
| 400 | バリデーションエラー表示 | `MESSAGEID` |
| 401 | ログイン画面へ遷移 | — |
| 500 | システムエラーメッセージ表示 | `MESSAGEID` |

---

## §10 データ定義

### 10.1 扱う内部データ構造

| 名前 | 型 | 有効な範囲 | 寿命 |
|---|---|---|---|

---

## §11 イベント処理

### 11.1 メイン画面イベント

| イベント | 発火要素 | 処理概要 |
|---|---|---|
| `EVENTNAME` | `ELEMENTNAME` | `PROCESSSUMMARY` |

### 11.2 サブコンポーネントイベント

| コンポーネント | イベント | 処理概要 |
|---|---|---|
| `COMPONENTNAME` | `EVENTNAME` | `PROCESSSUMMARY` |

### 11.3 複数ステップ処理の詳細

`MULTISTEPPROCESS`

### 11.4 キーボード操作

| キー | 発火条件 | 挙動 |
|---|---|---|
| Tab | フォーカス可能要素 | `TABORDERPOLICY` |
| Enter | `ELEMENTNAME` | `BEHAVIOR` |

---

## §12 領域別仕様

<!-- 領域ごとに ### 12.n <領域名> を追加する。雛形は 12.1 の 1 領域分 -->

### 12.1 `AREANAME`

#### 12.1.1 概要

`AREAPURPOSE`

#### 12.1.2 配置

`AREAPOSITION`

#### 12.1.3 項目定義

| 項目名 | 型 | 必須 | 書式 | 状態別編集可否 |
|---|---|---|---|---|
| `FIELDNAME` | `TYPE` | ○ / △ / × | `FORMAT` | ready: 可 / loading: 不可 |

#### 12.1.4 イベント・挙動

| イベント | 挙動 |
|---|---|
| `EVENTNAME` | `BEHAVIOR` |

#### 12.1.5 領域固有の制約

`AREACONSTRAINT`

---

## §13 定数・設定値（条件付き：画面固有の定数がある場合のみ）

<!-- 設計時は本書が契約。実装後は実装ファイルのパス参照に置換する -->

### 13.1 文字列定数

| 定数名 | 値 | 用途 |
|---|---|---|
| `CONSTANTNAME` | `"VALUE"` | `PURPOSE` |

### 13.2 選択肢定義

| 定数名 | 選択肢 | 用途 |
|---|---|---|
| `CONSTANTNAME` | `VALUEONE` / `VALUETWO` | `PURPOSE` |

### 13.3 列定義・表示設定

`TABLECOLUMNDEFINITION`

### 13.4 コード変換テーブル

| コード | 表示ラベル |
|---|---|
| `CODE` | `LABEL` |

### 13.5 区分定数

| 定数名 | 値一覧 |
|---|---|
| `CONSTANTNAME` | `VALUEONE` / `VALUETWO` |

### 13.6 参照する外部設定値と定義元

<!-- モジュール内で宣言された定数ではなく、実行環境から参照するグローバル設定値を列挙する。
     値そのものは定義元が正であり、本書は所在の記録に徹する。
     参照する設定値が無い場合は見出しと空表を削除し、章冒頭の非該当項目行へ理由付きで集約する -->

| 設定値名 | 用途 | 定義元 |
|---|---|---|
| `EXTERNALCONFIGNAME` | `EXTERNALCONFIGPURPOSE` | `EXTERNALCONFIGSOURCE` |

---

## §14 エラーハンドリング

### 14.1 ユーザー入力チェック

| チェック対象 | 条件 | エラー種別 |
|---|---|---|
| `FIELDNAME` | `VALIDATION` | 必須 / 形式 / 範囲 |

### 14.2 エラーメッセージ・警告

<!-- 文言の正はメッセージ定義書。本書はメッセージ ID で参照する -->

| タイミング | メッセージ ID | 表示位置 |
|---|---|---|
| `TIMING` | `MESSAGEID` | インライン / トースト |

### 14.3 システムエラー

| エラー種別 | 画面の挙動 | メッセージ ID |
|---|---|---|
| ネットワーク断 | エラーバナー表示・リトライ促進 | `MESSAGEID` |
| 認証切れ | ログイン画面へ遷移 | — |
| 予期しないエラー | エラーメッセージ表示 | `MESSAGEID` |

---

## §15 画面遷移仕様

<!-- §1.1 との役割分担: §1.1 は概要（遷移元・先の一覧）、本章は詳細（方式・パラメータ） -->

### 15.1 遷移先一覧

| 遷移先画面 | 遷移方式 | 条件 | 渡すパラメータ |
|---|---|---|---|
| `<画面名>` | push / replace / back | `TRANSITIONCONDITION` | `TRANSITIONPARAMETER` |

### 15.2 遷移方式の方針

`NAVIGATIONPOLICY`

---

## §16 非機能要件

<!-- 共通内容は共通設計書が正。本章は画面固有の制約のみ記述する -->

### 16.1 表示件数・データ量制限

| 対象 | 上限 | 超過時の挙動 |
|---|---|---|
| `LISTNAME` | `ITEMCOUNT` 件 | `BEHAVIOR` |

### 16.2 パフォーマンス目安

| 操作 | 目標応答時間 |
|---|---|
| 初期表示 | `SECONDS` 秒以内 |
| `ACTIONNAME` | `SECONDS` 秒以内 |

### 16.3 ローディング・処理中表示

<!-- 挙動詳細は §6・§14 参照。本節はローディング UI 仕様のみ -->

| 操作 | ローディング表示方式 |
|---|---|
| 初期表示 | スケルトン / スピナー（選択） |
| `ACTIONNAME` | ボタン無効化 / オーバーレイ（選択） |

### 16.4 タイムアウト・リトライ方針

<!-- 挙動は §14、方針・設定値は本節 -->

| 項目 | 値 | 参照先 |
|---|---|---|
| タイムアウト時間 | `SECONDS` 秒 | `EVIDENCE` |
| リトライ回数 | `RETRYCOUNT` 回 | `EVIDENCE` |

### 16.5 ブラウザ・画面解像度の動作保証範囲

`COMMONSPECDIFFERENCE`

---

## §17 共通仕様への準拠

<!-- 準拠バージョンは frontmatter の common_spec_version が正。画面横断レビューは本章同士の比較で行う -->

### 17.1 共通コンポーネント利用一覧

| コンポーネント名 | 利用箇所 | 独自実装の場合の理由 |
|---|---|---|
| `COMPONENTNAME` | `USAGELOCATION` | — |

### 17.2 共通ソート・表示順ルールの適用

| 対象リスト | 適用ルール | 逸脱理由 |
|---|---|---|
| `LISTNAME` | `RULENAME` | — |

### 17.3 用語・ラベルの統一

`GLOSSARYDIFFERENCE`

### 17.4 日付・数値・コード値の表示フォーマット

| 値の種類 | フォーマット | 空値時 |
|---|---|---|
| 日付 | `FORMAT` | `—` |
| 数値 | `FORMAT` | `—` |
| コード値 | `FORMAT` | `—` |

### 17.5 共通メッセージの利用

| 用途 | メッセージ ID |
|---|---|
| `PURPOSE` | `MESSAGEID` |

---

## §18 実装契約

<!-- 本章は実装開始時点の契約。マージ後はコードが正で、本章は構造の骨子のみ維持する -->

### 18.1 ファイル分割と export 一覧

| ファイルパス | export 名 | 種別 | 実体形状 | 配置ディレクトリ |
|---|---|---|---|---|
| `FILEPATH` | `EXPORTNAME` | コンポーネント / フック / ユーティリティ | `RUNTIMESHAPE` | pages/ or components/ or hooks/ or utils/ or types/ |
| utils/mockData.ts | mockStudents | モックデータ定数 | 配列（StudentRecord[]、要素数5） | utils/ |

### 18.2 型定義

<!-- 実装後は実装ファイルが正。1フィールド=1行。同一型名の複数フィールド行でも型名列を省略せず毎行明記する（機械ゲートが型名列を型ごとに抽出するため） -->

| 型名 | フィールド名 | 型 | 必須/任意 |
|---|---|---|---|
| `TYPENAME` | `FIELDNAME` | `TYPE` | 必須 / 任意 |

### 18.3 依存（import）一覧

| モジュール | import 内容 | 種別 |
|---|---|---|
| `MODULENAME` | `IMPORTNAME` | 内部 / 外部ライブラリ |

### 18.4 別画面コンポーネントとの型合わせ

`CROSSSCREENTYPESHARING`

### 18.5 スコープ分類

| 対象 | スコープ | 理由 |
|---|---|---|
| `STATEVARIABLENAME` | コンポーネントローカル / コンテキスト / グローバル | `REASON` |

### 18.6 スタイル定数一覧

<!-- 数値の正は DESIGN.md。定数名とキー名のみ参照し数値は持たない -->

| 定数名 | 用途 | 参照先 |
|---|---|---|
| `CONSTANTNAME` | `PURPOSE` | `DESIGN.md > DESIGNKEY` |

### 18.7 state 遷移シーケンス

<!-- 以下は screen_type: single-page の既定の遷移。server-rendered で画面内の状態遷移を持たない場合は、
     図を削除し、章冒頭の非該当項目行へ理由付きで集約する。§6.6 の状態一覧と必ず一致させる -->

<!-- §6.6 の状態遷移を実装観点で補足する -->

```
初期化 → loading → ready
                 ↘ error（リトライ可）
       → loading → empty（0 件時）
```

### 18.8 ルート登録

<!-- ルーティング表の1行で画面が成立する構成では、この節がないと画面を復元できない。
     ルート識別子の文字列だけでなく、登録定義の所在と前提フラグを記録する -->

| 項目 | 値 |
|---|---|
| 登録定義の所在 | `ROUTEREGISTRYEVIDENCE` |
| 認証・前提フラグの値と意味 | `ROUTEAUTHFLAGS` |
| 未登録識別子のフォールバック仕様 | `ROUTEFALLBACK` |

---

## §19 関連資料

| 正の種類 | ファイル | 本書との役割分担 |
|---|---|---|
| 根拠を記録する資料 | `<根拠資料のパス>` | 確定できなかった事項と根拠を記録する |
| スタイル・数値（共通） | `../../プロジェクト共通/DESIGN.md` | 複数画面共通のデザイン値の正 |
| スタイル・数値（画面固有差分） | `./DESIGN.md` | 画面固有の差分の正。本書はキー名参照のみ |
| 共通仕様 | `../../プロジェクト共通/共通設計書.md` | システム共通仕様の正。本書は準拠状況と画面固有制約のみ |
| 文言 | `../../プロジェクト共通/メッセージ定義書.md` | 文言の正。本書はメッセージ ID で参照 |
| 画面単体テスト | `../テスト設計/画面単体テスト設計書.md` | 関数・メソッド単位の観点とケース。詳細設計書に対応する |
| 画面テスト | `../テスト設計/画面テスト設計書.md` | 1画面の操作・表示の観点とケース。基本設計書に対応する |
| 操作シナリオ | `../テスト設計/操作シナリオ仕様書.md`（画面固有） | 単一画面内の操作の流れ。画面をまたぐ結合テストは上位文書が担う |

## 章マップ（付録B）

| 役割キー | § | 章名 | 区分 | 正 | 実装後の扱い | 保守方式 |
|---|---|---|---|---|---|---|
| 画面概要 | §1 | 画面概要 | 必須 | 本書 | 維持 | 手 |
| 機能一覧 | §2 | 機能一覧 | 必須 | 本書 | 維持 | 手 |
| 画面構造 | §3 | 画面構造 | 必須 | 本書（§3.6 の数値は DESIGN.md） | 維持 | 生成 |
| 業務ルール | §4 | 業務ルール | 条件付き | 本書 | 維持 | 手 |
| 状態管理 | §5 | 状態管理 | 必須 | 本書 | 維持 | 生成 |
| データフロー | §6 | データフロー | 必須 | 本書 | 維持 | 生成 |
| ロジック | §7 | ロジック | 必須 | 本書 | 維持 | 手 |
| 疑似コード | §8 | 疑似コード | 必須 | 本書 | 維持 | 手 |
| API通信 | §9 | API 通信仕様 | 条件付き | API 定義書（本書は画面固有の使い方のみ） | 維持 | 生成 |
| データ定義 | §10 | データ定義 | 必須 | 本書 | 維持 | 手 |
| イベント処理 | §11 | イベント処理 | 必須 | 本書 | 維持 | 生成 |
| 領域別仕様 | §12 | 領域別仕様 | 必須 | 本書 | 維持 | 混在 |
| 定数設定値 | §13 | 定数・設定値 | 条件付き | 設計時は本書 → 実装後は実装コード | 実装ファイルのパス参照に置換 | 混在 |
| エラーハンドリング | §14 | エラーハンドリング | 必須 | 本書 | 維持 | 混在 |
| 画面遷移 | §15 | 画面遷移仕様 | 必須 | 本書 | 維持 | 生成 |
| 非機能要件 | §16 | 非機能要件 | 必須 | 共通設計書（画面固有制約のみ本書） | 維持 | 手 |
| 共通準拠 | §17 | 共通仕様への準拠 | 必須 | 共通設計書（本書は逸脱のみ） | 維持 | 手 |
| 実装契約 | §18 | 実装契約 | 必須 | 設計時は本書 → 実装後はコード（§18.6 は DESIGN.md） | 構造の骨子のみ維持 | 生成 |
| 関連資料 | §19 | 関連資料 | 必須 | 本書および根拠資料 | 根拠資料への参照が存在 | 生成 |

保守方式: 手 = 人が保守する正 / 生成 = 納品時にコードからリバース生成 / 混在 = 生成した骨格に業務的意味を手で重ねる

外部文書の宣言規約: 「正」列に本書・共通設計書以外の外部文書を指定する場合、frontmatter に対応するパスキーを必須で置く（例: §9 の「API 定義書」に対し `api_spec`）。パスキーを伴わない外部文書の宣言を禁止する。宣言先の文書が実在しない場合は 本書の根拠資料へ記録し、status を authored にしない。

### 非該当項目の集約書式

<!-- facts.yml で章内の全項目が items: [] + reason の場合、下位見出し・空表をすべて削除し、
     章見出しの直下へ次の1行だけを書く。理由は facts.yml の reason を具体化し、失わないこと。
     > 本領域は全項目非該当（根拠: <factsセクション> の全分類が0件。<理由>）
     章内の一部だけが対象外の場合も対象外の下位見出し・空表は削除し、章の冒頭へ次の1行に集約する。
     > 非該当項目: §X.Y <項目名>（理由）; §X.Z <項目名>（理由）
     facts に項目がある節、実測委譲、本書の根拠資料へ記録すべき未確認事項は集約へ含めない。 -->
