---
doc_id: design-common
type: design
status: draft   # draft | traced（値が全て既存コードからの抽出値に置き換わった状態）
target_screen: _プロジェクト共通
updated: <YYYY-MM-DD>
colors:
  primary: "<実測値>"
  surface: "<実測値>"
  on-surface: "<実測値>"
  error: "<実測値>"
typography:
  heading: "<実測値>"
  body: "<実測値>"
  label: "<実測値>"
components:
  # 複数画面で共通して使われるコンポーネントの視覚原則をキーで記述する
rounded: "<実測値>"
spacing: "<実測値>"
---

# 共通デザインシステム（リバース版・プロジェクト共通/DESIGN.md）

本書は複数のレガシー画面から共通して観測されたスタイル・数値の記録です。既存コードに実際に存在する共通のデザイントークン（テーマ変数・共通スタイルシート等）をそのまま転記する。個別画面の `../画面/詳細設計/DESIGN.md` はここからの **差分（画面固有の逸脱）のみ** を持ち、同じ値を重複して書いてはならない。

## Overview

<!-- 共通スタイル・テーマ定義として観測された要素を 1〜2 文で書く。対象コードの位置は横断根拠台帳に記録する。 -->
<!-- アーキテクチャ調査書§4に「共有ファイル」行がある場合は、同じ要素を別名で二重記載しない。 -->

`<実測: 共通スタイル・テーマ定義の要約>`

## Colors

<!-- 複数画面で共通して観測された値のみを転記する。1画面にしか出てこない値は書かない -->

| トークン名 | 用途 |
|---|---|
| primary | 主要ボタン・リンク・フォーカスリング |
| surface | 画面背景・カード背景 |
| on-surface | 本文テキスト |
| error | エラーメッセージ・バリデーション失敗 |

## Typography

| トークン名 | 用途 |
|---|---|
| heading | 画面タイトル・セクション見出し |
| body | 本文・データ表示 |
| label | 入力ラベル・テーブルヘッダ |

## Components

<!-- 全画面共通のコンポーネント（ボタン・入力欄・カード等）の実装済み視覚原則を書く -->

| 共通コンポーネント | 実装済みの視覚原則 |
|---|---|
| `<コンポーネント名>` | `<実測した見た目の原則>` |

## レスポンシブ基準

| ブレークポイント名 | 実測した幅 | 実装済みレイアウト |
|---|---|---|
| `<ブレークポイント名>` | `<実測値>` | `<実測したレイアウト変更>` |

## アクセシビリティ基準

| 観点 | 実装済みの共通要件 |
|---|---|
| カラーコントラスト | `<実測値。基準未達の対象コードは横断根拠台帳へ記録する>` |
| フォーカス可視性 | `<実測した共通のフォーカス表示方式>` |
| スクリーンリーダー | `<実測した共通のaria対応状況>` |
