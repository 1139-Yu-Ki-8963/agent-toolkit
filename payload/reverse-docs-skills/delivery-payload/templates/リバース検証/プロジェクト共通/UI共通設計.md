---
doc_id: ui-common-design
type: ui-common-design
status: draft   # draft | traced（全節が実測値で埋まった状態）
updated: <YYYY-MM-DD>
---

# UI共通設計書（リバース版）

<!-- INTRODUCTION_GUIDANCE: 冒頭案内は delivery-payload/references/設計書様式.md §9 の記入規則に従う。読み手が各節で何を見て何をするかを示す。複数節では「節｜内容｜読み手へのお願い」の3列表を置き、本文の節ごとに1行を対応づける。作る側の判断の理由・保管方法・作業の経緯・文書作成の方針・件数の内訳は書かない。 生成する本文の自由記述は敬体（です・ます）で書く。 -->

本書は複数のレガシー画面から取り出した、UI の設計方針を上位から俯瞰する記録です。あるべき UI 設計を新たに構想するのではなく、既存コードに実際に存在する共通方針のみを扱う。個々の状態・操作規則の詳細は `./共通設計書.md`、数値・色・フォントは `./DESIGN.md` を正とし、本書はそれらの要約・俯瞰に徹する。対象コードの位置は記録しない。

## 本書に書かないもの

| 書かない内容 | 正の置き場 |
|---|---|
| 共通状態・操作規則の詳細（loading/empty/error 等） | `./共通設計書.md`（§1〜§3） |
| 数値・色・フォント（共通） | `./DESIGN.md` |
| 数値・色・フォント（画面固有差分） | 各画面の `../画面/詳細設計/DESIGN.md` |
| UI 文言 | `./メッセージ定義書.md` |
| コンポーネント実装規則（props 命名・分割方針等） | `./規約/コンポーネント設計規約.md` |
| 画面固有のレイアウト | 各画面詳細設計書 |

---

## §1 デザインシステム（実測）

> 区分: 仕様

| 項目 | 内容 |
|---|---|---|
| 使用ライブラリ・デザインシステム | `<実測: UIライブラリ名。独自実装のみの場合は「実在しない（理由: 独自実装のコンポーネントのみ）」>` |
| バージョン | `<実測>` |

---

## §2 共通コンポーネント一覧（実測）

> 区分: 仕様

対象は複数画面で横断的に使用されているコンポーネントに限る。1画面限定のコンポーネントの正の置き場は各画面詳細設計書。

| コンポーネント | 用途 | 使用画面数（頻度） |
|---|---|---|---|
| `<実測: コンポーネント名>` | `<実測: 用途>` | `<実測: N/M 形式>` |

---

## §3 レイアウト方針（実測）

> 区分: 仕様

対象は個別のレイアウト構造ではなく、複数画面で共通して観測された原則に限る。詳細な構造原則は `./共通設計書.md` §3 を参照する。

| 要素 | 実装済みの原則 |
|---|---|---|
| グリッドシステム | `<実測: 使用有無・カラム数等>` |
| ブレークポイント | `<実測: レスポンシブ分岐点>` |

---

## §4 テーマ・スタイル管理（実測）

> 区分: 仕様

| 項目 | 内容 |
|---|---|---|
| CSS 設計方針 | `<実測: CSS Modules/styled-components/Tailwind等>` |
| テーマ切替の有無 | `<実測 または 実在しない（理由: …）>` |
| レスポンシブ対応方針 | `<実測>` |

---

## §5 アクセシビリティ方針（実測）

> 区分: 仕様

| 項目 | 内容 |
|---|---|---|
| WAI-ARIA 対応 | `<実測: aria-* 属性の使用状況 または 実在しない（理由: …）>` |
| キーボード操作対応 | `<実測: 共通設計書.md §2 のフォーカス移動規則と重複させず要約のみ>` |

---

## §6 画面横断 UI 状態（実測）

> 区分: 仕様

対象はテーマ・通知・モーダル等、複数画面をまたいで参照される UI 状態に限る。1画面限定の状態の正の置き場は各画面詳細設計書。

| 状態名 | 保持場所（store/context等） | 影響範囲（画面・コンポーネント） | 初期値 |
|---|---|---|---|---|
| `<実測: 状態名>` | `<実測: 保持場所>` | `<実測: 影響範囲>` | `<実測: 初期値>` |

---

## traced の条件

- 全節の実測値が既存コードまたは `./共通設計書.md`・`./DESIGN.md` からの要約で埋まっていること
