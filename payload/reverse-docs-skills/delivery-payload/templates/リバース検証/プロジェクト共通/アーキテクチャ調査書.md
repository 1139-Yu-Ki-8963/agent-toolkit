---
doc_id: "common/アーキテクチャ調査書"
type: architecture-survey
status: draft   # draft | traced（全節が実測値で埋まった状態）
updated: <実測: YYYY-MM-DD>
---

# アーキテクチャ調査書（リバース版）

<!-- INTRODUCTION_GUIDANCE: 冒頭案内は delivery-payload/references/設計書様式.md §9 の記入規則に従う。読み手が各節で何を見て何をするかを示す。複数節では「節｜内容｜読み手へのお願い」の3列表を置き、本文の節ごとに1行を対応づける。作る側の判断の理由・保管方法・作業の経緯・文書作成の方針・件数の内訳は書かない。 -->

本書は対象リポジトリを決定的コマンド（`find`/`grep`/`ls`）のみで走査し、後続工程（共通採録・facts抽出・詳細設計執筆）が前提とする技術スタック・ディレクトリ責務・ユニット種別の実在有無を確定した記録です。

## §1 調査メタ

| 項目 | 内容 |
|---|---|
| 対象リポジトリ | `<実測: target_repo_path>` |
| 対象ブランチ | `<実測: target_branch。未指定なら「未指定」>` |
| 参照コミット（source_ref） | `<実測: source_ref。未指定なら「未指定」>` |
| 調査実施日 | `<実測: YYYY-MM-DD>` |

プロジェクト形態（単独プロジェクト / モノレポ）とサイト構成は §10 を参照する。

### 実行した調査コマンド一覧

<!-- Phase 2〜3 で実行した find/grep/ls をすべて記録する。再現可能性の担保。 -->

| コマンド | 目的 |
|---|---|
| `<実測: 実行コマンド>` | `<実測: 何を確認するためか>` |

---

## §2 技術スタック

| 項目 | 内容 | 参照先 |
|---|---|---|
| 言語・ランタイム | `<実測>` | `<実測: 参照先>` |
| フレームワーク | `<実測>` | `<実測: 参照先>` |
| パッケージマネージャ | `<実測>` | `<実測: 参照先（lockファイル等）>` |
| ルーティングライブラリ | `<実測>` | `<実測: 参照先>` |

---

## §3 ビルドと起動

| 項目 | 内容 | 参照先 |
|---|---|---|
| ビルドコマンド | `<実測>` | `<実測: 参照先>` |
| 起動コマンド（開発） | `<実測>` | `<実測: 参照先>` |
| 起動コマンド（本番） | `<実測>` | `<実測: 参照先>` |
| 環境変数定義の所在 | `<実測>` | `<実測: 参照先>` |

---

## §4 ディレクトリ責務マップ

対象は決定的走査（find/ls）で確認した実在ディレクトリに限る。根拠のないディレクトリ追加は禁止。例外として、複数ディレクトリから import・参照される共有ファイル（共通スタイル定数・テーマ定義ファイル等、単一ディレクトリの責務に還元できないファイル）に限り、ディレクトリ列に当該ファイルの相対パスをそのまま置ける。この場合、責務列は「共有ファイル（<N>ディレクトリから参照）」の形式とし、参照先列は検出に使った決定的コマンドと実測件数を示す。該当する共有ファイルが無いプロジェクトでは本行を追加しない（空行・プレースホルダのまま残すことを禁止する）。

| ディレクトリ | 責務 | 参照先 |
|---|---|---|
| `<実測: ディレクトリパス>` | `<実測: 責務の要約>` | `<実測: 参照先>` |

### 対象外ディレクトリ

<!-- 調査スコープ内に実在するが後続工程のサンプリング対象から意図的に除外するディレクトリを記録する。 -->
<!-- 除外理由は「実在しない（理由: …）」と同じ流儀で明記する。 -->
<!-- 対象外ディレクトリが無い場合は本サブセクション（見出し・テーブルすべて）を丸ごと削除する。 -->

| ディレクトリ | 除外理由 |
|---|---|
| `<実測: ディレクトリパス>` | `<実測: 除外理由>` |

---

## §5 エントリポイントとルーティング

| 項目 | 内容 | 参照先 |
|---|---|---|
| エントリポイント | `<実測>` | `<実測: 参照先>` |
| ルーティング定義の所在 | `<実測>` | `<実測: 参照先>` |
| ルーティング方式 | `<実測>` | `<実測: 参照先>` |

---

## §6 ユニット種別判定

| 種別 | 実在の判定 | 検出の手がかり | 参照先 |
|---|---|---|---|
| 画面 | `<実測: 実在する または 実在しない（理由: …）>` | `<実測: 再現可能なgrep/findパターン>` | `<実測: 参照先。実在しない場合は「-」>` |
| API | `<実測: 実在する または 実在しない（理由: …）>` | `<実測: 再現可能なgrep/findパターン>` | `<実測: 参照先。実在しない場合は「-」>` |
| テーブル | `<実測: 実在する または 実在しない（理由: …）>` | `<実測: 再現可能なgrep/findパターン>` | `<実測: 参照先。実在しない場合は「-」>` |
| バッチ | `<実測: 実在する または 実在しない（理由: …）>` | `<実測: 再現可能なgrep/findパターン>` | `<実測: 参照先。実在しない場合は「-」>` |
| 帳票 | `<実測: 実在する または 実在しない（理由: …）>` | `<実測: 再現可能なgrep/findパターン>` | `<実測: 参照先。実在しない場合は「-」>` |
| 外部連携 | `<実測: 実在する または 実在しない（理由: …）>` | `<実測: 再現可能なgrep/findパターン>` | `<実測: 参照先。実在しない場合は「-」>` |

<!-- キーは連番禁止。内容を要約した意味語で付ける（番号からは情報を得られないため） -->

---

## §7 判定不能と実在しない項目の理由

<!-- §6で「実在しない」とした種別、および技術スタック等で判定不能だった項目の理由を集約する。 -->

| キー | 内容 | 理由 |
|---|---|---|
| `<実測: 対象-要約>` | `<実測: 判定不能または実在しない項目>` | `<実測: 理由>` |

---

## §8 後続工程への申し送り

<!-- 共通採録スキル・facts抽出スキル・詳細設計執筆スキルが本調査書を読む際に留意すべき事項。 -->

| キー | 内容 |
|---|---|
| `<実測: 申し送り-要約>` | `<実測: 申し送り内容>` |

---

## §9 テスト基盤

### テストフレームワーク

| 種別 | フレームワーク | 設定ファイル | 備考 |
|---|---|---|---|
| 単体テスト | （調査結果を記入） | （パス） | |
| 結合テスト | （調査結果を記入） | （パス） | |
| E2E テスト | （調査結果を記入） | （パス） | |

### テストファイル概況

- テストファイル総数: （code-metrics.json または走査結果から記入）
- テストディレクトリ: （パターンを記入）
- CI テストジョブ: （.github/workflows 等から記入）

### カバレッジ

未計測（ツール選定はリバース段階で不可）

---

## §10 プロジェクト形態とサイト構成

<!-- ワークスペース定義ファイルの実在で判定する。app/ や apps/ や packages/ といったディレクトリ名の見た目では判定しない。 -->

| 項目 | 内容 | 参照先 |
|---|---|---|
| プロジェクト形態 | `<実測: 単独プロジェクト または モノレポ>` | `<実測: 参照先>` |
| ワークスペース定義 | `<実測: 検出したワークスペース定義。無ければ「実在しない（理由: …）」>` | `<実測: 参照先>` |

### サイト一覧

プロジェクト形態が「単独プロジェクト」の場合も 1 行のみとする（サイトキーは main、ルートディレクトリは . とする）。

<!-- サイトキーは連番禁止。内容を要約した意味語で付ける（web / admin / partner 等）。 -->
<!-- 参照先の粒度は §2〜§5 と同じ。リポジトリルートからの相対パスを書く。ファイル内の特定キーが根拠なら package.json:workspaces のようにコロンでキー名を添えてよい。 -->

| サイトキー | 表示名 | ルートディレクトリ | ビルドコマンド | 起動コマンド | 参照先 |
|---|---|---|---|---|---|
| `<実測: サイトキー>` | `<実測: 表示名>` | `<実測: リポジトリルートからの相対パス>` | `<実測: ビルドコマンド>` | `<実測: 起動コマンド>` | `<実測: 参照先>` |
