---
doc_id: infrastructure-design
type: infrastructure-design
status: draft   # draft | traced（全節が実測値で埋まった状態）
updated: <YYYY-MM-DD>
---

# 基盤設計書（リバース版）

<!-- INTRODUCTION_GUIDANCE: 冒頭案内は delivery-payload/references/設計書様式.md §9 の記入規則に従う。読み手が各節で何を見て何をするかを示す。複数節では「節｜内容｜読み手へのお願い」の3列表を置き、本文の節ごとに1行を対応づける。作る側の判断の理由・保管方法・作業の経緯・文書作成の方針・件数の内訳は書かない。 生成する本文の自由記述は敬体（です・ます）で書く。 -->

本書はアーキテクチャ調査書（`./アーキテクチャ調査書.md`）で確定した実測事実を、基盤構成の観点で整理・再構成した記録です。あるべき基盤構成を新たに設計するのではなく、既存コードに実際に存在する構成のみを扱う。個々の実測値（バージョン番号等）はアーキテクチャ調査書を正とし、本書では二重管理しない。対象コードの位置は記録しない。

## 本書に書かないもの

| 書かない内容 | 正の置き場 |
|---|---|
| 技術スタック・ビルドコマンドの実測値そのもの | `./アーキテクチャ調査書.md`（§2〜§3） |
| ディレクトリ・ファイルの命名規則 | `./規約/ディレクトリ構成規約.md` |
| コーディングスタイル | `./規約/コーディング規約.md` |
| 画面固有のビルド差分 | 各画面詳細設計書 |

---

## §1 フレームワーク構成（実測）

| 項目 | 内容 |
|---|---|---|
| フレームワーク | `<実測: アーキテクチャ調査書§2から転記>` |
| バージョン | `<実測>` |
| 選定理由 | `<実測: package.json・READMEコメント等から読み取れる場合のみ記載。読み取れない場合は「実在しない（理由: コード上に選定理由の記述なし）」>` |

---

## §2 ビルド設定（実測）

| 項目 | 内容 |
|---|---|---|
| ビルドツール | `<実測: アーキテクチャ調査書§3から転記>` |
| 環境別設定差分 | `<実測: 開発/本番等の設定ファイル差分>` |

---

## §3 デプロイ構成（実測）

| 項目 | 内容 |
|---|---|---|
| デプロイ先 | `<実測 または 実在しない（理由: …）>` |
| CI/CD パイプライン | `<実測 または 実在しない（理由: …）>` |

---

## §4 ディレクトリ構成方針（実測）

対象はディレクトリ個々の命名規則ではなく、ルートレベルの構成意図・責務分担です。命名規則は `./規約/ディレクトリ構成規約.md` を参照する。

| ディレクトリ | 構成意図 |
|---|---|---|
| `<実測: アーキテクチャ調査書§4のディレクトリ責務マップから転記>` | `<実測: 責務の要約>` |

---

## §5 環境変数管理（実測）

| 項目 | 内容 |
|---|---|---|
| 環境変数定義の所在 | `<実測: アーキテクチャ調査書§3から転記>` |
| 環境別の分離方式 | `<実測: .env.development/.env.production等の分離有無>` |

---

## §6 認証セッション・ストレージ方式（実測）

認可・画面制御の詳細は各画面詳細設計書を参照する。

| 項目 | 方式 | 保存先（cookie/localStorage/sessionStorage/メモリ） | 有効期限・失効契機 |
|---|---|---|---|---|
| `<実測: 項目>` | `<実測: 方式>` | `<実測: 保存先>` | `<実測: 有効期限・失効契機>` |

---

## traced の条件

- 全節の実測値がアーキテクチャ調査書の転記または追加調査コマンドの実測結果で埋まっていること
