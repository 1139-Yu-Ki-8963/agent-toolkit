# バックアップ手順書

実際の書き方で検査を試すための見本。実行の間隔と起点、失敗したときの扱いを書いた形。

## 実行の間隔

毎日 03:00 に実行する。起点は前回の実行が終わった時刻とする。

## 手順

1. 対象のデータベースを読み取り専用にする
2. 全体の写しを取る
3. 読み取り専用を解く

## 失敗したときの扱い

いずれかの手順が失敗した場合、状態を「中断」とし、読み取り専用を必ず解いてから止める。
3 回まで再実行し、それでも失敗すれば担当者へ知らせる。
