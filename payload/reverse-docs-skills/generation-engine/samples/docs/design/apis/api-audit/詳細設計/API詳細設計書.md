---
# API詳細設計書テンプレート（unit_kind=api・1エンドポイント1枚）
api_key: audit
api_id: api-audit
method: GET
path: /api/audit
feature_key: FileUploader
source_ref: src/api/audit.py
unit_kind: api
# chapter_map: 章の役割キーと §番号の対応（生成器と検査が章を特定するために使う機械向けの対応表。本文には表示しない）。
chapter_map:
  - role: API概要
    section: 1
  - role: APIリクエスト
    section: 2
  - role: APIレスポンス
    section: 3
  - role: API処理フロー
    section: 4
  - role: APIロジック
    section: 5
  - role: API業務ルール
    section: 6
  - role: APIエラー
    section: 7
  - role: API非機能要件
    section: 8
  - role: API関連資料
    section: 9
---

# audit API詳細設計書



**この文書の位置づけ**（別の言語で作り直す場合は、まず「仕様」から読む）

- 仕様: §1 API概要、§2 リクエスト、§3 レスポンス、§4 処理フロー、§5 ロジック、§6 業務ルールとバリデーション、§7 エラー、§8 非機能、§9 関連資料
- 現行実装: 該当なし
- 仕様／現行実装（詳細は各節の太字1行を見る）: 該当なし

本書は 1 つのエンドポイントの実装契約を記します。情報源は API 一覧の拡張マニフェストと実装コードの読解です。

実装から確定できない事項は推測で埋めず、本書の空欄のままとする。

## §1 API概要

**この節の位置づけ: 仕様**

### 1.1 基本情報

| 項目 | 値 |
|---|---|
| APIキー | audit |
| API ID | api-audit |
| メソッド | GET |
| パス | /api/audit |
| 所属する機能キー | FileUploader |
| 認証の要否 | 要 / 不要 |
| 確からしさ | high / medium / low |
| 判定方法 | （マニフェストの検出手法） |

### 1.2 業務上の役割

## §2 リクエスト

**この節の位置づけ: 仕様**

### 2.1 パスパラメータ

| 名前 | 型 | 必須 | 有効な範囲 | NULL許容 | 初期値 | 桁と精度 | 制約 |
|---|---|---|---|---|---|---|---|
| userId | 文字列 | 必須 | 利用者の識別子として実在する値 | 許容しない | なし | 制限を設けない | 利用者を指定する窓口でのみ使う |

### 2.2 クエリパラメータ

| 名前 | 型 | 必須 | 有効な範囲 | NULL許容 | 既定値 | 桁と精度 | 制約 |
|---|---|---|---|---|---|---|---|
| page | 整数 | 任意 | 1以上 | 許容しない | 1 | 整数の桁のみ | 記録の一覧を頁で区切る |
| page_size | 整数 | 任意 | 1以上200以下 | 許容しない | 50 | 整数の桁のみ | 200を超える値は200へ切り下げる |
| severity | 文字列 | 任意 | 警告の重大度として定義済みの値 | 許容する | なし | 制限を設けない | 未指定なら警告の全件を返す |

### 2.3 ヘッダ

| 名前 | 必須 | 有効な範囲 | NULL許容 | 初期値 | 桁と精度 | 用途 |
|---|---|---|---|---|---|---|
| Authorization | 必須 | 発行済みの認証の値 | 許容しない | なし | 制限を設けない | 呼び出し元を識別する |

### 2.4 ボディ

| 項目 | 型 | 必須 | 有効な範囲 | NULL許容 | 初期値 | 桁と精度 | 制約 |
|---|---|---|---|---|---|---|---|
| 本文-なし | 該当なし | 該当なし | 参照のみの窓口であり本文を受け取らない | 該当なし | 該当なし | 該当なし | 本文を伴う呼び出しは受け付けない |

## §3 レスポンス

**この節の位置づけ: 仕様**

### 3.1 正常系の構造

<!-- 本表がレスポンス（戻り値）の構造を表す。項目列を戻り値のフィールド名として扱う -->

| 項目 | 型 | 常に存在 | 有効な範囲 | NULL許容 | 初期値 | 桁と精度 | 意味 |
|---|---|---|---|---|---|---|---|
| items | 配列 | する | 0件以上の記録 | 許容しない | 空の配列 | 制限を設けない | 求めた条件に合う記録の並び |
| page | 整数 | 一覧の窓口でする | 1以上 | 許容しない | 1 | 整数の桁のみ | いま返している頁の番号 |
| pageSize | 整数 | 一覧の窓口でする | 1以上200以下 | 許容しない | 50 | 整数の桁のみ | 1頁あたりの件数 |
| userId | 文字列 | 利用者の窓口でする | 求めた利用者の識別子 | 許容しない | なし | 制限を設けない | どの利用者の記録かを示す |
| retentionDays | 整数 | 保持の設定の窓口でする | 1以上 | 許容しない | 365 | 整数の桁のみ | 記録を残す日数 |
| archiveEnabled | 真偽値 | 保持の設定の窓口でする | 真または偽 | 許容しない | 真 | 該当なし | 保管へ写す動きが有効か |

### 3.2 ステータスコード一覧

| コード | 発生条件 | 本文の形 |
|---|---|---|

### 3.3 空・境界の扱い

| 状況 | 返す内容 |
|---|---|

## §4 処理フロー

**この節の位置づけ: 仕様**

### 4.1 処理順序

| 順 | 処理 | 呼び出す関数 |
|---|---|---|

### 4.2 内部呼び出し

| 呼び出し先 | 引数 | 戻り値 |
|---|---|---|

### 4.3 外部呼び出し

| 呼び出し先 | 契機 | 失敗時の扱い |
|---|---|---|

## §5 ロジック

**この節の位置づけ: 仕様**

### 5.1 分岐

| キー | 条件 | 真の場合 | 偽の場合 |
|---|---|---|---|
| 頁の大きさ-上限 | 求められた1頁あたりの件数が上限を超えるか | 上限の値へ切り下げる | 求められた値をそのまま使う |
| 利用者の記録-不在 | 指定した利用者の記録が1件も無いか | 見つからない旨の誤りを送出する | 記録の一覧を返す |
| 重大度-未指定 | 重大度の指定が無いか | 警告の全件を返す | 指定した重大度に合うものだけを返す |

### 5.2 繰り返し

| キー | 対象 | 継続条件 | 打ち切りの条件 |
|---|---|---|---|
| 書き出し-流し込み | 監査の記録の全行 | 次の行がある間 | 全行を流し終えたとき |

### 5.3 判定表

| キー | 入力の組み合わせ | 結果 |
|---|---|---|
| 重大度-絞り込み | 重大度の指定あり、かつ合う警告が存在する | 合う警告だけを返す |
| 重大度-指定なし | 重大度の指定なし | 警告の全件を返す |

## §6 業務ルールとバリデーション

**この節の位置づけ: 仕様**

### 6.1 入力検査

| 対象 | 検査内容 | 違反時の応答 |
|---|---|---|

### 6.2 業務判断

| キー | 判断内容 | 判断材料 |
|---|---|---|

### 6.3 権限

| 利用者区分 | 可否 | 判定材料 |
|---|---|---|

## §7 エラー

**この節の位置づけ: 仕様**

### 7.1 エラー一覧

| キー | 発生条件 | 応答コード | 利用者に見える結果 |
|---|---|---|---|
| 利用者-不在 | 指定した利用者の記録が1件も無い | 404 | 該当する記録が無い旨を示す |
| 頁-範囲外 | 求めた頁に記録が無い | 200 | 空の一覧を返す |
| 認証-不足 | 認証の値が無いか有効でない | 401 | 認証をやり直す案内を示す |

### 7.2 メッセージ定義との対応

| メッセージID | 本文 | 使う箇所 |
|---|---|---|

## §8 非機能

**この節の位置づけ: 仕様**

### 8.1 件数と量の制限

| 対象 | 上限 |
|---|---|

### 8.2 タイムアウトと再試行

| 対象 | 方針 |
|---|---|

### 8.3 冪等性

| 観点 | 性質 |
|---|---|

## §9 関連資料

**この節の位置づけ: 仕様**

### 9.1 観測の出どころ

本文の実装仕様を読み取った実装箇所を記録する。ファイルと行はこの表だけに書き、本文へ括弧書きで埋め込まない。

| 対応する節・項目 | ファイル | 行 |
|---|---|---|

関連する資料を次の表に記録します。

| 資料 | パス | 本書との関係 |
|---|---|---|

