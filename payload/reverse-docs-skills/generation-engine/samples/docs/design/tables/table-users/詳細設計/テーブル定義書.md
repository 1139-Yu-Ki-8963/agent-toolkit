---
# テーブル定義書テンプレート（unit_kind=table・1テーブル1枚）
table_key: users
table_id: table-users
table_name: users
# table_subkind の値域: table（物理テーブル）/ view（ビュー）/ migration（マイグレーション）
table_subkind: table
source_ref: 001_create_users.sql
unit_kind: table
status: draft
---

# users テーブル定義書

<!-- 禁止事項: 原本の schema 定義（マイグレーションファイル・ORM のモデル定義・SQL の DDL）に
     書かれていない値を推測で埋めない。確定できない項目は空欄のままとする。 -->

## §1 構成要素

処理を担う単位を列挙する。クラスを持たない実装では、ファイルと関数の単位で記す。

| 要素名 | 種別 | 可視性 | 所在 |
|---|---|---|---|
| users | 表 | 公開 | 001_create_users.sql |

## §2 処理の定義

| 要素名 | 処理の概要 | 前提条件 | 事後条件 | 送出する例外 |
|---|---|---|---|---|
| users | 利用者の識別と連絡先、所属、作成の時点を保持する | 所属先の表が先に作られていること | 利用者1件につき1行を保持する | 一意の制約と外部キーの制約に反したときに実行時の誤りを返す |

## §3 ロジック

### 3.1 分岐

| キー | 条件 | 真の場合 | 偽の場合 |
|---|---|---|---|
| 分岐-なし | 表の定義であり手続きの分岐を持たない | 該当なし | 該当なし |

### 3.2 繰り返し

| キー | 対象 | 継続条件 | 打ち切りの条件 |
|---|---|---|---|
| 繰り返し-なし | 表の定義であり繰り返しを持たない | 該当なし | 該当なし |

### 3.3 判定表

| キー | 入力の組み合わせ | 結果 |
|---|---|---|
| 判定表-なし | 表の定義であり入力の組み合わせによる分岐を持たない | 該当なし |

## §4 疑似コード

<!-- 分岐と繰り返しの入れ子だけを日本語で書く。関数名は §8 実装契約（8.1 ファイルと関数）
     が定めるものを使ってよい。実装言語の記法（メソッド呼び出しの矢印・ハッシュの区切り等）
     と実装の変数名は使わない。変数の役割を述べる必要がある場合は役割を表す日本語
     （実行コンテキストの利用者情報 の類）を用いる。
     実際のコードを貼ることを禁じる。原本の行を指したい場合は §8 実装契約の「ファイルと関数」から辿らせる。 -->

`PSEUDOCODE`

## §5 入出力の値

<!-- 区分列は 入力 / 出力 に加えて 戻り値 も表す。戻り値を記録する場合は 区分=戻り値 の行を追加する -->

| 要素名 | 区分 | 名前 | 型 | 有効な範囲 | 空値の許容 | 初期値 | 桁と精度 |
|---|---|---|---|---|---|---|---|
| users | 入力 | id | 64ビットの整数 | 主キーとして一意の値 | 許容しない | なし | 整数の桁のみ |
| users | 入力 | email | 可変長の文字列 | 255文字まで | 許容しない | なし | 最大255文字 |
| users | 入力 | organization_id | 64ビットの整数 | 所属先の表に存在する識別子 | 許容する | なし | 整数の桁のみ |
| users | 入力 | created_at | 時刻 | 作成の時点 | 許容しない | なし | 実装が定める精度 |

## §6 エラー処理

| キー | 送出または捕捉の箇所 | エラーコード | リトライの判定 | ログの出力点 |
|---|---|---|---|---|
| 主キー-重複 | 行の追加 | データベースが返す一意制約の違反 | 再実行しない | データベースの記録に残る |
| 必須-未設定 | 行の追加と更新 | データベースが返す非空制約の違反 | 再実行しない | データベースの記録に残る |
| 外部キー-不一致 | 行の追加と更新 | データベースが返す外部キー制約の違反 | 再実行しない | データベースの記録に残る |

## §7 データ定義

### 7.1 カラム定義

| 物理名 | 論理名 | 型 | 長さ | 必須 | 既定値 | 制約 |
|---|---|---|---|---|---|---|
| id | 識別子 | BIGINT | 制限を設けない | 必須 | なし | 主キー |
| email | 連絡先 | VARCHAR | 255 | 必須 | なし | 非空 |
| organization_id | 所属先の識別子 | BIGINT | 制限を設けない | 任意 | なし | 所属先の表への外部キー |
| created_at | 作成の時点 | TIMESTAMP | 制限を設けない | 必須 | なし | 非空 |

### 7.2 索引

| 索引名 | 対象カラム | 一意性 |
|---|---|---|

### 7.3 外部キー

| カラム | 参照先のテーブル | 参照先のカラム | 削除時の動作 | 関連の種別 |
|---|---|---|---|---|
| organization_id | organizations | id | 指定しない | 多対一 |
| 疑似検証FK | users | id | RESTRICT | 一対多 |

### 7.4 整合性のチェック点

| 対象 | チェックの内容 | 実施の箇所 |
|---|---|---|

## §8 実装契約

### 8.1 ファイルと関数

| ファイル | 関数 | 役割 | 参照先 |
|---|---|---|---|

### 8.2 型定義

| 型名 | フィールド | 型 | 参照先 |
|---|---|---|---|

### 8.3 依存

| 依存先 | 用途 | 参照先 |
|---|---|---|

## §9 関連資料

他文書の所在はこの節へまとめる。確定できなかった事項は設計書へ残さず、本書の空欄のままとする。

| 資料 | パス | 本書との関係 |
|---|---|---|
| 対応する設計書・テスト設計書 | `../基本設計/テーブル定義書.md` | 本書と対応する他文書 |
