---
doc_id: unit-test-spec-screen-sales-dashboard
type: unit-test-spec
status: draft   # draft | implemented（テストコードが実装され、本書の各ケースがテストコードから参照される状態）
target_screen: screen-sales-dashboard
source_design: ../詳細設計/画面詳細設計書.md
unit_test_sheet: ../詳細設計/単体テスト観点表.md
updated: 2026-07-24
---

# 売上ダッシュボード 単体テスト仕様書

本書は `../詳細設計/単体テスト観点表.md` の各観点キーに対応する、具体的な入力値・手順・期待結果（アサーション）を記述する。観点表は「何を検証するか」の正であり、本書は Phase 4（設計書からのTDD実装）の SPEC ステップで実際にテストコードへ落とし込む前の、具体的ケースの設計時契約である。

**実装後はテストコードが正になる**（§15 実装契約と同じ扱い）。実装後、本書の各行は「実装後のテストファイル参照」列にファイルパスを記入し、以後の変更はテストコード側で行う。本書の入力値・アサーションを実装後に手で書き換えてはならない（乖離があれば観点表の乖離分類で処理する）。

## 本書に書かないもの

| 書かない内容 | 正の置き場 |
|---|---|
| 抽象的な観点（何を検証すべきかの一般論） | `../詳細設計/単体テスト観点表.md` |
| 実装後のテストコードの変更管理 | テストコード自体 |
| 複数画面にまたがる結合的な検証 | `../詳細設計/結合テスト観点表.md` + `結合テスト仕様書.md` |

## テストケース一覧

<!-- キーは連番禁止。単体テスト観点表.mdの観点キーと1:1または1:多で対応させる -->

| キー | 対応観点キー | 入力値 | 期待結果（アサーション） | 実装後のテストファイル参照 |
|---|---|---|---|---|
| 客単価-除算結果と一致 | 客単価-算出ロジック | `totalSalesAmount: 1000000`, `orderCount: 200` | `averageOrderValue` が `5000` を返す | |
| 開始終了正順-妥当性true | 期間妥当性-判定 | `periodRange.from: "2026-07-01"`, `periodRange.to: "2026-07-31"` | `isPeriodValid` が `true` を返す | |
| 開始終了逆転-即時エラー | 入力検証-期間逆転即時エラー | `from: "2026-07-31"`, `to: "2026-07-01"` | エラーが即座に表示され期間適用ボタンが無効化される | |
| 期間367日-上限超過無効化 | 入力検証-期間上限366日 | `from: "2026-01-01"`, `to: "2027-01-03"`（367日間） | 期間適用ボタンが無効化される | |
| url復元-期間集計単位反映 | url復元-期間集計単位書き戻し | URLパラメータ `from=2026-07-01&to=2026-07-31&granularity=monthly` | `periodRange`・`granularity` が該当値へ書き戻される | |
| ドリルダウン-カテゴリ期間引継push | 遷移-ドリルダウンpush | カテゴリ別内訳行クリック、`category: "家電"` | 集計期間・カテゴリを伴い `push` で注文一覧へ遷移する | |
| 再集計実行中-ボタン無効化 | 再集計活性-連打抑止 | `isRecalculating: true` | 再集計ボタンが無効化され連打しても二重実行されない | |
