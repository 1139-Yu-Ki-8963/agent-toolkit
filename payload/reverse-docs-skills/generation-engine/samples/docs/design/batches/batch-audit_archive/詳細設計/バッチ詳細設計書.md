---
# バッチ詳細設計書テンプレート（unit_kind=batch・1バッチ1枚）
batch_key: audit_archive
batch_id: batch-audit_archive
batch_name: audit_archive
# batch_trigger_type の値域: scheduled（時刻起動）/ triggered（外部トリガー起動）
batch_trigger_type: scheduled
source_ref: src/jobs/audit_archive.py
unit_kind: batch
status: draft
---

# audit_archive バッチ詳細設計書

<!-- 禁止事項: 実装コードに書かれていない値を推測で埋めない。確定できない項目は空欄のままとする。 -->

## §1 構成要素

処理を担う単位を列挙する。クラスを持たない実装では、ファイルと関数の単位で記す。

| 要素名 | 種別 | 可視性 | 所在 |
|---|---|---|---|
| audit_archive | 関数 | 公開 | src/jobs/audit_archive.py |

## §2 処理の定義

| 要素名 | 処理の概要 | 前提条件 | 事後条件 | 送出する例外 |
|---|---|---|---|---|
| audit_archive | 監査の記録を保管用の表へ写し、写した元を片付ける処理を待ち行列へ積む | 監査の記録の表と保管用の表がどちらも存在すること | 保管用の表へ写し終え、片付けの依頼が待ち行列へ入っている | データベースと待ち行列が送出する実行時の例外 |

## §3 ロジック

### 3.1 分岐

| キー | 条件 | 真の場合 | 偽の場合 |
|---|---|---|---|
| 分岐-なし | この処理は条件分岐を持たない | 該当なし | 該当なし |

### 3.2 繰り返し

| キー | 対象 | 継続条件 | 打ち切りの条件 |
|---|---|---|---|
| 繰り返し-なし | この処理は繰り返しを持たない。写しは1文の問い合わせで一度に行う | 該当なし | 該当なし |

### 3.3 判定表

| キー | 入力の組み合わせ | 結果 |
|---|---|---|
| 判定表-なし | この処理は入力の組み合わせによる分岐を持たない | 該当なし |

## §4 疑似コード

<!-- 分岐と繰り返しの入れ子だけを日本語で書く。関数名は §8 実装契約（8.1 ファイルと関数）
     が定めるものを使ってよい。実装言語の記法（メソッド呼び出しの矢印・ハッシュの区切り等）
     と実装の変数名は使わない。変数の役割を述べる必要がある場合は役割を表す日本語
     （実行コンテキストの利用者情報 の類）を用いる。
     実際のコードを貼ることを禁じる。実装の行を指したい場合は §8 実装契約の「ファイルと関数」から辿らせる。 -->

`PSEUDOCODE`

## §5 入出力の値

<!-- 区分列は 入力 / 出力 に加えて 戻り値 も表す。戻り値を記録する場合は 区分=戻り値 の行を追加する -->

| 要素名 | 区分 | 名前 | 型 | 有効な範囲 | 空値の許容 | 初期値 | 桁と精度 |
|---|---|---|---|---|---|---|---|
| audit_archive | 入力 | database | データベースへの接続 | 問い合わせを実行できる接続 | 許容しない | なし | 制限を設けない |
| audit_archive | 入力 | queue | 待ち行列への接続 | 依頼を積める接続 | 許容しない | なし | 制限を設けない |
| audit_archive | 戻り値 | なし | なし | 値を返さない | 該当なし | なし | 該当なし |

## §6 エラー処理

| キー | 送出または捕捉の箇所 | エラーコード | リトライの判定 | ログの出力点 |
|---|---|---|---|---|
| 写し-失敗 | 保管用の表への書き込みの呼び出し | 独自のコードを定めない | 再実行しない | 記録の出力を持たない |
| 依頼-失敗 | 待ち行列への積み込みの呼び出し | 独自のコードを定めない | 再実行しない | 記録の出力を持たない |

## §7 データ定義

### 7.1 扱うデータ構造

| 名前 | 型 | 有効な範囲 | 寿命 |
|---|---|---|---|
| 監査の記録 | 表の行の集合 | 監査の記録の表にある全行 | 写し終えるまで元の表に残る |
| 保管用の記録 | 表の行の集合 | 写した行の全体 | 保管用の表に残り続ける |
| 片付けの依頼 | 待ち行列の要素 | 片付けを表す1件 | 待ち行列が処理し終えるまで |

### 7.2 コミットの単位

| 対象 | コミットの単位 | 件数の目安 |
|---|---|---|

### 7.3 排他の制御

| 対象 | 排他の方式 | 取得と解放の箇所 |
|---|---|---|

### 7.4 件数と実行時間の目安

| 対象 | 件数 | 実行時間 |
|---|---|---|

## §8 実装契約

### 8.1 ファイルと関数

| ファイル | 関数 | 役割 | 参照先 |
|---|---|---|---|

### 8.2 型定義

| 型名 | フィールド | 型 | 参照先 |
|---|---|---|---|

### 8.3 依存

| 依存先 | 用途 | 参照先 |
|---|---|---|

## §9 関連資料

他文書の所在はこの節へまとめる。確定できなかった事項は設計書へ残さず、本書の空欄のままとする。

| 資料 | パス | 本書との関係 |
|---|---|---|
| 対応する設計書・テスト設計書 | `../基本設計/バッチ基本設計書.md` | 本書と対応する他文書 |
