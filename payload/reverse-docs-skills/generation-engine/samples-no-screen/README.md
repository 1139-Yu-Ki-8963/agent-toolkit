# samples-no-screen

画面を持たないプロジェクト（バックエンドのみ等）のポータル見本。6 種別すべてを該当なし記録として扱う経路の見本です。

再生成:

```
bash generation-engine/scripts/build-portal.sh <target_repo_path> generation-engine/samples-no-screen generation-engine/samples-no-screen --generated-at 2026-08-01T00:00:00+09:00
```

この見本は `test-portal-conventions.sh` の検査対象外である（検査は `generation-engine/samples/` を代表として参照するため）。壊れの検知は再生成コマンドの実行結果で行う。
