# 検証で繰り返し指摘された事項のうち未解決のもの

外部の検証（この配布物を対象コードに適用しない状態で、配布物そのものを対象コードとして機械検証すること）で繰り返し指摘され、内部の記録では「完了」と記録した後も指摘が再発した事項を、ここへ記載する。台帳側の記録（`docs/tasks/指摘改善一覧.md`）はこのリポジトリ自身の作業記録であり配布対象外のため、配布物に同梱するこのファイルへ写す。

各項目は「現象」「現在の実測」「なぜ完全には解決していないか」「解決の条件」の4点で書く。実測には検証した時点のコミットハッシュを添える（`.claude/rules/always/verification/reverse-verification/rule.md` 規約4に準じる）。

## 未解決の事項

現時点で未解決の事項は無い。以前に記載した6件は、いずれも実測により解消を確かめた。下記の「解消済みの事項」を参照する。

## 参考: 解消済みの事項

次の6件は、実測により解消を確かめた。検証した時点のコミットは `6caeddb1e66fae56c607d7cc9f926260fce2dd6c` である。

### ブラウザを使う自己テストの全体完走が、実行のたびに未確認のまま残る

通しで完走した。確かめたコマンドは `bash generation-engine/scripts/build-portal.sh --self-test` である。実行62件、不合格0件、判定不能0件だった。

実ブラウザの計測も含まれた。用語辞書ページの意味投影とUI契約の検証は `semantic glossary page runtime: PASS` を返した。検証した項目は絞り込みの切り替え・押下状態・改行キー・取り消しキー・焦点・展開状態・狭い画面での溢れである。生成した詳細ページ3種の実描画は `generated detail pages runtime: PASS (3 pages, pageerror=0)` を返した。

以前の記録は所要時間を815秒超と見ていたが、実測では大きく下回った。飛ばされた検査は1件のみで、表を持たないページに対する見出しの固定の検査である。これは対象が無いことによる正当な除外である。

### 設計文書だけからの一覧組み立てが、原本コードの参照解決に失敗し得る

`build-manifests-from-docs.sh` へ `--source-file-root` を足した。`source_ref` の実在確認を、設計文書の配置先ではなく対象プロジェクトのルートから行える。`build-portal.sh` からも渡される。確かめたコマンドは `grep -c source-file-root generation-engine/scripts/build-portal.sh` である。2件だった。

### 検証スクリプト群の集約実行で、失敗が0件にならない状態が繰り返し観測される

第1層の集約が失敗0本になった。確かめたコマンドは `bash generation-engine/scripts/verification/run-layer-machine-checks.sh` である。対象261本のうち成功257本、失敗0本だった。判定不能4本は実行環境に隔離環境が無いためであり、成果物の欠陥ではない。

### 詳細ページ3種別が、共通エンジンの既定値のままではポータル入口へ戻れない

3種別すべてで戻るリンクが正しくなった。確かめたコマンドは `bash docs/scripts/check-detail-page-backlink.sh` である。終了コード0だった。

### 置き場の値と表示見出しが分離済みだが、名前の衝突を検出する検査が無い

検査が第1層の集約に載った。確かめたコマンドは `bash generation-engine/scripts/verification/run-layer-machine-checks.sh --list` である。`check-portal-label-collision` が一覧に含まれる。

### 承認条件検査が、後発の課題対応で前提の見出しを失い機能停止した

現行テンプレートに対して異常終了しなくなった。確かめたコマンドは `bash docs/scripts/check-confirmation-ledger-current-template.sh` である。終了コード0だった。

## 参考: 資料参照切れの指摘は、機械検査の常設によって解消済みである

配布物内の資料参照（相対リンク）が実在しないファイルを指す指摘は、`check-payload-references.sh` による機械検査で継続的に確認できる状態になっている。同スクリプトは `--self-test` を実装しており、第1層の集約実行（`run-layer-machine-checks.sh`）が対象スクリプトを動的に収集する際の対象に自動的に含まれる（`run-layer-machine-checks.sh --list` で確認済み）。このため、資料参照切れが再発した場合は、往復検証のたびに実行する第1層の集約実行で機械的に検知される。現在の実測（コミット `6caeddb1e66fae56c607d7cc9f926260fce2dd6c` 時点）では `走査 79 件 / 違反 0 件` である。
