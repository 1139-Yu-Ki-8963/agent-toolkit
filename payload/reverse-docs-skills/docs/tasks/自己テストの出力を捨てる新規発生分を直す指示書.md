**状態**: 着手できる
**優先度**: 中
**前提**: なし
**元の指摘**: 1-57

## この指示書は何か

自己テストが失敗の理由を出力ごと捨てる形が、1-57の是正後に4本の新しいファイルへ再び持ち込まれた。この4本を、1-57が確立した形（失敗時だけ出力を添える形）へ揃える指示書である。

## なぜ必要か

1-57は2026-08-21に、`generation-engine/scripts/`配下の該当箇所を257件から13件へ減らした。修正の形は決まっている。検査対象コマンドの出力を変数へ受け、自己テストがFAILと判定する分岐でのみその出力を添える。

2026-08-25に同じ検出方法（`grep -rn "FAIL\]" <対象> --include="*.sh" -B3 | grep -c ">/dev/null 2>&1"`）で数え直した。`generation-engine/scripts/`配下は23件だった。13件から10件増えていた。

増えた10件は、1-57の是正より後に追加した4本のファイルにある。

| ファイル | 件数 |
|---|---|
| `tests/check-detail-design-frontmatter-keys.sh` | 4 |
| `unit-list/build-unit-list.sh` | 2 |
| `unit-list/build-screen-list.sh` | 2 |
| `unit-list/build-feature-list.sh` | 2 |

いずれも1-57の是正が確立した形を知らないまま書かれ、同じ構造（失敗の直前で出力を捨てる形）を再び持ち込んでいた。既存の13件（`scaffold-rule-definitions.sh`の5件を含む）は本指示書の対象外とし、変更しない。1-57の記録が理由を添えて対象外と判断した箇所であり、判断を覆す新しい根拠が無い。

## やること

1. `tests/check-detail-design-frontmatter-keys.sh`の4箇所を直す
2. `unit-list/build-unit-list.sh`の2箇所を直す
3. `unit-list/build-screen-list.sh`の2箇所を直す
4. `unit-list/build-feature-list.sh`の2箇所を直す
5. 各箇所は、検査対象のコマンドを`out="$(... 2>&1)"`のように変数へ受ける形に変える。成功時の出力は変えない。失敗を報告する行でのみ、受けた出力を`printf '%s\n' "$out" | sed 's/^/    /' >&2`のように添える
6. 直した各ファイルで`bash -n`を実行し、構文が壊れていないことを確かめる
7. 直した各ファイルの`--self-test`を実行し、直す前と同じ件数が同じ結果（PASS/FAIL）で終わることを確かめる
8. 故意に対象を失敗させる形（既存のフィクスチャの改変、またはダミー入力の追加）で、失敗の理由が出力に現れることを1箇所ずつ確かめる

## 完了の判定

1. `grep -rn "FAIL\]" generation-engine/scripts/ --include="*.sh" -B3 | grep -c ">/dev/null 2>&1"` が13である
2. `bash generation-engine/scripts/tests/check-detail-design-frontmatter-keys.sh --self-test` が終了コード0で終わる
3. `bash generation-engine/scripts/unit-list/build-unit-list.sh --self-test` が終了コード0で終わる
4. `bash generation-engine/scripts/unit-list/build-screen-list.sh --self-test` が終了コード0で終わる
5. `bash generation-engine/scripts/unit-list/build-feature-list.sh --self-test` が終了コード0で終わる

## 触らない範囲

- 既存の13件。1-57の記録が個別に対象外と判断済みである。内訳は`docs/tasks/指摘改善一覧.md`の1-57節を参照する
- `delivery-payload/templates/rules/checkers/`配下（9件）と`docs/scripts/`配下（9件）。1-57の反映先に含まれない。対象へ含めるかどうかは「決めていないこと」を参照する
- 各ファイルの検査ロジックそのもの。出力の扱いだけを変える

## 決めていないこと

| 何を決めるか | 既定（迷ったらこれを選ぶ） | 覆すときの条件 |
|---|---|---|
| `delivery-payload/templates/rules/checkers/`と`docs/scripts/`を対象へ含めるか | 含めない。1-57の反映先はもともと`generation-engine/scripts/`配下だけであり、範囲を広げる決定は本指示書の範囲を超える | 別の指示書として、この2箇所を対象にする決定がされた場合 |
| 直す順番 | 表の順（`check-detail-design-frontmatter-keys.sh`→`build-unit-list.sh`→`build-screen-list.sh`→`build-feature-list.sh`）。ファイル単位で独立しており、順番に依存する処理は無い | 特に無い |

## 他の指示書との関係

- `docs/tasks/指摘改善一覧.md`の1-57から本指示書へ追跡できる
- 1-57自身の是正作業（2026-08-21・46ファイル）とは対象が重ならない。本指示書は1-57の是正より後に増えた分だけを扱う
- 他の指示書には依存せず、本指示書だけで着手できる

## この指示書の位置づけ

1-57が確立した修正の形をそのまま適用するだけで完結する、単独の小さな指示書である。設計判断は不要で、そのまま実装に着手できる。

## 対応の記録

### 判定の充足状況

| 判定 | 確かめる手段 | 状態 | コミット | 確かめた内容 |
|---|---|---|---|---|
| 1. `generation-engine/scripts/`配下の該当箇所が13件になる | `grep -rn "FAIL\]" generation-engine/scripts/ --include="*.sh" -B3 \| grep -c ">/dev/null 2>&1"` | 未着手 | — | — |
| 2. `check-detail-design-frontmatter-keys.sh`の自己テストが通る | `bash generation-engine/scripts/tests/check-detail-design-frontmatter-keys.sh --self-test` | 未着手 | — | — |
| 3. `build-unit-list.sh`の自己テストが通る | `bash generation-engine/scripts/unit-list/build-unit-list.sh --self-test` | 未着手 | — | — |
| 4. `build-screen-list.sh`の自己テストが通る | `bash generation-engine/scripts/unit-list/build-screen-list.sh --self-test` | 未着手 | — | — |
| 5. `build-feature-list.sh`の自己テストが通る | `bash generation-engine/scripts/unit-list/build-feature-list.sh --self-test` | 未着手 | — | — |

### 判断の記録

| 何を決めたか | 選んだもの | 選んだ理由 | 覆すと何が変わるか |
|---|---|---|---|
| 対象範囲 | `generation-engine/scripts/`配下の新規4ファイルのみ | 1-57の元の反映先と同じ範囲に揃えた。範囲を広げると、既に対象外と判断した13件の扱いも同時に問い直すことになる | 対象を広げる場合は`delivery-payload/templates/rules/checkers/`と`docs/scripts/`を含む新しい完了の判定を追加する |
