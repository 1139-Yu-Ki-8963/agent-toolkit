**状態**: 着手できる
**優先度**: 中
**前提**: なし
**元の指摘**: 1-263

## 1. この指示書は何か

用語辞書の2本の検査が隔離環境の不在で判定不能になる状況について、実行環境を整備する手順を文書化する指示書である。

## 2. なぜ必要か

隔離環境がない状態では、`test-validate-semantic-glossary.sh` は `.venv/bin/python` を実行できずに終了コード2を返す。
`test-project-semantic-glossary-race.sh` は `python3` へ切り替えた後に PyYAMLを読み込めずに終了コード2を返す。
実測出力は「test -x で .venv/bin/python を実行可能なファイルとして確認できませんでした」と示した。
実測出力は「python3 への切替後に PyYAMLの読み込みに失敗」と示した。
判定不能は成果物の不合格ではないが、実行環境の整備方法がREADMEにないため再実行の経路が分からない。

## 3. やること

`docs/tasks/指摘改善一覧.md` へ1-263を起票する。
`generation-engine/scripts/glossary/README.md` へ隔離環境の整備手順と判定不能の意味を記載する。
検査中に隔離環境を自動作成しない文書化方針を採用する。
`.venv` を作成せず、版管理にも追跡させない。

## 4. 完了の判定

1. `bash docs/scripts/report-progress.sh` が `総数: 121件` を出して終了コード0を返す。
2. `bash docs/scripts/report-progress.sh --self-test` が終了コード0を返す。
3. `bash .claude/rules/always/tasks/instruction-format/check-instruction-format.sh` が不合格0件で
   終了コード0を返す。
4. `bash generation-engine/scripts/tests/check-broken-verdict-rows.sh` が
   終了コード0を返す。
5. `git ls-files generation-engine/scripts/glossary/.venv` の出力が空である。
6. `git status --porcelain` の出力が空である。
対象2検査の終了コード0は自動化方針だけの追加条件であり、本指示書の完了判定には含めない。

## 5. 触らない範囲

`generation-engine/scripts/glossary/tests/test-validate-semantic-glossary.sh` は変更しない。
`generation-engine/scripts/detail-pages/test-project-semantic-glossary-race.sh` は変更しない。
`validate-semantic-glossary.py`、`validate-semantic-glossary.sh`、`requirements.txt`、`.gitignore` は変更しない。
`.venv` を追加しない。
判定を弱めて通す変更をしない。

## 6. 決めていないこと

| 事項 | 具体的な既定 |
|---|---|
| Python 3.13のパッチ版 | PATHで利用できるPython 3.13系の最新パッチ版を使う。 |
| 依存取得元 | 組織で許可された標準PyPIミラーから `requirements.txt` の指定どおり取得する。 |
| `GLOSSARY_PYTHON` を使う別実行系 | 使用せず、既定の `.venv/bin/python` を使う。 |

## 7. 他の指示書との関係

1-244は第1層集約で見つかった既存の失敗と判定不能の実測記録である。
1-262は集約が実行件数を読む対応であり、1-263は残った2本の判定不能について実行環境の整備経路を示すため、変更対象は独立し集約結果だけが関係する。

## 8. この指示書の位置づけ

この指示書は実行環境整備の運用を文書化するものである。
検査の判定条件を緩めたり、成果物の不合格を隠したりする変更ではない。

## 9. 対応の記録

### 判定の充足状況

| 判定 | 確かめる手段 | 状態 | コミット | 確かめた内容 |
|---|---|---|---|---|
| 1. 進捗集計が総数121件を出して終了コード0を返す | `bash docs/scripts/report-progress.sh` | 完了 | 0c1f8e4 | — |
| 2. 進捗集計自己テストが終了コード0を返す | `bash docs/scripts/report-progress.sh --self-test` | 完了 | 0c1f8e4 | — |
| 3. 指示書形式検査が不合格0件で終了コード0を返す | `bash .claude/rules/always/tasks/instruction-format/check-instruction-format.sh` | 完了 | 0c1f8e4 | — |
| 4. 壊れた判定行検査が終了コード0を返す | `bash generation-engine/scripts/tests/check-broken-verdict-rows.sh` | 完了 | 0c1f8e4 | — |
| 5. 隔離環境が版管理へ入っていない | `git ls-files generation-engine/scripts/glossary/.venv` | 完了 | 0c1f8e4 | — |
| 6. 作業ツリーが空である | `git status --porcelain` | 完了 | 0c1f8e4 | — |

### 判断の記録

| 何を決めたか | 選んだもの | 選んだ理由 | 覆すと何が変わるか |
|---|---|---|---|
| 対応方針 | 整備の手順を文書化する | 隔離環境は生成物であり、検査の実行ごとに依存取得を自動化すると実行環境とネットワークに依存するため。 | 自動作成へ変えると、検査スクリプトの依存取得失敗時の扱いと回帰検査を追加する。 |
| 自動化の採否 | 自動化しない | `python3.13` がPATHにない環境では自動作成しても判定不能が残り、実行時に環境を変更しない方が検査の責務が明確なため。 | 自動化すると、Python 3.13と依存取得が可能な環境では2本の検査を実行できる。 |
| 2本の検査の終了コード | 文書化方針では選択ルートの完了条件に含めない | 文書化方針では2本の検査の終了コード0を満たさない | 自動化方針へ変えると、2本の検査の終了コード0が追加の完了条件になる。 |
| `.venv` の扱い | 生成物として無視し追跡しない | `.gitignore` が `.venv/` を除外しており、環境固有の実行ファイルと依存をリポジトリへ含めないため。 | 追跡する場合は環境固有の生成物が差分へ入り、リポジトリの再現性と容量に影響する。 |
