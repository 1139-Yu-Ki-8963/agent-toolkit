# 片付き判定の実測が上限を超えると静的確認へ逃げてしまう問題を記録する指示書

**状態**: 着手できる
**優先度**: 高
**前提**: なし
**元の指摘**: なし

## この指示書は何か

片付き判定（`docs/scripts/judge-task-done.sh`）の「確かめる手段」欄は、実測のコマンドを書く前提だが、実測が判定器の1コマンドあたりの上限（既定120秒）を超える場合、実測の代わりに常に成功する静的な確認（文字列の存在確認等）へ差し替えられることがある。この差し替えは完了の判定そのものを満たしたことにはならないが、外形上は「確かめる手段が終了コード0を返す」ため、片付き判定は区別できない。

## なぜ必要か

「高速化のあとに目視へ退いた判定を戻す指示書」の前提である「自己テストの所要時間が規約の件数に追いつかれる問題を片付ける指示書」（`docs/tasks/done/`）の判定1は、完了の判定で「所要時間が宣言値の7割以下であること」（210秒以下）を求めている。実測は126秒・153秒・145秒・141秒であり210秒以下を満たすが、判定器の1コマンドあたりの上限120秒を超える。記録には次の差し替え理由が明記されている。

> 判定器の1コマンドあたりの上限120秒を超えるため、確かめる手段を所要時間の計測から、1件だけを作り直す呼び方の実在確認（`grep -q 'restrict_ckey'`）へ差し替えた

この差し替えにより、確かめる手段は常に成功する文字列存在確認になり、実際の所要時間が210秒を超えて再び遅くなっても、片付き判定はそれを検知できない。

もう1つ、この指示書自身が`docs/tasks/done/`へ移った時点で、片付き判定の通常走査から構造的に外れることも判明した。`docs/scripts/judge-task-done.sh`が通常走査で使うglob（`$TASKS_DIR/*.md`）は`done/`を含まないため、一度移った指示書は自動で再判定されない。起票時は、手動で`--only`を指定して`done/`配下のファイルへかけ直しても「判定不能」を返した。原因と修正後の結果は「対応の記録」に残す。

**この問題には既存の別枠機構がある。** `docs/tasks/done/走っていない検査を集約へ載せる指示書.md`が記録するとおり、`generation-engine/scripts/verification/run-layer-machine-checks.sh`の`declared_long_running_known()`という関数がある。上限を超えた対象を「打ち切り」ではなく「宣言済み長時間」として扱うための受け口である。起票時は`generation-engine/scripts/build-portal.sh`・`generation-engine/scripts/unit-list/rebuild-screen-derived-pages.sh`の2本が登録されていたが、現在の登録は0本である。登録できるのは「単体で実行すれば完走するが、時間が上限を超えるもの」に限る。**落ちているものを登録してはならない**。落ちている事実が見えなくなるためである。この指示書が例示した`grep -q 'restrict_ckey'`への差し替えは、この既存の仕組みが知られていなかったために生じたと考えられる。

## やること

1. `docs/tasks/done/`配下の「確かめる手段」欄のうち、判定文が実測（所要時間・回数等）を求めているにもかかわらず、静的な確認（文字列の存在確認・ファイルの実在確認など、値を測らない確認）へ差し替えられている行を洗い出す。「確かめた内容」欄に「上限を超えるため差し替えた」旨の記述があるかを目印に探す
2. 洗い出した各行について、対象のコマンドが「単体で実行すれば完走するが、時間が上限を超えるもの」に当たるかを確認する。当たる場合は`generation-engine/scripts/verification/run-layer-machine-checks.sh`の`declared_long_running_known()`へ相対パスを1行追加し、「確かめる手段」を静的確認から実測のコマンドへ戻す。落ちているものは登録してはならない
3. 登録しても解決しない場合（対象コマンドが単体実行でも完走しない・`run-layer-machine-checks.sh`の走査対象外である等）に限り、「決めていないこと」の既定に従い明示欄（注記）方式を使う
4. `docs/scripts/judge-task-done.sh`が`done/`配下を走査対象に含めていないことをコードで確認し、その事実を本指示書の対応の記録へ残す
5. 手動で`done/`配下のファイルを`--only`で判定すると「判定不能」を返す件の原因を調査する。原因を特定できた場合は直す。特定できない場合は別指示書として切り出す

## 完了の判定

1. 洗い出した対象行すべてに、静的確認である旨の注記が「確かめた内容」欄に存在する
2. `docs/scripts/judge-task-done.sh`が`done/`を走査しないことの確認結果が本指示書に記録されている
3. `done/`配下のファイルを手動で`--only`判定した際の「判定不能」の原因調査結果（特定できた場合は修正結果、できなかった場合は切り出した指示書名）が記録されている

## 触らない範囲

- `docs/scripts/judge-task-done.sh`の判定ロジック自体（本指示書は記録上の注記追加までを対象とし、判定器の改修は別指示書に委ねる。今日直したばかりの22件の自己テストと干渉するため）
- 差し替え済みのコマンド自体（`grep -q 'restrict_ckey'`等）。有効な確認ではあるため削除しない。注記を添えるだけ

## 決めていないこと

| 何を決めるか | 既定（迷ったらこれを選ぶ） | 覆すときの条件 |
|---|---|---|
| 直し方（別枠を設けるか、明示欄を設けるか） | まず既存の別枠（`declared_long_running_known()`。`generation-engine/scripts/verification/run-layer-machine-checks.sh`に実装済み。現在の登録は0本）へ登録することを試す。登録できる条件は「単体で実行すれば完走するが時間が上限を超えるもの」に限り、落ちているものは登録してはならない。登録できない場合に限り明示欄（注記）方式を使う（「確かめた内容」欄の末尾に固定文言「（静的確認のみ。実測は上限<既定120秒>を超えるため置けない）」を追記する） | 既存の別枠が対象を受け付けない構造上の理由が分かった場合（例: `run-layer-machine-checks.sh`の走査対象外の判定コマンドである等） |
| done/の再判定不能の扱い | 本指示書のやること4で原因調査までは行う。調査の結果、判定器のロジック変更が必要と分かった場合は、その修正は別指示書として切り出す（今日直したばかりの22件の自己テストと干渉するため、本指示書では判定器自体を変えない） | — |

## 他の指示書との関係

- 「高速化のあとに目視へ退いた判定を戻す指示書」「自己テストの所要時間が規約の件数に追いつかれる問題を片付ける指示書」の対応中に発見した
- 「検査の判定不能規約」（`.claude/rules/always/verification/indeterminate-result/rule.md`）が定める「判定不能」と「不合格」の区別の考え方と関連する

## この指示書の位置づけ

片付き判定の仕組み自体の穴（確かめる手段に静的確認を書ける設計上の隙、およびdone/移動後に再判定されない構造）を記録する指示書であり、個別の指示書1件の対応ではないため独立させた。

## 対応の記録

### 判定の充足状況

| 判定 | 確かめる手段 | 状態 | コミット | 確かめた内容 |
|---|---|---|---|---|
| 1. 対象行すべてに注記がある | `rows="$(rg -e '^\x7c [0-9]+\.[^\x7c]*所要時間[^\x7c]*\x7c' -e '^\x7c [0-9]+\.[^\x7c]*秒以内[^\x7c]*\x7c' -e '^\x7c [0-9]+\.[^\x7c]*回数[^\x7c]*\x7c' -e '^\x7c [0-9]+\.[^\x7c]*自己テスト[^\x7c]*終了コード[^\x7c]*\x7c' docs/tasks/done --glob '*.md')"; rows="$(printf '%s\n' "$rows" \| LC_ALL=C grep -v -e '目視' -e 'bash ' -e 'node ' -e 'python ' -e 'python3 ' -e 'npm ' -e 'npx ' -e 'ruby ' -e 'perl ')"; [ "$(printf '%s\n' "$rows" \| grep -c .)" -eq 2 ] && [ "$(printf '%s\n' "$rows" \| LC_ALL=C grep -c '確かめる手段は静的確認である')" -eq 2 ]` | 完了 | 2ee8749 | 2026-08-23に実行し終了コード0。判定文が所要時間・秒以内・回数・自己テストの終了コードを求める表の行から、目視と実行コマンドを含む行を除き、静的確認だけの行を抽出した。現在該当するのは`docs/tasks/done/自己テストの所要時間が規約の件数に追いつかれる問題を片付ける指示書.md`の判定1・判定4の2行で、両方に静的確認である旨の注記がある。元の自己テストも実行し、25.19秒・全項目PASS・終了コード0を確認した。現在は120秒を下回るが、「触らない範囲」に従い差し替え済みコマンドは維持し、2026-08-19時点の固定注記と現在の実測を併記した。以前3行目に数えていた`docs/tasks/done/走っていない検査を集約へ載せる指示書.md`の判定4は、現在は加えた4本を実行するコマンドへ改められており、静的確認の対象ではない。`declared_long_running_known()`は現在登録0件であり、今回の2行は`judge-task-done.sh`が直接実行するため登録対象外である。 |
| 2. done/走査対象外であることの確認記録 | `block="$(awk '/^run_normal_mode\(\)/{inside=1} inside{print} inside && /^}/{exit}' docs/scripts/judge-task-done.sh)"; printf '%s\n' "$block" \| LC_ALL=C grep -qF 'target_files=("$TASKS_DIR"/*.md)' && ! printf '%s\n' "$block" \| LC_ALL=C grep -qF '"$DONE_DIR"/*.md'` | 完了 | 2ee8749 | 2026-08-23に実行し終了コード0。`run_normal_mode()`の通常対象は`target_files=("$TASKS_DIR"/*.md)`であり、`"$DONE_DIR"/*.md`を含まない。したがって通常の片付き判定は`docs/tasks/`直下だけを走査し、`done/`配下を再判定しない。段階3の台帳照合が`done/`も読むこととは別の走査である。 |
| 3. 判定不能の原因調査結果の記録 | `target=(docs/tasks/done/*自己テストの所要時間が規約の件数に追いつかれる問題を片付ける指示書.md); [ "${#target[@]}" -eq 1 ] && name="${target[0]##*/}" && out="$(bash docs/scripts/judge-task-done.sh --only "${target[0]}" 2>&1)"; rc=$?; movable="$(printf '%s\n' "$out" \| awk '/^移せる:/{inside=1; next} /^移せない:/{inside=0} inside')"; [ "$rc" -eq 0 ] && printf '%s\n' "$movable" \| LC_ALL=C grep -qF -- "- $name" && ! printf '%s\n' "$out" \| LC_ALL=C grep -qF '[UNKNOWN]'` | 完了 | c1c5027 | 2026-08-23に実行し終了コード0。手動`--only`は現在「公開の状態: 反映済み」となり、対象ファイルを「移せる」へ列挙する。「判定不能」は再現しない。過去の原因は、移動後も確かめる手段が移動前パスを`grep`し、ファイル不在の終了コード2を返したことだった。`e5ce95c`は、この自己参照を生成スクリプトという一次情報の検査へ置き換えた。さらに`c1c5027`で、確かめる手段が返す終了コード2は行頭`[UNKNOWN]`がある場合だけ判定不能、ラベルが無いコマンドエラーは不合格として区別するよう修正済みである。判定器自身が確かめる手段の起動前に失敗した場合は、別の分岐で判定不能にする。切り出された`docs/tasks/done/終了コード2に実行環境の制約とコマンド自身のエラーが衝突する問題を記録する指示書.md`も完了済みである。判定器の自己テスト29件も全件合格した。 |

### 判断の記録

| 何を決めたか | 選んだもの | 選んだ理由 | 覆すと何が変わるか |
|---|---|---|---|
| 直し方の選択 | 明示欄（注記）方式 | 別枠方式より実装コストが低く、判定器のロジックを変えずに済むため。今日直したばかりの22件の自己テストとの干渉を避けられる | 注記だけでは実効性が無いと分かった場合、別枠方式へ切り替える必要が生じる |
