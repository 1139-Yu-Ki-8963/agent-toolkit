# 廃止した名前の一覧が散らばり起票が古い名前を指す問題を直す指示書

**状態**: 着手できる
**優先度**: 高
**前提**: なし
**元の指摘**: なし

## この指示書は何か

廃止した資料の名前が、複数の検査にそれぞれ直接書かれている。新しく指示書を書く担当は、その名前が既に廃止されていることを知る手段を持たない。名前の一覧を1箇所へ集め、指示書の本文もその一覧と突き合わせる検査を作る。

## なぜ必要か

実測（2026-08-24）。「要確認事項の記録先を次の語へ向け直せ」という指示書を起票した。

```
根拠を記録する資料
```

ところが改善課題1-246（コミット `bd5d289`「【修正】納品物からコード行番号ベースの根拠記録を除く 1-246」）が既にこの語が指す資料を廃止していた。次の4語は、対象配下に0件であることを検査が要求する状態になっていた。

```
根拠資料
根拠を記録する資料
設計単位根拠台帳
共通文書根拠台帳
```

起票した既定どおりに進めれば、その指示書の完了の判定3（節構成の検査）を直ちに壊すところだった。担当が実測で気づいて方針を変えたため、実害は出ていない。**気づけたのは偶然であり、仕組みが止めたわけではない。**

同じ4語が2つの検査にそれぞれ直接書かれている。

```
generation-engine/scripts/tests/check-design-doc-section-consistency.sh
generation-engine/scripts/tests/test-api-design-decisions.sh
```

前者は `evidence_ref_count` という変数で0件を要求する。1-246 の以前は逆に26件を要求していた（期待値が26から0へ反転している）。

廃止した名前が増えるたびに、書かれている箇所をすべて探して直すことになる。探し漏らせば、片方の検査だけが古い一覧で判定する。加えて、起票する担当はどちらの検査も読まないため、廃止を知る経路がない。

## やること

1. **廃止した資料・節の名前の一覧を1つのファイルへ集める。** 置き場はこのリポジトリ固有の場所とする（`delivery-payload/references/` は配布物であり、このリポジトリの起票の運用は配布対象ではない）。各語について、廃止した改善課題の番号とコミットのハッシュ、置き換え先（あれば）を持たせる
2. **既存2つの検査を、その一覧を読む形へ変える。** 4語のハードコードを無くす
3. **`docs/tasks/` 配下の指示書の本文を走査し、一覧の語が含まれていたら不合格にする検査を作る。** 判定の式に縦棒（`|`）が入るため、式をスクリプトへ切り出す（片付けの判定器が縦棒を列の区切りと読み違え、判定行そのものを壊すため）
4. 新しい検査は `--self-test` を実引数として処理すること。第1層の集約（`generation-engine/scripts/verification/run-layer-machine-checks.sh`）が `--self-test` を持つ `.sh` を動的に集めるため、この形にすると自動的に検査対象へ載る
5. **一時ファイルを作る場合は置き場を明示すること**（`mktemp "${TMPDIR:-/tmp}/<名前>.XXXXXX"`）。引数なしの `mktemp` は既定の置き場へ書こうとして失敗する環境がある
6. `diff` や `comm` の引数へプロセス置換（`<(...)`）を渡さないこと。`/dev/fd/N` を開けない環境がある
7. 新しい検査の設計判断（必要性・代替案を採用しなかった理由・保守責任者・廃棄条件）を `docs/rules/portal/page-conventions/rule.md`（正本）へ追記し、`build-derived-rules.sh --apply` で派生物を再生成すること。`.claude/rules/scoped/portal/page-conventions/rule.md` は生成物であり直接編集してはならない

## 完了の判定

1. `bash docs/scripts/check-retired-terms.sh` が終了コード0を返す
2. `bash docs/scripts/check-retired-terms.sh --self-test` が終了コード0を返す
3. `bash generation-engine/scripts/tests/check-design-doc-section-consistency.sh --self-test` が終了コード0を返す
4. `bash generation-engine/scripts/tests/test-api-design-decisions.sh` が終了コード0を返す
5. `bash .claude/rules/always/tasks/instruction-format/check-instruction-format.sh` が終了コード0を返す
6. `bash generation-engine/scripts/verification/run-layer-machine-checks.sh --timeout 200` の成功本数が、着手前に測った本数を下回らない

## 触らない範囲

- 廃止した資料そのものを復活させない。1-246 の決定を覆さない
- `delivery-payload/` を変更しない（配布物であり、このリポジトリの起票の運用は配布対象ではない）
- 既存の指示書の本文を書き換えない。検査が不合格を返した場合は、その指示書を直すか一覧から語を外すかを判断し、記録に残す

## 決めていないこと

| 何を決めるか | 既定（迷ったらこれを選ぶ） | 覆すときの条件 |
|---|---|---|
| 一覧ファイルの置き場 | `docs/references/retired-terms.json`。`docs/` はこのリポジトリ自身の説明と設計と台帳の置き場であり、配布対象ではない | `docs/` 直下に `references/` を作ることが配置の許可リストに反すると分かったとき。その場合は `docs/design/` の下へ置く |
| 既存2つの検査を直す範囲 | 4語のハードコードを一覧の読み取りへ置き換えるだけ。判定の中身（何件を要求するか）は変えない | 一覧の読み取りへ変えると判定の中身も変わると分かったとき |
| 走査の対象 | `docs/tasks/` の直下と `docs/tasks/design/` の指示書。`done/` と `work-records/` は対象外 | 片付いた指示書からも古い名前を消す方針へ変わったとき |
| 一覧に載せる語 | 上記4語から始める。以後、廃止のたびに足す | 他にも廃止済みの語が見つかったとき |

## 他の指示書との関係

`docs/tasks/一時ファイルの置き場を指定せず検査が動かない問題を直す指示書.md` と対象が重なる（どちらも `generation-engine/scripts/tests/` 配下の `.sh` を触る）。あちらを先に片付けてから着手すること。

## この指示書の位置づけ

起票の内容そのものではなく、起票する担当が古い前提を持ち込まない仕組みを扱う。1-246 の見落としは、起票の直前に既存の決定を確かめる手順が無かったために起きた。人が毎回思い出す運用では、次も見落とす。

## 9. 対応の記録

### 判定の充足状況

| 判定 | 確かめる手段 | 状態 | コミット | 確かめた内容 |
|---|---|---|---|---|
| 1. 廃止語の検査が通る | `bash docs/scripts/check-retired-terms.sh` | 完了 | — | 台帳2件（指摘改善一覧.md・作業課題一覧.md）とコード柵内の引用の除外を実装済み。判断待ちだった14行目は、委任元の判断（選択肢(a)を採用）により、意味を変えずに廃止語をコード柵で囲んで解消した。終了コード0・残存0件 |
| 2. 廃止語の検査の自己テストが通る | `bash docs/scripts/check-retired-terms.sh --self-test` | 完了 | — | 25 PASS・0 FAIL・終了コード0（台帳除外・コード柵除外・done/work-records除外・design/配下の検出・--self-testの実引数処理を含む11ケース） |
| 3. 節構成の検査が通る | `bash generation-engine/scripts/tests/check-design-doc-section-consistency.sh --self-test` | 完了 | — | 45 PASS・0 FAIL・終了コード0（evidence_ref_countをdocs/references/retired-terms.jsonからのjq読み取りへ置換後も件数不変） |
| 4. 設計判断の検査が通る | `bash generation-engine/scripts/tests/test-api-design-decisions.sh` | 完了 | — | 終了コード0（廃止語グレップをdocs/references/retired-terms.jsonからのjq読み取りへ置換後も判定内容は不変） |
| 5. 指示書形式が適合する | `bash .claude/rules/always/tasks/instruction-format/check-instruction-format.sh` | 完了 | — | 対象85件（うちdone/は目視理由検査のみ79件）合格85件・不合格0件・終了コード0。本指示書自身も[OK] |
| 6. 第1層の成功本数が減らない | `bash generation-engine/scripts/verification/run-layer-machine-checks.sh --timeout 200` | 完了 | — | 着手前（clean stash実測・座談会提供値と一致）: 対象186本/成功167本/失敗17本/判定不能2本。着手後（変更を戻して再実測）: 対象186本/成功167本/失敗17本/判定不能2本。本数は不変で減少なし。新設スクリプト（docs/scripts/check-retired-terms.sh）は集約の走査対象（generation-engine/scripts・delivery-payload/templates/rules/checkers）の外にあるため対象本数には算入されない。失敗17本・判定不能2本は着手前後で内訳も一致し、いずれもこの指示書の変更と無関係な既存事象（build-matrix-pages.shのヘッダ配色コントラスト・複数の.cjsブラウザ依存テスト・PyYAML未導入によるUNKNOWN等）。`bash generation-engine/scripts/tests/check-layer1-declarations.sh`（自己テストのみ・集約は再実行しない）も3件PASS・終了コード0 |

### 判断の記録

| 何を決めたか | 選んだもの | 選んだ理由 | 覆すと何が変わるか |
|---|---|---|---|
| 台帳2件（指摘改善一覧.md・作業課題一覧.md）を廃止語検査の対象から除外するか | 除外する | `.claude/rules/always/tasks/commit-issue-trace/rule.md`「台帳を対象外にする理由」が同じ区別（台帳は指示書ではなく起きた出来事の記録）を既に定めており、そのまま再用した | 除外しないと台帳の履歴記述（実測33件超）が全件不合格として検出され、検査が実運用に耐えなくなる |
| コード柵（```）内の語の引用を検出対象から外すか | 外す。引用符「」は外さない | `.claude/rules/always/tasks/instruction-format/rule.md` 付属の `check-instruction-format.sh` の `strip_fences` と同じロジックを再用できる。引用符は、廃止語を使った生きた誤指示（例：「その◯◯へ書け」の形で本文中に再登場する場合）と過去の言及とを区別できず、本検査が捕まえたい失敗そのものを見逃すため外さなかった | 柵を外さないと本指示書16〜21行目の廃止語4件の例示まで検出され、指示書がこの検査自体を説明できなくなる |
| 完了条件1（`check-retired-terms.sh`の終了コード0）を満たすため本指示書14行目を書き換えるか | いったん書き換えず委任元へ判断を仰ぎ、選択肢(a)(b)(c)を提示した。委任元は(a)（コード柵で囲む）を選び、この1行に限り「本文を書き換えない」の例外とする指示を明示した | 触らない範囲への抵触を自己判断で決めず、判断を要する事実（意味を保ったまま検査を通す手段が3通りあり、どれを選ぶかは委任元の権限に属すること）を先に報告した。委任元は、検査が既にコード柵内の引用を除外する設計になっている点・14行目が正当な経緯説明である点・(b)(c)は検査を弱めるか使えなくする点を根拠に(a)を選んだ | 14行目を「実測（2026-08-24）。「要確認事項の記録先を次の語へ向け直せ」という指示書を起票した。」の後に廃止語1語だけをコード柵で囲んだ段落を挟み、「ところが改善課題1-246...が既にこの語が指す資料を廃止していた。」と続ける形に改めた。文意は変えていない。以後に同種の廃止語を経緯説明として引用する場合も同じ形（独立した段落としてコード柵で囲む）を使う |
