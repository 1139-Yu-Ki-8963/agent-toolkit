**状態**: 着手できる
**優先度**: 最高
**前提**: なし
**元の指摘**: なし

# 検証の仕組みの穴を塞ぐ指示書

## 1. この指示書は何か

2026-08-23の検証記録で見つかった9件を、検査器自身の終了コードと自己テストで是正する。対象0件・対象不在を合格と混同せず、判定不能は`[UNKNOWN]`・終了コード2として扱う。

## 2. なぜ必要か

再現性、自立性、配線、集約、追跡の各検査で、対象を一切確認しなくても終了コード0になった。さらに健全性集約は子の終了コード2を1へ変換し、機械検証集約は子の判定不能理由を落としていたため、合否と実行不能を区別できなかった。

## 3. やること

1. 再現性検査で同一パス、比較対象0件、比較一覧用`mktemp`失敗を`[UNKNOWN]`・終了コード2にする。
2. 自立性、追跡、配線、集約で対象不在・対象0件を`[UNKNOWN]`・終了コード2にする。
3. 健全性集約は、構造化UNKNOWNだけなら終了コード2を返す。FAILとUNKNOWNが混在する場合は全体を終了コード1にし、UNKNOWN表示と理由を保持する。契約違反の終了コード2は不合格にする。
4. 配線済みの一発実行テストは通し、集約に無い自己テストなし検査だけを不合格にする。不合格と判定不能の混在では終了コード1を優先する。
5. 機械検証集約は、子の終了コード2を行頭`[UNKNOWN]`、`操作: <非空値>`、`想定原因: <非空値>`を持ち、呼出しエラー語を含まないときだけ判定不能として集約する。`run_one`自身の`mktemp`失敗も同じ契約で終了コード2にする。
6. 上位入口の検証ループは、第1層と4判定の終了コード2・理由を保持する。FAIL混在では終了コード1、UNKNOWNだけなら終了コード2にする。
7. 変更した終了コード契約をコミット追跡規約と検証詳細設計書へ同期する。

## 4. 完了の判定

1. 穴1の同一物理ディレクトリ別名と、TMPDIR不正時の`mktemp`失敗ケースが自己テストで合格する。
2. 穴2の比較対象0件ケースが自己テストで合格する。
3. 穴3の非存在リポジトリへの自立性検査が`[UNKNOWN]`・終了コード2になる。
4. 穴4のUNKNOWN単独、FAIL混在、契約違反の終了コード2、11行以上の出力での理由保持が健全性検査の自己テストで合格する。
5. 穴5の`--max-commits 0`と非Gitリポジトリ経路が、ともに`[UNKNOWN]`・終了コード2になる。
6. 穴6の未配線候補と、不合格・判定不能混在時の終了コード1優先が配線検査の自己テストで合格する。
7. 穴7の非存在リポジトリへの配線検査が`[UNKNOWN]`・終了コード2になる。
8. 穴8の集約対象0件ケースが集約器の自己テストで合格する。
9. 穴9の構造化UNKNOWN、旧括弧形式、呼出しエラー、診断中のerror語、`mktemp`失敗、FAIL混在、UNKNOWN単独、および上位入口での伝播が各自己テストで合格する。
10. 指定された6コマンドとは判定表の3・10〜14であり、3だけが終了コード2、10〜14の5本が終了コード0を返す。他の自己テストや形式検査はこの6本へ数えない。
11. 指示書形式検査が不合格0件、6本のシェル構文検査と`git diff --check`が終了コード0を返す。

## 5. 触らない範囲

- `docs/tasks/work-records/` 配下
- `delivery-payload/templates/rules/checkers/` 配下
- `generation-engine/samples/` 配下

## 6. 決めていないこと

| 何を決めるか | 既定（迷ったらこれを選ぶ） | 覆すときの条件 |
|---|---|---|
| 対象0件・対象不在の扱い | `[UNKNOWN]`・終了コード2にする | 正当に0件を合格とする呼出し契約と、その契約を保証する機械検査が追加された場合 |
| 子検査が終了コード2を返した場合 | 行頭`[UNKNOWN]`、非空の`操作:`、非空の`想定原因:`を構文検証し、呼出しエラー語があれば終了コード1として集約する | 判定不能規約が契約項目を変更した場合 |
| 構造化フィールドの検証境界 | 集約器は非空値の構文までを検証し、子申告の意味的真偽は子検査の責任とする | 操作の真偽を機械判定できる共通契約が追加された場合 |

## 7. 他の指示書との関係

`docs/tasks/work-records/2026-08-23-検証の仕組みの検証.md`を入力記録とする。判定不能規約と指示書形式規約に従い、記録自体は変更しない。

## 8. この指示書の位置づけ

検証の実行対象、委譲結果、集約結果を同じ終了コード契約へそろえる限定的なバグ修正である。成果物や見本の内容は対象にしない。

## 9. 対応の記録

### 判定の充足状況

| 判定 | 確かめる手段 | 状態 | コミット | 確かめた内容 |
|---|---|---|---|---|
| 1. 穴1の同一物理別名拒否とmktemp失敗 | `out=$(bash generation-engine/scripts/verification/check-reproducible.sh --self-test 2>&1); rc=$?; [ "$rc" -eq 0 ] && case "$out" in *"[PASS] 判定不能-TMPDIR不正"*"[PASS] 独立性-同一物理ディレクトリの別名を拒否"*) exit 0 ;; *) exit 1 ;; esac` | 完了 | 12d0ef17ad8fa3abf1e01b0166de071f4fd83d70 | 自己テスト13件成功。同一物理別名とTMPDIR不正を判定不能として確認。 |
| 2. 穴2の比較対象0件を判定不能化 | `out=$(bash generation-engine/scripts/verification/check-reproducible.sh --self-test 2>&1); rc=$?; [ "$rc" -eq 0 ] && case "$out" in *"[PASS] 判定不能-対象0件"*) exit 0 ;; *) exit 1 ;; esac` | 完了 | 12d0ef17ad8fa3abf1e01b0166de071f4fd83d70 | 比較対象0件が`[UNKNOWN]`・終了コード2になることを確認。 |
| 3. 穴3の非存在リポジトリを判定不能化 | `out=$(bash generation-engine/scripts/verification/check-self-contained.sh --repo /nonexistent/path 2>&1); rc=$?; [ "$rc" -eq 2 ] && case "$out" in "[UNKNOWN]"*) exit 0 ;; *) exit 1 ;; esac` | 完了 | 12d0ef17ad8fa3abf1e01b0166de071f4fd83d70 | 非存在パスで`[UNKNOWN]`・終了コード2を確認。 |
| 4. 穴4のUNKNOWN契約・保持・FAIL優先 | `out=$(bash generation-engine/scripts/verification/check-sound.sh --self-test 2>&1); rc=$?; [ "$rc" -eq 0 ] && case "$out" in *"[PASS] 集計-理由中のerror語を許容"*"[PASS] 集計-小文字の呼出しエラー語を拒否"*"[PASS] 集計-契約違反の終了コード2"*"[PASS] 集計-長出力の先頭理由を保持"*"[PASS] 集計-判定不能を保持"*"[PASS] 集計-不合格と判定不能の混在"*) exit 0 ;; *) exit 1 ;; esac` | 完了 | 12d0ef17ad8fa3abf1e01b0166de071f4fd83d70 | 自己テスト12件成功。UNKNOWN保持、契約違反、FAIL優先を確認。 |
| 5. 穴5の空走査と非Git経路を判定不能化 | `tmp=$(mktemp -d); out_zero=$(bash generation-engine/scripts/tests/check-commit-issue-trace.sh "$PWD" --max-commits 0 2>&1); rc_zero=$?; out_non_git=$(bash generation-engine/scripts/tests/check-commit-issue-trace.sh "$tmp" 2>&1); rc_non_git=$?; rm -rf "$tmp"; [ "$rc_zero" -eq 2 ] && [ "$rc_non_git" -eq 2 ] \|\| exit 1; case "$out_zero" in "[UNKNOWN]"*) ;; *) exit 1 ;; esac; case "$out_non_git" in "[UNKNOWN]"*) exit 0 ;; *) exit 1 ;; esac` | 完了 | 12d0ef17ad8fa3abf1e01b0166de071f4fd83d70 | 走査0件と非Gitの両経路で`[UNKNOWN]`・終了コード2を確認。 |
| 6. 穴6の未配線候補と混在優先順位 | `out=$(bash generation-engine/scripts/tests/check-verification-wiring.sh --self-test 2>&1); rc=$?; [ "$rc" -eq 0 ] && case "$out" in *"[PASS] 検査2-配線漏れ候補を不合格にする"*"[PASS] 配線検査-集約一覧の失敗を判定不能にする"*"[PASS] 集約-両検査の判定不能は2を保持"*"[PASS] 検査1-存在しない列挙を不合格にする"*"[PASS] 集約-不合格と判定不能の混在は1を優先"*) exit 0 ;; *) exit 1 ;; esac` | 完了 | 12d0ef17ad8fa3abf1e01b0166de071f4fd83d70 | 自己テスト15件成功。未配線を不合格、FAIL混在時を終了コード1と確認。 |
| 7. 穴7の非存在リポジトリを判定不能化 | `out=$(bash generation-engine/scripts/tests/check-verification-wiring.sh /nonexistent/path 2>&1); rc=$?; [ "$rc" -eq 2 ] && case "$out" in "[UNKNOWN]"*) exit 0 ;; *) exit 1 ;; esac` | 完了 | 12d0ef17ad8fa3abf1e01b0166de071f4fd83d70 | 非存在リポジトリで`[UNKNOWN]`・終了コード2を確認。 |
| 8. 穴8の集約対象0件を判定不能化 | `out=$(bash generation-engine/scripts/verification/run-layer-machine-checks.sh --self-test 2>&1); rc=$?; [ "$rc" -eq 0 ] && case "$out" in *"[PASS] 判定不能-対象0本の集計行"*) exit 0 ;; *) exit 1 ;; esac` | 完了 | 12d0ef17ad8fa3abf1e01b0166de071f4fd83d70 | 自己テスト36件成功。集約対象0本を判定不能として確認。 |
| 9. 穴9のUNKNOWN契約・mktemp・上位伝播 | `out=$(bash generation-engine/scripts/verification/run-layer-machine-checks.sh --self-test 2>&1); rc=$?; loop_out=$(bash generation-engine/scripts/verification/run-verification-loop.sh --self-test 2>&1); loop_rc=$?; [ "$rc" -eq 0 ] && [ "$loop_rc" -eq 0 ] && case "$out" in *"[PASS] 実行-mktemp失敗を判定不能にする"*"[PASS] 判定不能-不合格混在は1を優先し理由を保持"*"[PASS] 判定不能-契約違反の終了コード2を不合格にする"*"[PASS] 判定不能-集約の終了コード2を保持"*) case "$loop_out" in *"[PASS] 集約-判定不能のみは2"*"[PASS] 集約-第1層の判定不能も2"*"[PASS] 集約-不合格と判定不能の混在は1"*"[PASS] 出力-判定不能理由を保持"*) exit 0 ;; *) exit 1 ;; esac ;; *) exit 1 ;; esac` | 完了 | 12d0ef17ad8fa3abf1e01b0166de071f4fd83d70 | 層検査36件・上位集約9件成功。UNKNOWN理由保持と優先順位を確認。 |
| 10. 正常な自立性検査が合格 | `bash generation-engine/scripts/verification/check-self-contained.sh --repo "$PWD"` | 完了 | 12d0ef17ad8fa3abf1e01b0166de071f4fd83d70 | 対象640本、適合640本、違反0本、終了コード0。 |
| 11. 被覆検査が合格 | `bash generation-engine/scripts/verification/check-coverage.sh --output generation-engine/samples --repo "$PWD"` | 完了 | 12d0ef17ad8fa3abf1e01b0166de071f4fd83d70 | 分母82件、存在82件、欠落0件、終了コード0。 |
| 12. 健全性検査が合格 | `bash generation-engine/scripts/verification/check-sound.sh --output generation-engine/samples --repo "$PWD"` | 完了 | 12d0ef17ad8fa3abf1e01b0166de071f4fd83d70 | 検査器2本成功、失敗0本、判定不能0本、終了コード0。 |
| 13. 配線検査が合格 | `bash generation-engine/scripts/tests/check-verification-wiring.sh` | 完了 | 12d0ef17ad8fa3abf1e01b0166de071f4fd83d70 | 対象182件すべて集約済み、未配線0件、終了コード0。 |
| 14. 追跡検査の自己テストが合格 | `bash generation-engine/scripts/tests/check-commit-issue-trace.sh --self-test` | 完了 | 12d0ef17ad8fa3abf1e01b0166de071f4fd83d70 | 自己テスト6件成功、失敗0件、終了コード0。 |
| 15. 指示書形式が適合 | `bash .claude/rules/always/tasks/instruction-format/check-instruction-format.sh` | 完了 | 12d0ef17ad8fa3abf1e01b0166de071f4fd83d70 | 指示書75件合格、不合格0件、終了コード0。 |
| 16. シェル構文と差分が適合 | `for f in generation-engine/scripts/verification/check-reproducible.sh generation-engine/scripts/verification/check-self-contained.sh generation-engine/scripts/verification/check-sound.sh generation-engine/scripts/verification/run-layer-machine-checks.sh generation-engine/scripts/tests/check-commit-issue-trace.sh generation-engine/scripts/tests/check-verification-wiring.sh; do bash -n "$f" \|\| exit 1; done && git diff --check` | 完了 | 12d0ef17ad8fa3abf1e01b0166de071f4fd83d70 | 対象シェルの構文検査と差分検査が終了コード0。 |

### 穴ごとの再現と是正

| 穴 | 修正前の再現 | 是正 | 修正後の結果 |
|---|---|---|---|
| 1 | 同じ生成物を2回指定して終了コード0。TMPDIR不正では空一覧として誤診 | 同一物理パスを拒否し、3つの`mktemp`を個別検査・安全に後片付け | 同じ指定とTMPDIR不正で理由付き`[UNKNOWN]`・終了コード2 |
| 2 | 空ディレクトリ2本で終了コード0 | 比較対象0件を判定不能化 | 同じ条件で`[UNKNOWN]`・終了コード2 |
| 3 | 非存在リポジトリで終了コード0 | リポジトリ不在と走査対象0件を判定不能化 | 非存在リポジトリで`[UNKNOWN]`・終了コード2 |
| 4 | 子の終了コード2を無条件にUNKNOWNとし、長い出力では先頭理由が欠落 | 構造化契約を検証し、UNKNOWN出力は全文保持 | 適合は`[UNKNOWN]`・終了コード2、契約違反は`[FAIL]`・終了コード1 |
| 5 | 非Gitリポジトリ、`--max-commits 0`が終了コード0 | 対象リポジトリ・履歴不在を判定不能化し両経路を同じ機械判定で実測 | 両方とも`[UNKNOWN]`・終了コード2 |
| 6 | 未配線候補がWARNのみで終了コード0、終了コード1・2混在で2を優先 | 未配線候補と混在時の1優先を実装 | 同じ負例と混在ケースで終了コード1 |
| 7 | 非存在リポジトリの配線検査が終了コード0 | 集約器・走査起点不在を判定不能化 | 非存在リポジトリで`[UNKNOWN]`・終了コード2 |
| 8 | 空リポジトリへの集約が終了コード0 | 集約対象0本を判定不能化 | 空リポジトリで`[UNKNOWN]`・終了コード2 |
| 9 | 旧括弧形式や呼出しエラーを受理し、`run_one`の`mktemp`失敗を終了コード1化 | 構造化フィールドと禁止語を検証し、`mktemp`失敗を構造化UNKNOWN化 | 契約違反は`[FAIL]`・終了コード1、契約適合と`mktemp`失敗は理由付き`[UNKNOWN]` |

### 判断の記録

| 何を決めたか | 選んだもの | 選んだ理由 | 覆すと何が変わるか |
|---|---|---|---|
| 穴1・2の扱い | `[UNKNOWN]`・終了コード2 | 独立した2回の生成または比較対象が無ければ再現性を判定していないため | 再現性検査の終了コード契約と自己テストが変わる |
| 穴3・5・7・8の扱い | `[UNKNOWN]`・終了コード2 | パス不在または列挙0件は検査未実施であり成果物の不合格ではないため | 各検査の呼出し側が判定不能を別途扱う必要がある |
| 穴4の扱い | 子の終了コード2を保持する | 実行不能を成果物の不合格へ変換しないため | 健全性集約のサマリと終了コードが変わる |
| 穴6の扱い | 未配線候補は終了コード1 | 検査対象の配線漏れは検査構成の不備であり、判定不能ではないため | 配線検査を合否判定に使う呼出し側が候補解消を要求する |
| 穴9の扱い | 契約適合だけを`[UNKNOWN]`、違反は終了コード1 | 未知引数などの呼出し失敗を判定不能へ誤分類しないため | 子検査の終了コード2が契約を満たす必要がある |
| UNKNOWNフィールドの責任境界 | 集約器は構文、子検査は申告内容の意味的真偽を担保 | `false`も実在コマンドであり、集約器のallowlistでは真偽を証明できないため | 子検査の自己テストと共通契約が必要になる |
| 呼出しエラー語の境界 | 独立した行頭の呼出しエラーだけを拒否 | `permission error`や`diagnostic: 0 errors`まで拒否すると正当な判定不能理由を不合格へ誤分類するため | soundとrun-layerの正規表現・自己テストが変わる |
| 上位入口の優先順位 | FAIL混在は1、UNKNOWNだけなら2 | 下位で区別した判定不能を入口で再び不合格へ潰さず、実在する不合格も隠さないため | 検証ループ利用者が0・1・2を区別する必要がある |

### コミット後の記録手順

実施済み。内容を含む最初のコミットは`12d0ef17ad8fa3abf1e01b0166de071f4fd83d70`であり、その全長ハッシュと実測結果を判定表16行へ記録した。最初のコミットへ`git commit --amend --no-edit`すると記録対象ハッシュ自体が消えるためamendしない。本更新は後続の記録コミットとして作成する段階であり、判定器が要求するのは実在しmainの祖先となる最初のコミットであって、指示書自身の最終コミットハッシュではない。
