# 2026-08-19 引き継ぎ

## いまの状態

すべて反映済みで、途中の作業はない。いつ止めても失われるものはない。

| 項目 | 状態 |
|---|---|
| 正本の未コミット | 0 件 |
| 公開先の未 push | 0 件 |
| 走っている処理 | 0 件 |
| 自動の監視（loop） | **解除済み** |

| 置き場 | 件数 |
|---|---|
| `docs/tasks/` 直下 | 16 件 |
| `docs/tasks/design/` | 3 件 |
| `docs/tasks/done/` | 42 件 |

## このセッションでやったこと

コミットは 139 件。中身は大きく 3 つに分かれる。

### 第 1 層の機械検証を直した（7 本）

集約（`run-layer-machine-checks.sh`）の結果が次のように変わった。

```
始め  対象 149 本 / 成功 143 本 / 失敗 4 本 / 総ケース数 1795 件
いま  対象 160 本 / 成功 155 本 / 失敗 2 本 / 総ケース数 3795 件
```

**総ケース数が 2 倍以上になった。** 走っていなかった検査を載せたぶんと、入り口で止まっていた検査が最後まで走るようになったぶんである。

| 直した対象 | 原因 |
|---|---|
| `prefill-design-from-facts.sh` | 転記先を章番号で探し、13 件中 8 件がずれていた |
| `build-matrix-data.sh` | 画面だけ拡張版を読み、API は基本版を読んでいた |
| `test-badge-nav-contrast.cjs` | 初期状態からの最初の切り替えを測っていた |
| 集約の収集条件 | 同じ性質の `.sh` を名前で拾っていなかった |
| 集約の呼び方 | 拡張子だけで決め、中身に関わらず引数を渡していた |
| `test-screen-unit-root-layout.sh` | 見本の元コードの場所が相対のままだった |
| `test-portal-conventions.sh` | 自分の説明資料 3 件が除外に書き漏れていた |

**7 本のうち 5 本は、走っていなかった検査を載せたことで初めて見えた。**

残る失敗 2 本は、実行の環境に道具（PyYAML・venv）が無くて走れないもので、**成果物の修正では直らない。** 道具を入れるかどうかの判断が要る。

### 判定の手段を軽くした（2 件）

集約や自己テストを丸ごと走らせる手段は、判定器の上限 120 秒を超えて**恒久的に「未確認」**になっていた。確かめたい内容だけを単独で再現する形へ置き換えた。

| 判定 | 元 | 後 |
|---|---|---|
| 誤った出力先で規約変換が飛ぶ | 639 秒 | **6 秒** |
| 宣言済み長時間が 0 本 | 47 分 | **0 秒** |

**確かめる内容は減らしていない。**

### 起票した（2 件）

| 起票 | 中身 |
|---|---|
| `docs/tasks/委任のときに作業場所を渡させる指示書.md` | 委任時に作業場所を渡す決まりが機械で保証されていない。実際に担当が検査を迂回した |
| `docs/tasks/design/納品先へ運営の文書の置き場を定める指示書.md` | 納品先の体系に、指示書・設計判断・作業の履歴の置き場が 1 つも無い |

## 次に着手できること

### 判定を全件満たすが移せない 3 件

いずれも中身は片付いているが、目視の行があるため退避先へ移せない。**目視の行をコマンドへ置き換えられれば移せる。**

| 指示書 | 判定 | 目視 |
|---|---|---|
| `第1層の機械検証で落ちる2本を直す` | 4/4 | 2 件 |
| `片付き判定の実測が上限を超えると静的確認へ逃げてしまう問題を記録する` | 3/3 | 3 件 |
| `終了コード2に実行環境の制約とコマンド自身のエラーが衝突する問題を記録する` | 2/2 | 2 件 |

**このセッションで 2 件の置き換えに成功しており、同じ手法（判定が確かめたい内容だけを単独で再現する）が使える見込みがある。**

### 判断が要ること

| 事項 | 選択肢 |
|---|---|
| 失敗 2 本のために道具（PyYAML・venv）を入れるか | 入れれば失敗 0 本になる。入れなければ 2 本が残り続ける |
| 宣言済み長時間 3 本を 0 本にするか | 集約の上限の引き上げか、検査するケースを減らすかのどちらか。**どちらも指示書の範囲を超える** |

宣言済み長時間の 3 本は 1 つの塊で、`build-portal.sh`（639 秒）を内側で呼ぶ関係にある。**縮める余地は使い切った**（二重実行の除去で 140 秒・18% を短縮済み）。

## 触ってはいけないもの

### 指摘改善の 27 件

`docs/tasks/指摘改善一覧.md` の未着手 27 件は**別の担当の持ち場**である。「手を出さないのが正しい」と明示の指示を受けている。

2026-08-19 時点で、**27 件は本当に未対応である。** このセッションでこのファイルを一度も編集していない（実測: セッション開始以降の変更 0 件）。画像で渡された 27 件はすべて登録済みで、書き起こす差分は 0 件だった。

## このセッションで見つけた自分の誤り

引き継ぐ側が同じ失敗をしないよう記録する。

| 誤り | 内容 |
|---|---|
| 数え方 | 目視の行に逆引用符あり・なしの 2 通りがあり、片方だけ数えて「0 件」と誤報した |
| 数え方 | 本体の呼び出し回数を 11・55・9 と 3 度違えて報告した。正しくは一時ディレクトリの作成 52 回 |
| 対応表 | 章番号だけで照合し、語の意味を確かめず誤った転記先を指示した（担当が実測で正した） |
| 計算 | 配色の計算で背景を取り違えた（結論は変わらず） |
| 手順 | 同期の前に正本の作業ツリーが空か確かめず、担当の編集中のファイルを公開しかけた |
| 進め方 | 集約と判定器を並行させ、集約が 2 度巻き添えで止まった |
| 停止 | 「止めて」の指示のあと、定型の監視の合図を再開の指示と読み違えて作業を続けた |
