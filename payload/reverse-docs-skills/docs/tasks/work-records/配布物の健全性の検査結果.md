# 配布物の健全性の検査結果

実施日: 2026-08-23

## 結果の一覧

| 検査 | 終了コード | 要点 |
|---|---|---|
| `bash generation-engine/scripts/check-payload-references.sh` | 未実行 | 指定パスにスクリプトが存在しない。`generation-engine/scripts/verification/` 配下には同名スクリプトが存在するが、今回は実行していない。 |
| `bash generation-engine/scripts/check-self-contained.sh` | 未実行 | 指定パスにスクリプトが存在しない。`generation-engine/scripts/verification/` 配下には同名スクリプトが存在するが、今回は実行していない。 |
| `bash generation-engine/scripts/check-delivery-doc-links.sh` | 未実行 | 指定パスにスクリプトが存在しない。`generation-engine/scripts/tests/` 配下には同名スクリプトが存在するが、今回は実行していない。 |
| `bash generation-engine/scripts/check-skill-reference-paths.sh` | 未実行 | 指定パスにスクリプトが存在しない。`generation-engine/scripts/tests/` 配下には同名スクリプトが存在するが、今回は実行していない。 |
| `bash generation-engine/scripts/check-guide-script-paths.sh` | 未実行 | 指定パスにスクリプトが存在しない。`generation-engine/scripts/skill-guide/` 配下には同名スクリプトが存在するが、今回は実行していない。 |
| `bash generation-engine/scripts/check-internal-jargon.sh` | 0 | 独自語彙を2件検出した。対象は2つのSkillファイルにある「検収」で、推奨置換語は「確認」である。 |

## 不合格の詳細

終了コードが0以外だった検査はない。
検査1〜5は指定パスにスクリプトが存在しないため未実行であり、終了コードはない。

## 所見

1. **優先度: 高** 指定された検査1〜5は所定パスに存在せず、指定コマンドでは実行できなかった。同名スクリプトは別のサブディレクトリにある。依頼されたコマンドと現行配置の整合を確認する必要がある。本検査では代替パスを実行していない。
2. **優先度: 中** `check-internal-jargon.sh` が検出した2箇所の「検収」を確認する必要がある。

   - `.claude/skills/generating-api-basic-design-for-reverse-docs/SKILL.md:254`
   - `.claude/skills/generating-api-detail-design-for-reverse-docs/SKILL.md:328`

   検査出力は「確認」への置換を示している。

## 正しい置き場での再実行（進行担当が追記）

検査1〜5は指定したパスが誤っていた。実際の置き場で実行し直した結果を次に示す。

| 検査 | 実際の置き場 | 終了コード |
|---|---|---|
| 参照の実在 | `generation-engine/scripts/verification/check-payload-references.sh` | 0 |
| 自立の確認 | `generation-engine/scripts/verification/check-self-contained.sh` | 0 |
| 配られない場所への参照 | `generation-engine/scripts/tests/check-delivery-doc-links.sh` | 0 |
| スキルの参照先 | `generation-engine/scripts/tests/check-skill-reference-paths.sh` | 0 |
| ガイドの書くパス | `generation-engine/scripts/skill-guide/check-guide-script-paths.sh` | 0 |

**5件すべて合格した。**配布物の参照・自立・リンクに問題は無い。

残る指摘は独自の言葉遣い2件（「検収」）のみである。
