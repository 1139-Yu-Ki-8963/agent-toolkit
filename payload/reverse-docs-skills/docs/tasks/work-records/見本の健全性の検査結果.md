# 見本の健全性の検査結果

実施日: 2026-08-23

## 結果の一覧

| 検査 | 実際の置き場 | 終了コード | 要点 |
|---|---|---|---|
| `check-derived-drift.sh status generation-engine/samples` | `generation-engine/scripts/check-derived-drift.sh` | 0 | 台帳と一致し、ずれは検出されなかった。 |
| `test-portal-conventions.sh generation-engine/samples` | `generation-engine/scripts/tests/test-portal-conventions.sh` | 0 | PASS 1290件、FAIL 0件、SKIP 5件だった。 |
| `check-sound.sh --output generation-engine/samples` | `generation-engine/scripts/verification/check-sound.sh` | 1 | 検査器2本のうち1本が成功し、`test-e2e-portal.sh` が失敗した。 |
| `check-design-doc-section-consistency.sh` | `generation-engine/scripts/tests/check-design-doc-section-consistency.sh` | 1 | コマンドは終了コード1で終了した。必須引数がなかったため、2026-08-23 の検査では対象の整合性を確認できなかった。 |
| `check-design-unit-file-completeness.sh --self-test` | `generation-engine/scripts/tests/check-design-unit-file-completeness.sh` | 0 | セルフテストは4件が成功し、失敗は0件だった。 |
| `check-placement-independence.sh --self-test` | `generation-engine/scripts/tests/check-placement-independence.sh` | 0 | セルフテストは3件すべてが成功した。 |

6件を上から順に実行した。5分を超えた検査はなく、検査前後の作業ツリーはクリーンだった。

## 不合格の詳細

### `check-sound.sh --output generation-engine/samples`

```text
[FAIL] test-e2e-portal.sh — 終了コード 1
/Users/MacPro/Projects/worktrees/reverse-docs-skills/review-sample-integrity/generation-engine/scripts/tests/test-e2e-portal.sh: line 291: LIST_PAGES[@]: unbound variable
検査器 2 本 / 成功 1 本 / 失敗 1 本
```

### `check-design-doc-section-consistency.sh`

```text
使い方: check-design-doc-section-consistency.sh <project_root>
        check-design-doc-section-consistency.sh --self-test
```

## 所見

1. **優先度: 高** `test-e2e-portal.sh` の291行目では、初期化済みの空の `LIST_PAGES` 配列を `set -u` の有効下で展開した際に未束縛変数エラーが起きた。これにより、内側の検査が途中で終了した。空配列でも安全に展開できる参照条件へ見直す必要があると判断した。
2. **優先度: 中** `check-design-doc-section-consistency.sh` は引数なしでは検査対象を決定できず、2026-08-23 の検査では対象の整合性を確認できなかった。見本を検査する際の `project_root` を指定するか、セルフテストを意図した呼び出しなら `--self-test` を付ける必要があると判断した。
3. **優先度: 低** 派生物台帳、ポータル規約、設計単位ファイルの完全性、および配置独立性の各検査は合格した。これらの検査では、2026-08-23 時点で直すべき点は検出されなかった。
