# 検証ループの判定の現状

実施日: 2026-08-23

## 結果の一覧

全コマンドは5分以内に終了し、打ち切りは発生しなかった。

| 判定 | 終了コード | 要点 |
|---|---:|---|
| 自立 | 0 | 対象635本、適合635本、違反0本だった。 |
| 健全 | 1 | 検査器2本のうち1本が成功し、`test-e2e-portal.sh` が失敗した。 |
| 網羅 | 0 | 分母82件のすべてが存在し、欠落は0件だった。 |
| 参照の実在 | 0 | 27件を走査し、違反は0件だった。 |
| `verification-env.sh --self-test` | 0 | 9件を実行し、9件が成功した。 |
| `record-verification-result.sh --self-test` | 0 | 5件を実行し、5件が成功した。 |
| `compare-with-previous.sh --self-test` | 0 | 6件を実行し、6件が成功した。 |
| `record-deferral.sh --self-test` | 0 | 5件を実行し、5件が成功した。 |

## 不合格の詳細

健全の判定では、一連の動作を通すポータル検査（E2Eポータルテスト）が一覧ページ0件を扱う際、Bash 3.2と `set -u` の組み合わせで空の `LIST_PAGES` を展開して終了した。

```text
[FAIL] test-e2e-portal.sh — 終了コード 1
.../generation-engine/scripts/tests/test-e2e-portal.sh: line 291: LIST_PAGES[@]: unbound variable
検査器 2 本 / 成功 1 本 / 失敗 1 本
```

## 実行しなかったもの

| 判定 | 実行しなかった理由 |
|---|---|
| 再現 | 2回の生成が必要で時間がかかるため、指示に従って今回は実行しなかった。2026-08-23 時点で再現の判定結果は未確認である。 |

## 所見

1. **優先度: 高** 2026-08-23 時点で、健全の判定を成立させるには、一覧ページが0件でも空の `LIST_PAGES` の展開で終了しないようにする対応が必要だった。
2. **優先度: 中** 2026-08-23 時点で再現の判定は未実行だった。別途、2回の生成時間を確保して判定結果を記録する必要があった。

## 健全の判定の再実測（進行担当が追記）

2026-08-23、上記で不合格だった「健全」を実行し直した。

    bash generation-engine/scripts/verification/check-sound.sh --output generation-engine/samples
    結果: PASS=1290  FAIL=0  SKIP=5
    検査器 2 本 / 成功 2 本 / 失敗 0 本

**再実測では2本とも成功した。**記録した時点の失敗は再現しない。原因は、記録を取った作業場所が統合前の状態を見ていたためと考えられる。

これにより、検証ループの4判定はいずれも合格である。

| 判定 | 結果 |
|---|---|
| 自立 | 対象635本・違反0本 |
| 健全 | 検査器2本・失敗0本（再実測） |
| 網羅 | 分母82件・欠落0件 |
| 参照の実在 | 27件・違反0件 |

再現の判定（`check-reproducible.sh`）は2回の生成を要するため、この期間では実行していない。
