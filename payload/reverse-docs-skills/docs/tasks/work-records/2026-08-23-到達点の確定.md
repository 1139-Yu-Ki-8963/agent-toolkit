# 2026-08-23 到達点の確定

実施日: 2026-08-23

## 検査の結果

| 検査 | 終了コード | 要点 |
|---|---:|---|
| `bash generation-engine/scripts/verification/run-layer-machine-checks.sh --timeout 120` | 0 | 対象180本、成功178本、失敗0本、判定不能2本、総ケース数4664件だった。 |
| `bash generation-engine/scripts/check-skill-frontmatter.sh` | 0 | 52件が合格し、不合格は0件だった。 |
| `bash generation-engine/scripts/check-internal-jargon.sh` | 0 | 独自語彙の検出は0件だった。 |
| `bash generation-engine/scripts/check-doc-claim-freshness.sh` | 0 | 137件の文書を検査し、時点の表明がない未完了の断定はなかった。 |
| `bash generation-engine/scripts/rules/validate-rule-judgment-coverage.sh` | 1 | 26件が一致し、名前の不一致が1件あった。 |
| `bash .claude/rules/always/tasks/instruction-format/check-instruction-format.sh` | 0 | 対象68件がすべて合格し、不合格は0件だった。 |
| `bash docs/scripts/check-improvement-index.sh --self-test` | 127 | `docs/scripts/check-improvement-index.sh` が存在せず、自己テストを起動できなかった。 |
| `bash docs/scripts/report-progress.sh --self-test` | 0 | 4件を実行し、4件が成功し、失敗は0件だった。 |
| `node generation-engine/scripts/check-skill-machine-inspection-consistency.mjs` | 0 | 対象12件を検査し、不合格は0件だった。 |

この到達点確認では、集約検査は1回だけ完走し、約583秒で終了した。再検証担当が集約検査を再起動したが、完了前に中止したため、上表には含めなかった。個別検査はすべて5分以内に終了した。

## 数

| 対象 | 件数 |
|---|---:|
| `docs/tasks/` 直下の `.md` | 7 |
| `docs/tasks/done/` の `.md` | 58 |
| `docs/tasks/指摘改善一覧.md` の `### N-N.` 形式の見出し | 109 |
| `.claude/skills/` 直下のフォルダ | 52 |

## 不合格の詳細

### ルール判断基準の被覆検査

> 名前の不一致: 1 件
>
> 単体テスト設計書の決まり: テスト設計書の節の役割

### 改善一覧の自己テスト

> bash: docs/scripts/check-improvement-index.sh: No such file or directory

## 所見

- 優先度: 高。2026-08-23 時点で `docs/scripts/check-improvement-index.sh` が存在せず、自己テストは終了コード127で起動できなかった。
- 優先度: 中。2026-08-23 時点でルール判断基準の名前に不一致が1件あり、被覆検査は終了コード1だった。
- 優先度: 低。集約検査は終了コード0だったが、2026-08-23 時点で判定不能が2本あった。

## 進行担当による訂正（追記）

2026-08-23、上記の2件の不合格を実測で確かめ直した。

### 台帳の検査スクリプトが存在しない（終了コード127）

**この記録を取った時点で、本体の作業ツリーが別の枝へ切り替わっていた。**スクリプトを作ったコミット（`d7e3594`）は枝の側にあり、本体からは見えない状態だった。

本体を `main` へ戻して枝を取り込んだところ、スクリプトは実在し、自己テストは終了コード0で合格した。

    ls docs/scripts/check-improvement-index.sh   → 実在
    bash docs/scripts/check-improvement-index.sh --self-test → 終了コード0

### 規則と判定の名前の不一致（終了コード1）

    | 単体テスト設計書の決まり | 3 | 4 | 名前の不一致 |
    不足: 0 件 / 過剰: 0 件

不足も過剰も0件で、**規則3件に対し判定4件があり、名前が対応していない。**判定の数が規則を上回っているため、規則の取りこぼしではない。1-248（テスト設計書の節の役割を統一）でテンプレートを変えた際に、判定の名前が規則と揃わなくなったものと考えられる。

この1件は 2026-08-23 時点で残っている。
