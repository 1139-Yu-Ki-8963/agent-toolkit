# いまの到達点

作成日: 2026-08-23

## 数

| 対象 | 件数 |
|---|---|
| 片付いた指示書（done/） | 40 |
| 直下に残る指示書 | 19 |
| うち記録がすべて完了のもの | 15 |
| うち完了以外を含むもの | 4 |

`done/`の件数は、同フォルダの42ファイルから指示書ではない`この場所について.md`と`スキル実装計画.md`を除いて数えた。
直下の件数は、ファイル名が`指示書.md`で終わるものを数えた。
状態の集計では、各指示書の`対応の記録`にある最新の`判定の充足状況`を使い、明示された過去記録の表は除いた。

## 残っている作業

| 指示書 | 何が残っているか |
|---|---|
| `宣言で除外された長時間の検査を走らせる指示書.md` | 最新の判定表では成功数144本以上が`未確認`だが、同じ指示書の後段で146本、より新しい2記録で168本を確認している。条件は満たしており、判定表と後段記録の状態をそろえる作業が残る。 |
| `理由の無い目視を減らす指示書.md` | 判定5は起票時の前提が誤っていたため`対象外`であり、実作業は残っていないが、状態は`完了`ではない。 |
| `第1層に残る失敗11本を片付ける指示書.md` | 2026-08-23 時点の最新記録では、集約全体の失敗0本という条件に対して6本が不合格であり、判定13が未着手だった。 |
| `第1層の機械検証で落ちる2本を直す指示書.md` | 対象の2本は合格したが、最新記録では第1層全体の失敗を1本以下にする条件に対して6本が不合格である。 |

第1層の最新記録で不合格だった6本は、`audit-consistency.sh`、`build-portal.sh`、`extract-table-metadata.sh`、`test-api-design-decisions.sh`、`test-e2e-portal.sh`、`validate-manifest.sh`である。
この実測後に関連ファイルの変更が入っているため、現在も同じ6本が不合格かは未再確認である。

## 検査で不合格として残っているもの

次の表には、現在も不合格と確認できるものに加え、後続修正後の合否が未再確認のものと、誤起動によって検査が成立しなかったものを含める。

| 検査 | 内容 | 直せる見込み |
|---|---|---|
| 指示書の記録（未再確認） | 検査記録には42行の違反がある。その後、冒頭の状態、目視理由、完了行のコミット欄、検証コマンドを直すコミットが入ったため、現在の合否は未再確認である。 | 高い。履歴上は是正コミットがそろっており、まず再検査して残件を確定できる。 |
| 配布物の健全性（独自語彙のみ未再確認） | 正しい置き場で再実行した5検査はすべて合格した。記録上の独自の言葉遣い2件も後続コミットで置換され、現行2ファイルには残っていない。未再確認なのは独自語彙検査と、その変更が影響し得る範囲である。 | 高い。独自語彙検査を再確認し、必要な範囲だけ記録を更新できる。 |
| 文書の品質（不合格2件、未成立1件） | 時点を示さない未完了の断定が2件あり、現行文書にも残っている。別の1検査は`.mjs`をBashで誤起動したため終了コード2となり、検査自体が成立していない。 | 高い。2文へ確認時点を補い、`.mjs`をJavaScriptとして正しく起動する。正しい起動後に別の不合格が出る可能性は残る。 |

## この期間に変わったこと

直近60コミットは、接頭辞別にマージ24件、ドキュメント19件、修正14件、バグ修正3件である。
60件の変更内容は、次のように整理できる。

- 第1層の検証基盤を整えた。残っていた11本と個別の2本を修正し、集約で6本の不合格と4本の判定不能が残るところまで実測した。
- 判定不能と不合格の区別を整えた。終了コード2の由来、集約の0件表示、`done/`移動後の自己参照、サンドボックス内の`mktemp`を修正した。
- 検査の実行条件を整えた。委任時の作業場所、全角文字に隣接する変数展開、除外目印、目視へ退いていた判定を扱った。
- 指示書の記録を整えた。満たさない状態語、実測結果、実在するコミットハッシュ、完了済み指示書の冒頭情報を更新した。
- ガイドと一覧を更新した。用語集との同期、週次成果の表示、API設計スキルの説明、独自の言葉遣いを修正した。
- 検査記録を追加した。指示書の記録、配布物の健全性、文書の品質について結果を残し、配布物は正しい配置での再実行結果も追記した。
- 上記の作業ブランチを順次取り込むマージを行った。

## 次に手を付けるとよいもの

1. **文書の品質で残る2文を直し、未成立の1検査を正しく実行する。** 現在の不合格が確認できる2件を先に解消し、未成立の検査結果を確定する。正しい起動後に別の不合格が出た場合は、その結果を新たな残件として扱う。
2. **第1層の集約を再確認し、最新記録の6本を切り分ける。** 実測後に関連変更が入り、現在の不合格数が未確認である。残っていれば、2つの指示書の完了条件を同時に止めるため優先して直す。
3. **指示書の記録と配布物の健全性を再検査し、記録を更新する。** 履歴上は是正済みであるため、現状を再確認すれば古い不合格一覧を現在の到達点へそろえられる。

## 未再確認だった項目の実測（進行担当が追記）

2026-08-23、上記の整理が「未再確認」とした項目を実際に実行して確かめた。

| 検査 | 終了コード | 結果 |
|---|---|---|
| 独自の言葉遣い | 0 | 合格 |
| 時点の表明 | 0 | 合格（この文書の追記前） |
| 機械検査の整合 | 0 | 対象12件すべて合格 |
| スキルの説明 | 0 | 52件すべて合格 |

指示書の記録の6観点も数え直した。

| 観点 | 件数 |
|---|---|
| 冒頭が完了になっているもの | 1（`指摘改善一覧.md`。台帳のため別規約が適用され違反ではない） |
| 実在しないコミット表記 | 0 |
| 冒頭の状態を持たない `done/` の指示書 | 0 |

**レビューが挙げた「未再確認」の3項目は、いずれも既に解消していた。**残る不合格は第1層の集約の6本だけである。
