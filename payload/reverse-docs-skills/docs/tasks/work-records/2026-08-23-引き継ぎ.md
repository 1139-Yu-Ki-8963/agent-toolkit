# 2026-08-23 引き継ぎ

作成日: 2026-08-23 10:20
更新時点: 2026-08-23 21:00
作成者: 進行担当のセッション

## いまどうなっているか

### 更新前からある記録

以下は、作成欄を午前10時20分とする更新前の本文を保持したものである。

**引き継ぎ時に残っていた3件はすべて終わった。**
Superset による中断も解消し、作業を再開した。

| 項目 | 状態 |
|---|---|
| 統合と公開 | 通算69件 |
| 直下 | 7件（うち台帳2件） |
| `done/` | 58件 |
| 台帳 | 109件（1-26から1-251）・未完了0件 |
| 第1層 | 対象180本・成功178本・失敗0本・判定不能2本 |

### 21時時点

件数は21時以降にこの作業場所で実行した3コマンドの結果である。

| 項目 | 実測 |
|---|---|
| `docs/tasks/` 直下のMarkdown | 16件 |
| `docs/tasks/done/` のMarkdown | 58件 |
| `docs/tasks/design/` の項目 | 0件 |
| 午後に本体への取り込みと公開を確認した作業 | 10件 |
| 検証担当 | 5人。4人は記録済み、1人は実行中 |

直下16件には、対応の記録が全判定を完了としている指示書と、取り込み済みの指示書が残っている。
別の担当が移動中であるため、直下16件を未完了件数とは扱わない。
`docs/tasks/design/` にファイルは無い。

## この期間に何が変わったか

### 更新前からある記録

以下は、作成欄を午前10時20分とする更新前の本文を保持したものである。

| 何が | どうなったか |
|---|---|
| 第1層の機械検証 | 失敗28本 → 0本（対象180本・成功178本・判定不能2本） |
| 指示書 | 直下21件 → 7件（うち台帳2件） / `done/` 42件 → 58件 |
| 指摘改善一覧 | 台帳109件（1-26から1-251）がすべて完了し、未完了0件になった |
| 指定6検査の合否 | 対象395件・合格395件・不合格0件 |
| コミットと課題の対応 | 違反 28件 → 19件（台帳由来11件を解消） |
| 規則と判定の対応 | 不足1件 → 0件 |
| 検証ループの4判定 | 網羅・自立・健全は合格。再現は2026-08-23 時点で未実行 |
| テスト一覧（1-247） | テストの一覧を全種別から作れるようにした |
| テスト設計書（1-248） | テスト設計書の節の役割を統一した |
| 資料パス（1-249） | 全種別の資料パスを実在判定し、`--argjson` の可変長渡しも是正した |
| 一覧の資料枠（1-250） | 一覧の資料枠と操作を正した |
| 指示書検査（1-251） | 指示書の書き方の検査を配布物へ加えた |
| セッションプロンプト | 6件995行から2件811行へまとめた |
| 台帳の検査 | check-improvement-index.sh を作った |
| git の設定 | Intel 版の残骸である `/usr/local/bin/gh` を指していた設定を直した |

### 21時までに追加で完了したもの

Git履歴で次の10件の対応を確認した。
進行担当の21時時点の実測では、いずれも本体への取り込みと公開を終えている。

| 何が | どうなったか |
|---|---|
| 入口のページ | 開発フローで更新し続ける仕組みを本体へ取り込んだ |
| 運営文書の置き場 | 納品先の配置を定め、本体へ取り込んだ |
| 規約の定義と生成物 | 分離をこのリポジトリへ適用し、本体へ取り込んだ |
| 判定器 | 表の縦棒を壊す問題と判定不能の扱いを直した |
| 取り込み後の検証で見つかった4件 | 4件を直し、本体へ取り込んだ |
| 台帳と置き場 | ずれを直し、本体へ取り込んだ |
| 第1層の判定 | 終了コードで判定する形へ直し、本体へ取り込んだ |
| 対象0件の検査 | 合格を返さない形へ直し、本体へ取り込んだ |
| ガイドの改行 | 52件のガイドと、違反を見逃していた自己検査を直し、本体へ取り込んだ |
| スキルの説明 | 初見で役割が伝わらなかった5件を書き直し、本体へ取り込んだ |

進行担当がこの引き継ぎへ反映するよう指定した午後の欠陥は、次のとおりである。
いずれも21時までに是正と公開を終えた。

- 鮮度検査が、表示コミットを取得できない場合を判定不能ではなく鮮度不足として扱っていた。
- 環境変数による受け渡しと一時ファイルによる受け渡しの2件に、素直でない実装を選んだ理由が無かった。
- 規約が設計判断と実装判断を混同していた。
- 一覧の検査が対象0件でも合格を返していた。
- 進行担当の実測では、ガイド52ファイルでブロック要素を行ごとに分ける規約への違反があり、`</td><td` の同一行配置だけで1,540箇所あった。
- 上の違反を見つける自己検査が合格を返していた。
- スキル5件の説明だけでは、初見の読み手が役割を特定できなかった。

一覧の検査は、対象が0件の状態を合格と誤判定した。
ガイドの自己検査は、対象に改行規約違反がある状態で合格を返した。
この2件では、検査自体が期待した判定をしていなかった。

## 検証の担当を立てた

5人には成果物を作らせず、誤りがある前提で異なる面を調べさせた。
4人は結果を記録し、修正は別の担当が行った。

| 担当 | 見た面 | 結果 | 記録 |
|---:|---|---|---|
| 1 | 当日の変更が4規約と6観点に適合するか | 誤り4件を確認 | `docs/tasks/work-records/2026-08-23-取り込み後の検証.md` |
| 2 | 配布物が納品先で動くか | 9検査中、不合格0件 | `docs/tasks/work-records/2026-08-23-配布物の健全性の検証.md` |
| 3 | 見本を定義から作り直せるか | 7検査中、不合格1件 | `docs/tasks/work-records/2026-08-23-見本と定義の対応の検証.md` |
| 4 | スキルの説明が読み手に伝わるか | 6観点中、不合格2観点。説明5件とガイドの改行および自己検査に問題があった | `docs/tasks/work-records/2026-08-23-スキルの使いやすさの検証.md` |
| 5 | 検証の仕組み自体が働いているか | 4観点を実行中 | 21時時点で記録なし |

## 判定不能2本について

第1層に残る判定不能2本は、**PyYAML を使えない実行環境と、隔離環境の Python 不在**が原因である。
「実行できなかった」と「不合格だった」の区別は、`.claude/rules/always/verification/indeterminate-result/rule.md` が定めている。
したがって、規約上は不合格ではなく判定不能として扱った。

## 次に手を付けるもの

### 更新前からある記録

以下は、作成欄を午前10時20分とする更新前の本文を保持したものである。

この資料が次に手を付けるものとしていた3件はすべて完了した。
台帳109件も未完了0件になった。
直下の非台帳5件も完了判定はすべて完了しており、完了判定上の残作業はない。
直下7件という数は、未完了件数を表すものではない。

### 21時時点

次に手を付けるものの1件目は、5人目が実行中の「検証の仕組み自体が働いているか」の4観点を完了し、結果を記録することである。
21時時点で、この担当に対応する検証記録は `docs/tasks/work-records/` に無い。

2件目は、見本と定義の対応を合否が付く状態で再検証することである。
引き継ぎ更新時に次のコマンドを再実行した。

    env VERIFY_RUN=1 bash generation-engine/scripts/unit-list/check-manifest-persistence.sh generation-engine/samples

判定対象0件のため、結果は `[UNKNOWN]`、終了コード2だった。
対象0件を合格にする欠陥は直っているが、見本と定義の対応が合格したことは確認できていない。
`docs/tasks/work-records/2026-08-23-見本と定義の対応の検証.md` は、同じ検査について次の未是正箇所を記録している。

- チェッカーが英語のフォルダ名と定義上のHTML名を対応できていない。
- 画面種別の拡張マニフェスト見本で、内容ハッシュ値の不一致と既存項目の保持違反がある。
- 生成器が元マニフェストの既存値を上書きし、再生成しても保持違反を再発させる。
- 整合検査が比較前に既存項目を削除し、保持違反を見落とす。

見本と定義の対応を合格へ戻すには、この4件の是正と再検証が残っている。

`docs/tasks/design/` は0件である。
`docs/tasks/` 直下の16件は移動中の指示書を含むため、直下の件数から新しい作業を選ばない。

### 前の2件をどう片付けるか

前の2件は、判定の「確かめる手段」を終了コードではなく、各判定条件に対応する集計欄を読む形へ書き換え、全件完了した。
この変更では、元の判定条件を保った。
`指摘改善一覧.md` に残っていた台帳自身の判定5件も完了した。

## 中断した理由

利用者が Superset を更新するため、キリの良いところで作業者を全件終了させた。
**作業者の実体は `/Users/MacPro/.superset/bin/codex` であり、Superset そのものが実行基盤だったため、更新中の実行を避けた。**

その後、Superset の更新は完了し、作業を再開した。
再開後に、引き継ぎ時に残っていた3件、1-247から1-251、セッションプロンプトの集約、台帳の検査スクリプト作成、git の設定修正を完了した。
2026-08-23 時点で、中断は解消した。

## 進め方の記録

この期間の進め方は次にまとめた。次に同じことをするときはこれを読む。

    docs/session-prompts/作業者のプロンプト.md
    docs/session-prompts/進行担当のプロンプト.md

内容は、測る3つの数（資源・再起動の記録・負荷）、数から決めること、衝突を避ける見方、渡すときに書くこと、裏取りの5手順、待ち方、気をつけることである。

## 作業者の報告で実際に起きた食い違い

**報告をそのまま信じてはならない。**この期間に次が起きた。

| 起きたこと | 回数 |
|---|---|
| 冒頭の状態を「完了」に変えた | 3回 |
| 実行していないのに「完了」と書いた | 2回 |
| コミット欄に実在しないハッシュを書いた | 2回 |
| 移したと報告したが実際には移っていなかった | 1回 |
| 規約が定めない語を状態の欄へ書いた | 1回 |
| 本体の作業ツリーで動いていた | 2回 |
| 止めたはずが動き続けた | 1回 |
| 本体の作業ツリーが作業場所の枝へ切り替わっていた | 1回 |
| 作業場所で判定器を実行し、`done/` へ移した（禁止事項） | 1回 |
| 判定の「確かめる手段」が説明文で、コマンドの形ではなかった | 2件 |
| 判定行のコマンド中のパイプが表の区切りと衝突していた | 3件 |
| 判定器を同じ場所で変更する担当同士が衝突し、片方を止めた | 1件。変更0件 |
| 作業場所を消した後もプロセスが動き続け、同時実行が上限3件を超えた | 1回。4件まで増加 |
| 本体に残った追跡外ファイルがマージを妨げた | 2回 |
| サンドボックスの制約で `mktemp` が失敗し、判定不能になった | 複数回 |
| 検査が合格を返したが、対象に欠陥があった | 2件 |

午前分はいずれも裏取りで見つけて是正した。
午後分では、担当同士の衝突は片方を止め、追跡外ファイルは退避して解消した。
**裏取りを省くと、報告との食い違いを見つけられない。**

## この期間に作った記録

| 記録 | 内容 |
|---|---|
| `いまの到達点.md` | 数・残っている作業・次に手を付けるとよいもの |
| `この期間の成果.md` | 何が変わったか・いまの合否・記録の一覧 |
| `第1層の失敗の是正記録.md` | 失敗3本を0本にした経緯 |
| `指示書の記録の検査結果.md` | 規約6観点での違反約40件と是正 |
| `配布物の健全性の検査結果.md` | 参照・自立・リンクの6検査 |
| `文書の品質の検査結果.md` | 鮮度・用語集・ガイド・説明の8検査 |
| `規約と生成物の整合の検査結果.md` | 規約定義と生成物の7検査 |
| `見本の健全性の検査結果.md` | 見本の6検査 |
| `検証ループの判定の現状.md` | 網羅・自立・再現・健全の4判定 |

## 資源の推移

対策（2026-08-22 20:12・PATH の並びの修正）から約14時間の実測。

    最高      68,881（大規模な作業2件を同時に走らせたとき）
    平常      300〜7,900
    危険水準   40,000
    再起動     2件のまま。増えていない

**対策前は作業1件で毎分8,000ずつ増え続けた。**対策後は作業量に応じて上下し、作業を止めれば減る。増え方が質的に変わっている。

負荷が25まで上がることがあるが、これは検証（180本を順に走らせる）が重いためであり、資源の枯渇とは別の現象である。

### 今日の後半

進行担当が2026-08-23 21時までに記録した値は次のとおりである。

    vouchers（資源数）          239〜344
    危険水準  40,000
    panic（再起動記録件数）    2件のまま。増加なし
    load      投入直後に19.77と21.54。いずれも15分以内に5前後へ低下

## 深夜帯（2026-08-23 21時〜2026-08-24 00時）に起きたこと

### 作業を受け持つ側が止まった

利用者から「codex は上限で稼働しないはず」との指摘を受けた。
測ったところ、プロセスは生きていたが CPU 時間が 0.00 秒だった。

    pid=80298  経過 10分28秒  CPU時間 0:00.00
    pid=88741  経過  1分51秒  CPU時間 0:00.00

10 分間で 1 秒も計算していない。ログの末尾も待機の表示で止まっていた。
成果を出した最後は 23:35 で、それ以降は 1 件も出していない。

**進行担当はこれを「稼働中」と報告し続けていた。**
プロセスの生存とログのバイト数だけを見て、CPU 時間を見ていなかった。

以後、稼働の判定には CPU 時間を含める。次で測る。

    ps -o pid=,etime=,time=,stat= -p <番号>

作業を受け持つ側は全件停止し、以降は進行担当が自分の手で作業した。

### 自分の手で直したもの

| 直したもの | 何が起きていたか |
|---|---|
| 宣言データ不在の誤報 | 定義ファイルが無い場合を「値が不正」と報告していた。配る先には定義ファイルが無いため、納品先では必ず不合格を返す状態だった |
| 台帳検査の不具合 | 全角文字が変数名に続く形（`$name（`）で `unbound variable` になり、検査自体が落ちていた |
| 判定の誤検出 | 「壊れた指示書」の判定が逆引用符の対を見ておらず、正しい行 2 件を誤って検出していた |
| 取り込み後に崩れる判定 3 件 | 「未コミットの変更が 0 件」等は作業していた場所でしか成り立たない。版管理へ入ったことを見る形へ改めた |

### 自分の変更が退行を起こしていた

22:42 に取り込んだ変更が、第 1 層の失敗を 0 本から 2 本へ増やしていた。

    朝    対象 180 本 / 成功 178 本 / 失敗 0 本 / 判定不能 2 本
    夜    対象 182 本 / 成功 177 本 / 失敗 2 本 / 判定不能 2 本

原因は、追加した検査が定義ファイルの存在確認を持たなかったことである。
検査そのものは実在の問題（宣言のない検査 2 件）を見つけたものなので消さず、
存在確認を足して通るようにした。定義を 1 件削ると今も終了コード 1 で検出する。

### 確かめて「問題なし」と分かったもの

7 人目の検証担当が「集約が判定不能の理由を出力から落とす」と報告した。
実際に子検査へ `[UNKNOWN]` を返させて確かめたところ、理由は残っていた。

    [UNKNOWN] generation-engine/scripts/x-unknown.sh — 実行 0 件 / 終了コード 2
      [UNKNOWN] 試験用に判定できません 操作: mktemp / 想定原因: 一時領域が無い
    対象 1 本 / 失敗 0 本 / 判定不能 1 本

契約の検査（ラベル・操作名・想定原因の 3 つを要求する）も実装済みだった。
**報告を受けても、直す前に自分で再現する。**この 1 件は是正が不要だった。

### 検証の担当を 8 人立てた

作らせるのではなく、誤りがある前提で探させ、直させず記録だけさせた。

| 見た面 | 結果 |
|---|---|
| 今日の変更が規約に適合するか | 誤り 4 件 |
| 配る対象が納品先で動くか | 不合格 0 件 |
| 見本が定義から作り直せるか | 不合格 1 件 |
| スキルの説明が読み手に伝わるか | 不合格 2 件 |
| 検証の仕組み自体が働いているか | 穴 9 件 |
| 配る規約の検査が違反を検出するか | 検査 29 件すべて検出。別の問題 3 件 |
| 判定不能が正しく数えられるか | 失敗が 2 本増えていることを発見 |
| 引数の扱いが揃っているか | 記録を残さず終了。やり直しが要る |

**見つけた問題のうち、進行担当が自分で再現して確かめたものは 7 件である。**
報告をそのまま信じず、実際にコマンドを走らせた。うち 1 件（語彙一覧の欠落）は
「直した」という報告が実際には片手落ちで、差し戻して初めて片付いた。

### 繰り返し現れた欠陥の形

**「調べる対象が 0 件のとき、または対象が存在しないとき、合格を返す」**
この形の欠陥が 1 日で 5 件見つかった。

    一覧の検査が対象 0 件を合格とする
    自立の検査が存在しないパスを合格とする
    鮮度の検査が判定できない場合を不合格とする
    ガイドの検査が 1540 箇所の違反を見逃す
    引数不足で終了コード 0 を返す検査がある

「何も見つからなかった」ことと「見て問題がなかった」ことは違う。
検査が合格を返すことと、対象に問題がないことは別である。

### 資源の推移（深夜帯）

    vouchers  232〜276 で推移。危険水準 40,000 には遠い
    panic     2 件のまま。増えていない
    load      投入直後に 24.63 の山が 1 回。15 分以内に 5 前後へ低下
