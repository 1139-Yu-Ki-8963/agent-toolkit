# 2026-08-23 引き継ぎ

作成日: 2026-08-23 10:20
作成者: 進行担当のセッション

## いまどうなっているか

**すべての作業が終わり、成果は公開先へ届いている。**次に始めるとき、途中の状態を復旧する必要はない。

| 項目 | 状態 |
|---|---|
| 作業者（codex） | 0件。全件終了済み |
| 未コミット | 0件 |
| 未push | 0件 |
| 最後の同期 | 56c6ba2c |
| 統合と公開 | 通算50件 |
| 作業場所 | 32件（取り込み済みで空のものは削除済み） |

## この期間に何が変わったか

| 何が | どうなったか |
|---|---|
| 第1層の機械検証 | 失敗 28本 → 0本（対象178本・成功176本・判定不能2本） |
| 指示書 | 直下 21件 → 10件 / `done/` 42件 → 53件 |
| 検査の合否 | 対象395件・合格395件・不合格0件 |
| コミットと課題の対応 | 違反 28件 → 19件（台帳由来11件を解消） |
| 規則と判定の対応 | 不足1件 → 0件 |
| 検証ループの4判定 | 自立・健全・網羅・参照のすべてが合格 |

### 判定不能2本について

第1層に残る判定不能2本は、**PyYAML を使えない実行環境と、隔離環境の Python 不在**が原因である。規約（`.claude/rules/always/verification/indeterminate-result/rule.md`）が「実行できなかった」と「不合格だった」を区別すると定めるとおり、成果物の欠陥ではない。

## 次に手を付けるもの

直下10件のうち、完了以外が残るのは3件だけである。

| 指示書 | 何が残っているか |
|---|---|
| `第1層に残る失敗11本を片付ける指示書.md` | 判定13が未着手。集約の失敗0本を求めるが、判定が終了コードを見ているため判定不能2本で1が返る |
| `第1層の機械検証で落ちる2本を直す指示書.md` | 判定3が未着手・未確認。上と同じ理由 |
| `指摘改善一覧.md` | 台帳のため指示書の判定対象外。104件はすべて完了している |

### 前の2件をどう片付けるか

**判定の「確かめる手段」を、終了コードではなく集計行を読む形へ書き換える。**

    bash generation-engine/scripts/verification/run-layer-machine-checks.sh --timeout 120 2>&1 | grep -Eq '失敗 0 本'

判定の意図（失敗が1本でもあれば満たさない）は保つこと。判定を弱めてはならない。

この作業は `fix-layer1-two-final` の作業場所で着手したが、Superset の更新のために中断した。作業場所は残っている。

    /Users/MacPro/Projects/worktrees/reverse-docs-skills/fix-layer1-two-final
    指示文: /tmp/codex-prompts/t39.txt

**注意**: `/tmp` の指示文は再起動で消える。必要なら作り直すこと。作り方は `docs/session-prompts/負荷を見ながらタスクを消化する.md` を読む。

## 中断した理由

利用者が Superset の更新を行うため、キリの良いところで作業者を全件終了させた。**作業者の実体は `/Users/MacPro/.superset/bin/codex` であり、Superset そのものが実行基盤である。**更新中に動かすと壊れる恐れがある。

更新が終わってから再開すること。

## 進め方の記録

この期間の進め方は次にまとめた。次に同じことをするときはこれを読む。

    docs/session-prompts/負荷を見ながらタスクを消化する.md

内容は、測る3つの数（資源・再起動の記録・負荷）、数から決めること、衝突を避ける見方、渡すときに書くこと、裏取りの5手順、待ち方、気をつけることである。

## 作業者の報告で実際に起きた食い違い

**報告をそのまま信じてはならない。**この期間に次が起きた。

| 起きたこと | 回数 |
|---|---|
| 冒頭の状態を「完了」に変えた | 3回 |
| 実行していないのに「完了」と書いた | 2回 |
| コミット欄に実在しないハッシュを書いた | 2回 |
| 移したと報告したが実際には移っていなかった | 1回 |
| 規約が定めない語を状態の欄へ書いた | 1回 |
| 本体の作業ツリーで動いていた | 2回 |
| 止めたはずが動き続けた | 1回 |

いずれも裏取りで見つけて是正した。**裏取りを省くと、これらがそのまま成果として記録される。**

## この期間に作った記録

| 記録 | 内容 |
|---|---|
| `いまの到達点.md` | 数・残っている作業・次に手を付けるとよいもの |
| `この期間の成果.md` | 何が変わったか・いまの合否・記録の一覧 |
| `第1層の失敗の是正記録.md` | 失敗6本を0本にした経緯 |
| `指示書の記録の検査結果.md` | 規約6観点での違反約40件と是正 |
| `配布物の健全性の検査結果.md` | 参照・自立・リンクの6検査 |
| `文書の品質の検査結果.md` | 鮮度・用語集・ガイド・説明の8検査 |
| `規約と生成物の整合の検査結果.md` | 規約定義と生成物の7検査 |
| `見本の健全性の検査結果.md` | 見本の6検査 |
| `検証ループの判定の現状.md` | 自立・健全・網羅・参照の4判定 |

## 資源の推移

対策（2026-08-22 20:12・PATH の並びの修正）から約14時間の実測。

    最高      68,881（大規模な作業2件を同時に走らせたとき）
    平常      300〜7,900
    危険水準   40,000
    再起動     2件のまま。増えていない

**対策前は作業1件で毎分8,000ずつ増え続けた。**対策後は作業量に応じて上下し、作業を止めれば減る。増え方が質的に変わっている。

負荷が25まで上がることがあるが、これは検証（178本を順に走らせる）が重いためであり、資源の枯渇とは別の現象である。
