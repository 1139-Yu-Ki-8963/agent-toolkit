# 2026-08-19 第 1 層を直した記録

## 何をしたか

第 1 層の機械検証で落ちていた検査を直し、走っていなかった検査を集約へ載せた。**7 本を直した。**

## 数字で見た結果

```
今夜の始め  対象 149 本 / 成功 143 本 / 失敗 4 本 / 総ケース数 1795 件
いま        対象 160 本 / 成功 155 本 / 失敗 2 本 / 総ケース数 3795 件
```

**総ケース数が 2 倍以上になった。** 走っていなかった検査を載せたぶんと、入り口で止まっていた検査が最後まで走るようになったぶんである。

## 直した 7 本

| 対象 | 何が起きていたか |
|---|---|
| `prefill-design-from-facts.sh` | 転記先を章番号で探しており、ひな形の番号が付け替わって 13 件中 8 件がずれていた |
| `build-matrix-data.sh` | 画面だけ拡張版のデータを読み、API は一覧の基本版を読んでいた |
| `test-badge-nav-contrast.cjs` | 初期状態からの最初の切り替えを測っており、遷移の途中の値を拾っていた |
| 集約の収集条件 | Node.js のテストは名前で拾うのに、同じ性質の `.sh` を拾っていなかった |
| 集約の呼び方 | 拡張子だけで決めており、中身に関わらず同じ引数を渡していた |
| `test-screen-unit-root-layout.sh` | 見本の元コードの場所が相対のままで、44 件すべて実在しないと判定されていた |
| `test-portal-conventions.sh` | 自分の説明資料 3 件が除外に書き漏れていた |

**7 本のうち 5 本は、走っていなかった検査を集約へ載せたことで初めて見えた。** 載せなければ今も誰も気づいていない。

## 残る 2 本

```
detail-pages/test-project-semantic-glossary-race.sh   PyYAML が無くて走れない
glossary/tests/test-validate-semantic-glossary.sh     venv が無くて走れない
```

**どちらも中身の不備ではない。実行の環境に道具が無いために走れない。** 成果物の修正では直らない。

## 判断が要ること

**この 2 本のために、このマシンへ道具を入れるかどうか。**

入れれば失敗は 0 本になる。入れなければ 2 本が残り続ける。判断の材料は次のとおり。

- 2 本が何を検査しているか（用語の一覧の整合。詳しくは各スクリプトの冒頭）
- 道具を入れる手間と、入れたあとの保守
- 検査が動かないまま残ることの害（用語の一覧の不整合に気づけない）

**決めていないため、いまは失敗 2 本のまま残している。**

## 途中で分かったこと

| 分かったこと | 実測 |
|---|---|
| 集約と片付き判定を並行させると集約が巻き添えで止まる | 2 度実測（45 本・32 本で停止） |
| 片付き判定の全件走査は 80 分を超える | 1 件あたり 93 秒 × 54 件 |
| 委任のときに作業場所を渡す仕組みが機械で保証されていない | 1 件、担当が検査を迂回した |

いずれも指示書へ記録済み。3 つ目は `docs/tasks/委任のときに作業場所を渡させる指示書.md` として起票した。

## 自分の誤りを 2 件正した

**1 件目**: 転記先の対応表で、実測を要する事項の行き先を「非機能要件」と指示したが、正しくは「要確認事項一覧」だった。章の入れ替えで番号だけが引き継がれており、番号で照合して語の意味を確かめていなかった。担当が実測で見つけて正した。

**2 件目**: サイドバーの配色を計算するとき、背景を取り違えた。結論（配色は正しい）は変わらなかったが、誤った値を残さないよう訂正した。
