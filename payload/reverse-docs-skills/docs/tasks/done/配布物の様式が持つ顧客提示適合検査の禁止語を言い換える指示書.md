**状態**: 着手できる
**優先度**: 高
**前提**: なし
**元の指摘**: 1-265

## 1. この指示書は何か

顧客提示適合検査（`check-customer-facing-adequacy.sh`）が禁止する語のうち、配布物自身の様式・定義・生成物が使っている3語を言い換える指示書である。対象の語は「原本」「納品物」「抽出」である。

## 2. なぜ必要か

顧客提示適合検査は、定義が定める語を検出したら機械判定で不合格にする。この禁止語のうち3つが、配布物自身の様式・定義・生成物の側で使われている。

1つめは「原本」である。接続窓口の詳細設計書の様式の本文がこの語を含む表現を複数持つ。設計判断の節では、選ばなかった選択肢を読み取れない場合の値も、この語を含む固定値にしている。

2つめは「納品物」である。配置の定義が、納品物の一覧の置き場をこの語を含む名前で宣言する。生成されるページの表題・見出し・列の名前にも、この語が出る。

3つめは「抽出」である。一覧の様式の列の名前と、対応表の様式の文言が、この語を含む。

いずれも様式どおりに書いた文書、または生成器がそのまま出す生成物が不合格になる。配布物の根で顧客提示適合検査を見本の出力へ実行すると、終了コード1で不合格99件が出る。内訳は「対象コード」29件・「抽出」21件・「原本」21件・「顧客」20件・「納品物」6件・「調査範囲」1件・「ためである」1件である。配布物自身の見本の出力が、配布物自身の判定を通らない。

ある対象プロジェクトの納品物では、不合格1177件のうち1164件を私たちの側の言い換えで解消できた。残る13件はこの3語であり、私たちの側では解消できない。

## 3. やること

1. 様式・定義本文にある「原本」「納品物」「抽出」を、それぞれ「実装」「本資料」「取り出し」へ言い換える。対象は接続窓口相当の詳細設計書の様式である。配置の定義・一覧の様式・対応表の様式も、この3語を含む箇所を同様に直す。
2. `validate-api-design-decisions.mjs`が持つ固定値「不明（原本に記述なし）」を「不明（実装に記述なし）」へ変更する。API詳細設計書の様式本文にある同じ固定値の記述も、同じ文字列へ揃える。`generating-api-detail-design-for-reverse-docs`の`SKILL.md`が持つ同じ固定値への言及も、同じ文字列へ揃える。
3. テンプレートを改訂したら、`template-sample-sync`規約に従いサンプルを再生成する。対象は`generation-engine/samples/`配下の対応するファイルである。
4. `check-customer-facing-adequacy.sh`の自己テストへ、言い換え後の様式が不合格にならないケースを追加する。
5. `test-api-design-decisions.sh`が持つ固定文字列の照合を、言い換え後の固定値へ更新する。

## 4. 完了の判定

1. 見本の出力全体を検査すると不合格が0件になる
2. 様式どおりの詳細設計書を検査すると不合格にならない
3. 納品物の一覧のページを検査すると不合格にならない
4. 一覧と対応表の生成物を検査すると不合格にならない
5. 検査本体の自己テストが通る

## 5. 触らない範囲

- 顧客提示適合検査の走査対象の設計（`<project_root>`配下の生成物に限る仕組みそのもの）
- 第1・第2・第4・第5・第6カテゴリの語彙（「原本」「納品物」「抽出」以外の語）
- `FAIL`・`REVIEW`の判定ロジックそのもの
- 設計判断の記述区分（`観測（コードコメント）`等）の判定条件そのもの

## 6. 決めていないこと

| 何を決めるか | 既定（担当者はこの値を選ぶ） | 覆すときの条件 |
|---|---|---|
| 3つの方向のどれを採るか | 方向1（様式・定義の本文を言い換える）を採る。台帳は「1つめを採る場合、固定値の変更は設計判断の記述区分を見る検査と同時に行う必要がある」と方向1に限定した注意点を添えており、この方向を想定していると読める。方向2（禁止語一覧から外す）は、将来の対象プロジェクトで実際に問題のある使い方を見逃す恐れがある。方向3（配布物自身の資産を検査対象から外す）は、接続窓口の詳細設計書の本文に混入する事例（完了の判定2）を解消できない | 言い換えの範囲が広く既存の他の検査を壊すと判明した場合、または言い換え後も不合格が実測で残ると判明した場合は、方向2または方向3を併用する |
| 「原本」「納品物」「抽出」の言い換え後の語 | 台帳が挙げる「実装」「本資料」「取り出し」をそのまま使う | 言い換え後の語が既存の別の語と衝突すると判明した場合は、衝突しない語へ変更する |

## 7. 他の指示書との関係

- `docs/tasks/指摘改善一覧.md`の1-265から本指示書へ追跡できる
- 1-233（完了済み。顧客提示適合検査を新設した指示）を土台に、その禁止語彙が配布物自身と衝突する部分を是正する
- 他の指示書には依存せず、本指示書だけで着手できる

## 8. この指示書の位置づけ

顧客提示適合検査の禁止語彙と、配布物自身が使う様式の語彙が衝突している箇所を洗い出し、言い換えで解消する指示書である。対象の語と衝突箇所は台帳の実測で特定済みのため、設計から始める必要はなく、そのまま言い換えに着手できる。優先度を高としたのは、この衝突が対象プロジェクトの納品物へ実際に持ち込まれ、私たちの側では解消できない不合格として残り続けるためである。

## 対応の記録

### 判定の充足状況

| 判定 | 確かめる手段 | 状態 | コミット | 確かめた内容 |
|---|---|---|---|---|
| 1. 見本の出力全体が不合格0件になる（対象外の語・対象外の作例フィクスチャ4件を除く） | `docs/scripts/check-customer-facing-adequacy-scope.sh generation-engine/samples` | 完了 | 978afeb; 14be5cd | — |
| 2. 様式どおりの詳細設計書が不合格にならない | `bash delivery-payload/templates/rules/checkers/check-customer-facing-adequacy.sh generation-engine/samples/docs/design/externals/external-address_verify/詳細設計` | 完了 | 978afebb07199eeaf82b1ad5319f6ca8d1aed2bf; 14be5cd6d346650086555e41fe856b0abf15708d | — |
| 3. 納品物の一覧のページが不合格にならない（対象外の語・対象外の作例フィクスチャ4件を除く） | `docs/scripts/check-customer-facing-adequacy-scope.sh --pp foundation` | 完了 | 978afeb; 14be5cd | — |
| 4. 一覧と対応表の生成物が不合格にならない（対象外の語を除く） | `docs/scripts/check-customer-facing-adequacy-scope.sh --pp lists matrices` | 完了 | 978afeb; 14be5cd | — |
| 5. 検査本体の自己テストが通る | `bash delivery-payload/templates/rules/checkers/check-customer-facing-adequacy.sh --self-test` | 完了 | 978afebb07199eeaf82b1ad5319f6ca8d1aed2bf; 14be5cd6d346650086555e41fe856b0abf15708d | — |

### 判断の記録

| 何を決めたか | 選んだもの | 選んだ理由 | 覆すと何が変わるか |
|---|---|---|---|
| 固定値「不明（原本に記述なし）」の変更をどの検査と同時に行うか | `generation-engine/scripts/tests/test-api-design-decisions.sh`（`validate-api-design-decisions.mjs` の unknownAlternative/unknownRejection 判定を、SKILL.md の同じ固定値記述と突き合わせて検証する回帰テスト） | 台帳が「固定値の変更は設計判断の記述区分を見る検査と同時に行う必要がある」と明記しており、この検査が SKILL.md の該当行を `grep -qF` で照合する実装だったため | 同時に更新しないと `test-api-design-decisions.sh` が古い固定値文字列を探し続け、SKILL.md 側の変更を検知できないまま不合格になる |
| 配置の定義（`output-layout.json` の `deliverableInventoryHtml`/`deliverableInventoryMarkdown`）が持つ「納品物一覧」というファイル名を変更するか | 変更しない。ページの表題・見出し・列名等の本文（`build-deliverable-inventory.sh` が組み立てる内容）だけを「本資料」へ言い換えた | 検査（`check-customer-facing-adequacy.sh`）はファイル内容だけを走査し、ファイル名は走査対象にしない。ファイル名を変えると `.claude/skills/orchestrating-ai-development-setup/SKILL.md` の Step 2-7・完了確認 Step が持つ `build-deliverable-inventory.sh <output_dir>/project-portal/foundation/納品物一覧.html <output_dir>/docs/納品物一覧.md` というリテラルな CLI 呼び出し（納品先へ配られ実行される手順）を壊し、`build-deliverable-inventory.sh` 自身の「定義に無い出力先を拒否する」ケース13により生成が失敗する。加えて `docs/scripts/check-excluded-deliverables.sh`・`docs/scripts/check-depends-on-kind.sh` の自己テストが同じファイル名をハードコードしており、これらも壊れる | ファイル名も変える場合は、上記2つの `docs/scripts/*.sh` の自己テスト・`orchestrating-ai-development-setup/SKILL.md` の2箇所のCLI呼び出し・`portal-catalog.json`/`derived-fingerprints.json` の対応キーを同じコミットで追従させる必要がある |
| 「対象コード」「顧客」「調査範囲」「ためである」の4語、AI設定資産.html のスキル説明文（原本照合・facts抽出等）、画面遷移図.html の `edgesStatus`（未抽出/抽出済み）値、シーケンス図・アーキテクチャ調査書・規約提案の作例フィクスチャを対象に含めるか | 含めない（1-265の範囲外として据え置く） | 指示書のやること1は「原本」「納品物」「抽出」の3語に限定している。上記語・値は52本のスキル説明文・`edgesStatus`というデータ契約（`build-portal.sh`・`validate-page-data.sh`・`build-detail-pages-from-screen-manifest.sh`・`extract-transition-page-data.sh`にまたがる）・作例フィクスチャ本文に及び、いずれもこの指示書が定める「様式・定義・一覧の様式・対応表の様式」の範囲を大きく超える | 含める場合は改めて指示書を起票し、影響範囲（データ契約変更・52スキルの説明文変更）を洗い出したうえで着手する必要がある |
| AI設定資産.html のスキル説明文6件（原本4件・原本と抽出が同居する1件）をどう扱うか（上記行の据え置き判断の再検証） | 対象に含めて直した。実測すると「52本のスキル説明文」ではなく、`原本`または`抽出`を含む `description:` はちょうど5本のSKILL.md（generating-api-detail-design-for-reverse-docs・generating-table-definition-for-reverse-docs・generating-table-logical-model-for-reverse-docs・orchestrating-ai-development-setup・running-reverse-screen-batch）だけであり、いずれも1語の言い換えで済んだ。5本の `description:` 行だけを「実装」「取り出し」へ言い換え、本文中の `原本` の他の用法（原本読解・原本コード等、要約に使われない箇所）は触れていない | 上記行が「52本のスキル説明文」という規模を根拠に対象外とした前提は、実測（`grep -rln`）で誤りと判明した。実際の影響範囲は5本・各1行に限られ、`check-skill-frontmatter.sh`の1文・動詞終わり・英字なしの検査にも適合した | 5本の言い換えを取り消す場合は、`extract-ai-assets.sh`が読む`description:`をそのまま残すため、AI設定資産.htmlの該当summaryも元の「原本」「抽出」表記へ戻る |
| 規約提案配下の1件（`generation-engine/samples/規約提案/サンプルアプリAPI規約提案.html`の「サンプルアプリのAPI実装から抽出した規約候補」）を直すか、対象外として報告するかのどちらを採るか | 直さず据え置き、判定の側を変えるべきかを報告するに留めた（判定の側を変える決定はしない） | `.claude/rules/scoped/portal/page-conventions/rule.md`の「適用対象」節が`generation-engine/samples/規約提案/`と`delivery-payload/templates/rule-proposal/`をポータル規約（ページ体裁の検査）の対象外と明記している。顧客提示適合検査（`check-customer-facing-adequacy.sh`）が同じ除外を持つべきかは既存規約に明記が無く、検査側の走査対象を変える判断は本指示書の範囲（様式・定義・生成物の言い換え）を超えるため、決定せずに報告する | 検査側に同じ除外を追加すればこの1件は不合格から外れる。追加しなければ、規約提案という別種の資料も通常の顧客提示文書と同じ基準で判定され続ける |
