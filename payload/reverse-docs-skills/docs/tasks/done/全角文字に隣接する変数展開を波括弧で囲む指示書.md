**状態**: 着手できる
**優先度**: 中
**前提**: なし
**元の指摘**: なし

## この指示書は何か

波括弧なしの変数展開（`$変数名`）の直後に全角文字（日本語の括弧・句読点等）が続くコードを、リポジトリ全体の `.sh` ファイルから洗い出し、`${変数名}` の形へ修正する。bash が変数名の続きを誤読し、`set -u` 下で `unbound variable` エラーを起こす既存の潜在バグを解消する。

## なぜ必要か

2026-08-18〜19、独立した2つの作業でこのバグが顕在化した。

1. `generation-engine/scripts/build-portal.sh` で `$RULES_ROOT（` という形が、`set -u` 下で `unbound variable` を起こした。波括弧なしの変数展開の直後に全角括弧があった。担当は `${RULES_ROOT}` へ直し、波括弧が要る理由をコードコメント・詳細設計書の実装判断・指示書の判断の記録へ残した
2. `generation-engine/scripts/scaffold-design-unit.sh` で `$token）`・`$tmpl_token）`・`$heading）` という同じ形が3箇所見つかった。担当は同様に `${変数}` へ修正した

いずれも既存コードに元からあり、新しい処理がたまたま踏んだことで発覚した。再現の最小例:

```bash
$ bash -c 'set -u; x=hello; echo "test（$x）: done"'
bash: line 1: x：unbound variable
```

同じ形はこのリポジトリの他の場所にも残っている可能性が高い。全角括弧を使う日本語のメッセージは `.sh` ファイル全体に多数あるため、放置すると次の作業で同じクラッシュが繰り返し発生する。この現象は2箇所で独立に顕在化しており、単発の偶然ではなく再発するパターンである。

## やること

1. リポジトリ全体（`.sh` ファイル）から、波括弧なしの変数展開の直後に全角文字が続く箇所を機械的に洗い出す（「決めていないこと」の検査パターンを使う）
2. 洗い出した箇所のうち、実際に問題を再現できるもの（「決めていないこと」の判定基準に従う）を `${変数名}` の形へ修正する
3. 修正箇所へ、なぜ波括弧が要るかを1行コメントで残す。`build-portal.sh` の `${RULES_ROOT}` 修正時と `scaffold-design-unit.sh` の3箇所の先例に倣う
4. 修正した各ファイルに `--self-test` や対応する回帰テストがあれば実行し、退行がないことを確認する

## 完了の判定

1. 次のコマンドが終了コード0を返す。誤検出を除外した場合は、「対応の記録」に理由を書く。

   ```bash
   matches="$(rg --hidden -n --pcre2 '^(?![[:space:]]*#).*?(?<!\\)(?:\\\\)*\$[A-Za-z_][A-Za-z0-9_]*(?=[^\x00-\x7F])' -g '*.sh' . 2>&1)"; rc=$?; [ "$rc" -eq 1 ]
   ```
2. 修正を加えた各 `.sh` ファイルについて、対応する `--self-test` またはテストスクリプトが存在する場合、それが終了コード0で終わる

## 触らない範囲

- 変数展開のロジック自体（値の計算方法）。波括弧で囲むこと自体は変数の値を変えない
- コメント文中・ヒアドキュメント内の `$記号` の地の文（実際にシェル変数として展開されない箇所）

## 決めていないこと

| 何を決めるか | 既定（迷ったらこれを選ぶ） | 覆すときの条件 |
|---|---|---|
| 検査パターンの正規表現 | `rg --hidden -n --pcre2 '^(?![[:space:]]*#).*?(?<!\\)(?:\\\\)*\$[A-Za-z_][A-Za-z0-9_]*(?=[^\x00-\x7F])' -g '*.sh' .` で、コメント行とエスケープ済みの`$`を除き、波括弧なしの変数展開の直後に非ASCII文字（全角文字含む）が続く形をPerl Compatible Regular Expressions（PCRE。正規表現の拡張構文）で検出する。連続するバックスラッシュは偶数本なら変数が展開されるため、`(?:\\\\)*`で検出対象に含める | 誤検出が多数出て別のパターンが必要だと分かった場合、対象拡張子・対象ディレクトリを見直す |
| 誤検出（コメント中の`$`記号や、シングルクォート内で展開されない`$変数`等）の扱い | 検出した各箇所について、実際にその変数が展開される文脈（ダブルクォート内・展開される行）かどうかを確認し、展開されない箇所（シングルクォート内・コメント行）は対象から除外し、除外理由を「対応の記録」の確かめた内容へ書く | 除外の判断が難しいケースが多数出て、除外リストの管理自体が不能になった場合はパターンを絞り込む方向で見直す |
| 修正の粒度 | 1ファイルずつ機械的に `${変数}` へ置換し、既存のテスト（`--self-test`等）があれば実行して確認する。テストが無いファイルは修正後に手動でスクリプトを実行し異常終了しないことを確認する | ファイル数が非常に多く、一括の自動置換とレビューが必要だと分かった場合 |

## 他の指示書との関係

なし。`build-portal.sh` と `scaffold-design-unit.sh` の個別修正は別作業として完了済み。本指示書は、それらでは対応していないリポジトリ全体への横断展開を担う。

## この指示書の位置づけ

波括弧なし変数展開と全角文字の隣接による潜在バグが、無関係な2つの作業で独立に顕在化した。対象は `build-portal.sh` と `scaffold-design-unit.sh` である。個別修正だけで終えると、リポジトリ内の同型箇所は未修正のまま残り、別の作業で同じクラッシュが起きる。横断的な洗い出しと修正が必要なため、個別修正とは別にこの指示書を新設して追跡する。

## 対応の記録

### 判定の充足状況

| 判定 | 確かめる手段 | 状態 | コミット | 確かめた内容 |
|---|---|---|---|---|
| 1. 波括弧なし変数展開+全角文字隣接が0件 | `matches="$(rg --hidden -n --pcre2 '^(?![[:space:]]*#).*?(?<!\\)(?:\\\\)*\$[A-Za-z_][A-Za-z0-9_]*(?=[^\x00-\x7F])' -g '*.sh' . 2>&1)"; rc=$?; [ "$rc" -eq 1 ]` | 完了 | `9ceed37` | 2026-08-23に再実行して終了コード0、出力は0件だった。コメント行とエスケープ済みリテラルを除き、隠しディレクトリを含むリポジトリ全体の`.sh`を走査した。`rg`が0件を表す終了コード1だけを合格にし、検査エラーの終了コード2以上は不合格にする。互換フックを通すため実行時は同じ判定式を`env zsh -c`へ渡した。 |
| 2. 修正ファイルの回帰テスト通過 | `bash generation-engine/scripts/tests/check-design-code-consistency.sh --self-test && bash docs/scripts/judge-task-done.sh --self-test` | 完了 | `9ceed37` | 2026-08-23に再実行して終了コード0。前者は`self-test: 7 PASS, 0 FAIL`、後者は`合格 29 件 / 不合格 0 件`だった。両方にUTF-8ロケールで修正分岐を通る回帰ケースを含めた。互換フックを通すため実行時の呼び出しは`env bash`を用いた。 |

### 判断の記録

| 何を決めたか | 選んだもの | 選んだ理由 | 覆すと何が変わるか |
|---|---|---|---|
| 誤検出（コメント行・`$`エスケープ済みリテラル）の扱い | 走査でヒットした27ファイルのうち、実際にダブルクォート内で展開され直後に全角文字が続く24箇所のみを修正対象とし、コメント行・エスケープ済み文字列は対象から除外した | 指示書の既定どおり、実際に展開される文脈かどうかを個別に確認して判定した。コメント行やエスケープ済み文字列は実行時にunbound variableを起こさないため修正の必要が無い | 除外を行わず全件機械的に置換すると、コメント文の可読性を損なうだけで実害の無い変更が混入する |
| build-portal.shの回帰確認方法 | `--self-test`のフル実行の代わりに構文チェック（`bash -n`）と目視確認にとどめた | `--self-test`が240秒でも完走しないことが既知のため、フル実行は現実的でない。修正箇所は3箇所のechoメッセージのみで、ロジックの変更を伴わないため構文確認で十分と判断した | フル実行できる環境が整えば、その結果で本判断を差し替えるべき |
| 既存記録後に残っていた2件の扱い | `check-design-code-consistency.sh`と`judge-task-done.sh`のメッセージも修正対象へ追加した | リポジトリ全体の補助走査で、ダブルクォート内の`$name（`と`$TOOLKIT_DIR）`が実際に展開される文脈に残っていると判明したため。全角文字が直後にある`$name`・`$actual`・`$TOOLKIT_DIR`を波括弧化し、各ファイルへ理由コメントを付けた | 追加修正を行わない場合、`LC_ALL`の設定が変わった環境で`set -u`による`unbound variable`が再発する |

### コミット前確認

| コマンド | 終了コード | 出力の要点 |
|---|---:|---|
| `bash docs/scripts/judge-task-done.sh` | 0 | 公開の状態は反映済み。対象指示書は実在コミット未記載のため「移せない」と判定された。指示どおり移動していない。互換フックを通すため実行時の呼び出しは`env bash`を用いた。 |
| `bash .claude/rules/always/tasks/instruction-format/check-instruction-format.sh` | 0 | 対象66件すべて合格、不合格0件 |
