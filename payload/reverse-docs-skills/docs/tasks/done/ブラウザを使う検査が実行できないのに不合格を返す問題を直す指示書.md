# ブラウザを使う検査が実行できないのに不合格を返す問題を直す指示書

**状態**: 着手できる
**優先度**: 最高
**前提**: なし
**元の指摘**: なし

## 1. この指示書は何か

ブラウザを使う検査が、実行環境の制約でブラウザを起動できないとき、
不合格（終了コード1）を返している。
実行できなかったことと不合格を区別する規約に反する。
判定不能（終了コード2）を返す形へ改める。

## 2. なぜ必要か

実測（2026-08-24）。第1層の集約を通しで走らせた結果は次のとおりだった。

    対象 186 本 / 成功 167 本 / 失敗 17 本 / 判定不能 2 本
    途中停止の疑い 0 本 / 総ケース数 4707 件

失敗17本のうち14本が「実行 0 件」である。ケースが1件も走らないまま不合格を返している。

    tests/test-badge-nav-contrast.cjs — 実行 0 件 / 終了コード 1
    tests/test-er-diagram-hub-card-font-size.cjs — 実行 0 件 / 終了コード 1
    tests/test-er-diagram-table-detail-tab.cjs — 実行 0 件 / 終了コード 1
    tests/test-matrix-header-compact-layout.cjs — 実行 0 件 / 終了コード 1
    tests/test-portal-disabled-card-interaction.cjs — 実行 0 件 / 終了コード 1
    tests/test-screen-doc-appendix-collapse.cjs — 実行 0 件 / 終了コード 1
    tests/test-screen-doc-column-width.cjs — 実行 0 件 / 終了コード 1
    tests/test-screen-doc-markdown-renderer.cjs — 実行 0 件 / 終了コード 1
    tests/test-screen-doc-section-order.cjs — 実行 0 件 / 終了コード 1
    tests/test-screen-doc-unresolved-callout.cjs — 実行 0 件 / 終了コード 1
    tests/test-scroll-cue-visibility.cjs — 実行 0 件 / 終了コード 1
    tests/test-semantic-glossary-page.cjs — 実行 0 件 / 終了コード 1
    tests/test-transition-diagram-initial-summary.cjs — 実行 0 件 / 終了コード 1
    tests/test-unit-list-format.cjs — 実行 0 件 / 終了コード 1

1本を実際に走らせて原因を確かめた。

    制限のある環境:
      Error: Chrome/Chromiumの起動に失敗しました（exit 21）
      bootstrap_check_in ...: Permission denied (1100)
      open .../Crashpad/settings.dat: Operation not permitted (1)
      Failed to create socket directory.
      → 終了コード1（不合格）

    制限を外した環境:
      INFO: dark配色 / サイドバー現在地項目の文字: コントラスト比11.34
      PASS: バッジ3種とサイドバーのナビ項目が、明るい配色・暗い配色の両方で
            コントラスト比4.5以上を満たす
      → 合格

成果物には問題が無い。ブラウザを起動できなかっただけである。

判定不能規約（`.claude/rules/always/verification/indeterminate-result/rule.md`）は次を定める。

- 実行環境の状態に依存して失敗しうる操作は、その戻り値を確認する
- 確認に失敗した場合、検査対象の合否を判定せず終了コード2で終了する
- 出力に `[UNKNOWN]` ラベルを付ける
- 既存の合格・不合格ラベルへ理由を書き添えるだけの表現を使わない

14本はこれに反している。

### なぜ重大か

第1層の合格本数を品質の指標として使えない。
失敗17本のうち14本が動かなかっただけであり、成果物の欠陥を1件も表していない。

同じ根の問題を、この周で既に2回直している。

| 直した問題 | 件数 |
|---|---|
| プロセス置換を外部コマンドへ渡す形 | 9箇所 |
| 一時ファイルの置き場を指定しない形 | 80箇所 |

これが3件目である。
いずれも実行環境によって答えが変わるという同じ性質を持つ。

### 影響の範囲

同じ形の検査を数えた。

    generation-engine/scripts/tests/ の .cjs と .mjs   20本
    うちブラウザを使うもの                            15本

15本が同じ問題を抱えうる。
実測で不合格を返したのは14本であり、残る1本は別の理由で通っている可能性がある。

## 3. やること

1. `generation-engine/scripts/tests/` 配下の `.cjs` と `.mjs` のうち、
   ブラウザを使う15本を洗い出す
2. 各本について、ブラウザの起動に失敗したときの扱いを調べる
3. 起動に失敗したとき、`[UNKNOWN]` と理由を出して終了コード2で終える形へ改める。
   理由には、ブラウザを起動できなかったこと・実行環境の制約が原因でありうることを含める
4. 起動に成功した後の判定は変えない。合格・不合格の判定はそのまま保つ
5. 共通の処理になる場合、1本のヘルパーへ集約してよい。
   集約する場合は置き場を `generation-engine/scripts/tests/` の下とする
6. 第1層の集約（`run-layer-machine-checks.sh`）が、終了コード2を
   「判定不能」として数えることを確かめる。数えていなければ集約の側も直す
7. 判定の式に縦棒（`|`）が入る場合はスクリプトへ切り出す。
   名前は `docs/scripts/check-browser-test-unknown.sh` とし、
   この指示書を実行する担当が作る

## 4. 完了の判定

1. `bash docs/scripts/check-browser-test-unknown.sh` が終了コード0を返す
2. `bash docs/scripts/check-browser-test-unknown.sh --self-test` が終了コード0を返す
3. 制限のある環境で第1層の集約を走らせたとき、失敗の本数が3本以下になる
   （14本が判定不能へ移るため）
4. 制限のある環境で第1層の集約を走らせたとき、判定不能の本数が16本以上になる
   （元の2本＋14本）
5. 制限を外した環境で第1層の集約を走らせたとき、成功の本数が着手前を下回らない
6. `bash .claude/rules/always/tasks/instruction-format/check-instruction-format.sh` が
   終了コード0を返す

条件3・4の着手前の値は次である（2026-08-24 実測、main のコミット `d17d6b3`）。

    制限のある環境: 対象 186 本 / 成功 167 本 / 失敗 17 本 / 判定不能 2 本

条件5は、過去に制限なしで実測した成功182本を基準とする。

## 5. 触らない範囲

- ブラウザの起動に成功した後の判定を変えない。合格・不合格の基準はそのまま
- ブラウザを使わない検査を変えない
- 第1層の集約の収集条件（どの検査を対象にするか）を変えない
- 他の指示書を変更しない

## 6. 決めていないこと

| 何を決めるか | 既定（迷ったらこれを選ぶ） | 覆すときの条件 |
|---|---|---|
| 共通のヘルパーへ集約するか | 集約する。15本が同じ処理を持つため | 本ごとに起動の仕方が違い、共通化すると分岐が増えると分かったとき |
| ヘルパーの置き場 | `generation-engine/scripts/tests/` の下 | 集約の収集対象に入って誤って実行されると分かったとき |
| 判定不能の理由の文面 | 「ブラウザを起動できないため判定できません（実行環境の制約が原因である可能性があります）」 | 起動の失敗に複数の原因があり、区別する必要があると分かったとき |
| 15本すべてを一度に直すか | 一度に直す。同じ書き換えであり、分けると途中の状態が混ざる | 1本ずつ起動の仕方が違い、一括では直せないと分かったとき |

## 7. 他の指示書との関係

`docs/tasks/一時ファイルの置き場を指定せず検査が動かない問題を直す指示書.md` と同じ根を持つ。
あちらは一時ファイルの置き場、本指示書はブラウザの起動である。
どちらも実行環境によって答えが変わるという性質を扱う。
対象のファイルは重ならない。

## 8. この指示書の位置づけ

検査そのものの中身ではなく、検査が動く条件を扱う。
動かなかったことを不合格として数えると、成果物に問題が無いのに品質が低いと読める。
逆に、本当の不合格が14件の中に埋もれても気づけない。

## 9. 対応の記録

### 判定の充足状況

| 判定 | 確かめる手段 | 状態 | コミット | 確かめた内容 |
|---|---|---|---|---|
| 1. 判定不能の検査が通る | `bash docs/scripts/check-browser-test-unknown.sh` | 完了 | `551729d3` | 対象15本すべてが判定不能(BrowserUnavailableError・[UNKNOWN])へ対応している形になっていることを静的検査で確認した |
| 2. 判定不能の検査の自己テストが通る | `bash docs/scripts/check-browser-test-unknown.sh --self-test` | 完了 | `551729d3` | 自己テスト15件すべて合格 |
| 3. 制限下の失敗が3本以下 | `bash docs/scripts/check-browser-test-unknown.sh --count-failures` | 完了 | `551729d3` | `CHROME_BIN=/usr/bin/false PLAYWRIGHT_BROWSERS_PATH=/nonexistent`を指定した第1層集約で、失敗2本(test-python-facts-flow.sh・detect-screens.sh)となり3本以下を確認した。両方はブラウザ対象外の既存不一致である |
| 4. 制限下の判定不能が16本以上 | `bash docs/scripts/check-browser-test-unknown.sh --count-unknowns` | 完了 | `551729d3` | 同じ集約で判定不能17本(元の環境依存2本＋ブラウザ対象14本＋build-matrix-pages.shの伝播1本)となり16本以上を確認した。ブラウザ対象14本を個別確認した |
| 5. 制限なしの成功が減らない | `bash docs/scripts/check-browser-test-unknown.sh --count-successes` | 完了 | `551729d3` | 制限なし専用ログの要約は`対象 190 本 / 成功 185 本 / 失敗 2 本 / 判定不能 2 本 / 途中停止の疑い 1 本 / 打ち切り 0 本 / 宣言済み長時間 0 本 / 総ケース数 4481 件`であり、`--count-successes`で過去の制限なし実測182本を下限として今回185本が下回らないことを確認した |
| 6. 指示書形式が適合する | `bash .claude/rules/always/tasks/instruction-format/check-instruction-format.sh` | 完了 | `551729d3` | 対象92件すべて合格(合格92件/不合格0件) |

### 判断の記録

| 何を決めたか | 選んだもの | 選んだ理由 | 覆すと何が変わるか |
|---|---|---|---|
| [UNKNOWN]の出力内容 | 固定文言のみ（ブラウザの標準エラー出力・元のErrorメッセージを含めない） | 第1層の集約(run-layer-machine-checks.sh)のhas_indeterminate_contract()は、出力全体にerror/usage等の行頭語を含む場合、判定不能とみなさず不合格へ落とす。ブラウザのクラッシュログをそのまま連結すると、行頭に紛れ込んだそれらの語で判定不能の判定自体が壊れる恐れがあった | 元のエラー内容を[UNKNOWN]行へ含める形にすると、判定不能規約への準拠と第1層の集約の実際の判定ロジックの両立が崩れ、条件3・4を満たせなくなる可能性がある |
| 分類の対象範囲（狭義） | 起動・接続フェーズ(findBrowser/launchBrowser/connectCdpの初期接続/chromium.launch/playwrightのrequire)だけを判定不能とし、計測フェーズ(Runtime.evaluate・cdp.send・rejectPending経由の再送)やMAX_DUMP_BYTES超過・180秒タイムアウト(test-screen-doc-section-order.cjs)・完全出力後の異常終了は対象外とした | 起動不能とは別の原因(本来の不合格)まで判定不能に含めると、成果物の不具合を隠す方向の変更になる。指示書のやること4「起動に成功した後の判定は変えない」に反する | 分類を広げると、本来FAILすべき不具合がUNKNOWNへ紛れ込み、品質低下を検知できなくなる |
| assert-generated-detail-pages-runtime.cjsの扱い | 他14本と同じ判定不能対応を適用し、build-portal.sh:1236の呼び出し側にも終了コード2→SKIPの分岐を追加した | 同ファイルは第1層の集約には収集されない(ファイル名がtest-*に一致しないため)が、build-portal.sh --self-testケース48dから直接呼ばれており、対応しないと「起動不能をFAILとして扱う」というバグを1階層上へ移すだけになる。追加した分岐は2行程度の低リスクな変更であり、実測でbuild-portal.sh --self-testは無関係の既存問題(mktemp/mkdir権限)でcase1から失敗しているため、この変更単体で第1層の集約の数値(条件3・4)に影響しないことを確認済み | この対応をしない場合、assert-generated-detail-pages-runtime.cjs単体を直接実行すれば[UNKNOWN]を返すが、build-portal.shの自己テスト経由ではFAILのまま残る |
| 判定5の機械化 | `check-layer1-declarations.sh --success-min` は集約を再実行して120秒上限を超えるため使わず、制限なし専用の固定保存ログ `.cache/check-browser-test-unknown/layer1-unrestricted.log` を読む `check-browser-test-unknown.sh --count-successes` を追加し、過去の制限なし実測182本を下限とする | 保存済みログを読むことで、集約を再実行せずに今回185本が過去の制限なし実測を下回らないことを機械判定できる | 覆すと再集約により恒常未確認になる |
| 制限環境の再現 | 現在環境ではブラウザが起動成功したため、条件3・4の再検証では`CHROME_BIN=/usr/bin/false PLAYWRIGHT_BROWSERS_PATH=/nonexistent`を指定し、ブラウザ起動不能経路を決定的に再現する | `CHROME_BIN`を使う12本と`PLAYWRIGHT_BROWSERS_PATH`を使う2本の両起動経路を再現するため。起動後の合否基準は変えず、既存のBrowserUnavailableError経路を実行する | 覆す場合は実サンドボックス制限環境で約16分の集約を再実行する必要がある |
| 集約で実測した失敗の扱い | 対象内の旧サンプルパス、判定不能を不合格へ変換するbuild-matrix-pages.sh、英字パス移行後のportal catalog goldenを修正し、test-python-facts-flow.shとdetect-screens.shは本指示書の対象外の既存不一致として変更しない | 初回は5失敗だった。scrollは旧パス参照、portal catalogはgolden未同期、build-matrixは子の終了コード2を1へ変換していたため修正可能だった。check-real-world-cases、check-reproducible、audit-consistencyは集約内だけの一時失敗で単独再実行0、最終集約では再発しなかった。残る2本はブラウザ起動前後と無関係なsurvey順序・画面検出の幅優先探索の既存不一致で、直すとユーザー指示を超える | 残る2本を直す場合は各機能の仕様・テストを別タスクで扱う必要がある。本指示書のブラウザ起動不能判定には影響しない |
| docs/scripts/check-browser-test-unknown.shの--count-failures/--count-unknowns/--count-successes | 第1層の集約をその場で実行せず、保存済みのログファイルを用途別に読む形にした。条件3・4は `.cache/check-browser-test-unknown/layer1-aggregate.log`、条件5は `.cache/check-browser-test-unknown/layer1-unrestricted.log` を読む | 集約の所要時間は約16分あり、片付けの判定器(judge-task-done.sh)の時間の上限(既定120秒)を超える。その場で実行すると必ず未確認になる。シェルごとに変わるTMPDIRでは保存側と読取側がずれるため固定化する | その場で実行する形に戻すと、条件3・4・5の判定が恒常的に未確認になり、機械で確かめられなくなる |
