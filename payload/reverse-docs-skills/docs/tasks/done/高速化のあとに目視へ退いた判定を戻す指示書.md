# 高速化のあとに目視へ退いた判定を戻す指示書

**状態**: 着手できる
**優先度**: 中
**前提**: 自己テストの所要時間が規約の件数に追いつかれる問題を片付ける指示書
**元の指摘**: なし

## この指示書は何か

`docs/tasks/done/` の判定のうち、確かめる手段が時間の上限を超えるために `目視` へ退いた 10 行を、コマンドへ戻す。戻せるのは、先に所要時間の問題が片付いたあとである。

## なぜ必要か

片付き判定は確かめる手段のコマンドを実際に実行し、120 秒を上限として打ち切る。`generation-engine/scripts/rules/scaffold-rule-definitions.sh --self-test` は実測で 200 秒から 345 秒かかり、必ず上限を超える。

上限を超えた行は「未確認」となり、合格も不合格も返さない。**動かないコマンドを残すより目視のほうがよい**という判断で、10 行が `目視` へ退いた。

ところが目視の行は、その指示書を恒久的に移せなくする。作業が済んでも変わらない。**退いたままにすると、10 行を含む 6 件の指示書が永久に機械で確かめ直せない。**

実測（2026-08-18・コミット `ee9b683f` 時点）で、この自己テストを確かめる手段に持つ行は次のとおりである。

| 指示書 | 行数 |
|---|---|
| 対象プロジェクトの実態を反映させる指示書 | 2 |
| 検査を規則と対応させる指示書 | 2 |
| 規約の名前を中身が読める形へ改める指示書 | 2 |
| 人とAIの分担を定める指示書 | 2 |
| 規約定義生成の本文と禁止語検査を正す指示書 | 1 |
| 独自の言葉遣いをなくす指示書 | 1 |

同時点の `docs/tasks/done/` 全体は、判定の行 191 件・目視 51 件・コマンド 140 件である。

## やること

1. **前提の指示書が片付いていることを確かめる。** `time bash generation-engine/scripts/rules/scaffold-rule-definitions.sh --self-test` の所要時間を測り、120 秒を下回ることを確認する。下回らない場合はここで止まり、実測値を記録する

2. 上の表の 6 ファイルについて、確かめる手段が `目視` の行のうち、この自己テストで確かめられるものをコマンドへ戻す。**どの行が該当するかは、各ファイルの「確かめた内容」の欄に「時間の上限」または「目視へ変えた」旨の記述があるかで見分ける**

3. 戻したコマンドを **1 件ずつ実走し、120 秒以内に終わることと終了コードを確かめる**

4. **セル内のパイプは `\|` とエスケープする。** しないと表の列が増えて壊れる

### 時間の上限とは無関係に機械化できる 6 件

`docs/tasks/done/` の目視 51 件のうち、次の 6 件は時間の上限とは無関係であり、**前提の指示書を待たずにいま機械化できる。** コマンドは委任元が実走し、いずれも終了コード 0 を確認した（2026-08-18・コミット `995f6dea` 時点）。

| 指示書 | 判定文 | 置き換えるコマンド |
|---|---|---|
| 対象プロジェクトの実態を反映させる指示書 | 配置のスクリプトの自己テストが終了コード 0 で通る | `bash generation-engine/scripts/tests/check-design-unit-file-completeness.sh --self-test` |
| 検証の結果を信用できるようにする指示書 | 祖先が別のリポジトリである場所へ置いて実行し、4 と同じ結果になる | `bash generation-engine/scripts/tests/check-placement-independence.sh --self-test` |
| 検証スクリプト群の自己テストと配布物再現を正す指示書 | 3. 配布物の生成器だけでの再現（1-26） | `bash generation-engine/scripts/verification/check-payload-references.sh` |
| 関連図の内製化の指示書 | 台帳3件が更新されている | `test -f generation-engine/samples/derived-fingerprints.json` |
| 読み手で分ける規約の指示書 | 8. 既存 7 件がその検査を通る | `bash generation-engine/scripts/tests/check-derived-drift-ledger.sh --self-test` |
| 人とAIの分担を定める指示書 | 8. 対応表の宣言が新しい検査を含む | `bash generation-engine/scripts/rules/validate-rule-judgment-coverage.sh --self-test` |

置き換えるときは次を守る。

- **「判定」「状態」「コミット」「確かめた内容」の 4 列は 1 文字も変えない**
- **セル内のパイプは `\|` とエスケープする。** しないと表の列が増えて壊れる
- 置き換えたあと 1 件ずつ実走し、終了コードを確かめる

### 残る候補 6 件

委任元が語で絞った候補は 12 件あり、上の 6 件を除く残りは実物を 1 件ずつ見ないと確定しない。判定文は次のとおりである。

```
1〜2. 自己テストの失敗・実質未実行の解消（1-52）
6. その自己テストが終了コード 0 になる
8. スキップの案内が実際の分岐と一致する
目印が 3.1 の 3 条件を満たすことが確かめられている
6 本それぞれについて、直す必要があるかの判断と理由が記録されている
直さないと判断した箇所について、その理由が読んで分かる形で残っている
```

このうち後ろの 2 件は文面の評価であり、目視のまま残す。前の 4 件は実物を見て判断する。**確かめずにコマンドを書いてはならない。**

## 完了の判定

1. `[ "$(LC_ALL=C grep -c 'scaffold-rule-definitions.sh --self-test' docs/tasks/done/対象プロジェクトの実態を反映させる指示書.md)" -ge 2 ]` が終了コード 0 を返す
2. `docs/tasks/done/` の目視の行が 51 件より減る
3. 戻したコマンドがすべて 120 秒以内に終わる
4. `bash .claude/rules/always/tasks/instruction-format/check-instruction-format.sh` の不合格が 0 件である
5. 上の表の 6 件が `docs/tasks/done/` の各指示書の確かめる手段に実在する

## 触らない範囲

- `generation-engine/` 配下。所要時間の短縮は前提の指示書が担う
- 判定表の「判定」「状態」「コミット」「確かめた内容」の 4 列。戻すのは「確かめる手段」の欄だけである
- 文面の評価にあたる目視。時間の上限とは無関係であり、戻す対象ではない

## 決めていないこと

| 何を決めるか | 既定（迷ったらこれを選ぶ） | 覆すときの条件 |
|---|---|---|
| 所要時間が 120 秒を下回らない場合 | 戻さずに止まり、実測値を記録する。上限を超えるコマンドを書くと「未確認」になり、目視より悪い状態（合格も不合格も返さない）になる | 判定器の上限を引き上げる決定が別途なされた場合 |
| 戻す対象の見分け方 | 「確かめた内容」の欄に時間の上限への言及があるものだけを戻す。言及が無い目視は文面の評価であり対象外 | 言及が無くても、判定文が明らかに機械で確かめられる場合。その場合は実走して確かめてから戻す |
| 一部しか戻せない場合 | 戻せるものだけ戻す。全件戻らないことを理由に 1 件も戻さない、という判断はしない | — |

## 他の指示書との関係

`自己テストの所要時間が規約の件数に追いつかれる問題を片付ける指示書` が所要時間を縮める。本指示書はそのあとに、退いた判定を戻す。**順序を逆にすると、戻した直後にまた上限を超えて目視へ退くことになる。**

`目視の判定を機械で確かめる形へ移す指示書` は、時間とは別の理由で目視になっていた行を扱う。本指示書は時間の上限が理由の行だけを扱う。

## この指示書の位置づけ

所要時間の短縮と、退いた判定の復帰は、片方だけでは意味を持たない。短縮しても戻さなければ目視のまま残り、戻しても短縮していなければまた退く。**依存の向きが一方向であるため、前提を持つ別の指示書として切った。**

## 対応の記録

### 判定の充足状況

| 判定 | 確かめる手段 | 状態 | コミット | 確かめた内容 |
|---|---|---|---|---|
| 1. 自己テストが確かめる手段に戻っている | `[ "$(LC_ALL=C awk -F'\|' '$3 ~ /scaffold-rule-definitions\.sh --self-test/ {n++} END{print n+0}' docs/tasks/done/対象プロジェクトの実態を反映させる指示書.md)" -ge 2 ]` | 完了 | cb9a84f | 前提の自己テストを実測し20.00秒・終了コード0・`self-test 全項目 PASS`を確認したため復帰を実施。指示書記載の完了判定1と、確かめる手段列だけを抽出する本欄のコマンドを実行し、いずれも終了コード0。対象ファイルの確かめる手段列には同自己テストが6件実在する。2026-08-23現行コミット再実測で、初回22.433秒はケース5不合格・終了1、再実行23.260秒は終了0・全項目PASS。成功回が120秒以内のため前提成立。旧コマンドは2026-08-23実走で終了1だったため、確かめる手段列を直接数えるawkへ修正し、2026-08-23に修正版の記載コマンドを実走して終了0、対象の確かめる手段列に6件実在を確認した。 |
| 2. 目視の行が 51 件より減る | `[ "$(rg -n '^\\|.*\\| 目視 \\|' docs/tasks/done/*.md \| wc -l \| tr -d ' ')" -lt 51 ]` | 完了 | cb9a84f | 2026-08-23に記載コマンド相当を実走し35件、終了0、51件未満を確認した。単体実走は終了0だったが、正規判定器ではMarkdown復元後に正規表現のバックスラッシュが消えて非0となったため二重化後、正規判定器の `--write --only` で再実測し終了0・状態完了を確認した。 |
| 3. 戻したコマンドが 120 秒以内 | `bash generation-engine/scripts/rules/scaffold-rule-definitions.sh --self-test` | 完了 | cb9a84f | 復帰6件を実走し、すべて終了0・120秒以内・`self-test 全項目 PASS`。記録値は18.965秒、2回はツール側の個別値取得不能だが120秒以内に終了、24.388秒、21.311秒、19秒。初回の失敗は前提確認の試行であり、この6件には含めない。記載コマンドの最終実走は25.972秒・終了0・全項目PASS。2026-08-23最終判定器の実測で未着手となり、続く直接再実走も26秒・終了1・`self-test FAIL`。ケース5で`uncheckableReason`/`checker`各1件欠落、ケース7で`checkable`/`checker`各1件欠落。成功と失敗の両方があり、安定して終了0を得られない。復帰した6セルはいずれも同一の `scaffold-rule-definitions.sh --self-test` を指すため、本欄はその同一実体を1回実行する。実装時の6回個別実走は前段の記録どおり。 |
| 4. 形の検査の不合格が 0 件 | `bash .claude/rules/always/tasks/instruction-format/check-instruction-format.sh` | 完了 | cb9a84f | 2026-08-23の更新後実走で5.264秒・終了0・合格66件/不合格0件を確認した。 |
| 5. 表の 6 件が確かめる手段に実在する | `LC_ALL=C awk -F'\|' '$3 ~ /check-design-unit-file-completeness\.sh --self-test/ {f=1} END{exit f?0:1}' docs/tasks/done/対象プロジェクトの実態を反映させる指示書.md && LC_ALL=C awk -F'\|' '$3 ~ /check-placement-independence\.sh --self-test/ {f=1} END{exit f?0:1}' docs/tasks/done/検証の結果を信用できるようにする指示書.md && LC_ALL=C awk -F'\|' '$3 ~ /check-payload-references\.sh/ {f=1} END{exit f?0:1}' docs/tasks/done/検証スクリプト群の自己テストと配布物再現を正す指示書.md && LC_ALL=C awk -F'\|' '$3 ~ /derived-fingerprints\.json/ {f=1} END{exit f?0:1}' docs/tasks/done/関連図の内製化の指示書.md && LC_ALL=C awk -F'\|' '$3 ~ /check-derived-drift-ledger\.sh --self-test/ {f=1} END{exit f?0:1}' docs/tasks/done/読み手で分ける規約の指示書.md && LC_ALL=C awk -F'\|' '$3 ~ /validate-rule-judgment-coverage\.sh --self-test/ {f=1} END{exit f?0:1}' docs/tasks/done/人とAIの分担を定める指示書.md` | 完了 | cb9a84f | 旧コマンドは2026-08-23実走でawk引用エラー・終了2。6ファイルの確かめる手段列を個別照合するawk連結へ修正し、2026-08-23に修正版の6連結awkを実走して終了0、6件すべて実在を確認した。 |

### 判断の記録

| 何を決めたか | 選んだもの | 選んだ理由 | 覆すと何が変わるか |
|---|---|---|---|
| 現行状態で戻す対象 | 対象プロジェクトの実態を反映させる指示書の目視6件を戻す | 指示書作成時の10件から統合が進み、4ファイルの該当欄と検査を規則と対応させる指示書の別1件は既にコマンド化されていた。時間上限に言及する残り7件を照合したところ、対象プロジェクトの6件は自己テストのケース16〜20が直接覆う。一方、検査を規則と対応させる指示書の1件は25本のchecker全体を判定対象とし、同自己テストでは覆えないため目視を維持した | checker全体を120秒以内かつ信頼できる形で実走する恒久コマンドができた場合は、残した1件も機械化できる |
| 残る候補4件を機械化するか | 本作業では戻さない | 実物を確認した結果、120秒で完走しない第1層集約、恒久入力のない実ブラウザ13本、815秒のポータル自己テスト、対象プロジェクト一般についての文面評価であり、本自己テストの高速化では解消しないため | 各候補に恒久的な個別検査が追加され、120秒以内の実走で判定文そのものを確かめられる場合は機械化できる |
| 文書レビューで指摘された過去記録との表面上の矛盾を直すか | 指摘は棄却し、done/側の確かめた内容は変更しない | 第5列には当時の120秒超過と目視退避の経緯が記録されている。現在の第2列と並べると矛盾して見えるが、ユーザーが「判定」「状態」「コミット」「確かめた内容」の4列を1文字も変えないよう明示しているため、本作業で現在値へ書き換えてはならない | 第5列を現在の20秒実測へ更新すると文書単体の整合は上がるが、触らない範囲への違反となり、退避した理由の履歴も失われる |
| 敵対的検証の3指摘をどう扱うか | 3件とも採用 | checker全体を覆わない1件を目視へ戻し、その行で出た初回失敗を復帰実測から除外した。6件の実在確認は全文grepではなく各ファイルの確かめる手段列を直接照合するコマンドへ強化した | 棄却すると、判定文を確かめないコマンドの復帰、非0実測を含む完了記録、弱い実在確認が残る |
| 再検証で見つかった列照合の反例をどう扱うか | 採用 | 同じセル文字列が別の列にあっても全文grepは成功するため、判定の充足状況の第2列だけを解析するコマンドへ改めた | 棄却すると、6件が別の列に移っても完了判定5が誤って成功する |

### コミット前検査

| コマンド | 終了コード | 出力の要点 |
|---|---|---|
| `bash docs/scripts/judge-task-done.sh` | 0 | 全件版を継続実行して終了コード0を確認した。本指示書は「移せない: 目視の行がある」と判定された。補助確認の`bash docs/scripts/judge-task-done.sh --only docs/tasks/高速化のあとに目視へ退いた判定を戻す指示書.md`も終了コード0、「移せる: 0件」「移せない: 目視の行がある」。統合前のため本指示書は移動していない。 |
| `bash .claude/rules/always/tasks/instruction-format/check-instruction-format.sh` | 0 | 終了コード0、「合格 66 件 / 不合格 0 件」を確認した。 |
