# 新規スキル実装計画（1スキルずつ・汎用性検証を積み上げる）

**状態**: 着手できる
**優先度**: 中
**前提**: なし
**元の指摘**: なし

- 状態: 計画確定・Stage 1 から着手
- 作成日: 2026-07-07
- 関連文書: 同フォルダ リバース工程設計.md（Phase/Step×スキル対応の正本）・納品物フォルダ体系.md（成果物の置き場の正本）
- 完了条件（機械定義）: 作成した設計書だけから judge status=PASS（env_check全通過 ∧ 全scenario render-ready ∧ L2'内容一致 ∧ L2(ARIA)一致 ∧ L4 console一致 ∧ L3画素差分閾値内0.1% ∧ 該当時L5操作一致）の再生成コードが得られること。これが「まったく同程度の画面・機能が使える精度の設計書」の定義であり、主観判定を排除する。

## 設計の核: 鶏卵問題の解

共通基盤（アーキテクチャ調査・共通文書）を先に分析しないと各画面のリバースが破綻する。一方、共通基盤に何を書くべきかは各画面のリバースをしてみて初めて分かる。この鶏卵問題は「共通基盤は先に v0 を作り、画面ごとの垂直ループの NG 帰着で追記して育てる」二拍子で解く。

```
Stage 1        Stage 2          Stage 3        Stage 4（本丸・完了条件の検証）
アーキ調査 → 共通採録(v0) → 事実抽出 → 詳細設計執筆
              ▲                ▲              │
              │(c)共通文書の   │(b)factsの     ▼
              │ 欠落を追記     │ 欠落を        既存検証スキルで判定
              │                │ プロファイル   rebuilding-screen-unit(6計測)
              │                │ 改訂          → rebuilding-code + syncing
              │                │              → judge PASS/FAIL
              └────────────────┴──────────────┘
              NG分類で原因を(a)執筆規律/(b)facts/(c)共通文書
              の3系統に帰着して還元
```

## 全Stage共通の検証原則

- 1スキルずつ実装し、検証PASSまで次Stageへ進まない
- 汎用性の検証 = ①SKILL.md本体に固有文字列grepゼロ ②異種プロジェクトで手直しなしに成立 ③sonnetサブエージェントがSKILL.mdだけで単独完走（メインが救済したらスキル不備として改訂）
- レビューは決定的出力のみ（ファイル実在・exit code・diff値。自然文の自己申告での合格判定禁止）

## Stage 1: アーキテクチャ調査スキル（Phase 2 / Step 3-7）

| 項目 | 内容 |
|---|---|
| 実装物 | SKILL.md＋アーキテクチャ調査書テンプレ（自己検証はSKILL.md内のgrep手順で開始。スクリプト化は承認後） |
| 前提 | なし（最初に作れる唯一のスキル） |
| 検証想定 | ①<対象リポジトリA>で実行 → Step 6ゲート全PASS（記載パス実在100%・6種別判定網羅・推測語0件） ②異種スタック（<異種スタックリポジトリ>: Spring Boot）で再実行 → 同スキル・同テンプレで調査書が成立 ③判定不能項目が「実在しない（理由）」で埋まり空欄ゼロ |
| 合格条件 | 2プロジェクトでスキル無改造のまま調査書成立 |

## Stage 2: プロジェクト共通採録スキル（Phase 4 / Step 12-16）— v0 で止める

| 項目 | 内容 |
|---|---|
| 実装物 | SKILL.md＋サンプリング規則reference。出力は既存テンプレ7文書（共通設計書・メッセージ定義書・DESIGN.md 等の共通6文書＋サンプル記録.md）。規約定義は対象外。`docs/rules/` の親7・子27は scaffold-rule-definitions.sh が配る。2026-08-14 の移行により 27 子すべてに本文入りを配る。対象リポジトリ固有の中身は、規約提案の取り込みが「このプロジェクトの規則」の節へ入れる |
| 前提 | Stage 1 の調査書（ディレクトリ責務マップから層化サンプリング） |
| 検証想定 | ①全規則に実例≥3箇所・頻度・例外率が付くこと（機械grep） ②プレースホルダ監査PASS ③既存 rebuilding-code-from-docs の Phase 1 preflight がこの共通文書を「正」として読み込めること ④理想論表現の検出0件 |
| 合格条件 | 監査PASSの初版（v0）。ここで完成を狙わない — Stage 4 の画面ループで追記が出る前提の設計 |

## Stage 3: ユニット事実抽出スキル（Step 18-21）— 画面プロファイルのみ

| 項目 | 内容 |
|---|---|
| 実装物 | SKILL.md＋抽出プロファイル（screenのみ。他5種別は作らない）＋封印検証手順 |
| 前提 | 調査書（方式→プロファイル選択の根拠）。目録は既存の画面一覧が使えるため目録スキル改修は不要 |
| 検証想定 | ①<画面A>の対象ファイルでfacts生成 → 独立再計数ゲートPASS（件数乖離≤5%・空フィールド率≤30%・孤児0） ②改ざん検知（facts.ymlを1バイト変更→封印検証FAIL） ③再現性（同一コードで2回実行→diff 0） ④別画面（<画面B>）でスキル無改造のまま成立 |
| 合格条件 | 2画面でSEALED到達＋改ざん・再現性試験PASS |

## Stage 4: 詳細設計執筆スキル（Step 25-28）＋ 垂直ループ＝完了条件の検証そのもの

| 項目 | 内容 |
|---|---|
| 実装物 | SKILL.md＋執筆規律reference（章マップ・監査スクリプトは既存流用） |
| 前提 | Stage 1-3の成果物＋既存検証スキル群（これが採点者） |
| パイロット | <画面A>（baseline確立済み・現行設計書が最小スタブ＝執筆対象として最適・過去の検証記録が比較基準になる） |
| ループ | facts→執筆→監査→ rebuilding-screen-unit-from-docs（6計測）→ rebuilding-code-from-docs（implement→judge）＋syncing-reverse-env（dry-run） |
| 完了条件 | judge status=PASS（冒頭の機械定義） |
| NG帰着 | FAILごとに原因を3系統に分類して還元: (a)執筆規律不足→writing-rules/SKILL.md改訂 (b)facts欠落→抽出プロファイル改訂(Stage 3へ) (c)共通文書欠落→共通採録の追記(Stage 2へ)。分類は既存 ng-classification.md の10失敗クラスに帰着させ、test-item-patterns.md に蓄積 |
| 収束判定 | 同一画面で2連続PASS → 次画面へ（<画面B> → <画面C> → <画面D>） |
| 汎用性合格条件 | 3画面目以降、SKILL.md本体の改訂ゼロ（reference・プロファイル・共通文書の追記だけでPASSする状態） |

## Stage 5: 統括スキル改修（Phase 1/7 ＋ 状態遷移の配線）

| 項目 | 内容 |
|---|---|
| 実装物 | contract.md 状態表への新状態追記（調査・共通・facts・執筆）、既存 S0-S4 との整合 |
| 前提 | Stage 1-4が個別スキルとして完走実績を持つこと（配線してから中身を作らない） |
| 検証想定 | ①レジューム試験（各Phase完了状態から起動→正しいStepに再入、全Phase分） ②差し戻し試験（調査書の検出手がかりを故意に欠落→Step 8 BLOCKED→Step 5改訂の自動起動を確認） ③一気通貫試験: 未着手画面1つを起動一発でStep 30 judgeまで自動走行 |
| 合格条件 | 一気通貫試験で人間の介在ゼロ（Step 2のスコープ確認を除く）でjudge到達 |

## Stage 6: 横展開系（後回しを明示的に確定）

目録スキルのユニット汎用化改修・上位文書抽象化スキル・画面以外の抽出プロファイル5種は、完了条件の達成に不要なため実装しない。着手判断はStage 4の汎用性合格後。理由: パイロットでは画面一覧が既存で目録改修は不要、上位文書は詳細設計の量が揃ってから、他ユニット種別は往復検証（採点者）が画面専用のため精度検証ができない。

## 要点

1. 採点者（検証スキル群）が既に存在するため、Stage 3から先は毎回機械採点付きで実装できる
2. 鶏卵問題は「共通基盤v0→画面ループのNG帰着(c)で追記」の設計で吸収し、共通文書の完成をブロッカーにしない
3. 完了条件はjudge PASSという機械的定義に固定し、スコアの主観判定を排除する

## 出典

`スキル実装計画.md`（提供されたスクリーンショット4枚）の文字起こし。文字起こしのみを目的とし、記載されている「共通採録スキル」等の新規スキル自体の実装は行っていない。

## 複雑度プロファイリング（第9回追加）

### 実装物

| 資産 | パス | 概要 |
|---|---|---|
| --profileサブコマンド | generation-engine/scripts/unit-list/detect-screens.sh | マニフェストの全画面に対しLOC+8軸スコアを算出し、六分位でG1〜G6層に分類。層内サンプリングで代表画面を選定する |
| 複雑度プロファイル.json | <output_dir>/画面一覧/複雑度プロファイル.json | screens[]/layers{G1,G2,G3,G4,G5,G6}/axisBreakdown/diagnostics/stratifiedを含む確定スキーマの出力 |

### 検証結果

- self-test: profile関連12ケース（G1〜G6割当・出力スキーマ確定・N6未満ALL縮退・全同値縮退・sampleK数値一致・n5縮退・n6各層1件・n7割当検証・n12単調非減少・同点境界・n250大規模）＋recount-script不在検査
- 実走: 260画面規模の実プロジェクトで完走確認

## 完了の判定

本文が挙げる合格条件をそのまま判定とする。
新しい基準は起こさない。
判定キーは連番禁止。内容を要約した意味語で付ける（番号からは情報を得られないため）。

| 判定 | 出典 | 判定の内容 |
|---|---|---|
| 全体-judge到達 | 冒頭「完了条件（機械定義）」 | 作成した設計書だけから judge status=PASS の再生成コードが得られる |
| Stage1-2プロジェクト成立 | Stage 1 合格条件 | 2 プロジェクトでスキル無改造のまま調査書が成立する |
| Stage2-監査PASS | Stage 2 合格条件 | 監査 PASS の初版（v0）が揃う |
| Stage3-2画面SEALED | Stage 3 合格条件 | 2 画面で SEALED へ到達する。改ざん試験と再現性試験が PASS する |
| Stage4-judgeとSKILL無改訂 | Stage 4 完了条件・汎用性合格条件 | judge status=PASS を満たす。3 画面目以降は SKILL.md 本体の改訂が 0 件になる |
| Stage5-一気通貫 | Stage 5 合格条件 | 一気通貫試験で人間の介在ゼロのまま judge へ到達する |

Stage 6 は本文が「実装しない」と決めている。
よって判定を起こさない。

この 6 判定は、このリポジトリでは検証しない。
検証の対象となるリポジトリと画面がここに無いためである。

## 対応の記録

対応する担当者が周ごとに更新する。状態は「未着手」「対応中」「完了」「対象外」の4つを使う。
6 判定は、実行を試みたうえで実行できなかったことを `docs/tasks/work-records/退避記録.md` に記録し、対象外とした。

### 判定の充足状況

| 判定 | 確かめる手段 | 状態 | コミット | 確かめた内容 |
|---|---|---|---|---|
| 全体-judge到達 | `grep -q -e '<対象リポジトリA>' -e '<異種スタックリポジトリ>' -e '<画面A>' -e '<画面B>' -e '<画面C>' -e '<画面D>' docs/tasks/done/スキル実装計画.md` | 対象外 | `387ad306` | 実行を試みた。`docs/tasks/work-records/退避記録.md` の「スキル実装計画.md の6判定」項目のとおり、検証想定（38・56・65・69行目）の実行対象（`<対象リポジトリA>`・`<異種スタックリポジトリ>`・`<画面A>`〜`<画面D>`）がプレースホルダのまま特定されておらず、`prepare-verification-input.sh` は設計文書の疑似入力を作るツールで Stage1/Stage3 が要求する原本コード・調査対象を提供しないため代替にならない。実測の結果、判定が成立しないと分かったため対象外とする。確かめる手段はプレースホルダが今も本文に残っている（＝対象外の理由が今も成立している）ことを確認する |
| Stage1-2プロジェクト成立 | `grep -q -e '<対象リポジトリA>' -e '<異種スタックリポジトリ>' -e '<画面A>' -e '<画面B>' -e '<画面C>' -e '<画面D>' docs/tasks/done/スキル実装計画.md` | 対象外 | `387ad306` | 同上（退避記録の同一項目を参照） |
| Stage2-監査PASS | `grep -q -e '<対象リポジトリA>' -e '<異種スタックリポジトリ>' -e '<画面A>' -e '<画面B>' -e '<画面C>' -e '<画面D>' docs/tasks/done/スキル実装計画.md` | 対象外 | `387ad306` | 同上（退避記録の同一項目を参照） |
| Stage3-2画面SEALED | `grep -q -e '<対象リポジトリA>' -e '<異種スタックリポジトリ>' -e '<画面A>' -e '<画面B>' -e '<画面C>' -e '<画面D>' docs/tasks/done/スキル実装計画.md` | 対象外 | `387ad306` | 同上（退避記録の同一項目を参照） |
| Stage4-judgeとSKILL無改訂 | `grep -q -e '<対象リポジトリA>' -e '<異種スタックリポジトリ>' -e '<画面A>' -e '<画面B>' -e '<画面C>' -e '<画面D>' docs/tasks/done/スキル実装計画.md` | 対象外 | `387ad306` | 同上（退避記録の同一項目を参照） |
| Stage5-一気通貫 | `grep -q -e '<対象リポジトリA>' -e '<異種スタックリポジトリ>' -e '<画面A>' -e '<画面B>' -e '<画面C>' -e '<画面D>' docs/tasks/done/スキル実装計画.md` | 対象外 | `387ad306` | 同上（退避記録の同一項目を参照） |

コミット欄にはハッシュを書く。確かめた内容には、実行したコマンドと結果の要点を1行で書く。

### 判断の記録

既定に従って決めた結果を書く。ユーザーの返事を待つための表ではない。

| 何を決めたか | 選んだもの | 選んだ理由 | 覆すと何が変わるか |
|---|---|---|---|
| 6判定の状態をどうするか | 未着手のまま放置せず、実行を試みたうえで退避記録へ記録し「対象外」へ移す | `.claude/rules/always/verification/reverse-verification/rule.md` は実行省略を「実行を試みたうえで実行できなかった場合だけ」認める。本文の検証想定は実行対象がプレースホルダのままで具体化されておらず、これは「実行が要るから省略」ではなく「判定そのものが実行可能な形で書かれていない」という別の事実であり、instruction-format規約の対象外の定義（実測の結果、判定が成立しないと分かった）に合致する。初見読解（eliciting-plan-tacit-knowledge、plan-comprehension-prober）でも「プレースホルダが仮置きか未確定かが不明」「SEALED・独立再計数ゲート等の用語が本文内で未定義」という同種の欠落が指摘され、判定の実行可能性の低さを裏付けた | 対象リポジトリ・画面が具体名で指定されれば、退避記録の再実行条件を満たし対象外から未着手へ戻して検証を試みることになる |
| 計画の実装前提が現状と一致しているか | 一致していない可能性を記録するにとどめ、本文は書き換えない | `ls .claude/skills/` を実行した結果、次の5件が実在した: `surveying-architecture-for-reverse-docs`（Stage1名と対応する語）・`generating-reverse-common-docs`（Stage2名と対応する語）・`extracting-unit-facts-from-code`（Stage3名と対応する語）・`generating-reverse-detailed-design`（Stage4名と対応する語）・`orchestrating-ai-development-setup`（Stage5の統括スキルと対応する語）。名前の対応から推測できるだけで実装内容がStage定義と一致するかまでは確認していないが、本指示書が2026-07-07に「これから実装する」計画として書いた前提と、現状のスキル一覧が食い違っている可能性がある | 前提の食い違いを詳しく調べる場合、各スキルのSKILL.md本文とStage定義本文との突合、および本文（Stage定義・検証想定）の全面的な見直しが必要になる。今回はその判断をせず事実の記録にとどめた |
