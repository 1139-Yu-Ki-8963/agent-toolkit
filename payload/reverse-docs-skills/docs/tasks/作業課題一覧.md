# 作業課題一覧

**状態**: 着手できる
**優先度**: 高
**前提**: なし
**元の指摘**: なし

**状態の実測日**: 2026-08-23

## この指示書は何か

作業課題を 1 つの入口から見渡せるようにする一覧。対象は全 26 件で、うち 15 件は `docs/tasks/` 直下に残るものと `docs/tasks/done/` へ移ったものを含む指示書を持つ。指示書を持つ項目は指示書ファイルへのパスを持ち、詳細な実測記録と対応の記録は指示書側に残す。残る 11 件は指示書を持たず、旧番号がある項目は末尾へ記録する。`docs/tasks/指摘改善一覧.md`（画像から書き起こした指摘の一覧）とは別の入れ物であり、合流させない。

## なぜ必要か

課題が 15 件のファイルに分かれており、全体を見る手段が無かった。何件残っているか、どれが着手中かを知るには 15 件を個別に開くしかなく、同じ課題を複数のセッションが同時に触る事態を招いていた（2026-08-19 実測）。入口となる一覧を 1 枚置き、各項目から指示書へ辿れるようにすることで、この事態を防ぐ。

## やること

対象とする 25 件を、優先度の高い順に「見出し」「現象・修正内容・検収方法・完了条件・反映先・優先度・状態」の形で並べる。指示書を持つ 15 件は、見出しを指示書ファイル名とし、現象・修正内容を対応する指示書の「この指示書は何か」の節から 1〜2 文へ要約する。状態は実際の置き場から決め、`docs/tasks/done/` にあれば「完了」、`docs/tasks/` 直下にあれば対応する指示書の「着手できる」を記録する。指示書を持たない 10 件は、見出しを課題の内容を要約した日本語とし、旧番号を末尾に記録する。

この文書全体は冒頭 4 行と必須 8 節を持つ指示書形式であり、冒頭の状態には「着手できる」を使う。一方、各見出し配下の状態は 25 件の進捗を追う記録であるため、完了済みには「記録の状態の 5 つの意味」で定めた「完了」を使う。直下に残る指示書は、実装可能性を示す元の指示書の状態「着手できる」をそのまま保つ。

一覧への書き込みは進行を管理する側が行う。担当のセッションは対応する指示書へ実測と対応の記録を残して報告し、この一覧の更新は管理する側が担う。複数のセッションが一覧を直接同時に編集すると競合するため（2026-08-19 実測）、この分担を保つ。

指示書を新規に追加した場合、または `docs/tasks/done/` へ移した場合は、この一覧へその都度反映する。

指示書が `docs/tasks/done/` へ移ったら、この一覧の項目は残したまま状態を「完了」へ変える。置き場と状態の食い違いは `docs/scripts/check-task-ledger-sync.sh` で検出する。

### スキル定義と利用者向けガイドの一致を検査する

**現象**: 依頼時の対象数は52件だったが、現行の`.claude/skills/*/SKILL.md`と`references/guide.html`は53組だった。追加済みの結合テスト仕様書生成スキルを除外する根拠は無いため、53組を測定した。スキル名は一致53件、食い違い0件だった。日本語名は一致0件、食い違い53件だった。Phase数は一致48件、食い違い5件だった。依存は一致0件、食い違い53件だった。4項目がすべて一致する組は0件で、1項目以上が食い違う組は53件だった。
**修正内容**: 53件のガイド見出しをfrontmatterの`日本語名`へ揃えた。ガイドの`data-phase-count`へSKILL.mdのPhase数を明示し、Phase数が食い違った5件は動作フローの本文も修正した。全ガイドの依存欄へ`allowed-tools`とSKILL.md本文が参照する実行用スクリプトを追記した。結合テスト仕様書生成ガイドには欠けていたメタテーブルを追加した。名前・日本語名・Phase数を照合する検査と、自己テストを呼ぶ薄い入口を追加した。依存と説明の言い換えは文脈判断が必要なため、機械検査から除外した。
**検収方法**: `bash docs/scripts/check-skill-guide-consistency.sh`を実行する。続けて`bash docs/scripts/check-skill-guide-consistency.sh --self-test`と`bash generation-engine/scripts/tests/check-skill-guide-consistency.sh`を実行する。既存の体裁検査は`bash generation-engine/scripts/skill-guide/apply-guide-style.sh --self-test`で確認する。指示書形式は`bash .claude/rules/always/tasks/instruction-format/check-instruction-format.sh`で確認する。
**完了条件**: 通常検査が対象53件、一致53件、食い違い0件を報告して終了コード0になる。自己テストと薄い入口が終了コード0になる。既存の体裁検査と指示書形式検査が不合格0件になる。
**反映先**: `.claude/skills/*/references/guide.html`、`docs/scripts/check-skill-guide-consistency.sh`、`generation-engine/scripts/tests/check-skill-guide-consistency.sh`、`docs/tasks/作業課題一覧.md`
**優先度**: 高
**状態**: 完了

### 規約の機械強制と settings 登録の整合を検査する

**現象**: `.claude/rules/` の17規約に `## 機械強制` がある。表から得た固有スクリプトは17本で、17本すべて実在した。hook timing を宣言する9本は、9本すべて `.claude/settings.json` に timing と matcher が一致して登録済みだった。残る8本は手動実行または集約実行であり、settings への登録対象ではない。実在と登録の両方が欠ける hook は0本だった。
**修正内容**: 規約表のスクリプト実在と hook 登録を照合する検査を `docs/scripts/` に追加した。`generation-engine/scripts/tests/` には自己テストを呼ぶラッパーを追加した。欠落が0件だったため、規約行の削除、スクリプトの追加、settings の登録変更は行わない。検査本体は表の重複行を固有 timing とスクリプトの組へまとめる。手動実行と集約実行は登録対象外として分けて数える。スクリプト化の必要性は、17規約の表とsettingsの多対多照合を同じ条件で繰り返すためである。Bashの都度実行、Makefile拡張、package.jsonへの追加は自己テストを持つ独立検査の正本にならないため採用しない。保守責任者は人手とする。規約表とsettingsを同時に検証する上位機構へ統合された時に廃棄できる。
**検収方法**: `bash docs/scripts/check-rule-machine-enforcement-registration.sh` と `bash docs/scripts/check-rule-machine-enforcement-registration.sh --self-test` を実行する。続けて `bash generation-engine/scripts/tests/check-rule-machine-enforcement-registration.sh --self-test` を実行する。
**完了条件**: 実検査が固有スクリプト17本、実在17本、登録対象9本、登録済み9本、登録対象外8本、スクリプト不在0本、hook登録漏れ0本、両方が欠けるhook 0本を報告して終了コード0となる。自己テストとラッパーも終了コード0となる。
**反映先**: `docs/scripts/check-rule-machine-enforcement-registration.sh`、`generation-engine/scripts/tests/check-rule-machine-enforcement-registration.sh`、`docs/tasks/作業課題一覧.md`
**優先度**: 高
**状態**: 完了

### 規約の機械強制表を実物へ当てた結果を記録する

**現象**: 実物へ検査を実行した。規約が古いものは0件、登録が抜けているものは0件、意図的に登録しないものは8件だった。固有スクリプト17本はすべて実在した。hook登録対象9本はすべて登録済みで、実在と登録の両方が欠けるhookは0本だった。
**修正内容**: 不在スクリプトと登録漏れが無いため、規約の表は変更しない。登録対象外8本は、手動実行6本、自己テスト経由1本、集約実行1本である。いずれも規約表のtimingがhookイベントではないため、意図的に登録しないものと確認した。`settings.json` は変更しない。
**検収方法**: `bash docs/scripts/check-rule-machine-enforcement-registration.sh` を実行し、固有17本、実在17本、登録対象9本、登録済み9本、登録対象外8本、各欠落0本、終了コード0を確認する。続けて `bash .claude/rules/always/tasks/instruction-format/check-instruction-format.sh` を実行し、不合格0件を確認する。
**完了条件**: 登録が抜けているものが0件と明記されている。実検査と指示書形式検査が終了コード0になる。規約表と`settings.json`に不要な変更が無い。
**反映先**: `docs/tasks/作業課題一覧.md`
**優先度**: 高
**状態**: 完了

### 片付き判定に実測の段階を足す指示書.md

**現象**: `docs/scripts/judge-task-done.sh` の片付き判定が、記録の状態欄の語（「完了」等）を読むだけで、条件を実際に満たしているかを確かめていない。
**修正内容**: 判定の条件を実際に走らせて満たすか確かめる段階と、元の指摘を覆っているか照合する段階を足す。
**検収方法**: 反映先の指示書に記載の確かめる手段を実行する
**完了条件**: 反映先の指示書の完了の判定を満たす
**反映先**: `docs/tasks/片付き判定に実測の段階を足す指示書.md`
**優先度**: 最高
**状態**: 完了

### 集約が判定不能を不合格と区別しない問題を片付ける指示書.md

**現象**: 第1層の集約が、終了コード2（判定不能）を終了コード1（不合格）と同じ扱いにしている。
**修正内容**: 判定不能の規約が求める区別を集約へ実装する。
**検収方法**: 反映先の指示書に記載の確かめる手段を実行する
**完了条件**: 反映先の指示書の完了の判定を満たす
**反映先**: `docs/tasks/集約が判定不能を不合格と区別しない問題を片付ける指示書.md`
**優先度**: 高
**状態**: 完了

### 片付き判定の実測が上限を超えると静的確認へ逃げてしまう問題を記録する指示書.md

**現象**: 片付き判定の「確かめる手段」の実測が判定器の1コマンドあたりの上限（既定120秒）を超える場合、実測の代わりに常に成功する静的な確認へ差し替えられることがある。この差し替えは完了の判定を満たしたことにはならないが、外形上は区別できない。
**修正内容**: この問題を記録し、対応方針を検討する。
**検収方法**: 反映先の指示書に記載の確かめる手段を実行する
**完了条件**: 反映先の指示書の完了の判定を満たす
**反映先**: `docs/tasks/片付き判定の実測が上限を超えると静的確認へ逃げてしまう問題を記録する指示書.md`
**優先度**: 高
**状態**: 完了

### 走っていない検査を集約へ載せる指示書.md

**現象**: 判定スクリプトが7件、第1層の集約に載っておらず一度も走っていない。
**修正内容**: 集約が拾う形へ直すか、拾えない理由を記録する。
**検収方法**: 反映先の指示書に記載の確かめる手段を実行する
**完了条件**: 反映先の指示書の完了の判定を満たす
**反映先**: `docs/tasks/走っていない検査を集約へ載せる指示書.md`
**優先度**: 高
**状態**: 完了

### 第1層の機械検証で落ちる2本を直す指示書.md

**現象**: 第1層の機械検証（`generation-engine/scripts/verification/run-layer-machine-checks.sh`）が149本を走らせ、3本が失敗する。
**修正内容**: そのうち2本を直す（残る1本は別の指示書が扱う）。
**検収方法**: 反映先の指示書に記載の確かめる手段を実行する
**完了条件**: 反映先の指示書の完了の判定を満たす
**反映先**: `docs/tasks/第1層の機械検証で落ちる2本を直す指示書.md`
**優先度**: 高
**状態**: 完了

### 終了コード2に実行環境の制約とコマンド自身のエラーが衝突する問題を記録する指示書.md

**現象**: `judge-task-done.sh` の判定不能の解釈ロジックは終了コード2を実行環境の制約による判定不能として扱うが、`grep` がファイルを開けないときも終了コード2を返すため、2つの意味が1つの値の上で衝突している。
**修正内容**: この衝突を記録し、対応方針を示す。
**検収方法**: 反映先の指示書に記載の確かめる手段を実行する
**完了条件**: 反映先の指示書の完了の判定を満たす
**反映先**: `docs/tasks/終了コード2に実行環境の制約とコマンド自身のエラーが衝突する問題を記録する指示書.md`
**優先度**: 高
**状態**: 完了

### 宣言で除外された長時間の検査を走らせる指示書.md

**現象**: 第1層の機械検証で「宣言済み長時間」として除外されている2本が、実際には走っていない。
**修正内容**: 実際に走る形へ縮める。
**検収方法**: 反映先の指示書に記載の確かめる手段を実行する
**完了条件**: 反映先の指示書の完了の判定を満たす
**反映先**: `docs/tasks/宣言で除外された長時間の検査を走らせる指示書.md`
**優先度**: 中
**状態**: 完了（反映先の指示書は docs/tasks/done/ へ移動済み。判定3つすべてが完了で、コミット e147868 と b7ee4eb が記録されている。移動のコミットは a6c75c5）

### テンプレートの未置換の欄を検知できるようにする指示書.md

**現象**: `scaffold-design-unit.sh --verify` が種別ごとに1つのトークンしか検査しておらず、それ以外の欄が未記入のままでも合格する。
**修正内容**: 検査の対象をテンプレート由来の全大文字トークン全体へ広げる。
**検収方法**: 反映先の指示書に記載の確かめる手段を実行する
**完了条件**: 反映先の指示書の完了の判定を満たす
**反映先**: `docs/tasks/テンプレートの未置換の欄を検知できるようにする指示書.md`
**優先度**: 中
**状態**: 完了

### 全角文字に隣接する変数展開を波括弧で囲む指示書.md

**現象**: 波括弧なしの変数展開（`$変数名`）の直後に全角文字が続くコードがあり、`set -u` 下で `unbound variable` エラーを起こす潜在バグがある。
**修正内容**: リポジトリ全体の `.sh` ファイルから洗い出し、`${変数名}` の形へ修正する。
**検収方法**: 反映先の指示書に記載の確かめる手段を実行する
**完了条件**: 反映先の指示書の完了の判定を満たす
**反映先**: `docs/tasks/全角文字に隣接する変数展開を波括弧で囲む指示書.md`
**優先度**: 中
**状態**: 完了

### 検査の除外の目印を内容が読める形へ改める指示書.md

**現象**: 配線漏れの検査が、警告から外す目印として通し番号を含む固定の文字列を使っており、番号からは条件が読み取れない。
**修正内容**: 内容が読める文字列へ改める。
**検収方法**: 反映先の指示書に記載の確かめる手段を実行する
**完了条件**: 反映先の指示書の完了の判定を満たす
**反映先**: `docs/tasks/done/検査の除外の目印を内容が読める形へ改める指示書.md`
**優先度**: 中
**状態**: 完了

### 高速化のあとに目視へ退いた判定を戻す指示書.md

**現象**: `docs/tasks/done/` の判定のうち、確かめる手段が時間の上限を超えるために「目視」へ退いた10行がある。
**修正内容**: 自己テストの所要時間の問題が片付いたあと、コマンドへ戻す。
**検収方法**: 反映先の指示書に記載の確かめる手段を実行する
**完了条件**: 反映先の指示書の完了の判定を満たす
**反映先**: `docs/tasks/高速化のあとに目視へ退いた判定を戻す指示書.md`
**優先度**: 中
**状態**: 完了

### 理由の無い目視を減らす指示書.md

**現象**: `docs/tasks/done/` の判定表に、理由の書かれていない「目視」が47件ある。
**修正内容**: 1件ずつ判定文を読み、機械で確かめられるものはコマンドへ戻し、確かめられないものには理由を書く。
**検収方法**: 反映先の指示書に記載の確かめる手段を実行する
**完了条件**: 反映先の指示書の完了の判定を満たす
**反映先**: `docs/tasks/理由の無い目視を減らす指示書.md`
**優先度**: 中
**状態**: 完了

### 委任のときに作業場所を渡させる指示書.md

**現象**: 作業を委任する際に作業場所（git worktree）のパスを渡す決まりがあるが、これを機械で確かめる仕組みが無く、渡さないまま委任できてしまう。
**修正内容**: 委任のプロンプトに作業場所のパスが含まれているかを機械で確かめ、無ければ止める。
**検収方法**: 反映先の指示書に記載の確かめる手段を実行する
**完了条件**: 反映先の指示書の完了の判定を満たす
**反映先**: `docs/tasks/委任のときに作業場所を渡させる指示書.md`
**優先度**: 中
**状態**: 完了

### 用語集とガイドの食い違いを解消する指示書.md

**現象**: 用語集（`docs/guides/用語集.md`）とスキルのガイドの用語が食い違っており、用語集を唯一の情報源とする建て付けが機能していない。
**修正内容**: 食い違いを解消する。
**検収方法**: 反映先の指示書に記載の確かめる手段を実行する
**完了条件**: 反映先の指示書の完了の判定を満たす
**反映先**: `docs/tasks/用語集とガイドの食い違いを解消する指示書.md`
**優先度**: 中
**状態**: 完了

### check-instruction-formatのmktempをサンドボックス下で成功させる指示書.md

**現象**: `check-instruction-format.sh` が使う引数なし `mktemp` 2箇所が、サンドボックス環境下で失敗し検査が「判定不能」で止まる。
**修正内容**: 明示パス指定の `mktemp` へ書き換え、サンドボックス環境下でも一時ファイルの作成が成功するようにする。
**検収方法**: 反映先の指示書に記載の確かめる手段を実行する
**完了条件**: 反映先の指示書の完了の判定を満たす
**反映先**: `docs/tasks/check-instruction-formatのmktempをサンドボックス下で成功させる指示書.md`
**優先度**: 中
**状態**: 完了

### 実装が変わっても設計書の表示コミットを追従させる段が開発フローに無い

**現象**: 版の管理はコミットのハッシュ（全長40桁）で行う設計である。設計書の前付けは `source_ref` に抽出時点のハッシュを持つ。`delivery-payload/references/facts-schema.md` はこの値を「抽出時点のコミットSHA」と定義する。定義の実測コマンドは `git -C <target_repo_path> rev-parse HEAD` である。`generation-engine/scripts/check-derived-values.sh` は前付けの値とポータル・画面ページの表示値の一致を検査する。ところが配る開発フローの Phase5 は更新の段を持つ。`delivery-payload/templates/delivered-skills/dev-flow/SKILL.md` がその定義元である。この段は「入口のページを更新する」だけである。この記述は同ファイル212行目にある。この段は `check-portal-entry-freshness.sh` で入口のページの鮮度を判定する。古ければ依存の宣言が選んだ成果物を作り直す。完了記録にも「入口のページ」の更新コミットを残す欄がある。この欄は同ファイル241〜242行目にある。個々の設計書の `source_ref` を実装の変更時に更新する段は存在しない。同ファイルを `source_ref` で検索すると0件である。コードを直しても、設計書の前付けは抽出時点のハッシュを指したままになる。この食い違いは `check-derived-values.sh` が「表示コミット-ポータル」「表示コミット-画面」として検知する。検知はするが、開発フローの中に直す段が無い。検知された後に誰が何をするかが定まっていない。
**修正内容**: 2つを行う。第1に、配る開発フローの Phase5 へ、実装の変更が及んだ設計単位の `source_ref` を更新する段を足す。更新の対象は人が選ばない形にする。入口のページの段が既に採っている形と揃える。依存の宣言に相当するものを定め、それが選んだ設計単位を全部更新する。第2に、完了記録の形式へ、更新した設計単位とその更新後のハッシュを残す欄を足す。入口のページと同じく、更新しなかった場合は影響が無い理由を残す。
**検収方法**: 1. 配る開発フローの Phase5 に `source_ref` の更新の段が存在すること。2. 更新の対象を人が選ばない形になっていること。3. 対象の選び方が機械で決まること。4. 完了記録の形式に更新した設計単位とハッシュの欄があること。5. `check-derived-values.sh` が検知した後にやることが開発フローの本文から辿れること。6. 更新の段を持つことを機械で確かめる検査が存在すること。7. その検査が段を欠いた入力に対して不合格を返すこと。
**完了条件**: 配る開発フローのPhase5への段の追加、完了記録の形式への欄の追加、その段の有無を確かめる検査の新設までを本項目の範囲とする。既に納品済みの対象プロジェクトの開発フロー定義の是正は範囲外とする。`check-derived-values.sh`が既に検知している個別の不一致の解消も範囲外とする。
**反映先**: 未定
**優先度**: 中
**状態**: 完了（コミット: ac8dff9）。配る開発フローのPhase5へ「設計書の表示コミットを更新する」段を新設した。更新の対象を人が選ばない形にした。完了記録の形式にも、更新した設計単位とハッシュの欄を足した。検査 generation-engine/scripts/tests/check-source-ref-refresh-step.sh を新設した。通常実行・機械的な選び方・検知との連結・自己テストの4つがすべて終了コード0を返す。
**旧状態**: 完了。コミット ac8dff9。配る開発フローのPhase5へ「設計書の表示コミットを更新する」段を新設し、更新の対象を人が選ばない形にした。完了記録の形式にも更新した設計単位とハッシュの欄を足した。検査 generation-engine/scripts/tests/check-source-ref-refresh-step.sh を新設した。通常実行・機械的な選び方・検知との連結・自己テストの4つがすべて終了コード0だった。
**旧番号**: 1-259（外部の課題管理と食い違っていたため2026-08-25に外した）

### 画面検出の自己テストで一時パスの重複スラッシュが期待値だけに残る

**現象**: `detect-screens.sh` の `_mktemp_or_unknown` は `${TMPDIR:-/tmp}/名前.XXXXXX` を組み立てる。macOSで実測した `TMPDIR` は末尾が `/` である。自己テストの作業ディレクトリは `/var/folders/.../T//detect-screens.XXXXXX` になった。期待集合はこの値をそのまま使うため `//` を持つ。一方、import解決の `normalize_path()` は連続するスラッシュの空要素を捨てる。実際の追加ファイルは `/var/folders/.../T/detect-screens.XXXXXX/...` となる。この差で、エイリアス解決・bare import不追跡・循環収束・深さ上限・他画面ID境界の5件が不一致になる。必須コマンドの実行自体は `CLAUDE-COMPAT-BLOCK` に停止された。この時点の終了コードは未取得である。
**修正内容**: `_mktemp_or_unknown` で一時領域の末尾スラッシュを除いてからテンプレート名を連結する。作成時点で重複スラッシュを持たないパスへ統一する。置き場を明示する方式は維持する。失敗時に `[UNKNOWN]`・終了コード2を返す契約も維持する。
**検収方法**: 1. `bash generation-engine/scripts/unit-list/detect-screens.sh --self-test` が終了コード0を返すこと。2. `bash docs/scripts/check-mktemp-placement.sh` が終了コード0を返すこと。3. `bash generation-engine/scripts/tests/check-broken-verdict-rows.sh` が終了コード0を返すこと。
**完了条件**: 5件の集合比較がすべて合格すること。一時ファイルの置き場明示と判定不能契約を保った状態で、関連する横断検査が終了コード0を返すこと。
**反映先**: 未定
**優先度**: 中
**状態**: 完了
**旧状態**: 完了。
**旧番号**: 1-260（外部の課題管理と食い違っていたため2026-08-25に外した）

### Python facts-onlyの三文書で入口対話契約が食い違う

**現象**: `test-python-facts-flow.sh` は三文書を読む。読む対象は統括スキルの `SKILL.md`、`references/contract.md`、`references/guide.html` である。行位置と本文を実測すると、survey_doc_pathを確定してからfacts抽出へ進む順序は三文書とも一致していた。食い違っていたのは入口契約である。SKILL.mdとcontract.mdは「対話による確認は行わない。不足値を推測せず中断」と定める。一方、guide.htmlだけが「起動引数が空の対話実行ではprofileを質問」と記載する。さらにテストは旧いAskUserQuestion契約がSKILL.mdにあることを要求していた。実際の失敗原因を「survey順序」とだけ報告していた。必須コマンドの実行自体は `CLAUDE-COMPAT-BLOCK` に停止された。この時点の終了コードは未取得である。
**修正内容**: guide.htmlをSKILL.mdとcontract.mdの非対話契約へ合わせる。テストはsurvey_doc_path確定→facts抽出の順序検査を維持する。三文書すべてが非対話契約を持つことも検査する。旧い対話契約がguide.htmlへ残っていないことも検査する。失敗名は入口契約と順序のどちらが原因になり得るかを表す文言へ改める。
**検収方法**: 1. `bash generation-engine/scripts/tests/test-python-facts-flow.sh` が終了コード0を返すこと。2. 指示書形式検査が不合格0件で終了コード0を返すこと。3. `bash generation-engine/scripts/tests/check-broken-verdict-rows.sh` が終了コード0を返すこと。
**完了条件**: 三文書の入口契約が非対話で一致すること。survey_doc_path確定→facts抽出の順序検査を保った自己テストと、関連する横断検査が終了コード0を返すこと。
**反映先**: 未定
**優先度**: 中
**状態**: 完了
**旧状態**: 完了。
**旧番号**: 1-261（外部の課題管理と食い違っていたため2026-08-25に外した）

### 集約がケース数を読めず実行済みの検査を途中停止と報告する

**現象**: inventory approvalの自己テストは `self-test: PASS=18 FAIL=0` を出して終了コード0だった。source ref refreshの自己テストは `self-test: PASS=28 FAIL=0` を出して終了コード0だった。しかし第1層集約の `count_cases` へ各出力を渡すと、どちらも0件になる。現行10経路を各実出力へ照合したところ、両方とも全経路で不一致だった。10経路の内訳はコメント上の9書式とTest Anything Protocol（TAP）の要約である。同じ `self-test: PASS=<数字> FAIL=<数字>` 形式を出す実装は5本あり、すべて第1層集約の対象である。
**修正内容**: `count_cases` に `self-test: PASS=<数字> FAIL=<数字>` を読む経路を追加する。PASS件数とFAIL件数は10進数として合算する。新経路は既存10経路の後に置く。行全体が一致した要約だけを読む。集約自身の自己テストへ、厳密一致・優先順位・先頭ゼロの回帰ケースを加える。個別検査5本の出力契約と検査の判定は変更しない。
**検収方法**: 1. 対象2本の自己テストが終了コード0を返すこと。2. 集約自身の自己テストが新しい書式からPASS・FAIL合計を読むこと。3. 進捗集計・指示書形式・壊れた判定行の横断検査が終了コード0を返すこと。
**完了条件**: 新形式を出す集約対象が実行済み件数として数えられること。対象2本が「途中停止の疑い」にならないこと。既存のケース数読取り経路と判定の強さを維持すること。
**反映先**: 未定
**優先度**: 中
**状態**: 完了（コミット: 334c7ba）。集約の件数の読み取りへ「self-test: PASS=<数字> FAIL=<数字>」の形を読む経路を足した。実行済みの検査2本が「実行0件」と報告されなくなった。集約の自己テストが43件合格した。
**旧状態**: 完了。コミット 334c7ba。集約の件数の読み取りへ「self-test: PASS=<数字> FAIL=<数字>」の形を読む経路を足した。実行済みの検査2本が「実行0件」と報告されなくなった。集約の自己テストが43件合格した。
**旧番号**: 1-262（外部の課題管理と食い違っていたため2026-08-25に外した）

### 用語辞書の検査が隔離環境の不在で判定不能のまま残る

**現象**: `generation-engine/scripts/glossary/requirements.txt` には2行がある。1行目は `PyYAML>=6,<7` である。2行目は `jsonschema[format]>=4.23,<5` である。`validate-semantic-glossary.sh` は102行目で `.venv/bin/python` を探す。132から133行目で隔離環境の整備手順を出力する。`README.md` には隔離環境を構築する手順がない。`generation-engine/scripts/glossary/.gitignore` の1行目は `.venv/` を除外する。`python3.13` はPATHになく、`generation-engine/scripts/glossary/.venv` も存在しない。`test -x` で `.venv/bin/python` を実行可能なファイルとして確認できなかった。`generation-engine/scripts/glossary/tests/test-validate-semantic-glossary.sh` を実行した。`.venv/bin/python` の不在を検出した。この検査は `[UNKNOWN]` と終了コード2を返した。`generation-engine/scripts/detail-pages/test-project-semantic-glossary-race.sh` を実行した。python3 への切替後にPyYAMLの読み込みに失敗した。この検査も `[UNKNOWN]` と終了コード2を返した。
**修正内容**: `generation-engine/scripts/glossary/README.md` へ隔離環境の整備手順を記載する。`cd generation-engine/scripts/glossary` を実行する。`python3.13 -m venv .venv` を実行する。`.venv/bin/pip install -r requirements.txt` を実行する。終了コード2の `[UNKNOWN]` は成果物の不合格ではなく未実行判定であることを記載する。検査中に隔離環境を自動作成しない。
**検収方法**: 1. `bash docs/scripts/report-progress.sh` が総数121件を出して終了コード0を返すこと。2. `bash docs/scripts/report-progress.sh --self-test` が終了コード0を返すこと。3. 指示書形式検査が不合格0件で終了コード0を返すこと。4. `bash generation-engine/scripts/tests/check-broken-verdict-rows.sh` が終了コード0を返すこと。5. `git ls-files generation-engine/scripts/glossary/.venv` の出力が空であること。6. `git status --porcelain` の出力が空であること。
**完了条件**: 一覧・指示書・READMEの3文書へ実測事実・文書化方針・整備手順を反映すること。隔離環境を版管理へ入れないこと。6つの検収条件を満たすこと。
**反映先**: 未定
**優先度**: 中
**状態**: 完了（コミット: 0c1f8e4）。隔離環境の整備手順を README へ明記した。2本の判定不能がそれぞれ何に由来するかを書いた。整備を自動化しない判断は、特定のPythonが無い環境では自動で作っても判定不能が残るためである。指示書は docs/tasks/done/ へ移動済みである。
**旧状態**: 完了。コミット 0c1f8e4。隔離環境の整備手順を README へ明記した。2本の判定不能がそれぞれ何に由来するかを書いた。整備を自動化しない判断は、特定のPythonが無い環境では自動で作っても判定不能が残るためである。指示書は docs/tasks/done/ へ移動済みだった。
**旧番号**: 1-263（外部の課題管理と食い違っていたため2026-08-25に外した）

### 規約とフローの対応が生成器を持ちながら見本に作られていない

**現象**: `generation-engine/scripts/rules/build-rule-flow-map.sh` は実在する。入力の `delivery-payload/references/rule-taxonomy.json` も実在する。`delivery-payload/references/portal-catalog.json` の `rule-flow-map` 登録も実在する。分類は親7件・子27件である。見本側の `generation-engine/samples/docs/rules/` にも親7件・子27件の `rule.md` がある。一方、宣言された出力先 `generation-engine/samples/project-portal/foundation/規約とフローの対応.html` は実在しない。生成器は `build-derived-rules.sh` から独立している。`build-portal.sh` にも呼び出しが無い。他の基盤見本は `build-detail-page.sh` などの個別生成器を明示して再生成した記録がある。その記録に `build-rule-flow-map.sh` は含まれない。現行の見本再生成経路にこの独立生成器が含まれず、出力だけが取り残されたと判断した。納品物一覧の定義で `rule-flow-map` は `evidenceSource: none` と宣言される。一覧生成器はHTMLが無いままこの値を読むと `未生成`・`判定の材料が無い` を返す。自己テストの実行は `CLAUDE-COMPAT-BLOCK` に停止され、終了コードを取得できなかった。
**修正内容**: 既存の `build-rule-flow-map.sh` を実行する。見本の `rule-taxonomy.json` と見本ルートを入力にする。宣言済みの出力先へHTMLを生成する。生成器、カタログ、状態判定の強さは変更しない。続けて `build-deliverable-inventory.sh` でMarkdown版とHTML版の納品物一覧を再生成する。すべての生成が終わった後に `check-derived-drift.sh record` で見本の派生物台帳を更新する。
**検収方法**: 1. `generation-engine/samples/project-portal/foundation/規約とフローの対応.html` が実在すること。2. 納品物一覧の「規約とフローの対応」が「出力あり」であること。3. 派生物台帳の検査が終了コード0を返すこと。4. 見本ポータル規約の検査が不合格0件であること。5. 進捗集計が総数122件を出して終了コード0を返すこと。6. 指示書形式検査が不合格0件で終了コード0を返すこと。7. 壊れた判定行の検査が終了コード0を返すこと。
**完了条件**: 既存生成器による見本HTMLの生成、派生物台帳の更新、Markdown版とHTML版の納品物一覧の再生成、上記7点の検収を満たすこと。課題キー `1-264` を含むコミット後に作業ツリーが空であること。
**反映先**: 未定
**優先度**: 中
**状態**: 完了（コミット: 725e740）。生成器は実在し自己テストも通るのに、生成の連鎖から呼ばれておらず見本が作られていなかった。見本を生成し、納品物一覧が「出力あり」を報告するようにした。ポータル規約の合格は1130件から1144件へ増えた。
**旧状態**: 完了。コミット 725e740。生成器は実在し自己テストも通るのに、生成の連鎖から呼ばれておらず見本が作られていなかった。見本を生成し、納品物一覧が「出力あり」を報告するようにした。ポータル規約の合格は1130件から1144件へ増えた。
**旧番号**: 1-264（外部の課題管理と食い違っていたため2026-08-25に外した）

### アーキテクチャ調査書が一覧の期待と実態で食い違う

**現象**: `.claude/skills/surveying-architecture-for-reverse-docs/SKILL.md` は出力を1本と定める。出力先は `<output_dir>/<commonRoot>/アーキテクチャ調査書.md` である。見本の `generation-engine/samples/docs/design/common/アーキテクチャ調査書.md` も実在する。一方、`portal-catalog.json` の `architecture-survey` は `dir: project-portal/foundation` を宣言する。`discovery.glob` は `project-portal/foundation/アーキテクチャ調査書.html` である。そのHTMLは見本に無い。`deliverable-inventory.json` の `evidenceSource: none` は、HTMLが無く対象外記録も無い場合の宣言である。この場合、一覧生成器は「未生成」「判定の材料が無い」と返す。ただしHTML生成器が無いという見立ては現行コードには当てはまらない。`build-portal.sh` は `surveyDoc` を既知の基盤文書として読む。同じ作成スキルがMarkdownだけを出力する基盤設計書・共通設計書などと同じ経路がある。その経路が `docs/design/common/アーキテクチャ調査書.md` を `project-portal/foundation/アーキテクチャ調査書.html` へ変換する実装を持つ。カタログとスキルの契約不一致ではない。既存の汎用変換を通した見本HTMLだけが欠けた状態である。実物の見本を出力先にして `build-portal.sh` を実行すると、対象HTMLの生成前に終了コード1になることも実測した。原因は、prepared inputが無い既存3ページ（技術スタック・環境構築手順・用語辞書）をstaleと検知するためである。
**修正内容**: 見本をworktree内の明示的な一時ディレクトリへ複製する。`build-portal.sh` の `run_prepared_detail_pages_self_test` と同じ正規schemaのprepared input 3件を一時コピーにだけ置く。この自己テストはtechstack/env/glossaryのprepared inputを作る。その複製上で汎用生成器を完走させる。生成したアーキテクチャ調査書HTMLだけを実物の宣言済み出力先へ反映する。カタログ・スキルのMarkdown出力契約・`evidenceSource`・変換ロジック・実物のprepared inputは変更しない。続けてMarkdown版とHTML版の納品物一覧を再生成する。すべての生成後に見本の派生物台帳を更新する。
**検収方法**: 1. `grep -c '未生成' generation-engine/samples/docs/納品物一覧.md` の出力が0であること。2. 派生物台帳の検査が終了コード0を返すこと。3. 見本ポータル規約の検査が不合格0件であること。4. 進捗集計が総数123件を出して終了コード0を返すこと。5. 指示書形式検査が不合格0件で終了コード0を返すこと。6. 壊れた判定行の検査が終了コード0を返すこと。7. コミット後の作業ツリーが空であること。
**完了条件**: 既存の汎用変換による見本HTMLの生成、Markdown版とHTML版の納品物一覧の再生成、派生物台帳の更新、上記7点の検収を満たすこと。課題キー `1-265` を含むコミット後に作業ツリーが空であること。
**反映先**: 未定
**優先度**: 中
**状態**: 完了（コミット: a9220ed）。一覧は `project-portal/foundation/アーキテクチャ調査書.html` というHTMLを期待していた。生成元のスキルはMarkdownしか作っていなかった。一覧の側をMarkdownへ合わせる案と、個別のHTML生成器を新設する案も検討した。`build-portal.sh` が `surveyDoc` を共通Markdown変換へ既に含んでおり、宣言済みのHTML出力先へ変換できた。そのため、既存の汎用ポータル生成器でHTMLを作る方向を選んだ。納品物一覧の未生成が0件になった。見本ポータル規約の合格は1144件から1158件へ増えた。
**旧状態**: 完了。コミット a9220ed。一覧はHTML（`project-portal/foundation/アーキテクチャ調査書.html`）を期待していた。生成元のスキルはMarkdownしか作っていなかった。一覧の側をMarkdownへ合わせる案と個別のHTML生成器を新設する案も検討した。`build-portal.sh` が `surveyDoc` を共通Markdown変換へ既に含み、宣言済みのHTML出力先へ変換できたため、既存の汎用ポータル生成器でHTMLを作る方向を選んだ。納品物一覧の未生成が0件になった。見本ポータル規約の合格は1144件から1158件へ増えた。
**旧番号**: 1-265（外部の課題管理と食い違っていたため2026-08-25に外した）

### 統合先の枝の検査が作業用の枝でのコミットも止める

**現象**: 修正前の `delivery-payload/references/checker-real-world-cases.json` を確認した。対象は `check-direct-commit-to-integration-branch` の3件である。2件はラベルが「作業用の枝の上でコミット」「作業用の枝でコミット」だった。期待値は `clean`、入力はいずれも `cwd: REPO_ROOT` と `git commit -m x` だった。現在の `fix/branch-check` で各入力の `REPO_ROOT` をこのworktreeへ置き換えて検査へ直接流すと、2件とも終了コード0だった。一方、修正前の `check-real-world-cases.sh` は `REPO_ROOT` を実行中のリポジトリへ置換するだけだった。ラベルどおりの作業用の枝を自前で用意しなかった。この2件の判定は検査実行時の枝に依存した。進行担当の実測では93件中この2件が終了コード2で不合格だった。作業用の枝を検査自身が止めたという見立ては、現在のworktreeでは再現しなかった。統合先の枝を用意する一時複製による直接再現は、互換フックが終了コード2で停止したため、この時点では実行結果を取得できなかった。
**修正内容**: 統合先の枝を止める検査本体の判定は維持する。実際の書き方の入力集では、作業用の枝を表す2件の `cwd` を専用の印へ変える。`check-real-world-cases.sh` が置き場を指定した一時Gitリポジトリを作る。そのリポジトリへ作業用の枝と初期コミットを用意してから印を実在パスへ置き換える。通し検査をどの枝から実行しても、作業用の枝の2件は終了コード0になるようにする。一時リポジトリを残さない。検査本体の回帰テストが持つ、統合先の枝を終了コード2にするケースも維持する。
**検収方法**: 1. 実際の書き方の検査が不合格0件で終了コード0を返すこと。2. 検査本体の回帰テストが終了コード0を返すこと。3. 回帰テスト内で `main`、`master`、`develop`、`trunk` への直接コミットが終了コード2になること。4. 実際の書き方の検査が作る作業用の枝の入力が、実行元の現在の枝に依存しないこと。5. 一時リポジトリの作成に置き場を指定し、実行後に残さないこと。6. 進捗集計が総数124件を出すこと。7. 指示書形式検査と壊れた判定行の検査が不合格0件で終了コード0を返すこと。
**完了条件**: 実際の書き方の検査に渡す作業用ブランチの値を、検査を動かす枝から独立させること。統合先の枝を止める既存判定を維持したまま指定検査を通すこと。課題キー `1-266` を含むコミット後に作業ツリーが空であること。
**反映先**: 未定
**優先度**: 中
**状態**: 完了（コミット: b1aa183）。真因は検査本体ではなく、実際の書き方の入力の側にあった。「作業用の枝」と称する入力の `cwd` が実行元リポジトリの `REPO_ROOT` へ単純置換されていた。進行担当が `fix/branch-check` から実行すると作業用の枝を指したが、統合先の枝から実行すると main を指した。検査本体は正しく main を止めており、それが不合格として現れていた。判定の中核、つまり統合先4枝を終了コード2で止める判定は変更しなかった。`checker-real-world-cases.json` の作業用の枝2件の入力を専用リポジトリへ分離した。`check-direct-commit-to-integration-branch.sh --self-test` に develop・trunk への直接コミットを拒否するケースを追加した。実際の書き方の検査は91/93件から93/93件、不合格0件へ改善した。
**旧状態**: 完了。コミット b1aa183。真因は検査本体ではなく実際の書き方の入力の側にあった。「作業用の枝」と称する入力の `cwd` は、実行元リポジトリの `REPO_ROOT` へ単純置換されていた。進行担当が `fix/branch-check` から実行すると作業用の枝を指したが、統合先の枝から実行すると main を指した。検査本体は正しく main を止めており、それが不合格として現れていた。判定の中核（統合先4枝を終了コード2で止める判定）は変更しなかった。`checker-real-world-cases.json` の作業用の枝2件の入力を専用リポジトリへ分離した。`check-direct-commit-to-integration-branch.sh --self-test` に develop・trunk への直接コミットを拒否するケースを追加した。実際の書き方の検査は91/93件から93/93件（不合格0件）へ改善した。
**旧番号**: 1-266（外部の課題管理と食い違っていたため2026-08-25に外した）

### 文章の検査の子プロセスに時間の上限が無く残留する【暫定対処済み】

**現象**: 文章の検査を起動する箇所は `dispatch-pre-bash-checks.sh` の4箇所にある。`timeout` と `gtimeout` はどちらも存在しない。フック登録の10秒上限で親が失われても、textlint の子プロセスには上限が無い。2026-08-25早朝に6件が最長6時間51分、CPU約90%で残留した。負荷が22.82まで上昇した。その後の再現でも親を失った3件が33分48秒残留した。
**修正内容**: `generation-engine/scripts/verification/run-layer-machine-checks.sh` の先例に合わせる。4箇所すべての textlint を `set -m` で専用プロセスグループとして起動する。0.2秒ごとにグループ自体の生存を確認する。1回5秒、フック内合計8秒を上限とする。超過時は SIGTERM と SIGKILL で子孫ごと止める。docs と HTML の grep・perl へつながるパイプは維持する。打ち切りは判定不能とする。commit と PR・issue 作成は止める。通知専用のコメントだけは判定不能を通知して通す。配備側 `agent-home` への直接修正は暫定対処である。本指示書を定義への反映経路として残す。
**検収方法**: 1. 上限付き起動が4箇所にあること。2. 30秒待つ代替子プロセスが5秒の監視上限で打ち切られ子も残らないこと。3. 実際のコミットが通り残留が0件であること。4. 進捗集計が総数125件で終了コード0を返すこと。5. 指示書形式検査が不合格0件で終了コード0を返すこと。6. 壊れた判定行の検査が終了コード0を返すこと。7. textlint.js の残留が0件であること。8. 両リポジトリの対象変更をコミットした後に作業ツリーが空であること。
**完了条件**: 受け渡し側の指示書を定義へ反映すること。配備側の暫定対処と同じ時間上限・判定不能の扱いを持たせること。上記8点を実測すること。課題キー `1-267` を含むコミット後に作業ツリーが空であること。
**反映先**: 未定
**優先度**: 中
**状態**: 完了（コミット: 462b639・af64437）。設定リポジトリのコミット 462b639 で4箇所すべてへ上限付き起動を入れた。af64437 で回帰試験38件を足した。子1回5秒・スクリプト内合計8秒を上限とし、超過時は判定不能として扱う。実測で回帰試験38件が合格した。コミット後の残留が0件であることを確かめた。
**旧状態**: 未着手
**旧番号**: 1-267（外部の課題管理と食い違っていたため2026-08-25に外した）

### 規則の本文が自分の禁止語で落ちる問題を直す

**現象**: 第1層の機械検証のうち `scaffold-rule-definitions.sh --self-test` のケース11が不合格だった。原因は `document-writing.md` が定める規則の理由列にある「読者である納品先には何を指すか分からず」の記述だった。この記述中の「納品先」が禁止語一覧の「納品」に一致していた。規則を定義する文書が自分自身の禁止語検査に引っかかる構図だった。
**修正内容**: `scan_banned_terms`（`scaffold-rule-definitions.sh`）へ除外の仕組みを設けた。除外の対象は `rule-banned-terms.json` の新設キー `exemptions` に定義側として持たせた。検査スクリプトへは直書きしていない。除外は `documentation-standards/document-writing` の rule.md・rule.html だけに限った。それ以外の文書は従来どおり対象のままとした。除外した件数を `禁止語検査: N件（除外 M件）` の形で必ず出力するようにした。
**検収方法**: `scaffold-rule-definitions.sh --self-test` を実行し全23件PASSを確かめる。非除外の文書に禁止語を仕込んだ合成入力でも不合格が従来どおり検出されることを確かめる。
**完了条件**: ケース11がPASSすること。除外の範囲が `document-writing` の rule.md・rule.html に限られること。他の文書への検出精度を落とさないこと。
**反映先**: 未定
**優先度**: 高
**状態**: 完了（コミット: 6acbbe1）。除外を定義側（`rule-banned-terms.json` の `exemptions`）へ持たせた。`documentation-standards/document-writing` の rule.md・rule.html だけを対象に除外した。除外件数を出力へ含めた。自己テスト23件全PASSを確認した。合成入力（`some-parent/some-key/rule.md` に「納品」を含む行）では従来どおり不合格として検出されることを確認した。見本に対する `check-customer-facing-adequacy.sh` の不合格件数は66件のまま変化なし。

### 規約の生成元と派生が食い違い、再生成すると加筆が消える状態だった

**現象**: 課題1-268の対応は `.claude/rules/scoped/portal/page-conventions/rule.md` だけを直接編集した。生成元 `docs/rules/portal/page-conventions/rule.md` には反映しなかった。もう一方の派生 `.cursor/rules/portal-page-conventions.mdc` にも反映しなかった。実測では、この段落（「**1-268での拡張**」）は派生側に1件あった。生成元と `.cursor` 側にはそれぞれ0件だった。この状態で `generation-engine/scripts/rules/build-derived-rules.sh --apply` を実行すると、派生が生成元から丸ごと上書きされる。派生にしかない当該段落（直前の空行を含む2行）が消えることを、使い捨ての作業場所で確認した。生成物のずれ検知（`check-derived-drift.sh`）は `generation-engine/samples/` 配下のHTMLだけを対象とする。規約の生成元と派生の食い違いは検知しない。
**修正内容**: 派生にしかなかった段落を生成元へ転記した。文を100字以内へ分割し、文章の決まり（textlint）に適合させた。転記後に `build-derived-rules.sh --apply` を実行し、生成元から3ファイル（生成元・`.claude`・`.cursor`）を作り直した。
**検収方法**: 1. 生成元・`.claude`・`.cursor`の3ファイルで、段落の出現数がいずれも1件であること。2. `build-derived-rules.sh --apply` を2回連続で実行しても、ハッシュが変わらないこと。3. `validate-rule-definitions.sh` が終了コード0を返すこと。4. `validate-rule-judgment-coverage.sh` の各件数が着手前と変わらないこと。
**完了条件**: 3ファイルの段落が一致すること。上記4点の検収を満たすこと。
**反映先**: 未定
**優先度**: 中
**状態**: 完了（コミット: 43a6434）。生成元へ段落を転記し、文体を100字以内へ整えたうえで `--apply` を実行した。派生2つが生成元から作り直され、3ファイルとも同一の段落を持つ状態になった。`validate-rule-definitions.sh` は終了コード0（検査宣言合格29件・検査合格27件）だった。`validate-rule-judgment-coverage.sh` は着手前と同じ（一致26件・不足1件・過剰0件・検査なし0件・名前の不一致0件）で悪化はなかった。

### test-screen-unit-root-layout.shのフィクスチャが課題1-268の様式一致検査に対して回帰していた

**現象**: 課題1-268（コミット4ca2d59）が単体テスト設計書へ様式一致検査を追加した。テンプレートの全節・全表列と完全一致することを求める検査である。`test-screen-unit-root-layout.sh` の試験用フィクスチャはそれより568コミット前の古い形（§1・§2の2節のみ）のままだった。このためこの検査を満たさなかった。第1層の集約で本スクリプトが「実行0件/終了コード1」で不合格になっていた。加えて、同スクリプトの `build-portal.sh` 呼び出しが末尾に `>/dev/null` を付けていた。標準出力を捨てるため、失敗の理由（`FAIL 必須節-欠落`等）が見えなかった。単独実行では終了コードを確認しない限り通っているように見えた。
**修正内容**: フィクスチャへ様式が定める全節と全表列見出しを、テンプレートと完全一致する形で追加した。対象は `delivery-payload/references/design-doc-required-sections.json` と対応するテンプレートである。§1配下の「観点の導出元」表は、データ行の1列目を `<...>` 形式にした。`aggregate-test-viewpoints.sh` のプレースホルダ除外規則を使い、既存のtotalCount==2検査を壊さないようにした（実測で確認済み）。`build-portal.sh` 呼び出しの `>/dev/null` は外した。出力を一時ファイルへ捕捉し、失敗時だけ表示する形へ変えた（39・40行目の `>/dev/null` は対象外のまま変更していない）。
**検収方法**: 1. `test-screen-unit-root-layout.sh` が終了コード0を返すこと。2. `check-design-doc-section-consistency.sh --self-test` が62 PASS・0 FAILのまま変化しないこと。3. `build-portal.sh --self-test` のFAIL集合が着手前と一致すること。件数は97件で変化なし。既存の未解消の失敗であり本課題の対象外である。
**完了条件**: 上記3点の検収を満たすこと。
**反映先**: 未定
**優先度**: 中
**状態**: 完了（コミット: 4602b21）。フィクスチャへ全11節と全表列見出しを追加した。`test-screen-unit-root-layout.sh` が終了コード0で完走することを確認した（実測16.19秒）。フィクスチャをわざと壊すと `FAIL テンプレート見出し-不一致` が標準エラーへ現れることを確認した。確認後は元へ戻した。`check-design-doc-section-consistency.sh --self-test` は62 PASS・0 FAILで変化なし。`build-portal.sh --self-test` はFAIL 97件で着手前と完全一致（差分なし）で悪化はなかった。

### 検証の判定と退避の台帳が設計どおりの場所に実在しなかった

**現象**: `docs/design/reverse-verification/設計.md`「記録の置き場」は、判定の結果と退避の記録の置き場を定めていた。判定の結果は `docs/design/reverse-verification/実行記録.md` へ、退避の記録は同ディレクトリの `退避記録.md` へ置くと定めていた。ところがこの2ファイルは実在しなかった。`record-verification-result.sh` と `record-deferral.sh` がどちらも「台帳が見つかりません」で失敗していた。判定の結果を一度も記録できていなかった。
**修正内容**: `docs/design/reverse-verification/実行記録.md` と同ディレクトリの `退避記録.md` を新設した。両スクリプトが要求する `## 記録` 見出しを持つ形にした。実際に `record-verification-result.sh` で今回の検証結果を1件記録し、追記が機能することを確認した。
**検収方法**: 1. 両ファイルが実在すること。2. `実行記録.md` が `## 記録` 見出しを1件持つこと。3. `record-verification-result.sh` の実行が終了コード0で節を追記すること。4. `record-verification-result.sh --self-test` と `record-deferral.sh --self-test` がそれぞれ終了コード0であること。5. `check-instruction-format.sh` の不合格が0件であること。
**完了条件**: 上記5点の検収を満たすこと。
**反映先**: `docs/design/reverse-verification/`
**優先度**: 高
**状態**: 完了。両ファイルを新設し、`grep -c '^## 記録$' 実行記録.md` が1を返すことを確認した。`record-verification-result.sh` を実行し終了コード0で節を追記した。`record-verification-result.sh --self-test`・`record-deferral.sh --self-test` はいずれも5件全PASS・終了コード0。`check-instruction-format.sh` は不合格0件だった。

## 完了の判定

1. この一覧に載る、指示書を持つ 15 件の状態と実際の置き場が一致する: `bash docs/scripts/check-task-ledger-sync.sh`
2. この一覧が指示書の形の検査を通る: `bash .claude/rules/always/tasks/instruction-format/check-instruction-format.sh docs/tasks/作業課題一覧.md`
3. 一覧の整合検査の自己テストが正常・食い違いの両ケースを通る: `bash docs/scripts/check-task-ledger-sync.sh --self-test`

## 触らない範囲

- `docs/tasks/指摘改善一覧.md`。合流させない
- `docs/tasks/` 直下の既存の指示書 15 件の中身
- `docs/tasks/design/`・`docs/tasks/work-records/`・`docs/tasks/done/` の中身
- `docs/scripts/judge-task-done.sh` などの片付き判定器

## 決めていないこと

| 何を決めるか | 既定（迷ったらこれを選ぶ） | 覆すときの条件 |
|---|---|---|
| 状態・優先度の二重管理 | 状態は置き場から決め、検査で食い違いを止める。優先度は指示書の値を正とする | 指示書以外の完了判定の正本が導入されたら、その正本との照合へ置き換える |
| 一覧の更新の担い手 | 進行を管理する側が更新する。担当のセッションは指示書側の対応の記録を更新し、一覧の更新は求めない | 単一セッション運用へ戻り競合の懸念が無くなったら、担当のセッションが直接更新してよい |

## 他の指示書との関係

- 個々の指示書が持つ前提関係（例: 「理由の無い目視を減らす指示書.md」は「高速化のあとに目視へ退いた判定を戻す指示書.md」を前提とする）は各指示書の冒頭4行に残したままとし、この一覧では重複して管理しない
- `docs/tasks/指摘改善一覧.md` とは独立した別の一覧であり、合流させない
- `docs/tasks/design/` に置かれた3件（具体を決める作業から要る指示書）は対象外とする。この一覧は `docs/tasks/` 直下の「着手できる」指示書だけを対象とする

## この指示書の位置づけ

`docs/tasks/` 直下と `docs/tasks/done/` が指示書の状態に応じた既存の置き場であるという規約を保ったまま、対象 15 件の現在地を見渡せるようにする。新しい置き場を作らず、既存の指示書への索引と進捗記録としてのみ機能する 1 ファイルに留める。

### 除外の直書きを防ぐ検査が第1層の集約に拾われない

**現象**: `generation-engine/scripts/tests/check-document-style-register-exclusion-source.sh` は実在し、単体で実行すると終了コード0を返す。ところが第1層の集約（`run-layer-machine-checks.sh --list`）で数えると0件だった。集約の収集条件は「`--self-test` を実引数として処理する `.sh`」または「`test-*.cjs`・`test-*.mjs`」であり、この検査はどちらにも当てはまらない。**検査が存在するのに一度も実行されない。**同種の構造は課題1-62（索引ページの生成器が自己テストは通るのに呼び出し元が0件）と同じである。ほかの検査で同じ状態のものがあるかを走査したところ、この1件だけだった。

**修正内容**: 集約が拾える形へ揃える。`--self-test` を実引数として受け付ける分岐を足すか、名前を `test-` で始める形へ改める。どちらを採るかは、既存の検査群の慣例に合わせて決める。

**検収方法**: 1. `run-layer-machine-checks.sh --list` の出力にこの検査が現れること。2. 集約の実行でこの検査が実行され、成功として数えられること。3. ほかの検査が拾われなくなっていないこと（対象の本数が減らないこと）。

**完了条件**: 上記3点の検収を満たすこと。

**反映先**: 未定

**優先度**: 中

**状態**: 完了（本コミットで対応）。`--self-test` を実引数として受け付ける分岐を足した。改名はしていない。`run-layer-machine-checks.sh --list` にこの検査が現れることを確認した。`--self-test` 実行は終了コード0で、既存の判定内容は変えていない。集約全体を実行し、対象本数が193本から201本へ増えたことを確認した（本項目分は+1）。`--list` の差分（`comm -23`）で既存の対象が消えていないことも確認した。

### 生成スキルの手順が置き場の定義を読まず日本語の階層名を直接指示する

**現象**: `.claude/skills/generating-*-for-reverse-docs/SKILL.md` のうち14件が対象である。当初の記載11件は数え漏れだった。対象は、生成の手順の中にある。手順が `基本設計/`・`詳細設計/` という日本語の階層名を直接書いている。これらは `scaffold-design-unit.sh` を呼ばない。様式を読み書きして複製する、別の経路を使っている。課題1-210で `scaffold-design-unit.sh` の側は直した。直し方は `unitPhaseDirNames`（`basic-design`・`detail-design`）から解決する形である。この14件は追従していなかった。**同じ成果物を作る経路が2つあり、片方だけが定義を読む状態である。**

**修正内容**: 14件の手順が置き場の定義を読む形へ揃える。または`scaffold-design-unit.sh`を呼ぶ経路へ寄せ、手順から直書きを無くす。どちらを採るかは、既存の生成の連鎖との整合で決める。

**検収方法**: 1. 対象14件から日本語の階層名の直書きが0件になること。2. 生成される階層の名前が `basic-design`・`detail-design` になること。3. 既存の自己テストが通ること。

**完了条件**: 上記3点の検収を満たすこと。

**反映先**: 未定

**優先度**: 中

**状態**: 完了（本コミットで対応）。API・テーブル・バッチ・帳票・外部連携・機能の6種別を扱う11件を直した。直し方は`unitPhaseDirNames.basic`・`.detail`を指す表記への置き換えである。値の根拠も注記した。残る2件は画面固有の固定名を参照していた。対象はシーケンス図生成とテスト観点表一覧生成である。画面はunitPhaseDirNamesの対象外である。この2件は画面専用のプレースホルダへ置き換えた。対象外である旨も注記した。`grep -rcE '基本設計/|詳細設計/'`による不合格件数が0であることを確認した。3つの自己テストもすべて合格した。

### 疑似入力を配置するスクリプトの説明が実装と食い違う

**現象**: `generation-engine/scripts/verification/prepare-verification-input.sh` は冒頭で「`scaffold-design-unit.sh` と同じ命名規則」と述べている。ところが実際は日本語の階層名を独自に直書きしており、`scaffold-design-unit.sh` が定義から解決する形へ変わった後もその主張のままである。**説明が実装を表していない。**自己完結しており自己テストは通るため、いまのところ実害は出ていない。

**修正内容**: 説明を実装に合わせる。または実装を説明どおり（定義から解決する形）へ直す。

**検収方法**: 1. 冒頭の説明と実装が一致すること。2. 自己テストが通ること。

**完了条件**: 上記2点の検収を満たすこと。

**反映先**: 未定

**優先度**: 低

**状態**: 完了（実測）。定義値を新たに配置パスへ使う変更は境界検証とカスタムphase名の回帰検査も要するため、挙動を変えず説明を実装へ合わせた。検収1「冒頭の説明と実装が一致すること」は合格（`grep -nE '検証用の固定構造|unitPhaseDirNames\.basic / \.detail とは連動しない|/(基本設計|詳細設計)/' generation-engine/scripts/verification/prepare-verification-input.sh`、終了コード0。固定構造で定義と連動しない説明、および日本語階層名を使う配置実装を出力して照合）。検収2「自己テストが通ること」は合格（`bash generation-engine/scripts/verification/prepare-verification-input.sh --self-test`、終了コード0、11件中11件成功）。機械だけで確かめられない項目は検収1の自然言語とコードの意味一致であり、上記コマンドの出力を人手で照合した。

### 判定表のセルに句点があると判定器がコマンドを切り出せない

**現象**: 片付けの判定器（`docs/scripts/judge-task-done.sh`）の `strip_code_span` は、判定表の「確かめる手段」欄から逆引用符で囲まれたコマンドを取り出す。ところが逆引用符を閉じた直後に句点があると剥がせず、コマンドが逆引用符付きのまま渡る。その結果、**手で実行すると終了コード0なのに、判定器は「実測で満たさない」と記録する。**2人の担当が独立にこの現象へ当たり、いずれも判定表の側（句点の除去）で回避した。判定器の側は直っていない。

**修正内容**: `strip_code_span` が逆引用符の直後にある句点・読点を取り除いてからコマンドを切り出す形へ直す。または、判定表の記入規則として「確かめる手段の欄に句点を書かない」を明記し、形の検査で機械的に止める。どちらを採るかは、既存の記入規則との整合で決める。

**検収方法**: 1. 逆引用符の直後に句点を持つ判定行を1件含む合成の指示書で判定器を実行し、コマンドが正しく切り出されること。2. 既存の指示書の判定が変わらないこと。3. 判定器の自己テストが通ること。

**完了条件**: 上記3点の検収を満たすこと。

**反映先**: 未定

**優先度**: 高

**状態**: 完了（コミット: b81eab4038d618c5226959beda276bbf8d85b9ce）。`strip_code_span`を直し、逆引用符を閉じた直後の句点・読点を取り除いてから閉じ位置を探すよう改めた。自己テストへ3件（句点・読点・`--write`後もセルを保つこと）を足し、38件全て合格した。`docs/tasks/`配下159件のMarkdownを新旧の抽出関数で突き合わせ、差分が0件であることを確認した。既存の判定は変わらない。半角の句点や閉じ括弧など他の記号は対象外のまま残る（コミット欄はcherry-pick取り込みでハッシュが変わったため、main上の実際のコミットへ差し替えた。取り込み前の枝上のハッシュはff02f72ed7ce6b4cbebbd465702b8b4971b506d1）。

### 検査が7件、第1層の集約に載らないまま実行されない

**現象**: 第1層の集約（`run-layer-machine-checks.sh --list`）が拾う条件は2つである。`--self-test` を実引数として処理する `.sh` か、`generation-engine/scripts/tests/` 配下の `test-*.cjs`・`test-*.mjs` である。この条件のどちらにも当てはまらない検査が7件あり、**一度も実行されないまま残っている。**内訳は `check-phase-step-structure.mjs`・`check-improvement-ledger.mjs`・`check-skill-machine-inspection-consistency.mjs`・`check-confirmation-ledger.mjs`・`check-improvement-ledger.test.mjs`・`check-detailed-design-conventions.cjs`・`test-sequence-kind-policy.mjs` である。

**実害の記録**: 課題1-73 でシーケンス図の検査を直した後、課題1-83 の変更で同じ検査がまた落ちた。集約に載っていれば、1-83 を入れた時点で失敗として現れた。**載っていないため、次に誰かが手で実行するまで気づけない。**同種の問題は `check-document-style-register-exclusion-source.sh` でも起きており、別項目として記録済みである。

**修正内容**: 7件を集約が拾える形へ揃える。`--self-test` を実引数として受け付ける分岐を足すか、名前と置き場を条件に合わせる。あるいは集約の収集条件を広げる。どれを採るかは、既存の慣例との整合で決める。

**検収方法**: 1. `run-layer-machine-checks.sh --list` の出力に7件すべてが現れること。2. 集約の実行で7件が実行され、対象の本数が7増えること。3. 既存の検査が拾われなくなっていないこと。

**完了条件**: 上記3点の検収を満たすこと。

**反映先**: 未定

**優先度**: 高

**状態**: 完了（本コミットで対応）。7件を集約へ配線した。

- `check-phase-step-structure.mjs` は既存の回帰テスト本体（`check-phase-step-structure.test.sh`）を使う。ここへ `--self-test` の分岐を足す最小差分で済んだ。改名はしていない。
- 残る6件は新規の薄い `exec` ラッパーを新設した。新設先は次のとおりである。

  - `check-skill-machine-inspection-consistency.test.sh`
  - `check-improvement-ledger-cli.test.sh`
  - `check-improvement-ledger-unit.test.sh`
  - `check-detailed-design-conventions.test.sh`
  - `test-sequence-kind-policy.test.sh`
  - `check-confirmation-ledger.test.sh`

- `--list` に7件すべてが現れることを確認した。
- 集約全体を実行し、対象本数が193本から201本へ増えたことを確認した（本項目分は+7）。
- `--list` の差分（`comm -23`）で既存の対象が消えていないことも確認した。

**配線した結果、集約の対象8件のうち3件が不合格になった。**該当は次の3件である。

- `check-improvement-ledger-cli.test.sh`
- `check-improvement-ledger-unit.test.sh`
- `check-phase-step-structure.test.sh`

3件とも、ラッパーではなく本体を直接実行した。本体とは次の3つである。

- `node check-improvement-ledger.mjs`
- `node check-improvement-ledger.test.mjs`
- `node check-phase-step-structure.mjs --strict-warnings`

いずれも同じ終了コード1になることを確認した。**配線の問題ではなく中身の問題である。**

捕まえた内容は次の2項目として本一覧へ新たに記録した。

- 改善反映台帳の分類できない行が集約の配線で見つかった
- 機械検査Stepの使用ツール警告2件が集約の配線で見つかった

### 改善反映台帳の分類できない行が集約の配線で見つかった

**現象**: `check-improvement-ledger.mjs` は引数なしで既定の台帳を読む本番経路である。これを集約に配線した検査（`check-improvement-ledger-cli.test.sh`）から実行すると、終了コード1で不合格になる。対象の台帳は `docs/tasks/work-records/改善反映台帳.md` である。実測では、台帳の総データ行362件のうち、分類できない行が21件ある。分類の内訳は、実データ相当8・自己テストのみ116・未検証217である。この合計は341件で、総データ行数と一致しない。同じ台帳を読む単体テスト（`check-improvement-ledger.test.mjs`）もある。その「the checked-in improvement ledger satisfies every condition」ケースも、同じ理由で不合格になる。

**修正内容**: 台帳の該当21行を、既存の分類語彙（実データ相当・自己テストのみ・未検証）のいずれかに当てはまる形へ書き直す。または、分類ロジック側に新しい語彙を認める必要があるかを判断する。

**検収方法**: 1. `node generation-engine/scripts/check-improvement-ledger.mjs` が終了コード0を返すこと。2. `node generation-engine/scripts/tests/check-improvement-ledger.test.mjs` が全ケース合格すること。

**完了条件**: 上記2点の検収を満たすこと。

**反映先**: 未定

**優先度**: 中

**状態**: 完了（検収1 PASS: `node generation-engine/scripts/check-improvement-ledger.mjs` を実行し終了コード0、総データ行362件＝実データ相当16件＋自己テストのみ121件＋未検証225件、分類不能0件・多重分類0件を確認。検収2 PASS: `node generation-engine/scripts/tests/check-improvement-ledger.test.mjs` を実行し終了コード0、21/21ケース合格を確認。全行が既存3語彙へ一意に分類できるため、分類ロジックへの新語彙追加は不要。機械で確かめられない項目なし）

### 機械検査Stepの使用ツール警告2件が集約の配線で見つかった

**現象**: 対象は `check-phase-step-structure.mjs --strict-warnings` である。集約に配線した検査（`check-phase-step-structure.test.sh`）から実行される。実行すると、終了コード1で不合格になる。警告は次の2件である。

- `generating-api-detail-design-for-reverse-docs/SKILL.md:277`。Step 4-1 の使用ツール `Skill` が allowed-tools にない
- `generating-feature-design-for-reverse-docs/SKILL.md:126`。Step 3-1 に使用ツールの明示がない

既定実行（`--strict-warnings` なし）は、警告があっても終了コード0のままである。

**修正内容**: 1件目は該当スキルの allowed-tools へ `Skill` を追加するか、Step 4-1 の使用ツール記載を見直す。2件目は Step 3-1 へ使用ツールを明示する。

**検収方法**: 1. `check-phase-step-structure.mjs --strict-warnings` が終了コード0を返すこと。

**完了条件**: 上記1点の検収を満たすこと。

**反映先**: 未定

**優先度**: 中

**状態**: 完了（検収1 PASS: `node generation-engine/scripts/check-phase-step-structure.mjs --strict-warnings` を実行し終了コード0、skills=53・references=83・orchestrator_phases=7・global_steps=41・warnings=0を確認。指摘されたStep 4-1のallowed-tools不整合とStep 3-1の使用ツール未明示を含め、厳格警告は残っていない。機械で確かめられない項目なし）

### 判定表のコミット欄が枝の取り込みで参照先を失う

**現象**: 判定表のコミット欄が指す先を失う事象が5回起きた。

| 回 | 課題 |
|---|---|
| 1 | 1-57 |
| 2 | 1-71・1-79・1-80・1-83（コミット欄が空） |
| 3 | 1-205・1-72・1-210 |
| 4 | 1-73 |
| 5 | 台帳10件 |

原因は取り込みの方式にある。進行を管理する側は `git cherry-pick` で作業枝の変更をmainへ取り込む。cherry-pickはコミットを作り直すため、ハッシュが変わる。担当が判定表へ書いたハッシュは、取り込み後には存在しない。

**修正内容**: 次の方向を挙げる。どれを採るかは、この項目を実装する担当が決める。

- 取り込み後にハッシュを書き換える手順を、取り込む側の手順書へ加える。
- 判定表のコミット欄を、取り込み後に機械が埋める形にする。
- コミットの識別に、ハッシュではなくメッセージを使う。
- 取り込み方をcherry-pick以外へ変える。

**検収方法**: 1. 判定表のコミット欄に書かれたハッシュが、mainの祖先であることを機械で確かめられること。2. 取り込みを行った後、この確認を毎回手動で行わずに済む仕組みがあること。

**完了条件**: 上記2点の検収を満たすこと。

**反映先**: `docs/scripts/check-verdict-hash-ancestry.sh`

**優先度**: 高

**状態**: 完了（新設した検査`docs/scripts/check-verdict-hash-ancestry.sh`が判定表・記録のコミット欄を走査し、mainのコミットとして存在し祖先であることを機械で確かめる。自己テスト5件PASS。第1層の集約からも`generation-engine/scripts/tests/check-verdict-hash-ancestry.sh`経由で拾われる。取り込み手順（`docs/session-prompts/進行担当のプロンプト.md`「取り込みと公開の順序」）を`merge --ff-only`優先・`merge --no-ff`フォールバックへ改め、`cherry-pick`を使わない旨と取り込み直後の本検査実行を明記した。`docs/tasks/`全体へ実際に当てたところ624件中183件がmainのコミットとして存在せず（2026-08-21のコミット履歴再作成が原因と推定）、1件がmainの祖先でないハッシュだった。既存の食い違いの解消自体は本項目の範囲外とし、今後の再発検知に限定する）

### 履歴再作成より前の切れを判定不能として区別する

**現象**: 上記「判定表のコミット欄が枝の取り込みで参照先を失う」が実測した624件中183件の「存在しない・祖先でない」は、確かめ直した結果、内訳が2種類に分かれることが分かった。182件は2026-08-21のコミット`2ee874944bdfc0ac9e6c354f4e9ceac5f5464f27`（履歴を単一の初回コミットへ作り直した）より前に書かれ、いま復元する手段が無い。残る1件（`docs/tasks/作業課題一覧.md:410`、ハッシュ`ff02f72ed7ce6b4cbebbd465702b8b4971b506d1`）は2026-08-26付けの新しい記録で、cherry-pick取り込みでハッシュが変わったために祖先でないと判定されていた。この182件を検査が毎回不合格として報告し続けると、今後生じる本物の新しい切れが埋もれる。

**修正内容**: `check-verdict-hash-ancestry.sh`を、履歴再作成より前で復元不能と実測済みのハッシュを判定不能として除外する形へ改めた。実測した182件（重複除去後83件）を`docs/references/pre-rewrite-hashes.json`へ固定の一覧として記録し、この一覧に載るハッシュだけを判定不能とする。一覧は今回の実測時点で固定し、以後は自動でも手でも追記しない。祖先違反1件は、main上の実際のコミット`b81eab4038d618c5226959beda276bbf8d85b9ce`（`ff02f72`と同内容でcherry-pick後にハッシュが変わったもの）へ判定表の記載を直接差し替えて解消した。

**検収方法**: 1. `bash docs/scripts/check-verdict-hash-ancestry.sh`が終了コード0で終わり、判定不能182件が不合格に数えられないこと。2. `bash docs/scripts/check-verdict-hash-ancestry.sh --self-test`が全ケース合格し、一覧に無い新しい切れは引き続き不合格として検出されるケースを含むこと。3. `run-layer-machine-checks.sh --list`の本数が減っていないこと。

**完了条件**: 上記3点の検収を満たすこと。

**反映先**: `docs/scripts/check-verdict-hash-ancestry.sh`・`docs/references/pre-rewrite-hashes.json`

**優先度**: 高

**状態**: 完了（`check-verdict-hash-ancestry.sh`へpre-rewrite一覧の読み込みと判定不能の除外処理を追加し、自己テストへ2件（一覧に載る切れは合格・一覧に無い新しい切れは不合格のまま）を足して全7ケース合格した。実測で新しく切れたハッシュ0件・判定不能182件・走査したハッシュ624件を確認した。祖先でなかった1件は`b81eab4038d618c5226959beda276bbf8d85b9ce`へ差し替えて解消した。`run-layer-machine-checks.sh --list`は202本のまま変化なし）

### 確認回答の反映処理が削除済み見出しを要求する

**現象**: `apply-confirmation-answers.mjs`を現行の画面詳細設計書テンプレートと回答済み1件の台帳に対して実行した。
終了コード1と`ERROR: 設計書に「要確認事項一覧」節がありません`を返した。
自己テストの合成設計書だけが削除済みの見出しと表を持っていたため、この食い違いを検知できなかった。
また、本体の自己テストを第1層から実行する入口が無かった。

**修正内容**: 設計書の確認事項表を読む処理と表から行を削除する処理を廃止した。
台帳の回答済み項目を対象セルへ反映して状態を反映済みへ変える処理だけにした。
自己テストの合成設計書を現行様式へ揃えた。
`generation-engine/scripts/tests/check-apply-confirmation-answers.sh`を追加した。
本体の自己テストを第1層から呼ぶ形にした。
配布テンプレートとサンプル内の2複製を同じ内容へ揃えた。

**検収方法**:

1. 本体の`--self-test`が合格すること。
2. 現行の画面詳細設計書テンプレートへ回答を反映できること。
3. 本体に削除済みの見出しが0件であること。
4. 第1層の一覧が203本になること。
5. 3箇所の本体が一致すること。

**完了条件**: 上記5点の検収を満たすこと。

**反映先**: `delivery-payload/templates/delivered-skills/resolving-confirmation-items/scripts/apply-confirmation-answers.mjs`・`generation-engine/scripts/tests/check-apply-confirmation-answers.sh`

**優先度**: 高

**状態**: 完了（自己テスト合格、現行様式への回答1件の反映に成功、削除済み見出し0件、第1層203本、複製3箇所一致を確認した）

### 結合テスト仕様書追加時の派生検査同期漏れ

**現象**: 結合テスト仕様書のテンプレート・生成スキル・カタログを追加した後、第1層の個別検査で冒頭案内、スキル構造、ガイド、節整合、相互参照、カタログ、網羅分母、見本台帳の不一致が発生した。

**修正内容**: 新設テンプレートを既存様式へ揃え、案内表を節構成から除外し、コメント内の参照表記を検査対象外にした。新規成果物をカタログと網羅分母へ反映し、見本台帳を更新する。

**検収方法**: 関係する個別検査が終了コード0になり、見本に対象コードの行番号が0件であることを確認する。

**状態**: 完了（対象9本を個別に再実行して全件終了コード0。結合テスト仕様書の冒頭案内・スキル構造・ガイドを補完し、案内表とコメントを既存検査が本文仕様として誤判定する欠陥を修正した。カタログgoldenと網羅分母を新規成果物へ追従させ、見本台帳78件を再記録した。見本の対象コード行番号は0件）

### 行番号除去で用語辞書の正本一致検査が壊れる

**現象**: 見本から保守不能な対象コードの行番号を除去した際、`generation-engine/samples/project-portal/lists/semantic-glossary/用語辞書.html`の埋め込みデータだけが`src/domain/customer.ts`へ変わり、検査側の基準fixtureと再生成元には`src/domain/customer.ts:12`が残った。このため、行番号を除去した見本が正しいにもかかわらず、ケース41の正本一致検査が不合格になった。

**修正内容**: 検査側の基準fixtureを作る`valid-glossary.yaml`とcanonical registryの参照を、行番号を含まない`src/domain/customer.ts`へ統一した。基準fixtureも同じ値へ直し、既に行番号を除去済みの見本と一致させた。見本へ行番号は戻していない。

**検収方法**: 1. `node generation-engine/scripts/tests/test-semantic-glossary-page.cjs`が終了コード0を返すこと。2. `bash generation-engine/scripts/build-portal.sh --self-test`のケース41がPASSになること。3. 行番号禁止検査の自己テストが合格し、`generation-engine/samples/`の対象コード行番号が0件であること。

**完了条件**: 上記3点の検収を満たすこと。

**反映先**: `generation-engine/scripts/glossary/fixtures/valid-glossary.yaml`・`generation-engine/scripts/glossary/fixtures/canonical-registry/customer-update-proposal.yaml`・`generation-engine/scripts/detail-pages/fixtures/semantic-glossary-sample-page-data.json`・`generation-engine/samples/project-portal/lists/semantic-glossary/用語辞書.html`

**優先度**: 高

**状態**: 修正済み（再生成元と基準fixtureから対象コード行番号を除去した。行番号禁止検査と見本全体の0件確認は合格。ブラウザを使うケース41は委任元による実行待ち）

### AI設定資産抽出がホーム配下の除外定義へ依存する

**現象**: 第3層の「自立」検査で、納品対象の`generation-engine/scripts/extract/extract-ai-assets.sh`が既定値としてホーム配下の除外定義ファイルを参照し、配布先に存在しないマシン固有パスへ依存していた。

**修正内容**: 除外定義ファイルの既定値を空にした。環境変数`PAYLOAD_FORBIDDEN_NAMES_FILE`が設定された場合だけ定義ファイルを読み、未設定またはファイル不在なら除外しない従来のfail-open動作を維持した。除外の仕組み自体は残した。

**検収方法**: 1. `bash generation-engine/scripts/verification/check-self-contained.sh`が「違反 0 本」を返すこと。2. `bash generation-engine/scripts/extract/extract-ai-assets.sh --self-test`が終了コード0で終わること。3. `PAYLOAD_FORBIDDEN_NAMES_FILE="$HOME/agent-home/state/payload-forbidden-content.json" bash generation-engine/scripts/extract/extract-ai-assets.sh --self-test`が終了コード0で終わり、環境変数指定時も除外が従来どおり効くこと。4. `bash generation-engine/scripts/build-portal.sh --self-test`が不合格0件で終わること。

**完了条件**: 上記4点の検収を満たすこと。

**反映先**: `generation-engine/scripts/extract/extract-ai-assets.sh`

**優先度**: 高

**状態**: 完了（検収1 PASS: `bash generation-engine/scripts/verification/check-self-contained.sh`を実行し終了コード0、対象687本・適合687本・違反0本。検収2 PASS: `bash generation-engine/scripts/extract/extract-ai-assets.sh --self-test`を実行し終了コード0、全項目PASS。検収3 PASS: `PAYLOAD_FORBIDDEN_NAMES_FILE="$HOME/agent-home/state/payload-forbidden-content.json" bash generation-engine/scripts/extract/extract-ai-assets.sh --self-test`を実行し終了コード0、環境変数指定時も全項目PASS。検収4 PASS: `bash generation-engine/scripts/build-portal.sh --self-test`を実行し62件中不合格0件、変更箇所を通るケース48a・48b・48cもPASS。ただしブラウザを起動できない実行環境のため8件が委任元による実行待ちとなり、コマンドの終了コードは2。機械で確かめられない項目は、この環境でブラウザを起動できない8件のみ）
### 第3層（一気通貫の生成連鎖）が結合テスト仕様書を生成しない

**現象**: 第3層の生成連鎖は10段すべて成功して424ファイルを生成したが、網羅判定では分母83件のうち`docs/test-cases/結合テスト仕様書.md`だけが欠落した。`portal-catalog.json`は`integration-test-spec`を網羅分母として宣言していたが、生成連鎖は依存一覧と実行段から生成器を呼び出していなかった。

**修正内容**: `generate-integration-test-spec.sh`を第3層の依存一覧へ追加し、ポータル生成より前の一覧生成段で実行する。第3層の自己テストでは、生成器の実在と呼び出しの配線を検査する。

**検収方法**: 1. 第3層を版管理外の出力先へ実行し、10段すべてが成功すること。2. 同じ出力先へ網羅判定を実行し、分母83件、存在83件、欠落0件になること。3. 第3層の自己テストが終了コード0になること。

**完了条件**: 第3層の実生成で結合テスト仕様書が生成され、網羅判定の欠落が0件になり、第3層の自己テストが合格すること。

**反映先**: `generation-engine/scripts/verification/run-layer-full-pipeline.sh`および`docs/tasks/作業課題一覧.md`

**優先度**: 高

**状態**: 完了（項目1=満たす: `bash generation-engine/scripts/verification/run-layer-full-pipeline.sh --output /var/folders/qb/_25w1xv13fs8lh1z_6cwmvbc0000gn/T//layer-full-integration-spec-review.Tiy2fw --repo . --keep`を実行し、10段のうち成功10、失敗0、スキップ0、生成ファイル数425を確認した。項目2=満たす: `bash generation-engine/scripts/verification/check-coverage.sh --output /var/folders/qb/_25w1xv13fs8lh1z_6cwmvbc0000gn/T//layer-full-integration-spec-review.Tiy2fw`を実行し、分母83件、存在83件、欠落0件を確認した。項目3=満たす: `bash generation-engine/scripts/verification/run-layer-full-pipeline.sh --self-test`を実行し、24件中24件成功、終了コード0を確認した。全項目を機械検査できるため、機械で確かめられない項目はない）

### 判定不能が欠陥を覆い隠す構造の記録

**現象**: 判定不能・未確認・SKIPとして記録された検査が3件あり、いずれも実行環境の制約を外すまで本物の欠陥を検知できていなかった。用語検査のフィクスチャのハッシュずれはPython 3.13の未導入により隠れていた。用語辞書の見本の戻るリンクの1階層ずれはPyYAMLの未導入によるSKIPで隠れていた。build-portalの自己テストの不合格1件は、判定器の上限120秒に対し実測122秒という2秒差の中に隠れていた。

**修正内容**: `.claude/rules/always/verification/indeterminate-result/rule.md`へ「判定不能を放置しない」の節を新設した。判定不能が「対象に問題がない」ではなく「まだ測れていない」を意味することを記録した。上記3件の実測を表で記録した。制約を外す作業を成果物の修正と同じ優先度で扱う規則と、制約を外す手立ての例を記録した。

**検収方法**:

1. `grep -c '判定不能を放置しない' .claude/rules/always/verification/indeterminate-result/rule.md`が1以上を返すこと。
2. `bash .claude/rules/always/tasks/instruction-format/check-instruction-format.sh`が不合格0件を返すこと。
3. `bash docs/scripts/report-progress.sh --self-test`が失敗0件を返すこと。

**完了条件**: 上記3点の検収を満たすこと。

**反映先**: `.claude/rules/always/verification/indeterminate-result/rule.md`

**優先度**: 中

**状態**: 完了（検収1・2・3を実行し全件満たすことを確認した）

### 設計文書の参照切れが置き場の移動に追従していない

**現象**: `docs/design/AI駆動開発セットアップ構想.md` の2箇所が実在しない場所を指していた。1箇所は `docs/reverse-docs-overview.html`（実体は `docs/guides/reverse-docs-overview.html`）。もう1箇所は `.claude/CLAUDE.md`（実体はリポジトリのルート直下の `CLAUDE.md`）。同種の参照切れを見つける検査が `docs/design/` 配下には1本も無かった。

**修正内容**: 2箇所の参照を実在する場所へ直した（行番号での指定は禁止のため、移動先の見出しを指す形へ改めた）。`docs/design/` 配下の全 `.md` を横断して逆引用符で囲まれたパス付きの参照を抽出し、実在を確かめる検査 `docs/scripts/check-design-doc-references.sh` を新設した。抽出時に大量の誤検出（納品先の出力配置の説明・群同士の省略記法・外部リポジトリのパス等）が生じることが実測で判明したため、絞り込みの規則を設けたうえで、残った72件の候補を分類した。実在するパスへ移設すれば直る参照（8種の文字列）は path のみを移設して直し、行番号付きの壊れた参照を1件追加で見つけて直した。履歴の書き換えで確認手段が失われた参照・改称記録内で意図的に残した旧名・検証時点の一時的な記述の14件は `docs/references/design-doc-reference-exclusions.json` へ理由付きで記録し、判定対象から除外した。

**検収方法**:

1. `bash docs/scripts/check-design-doc-references.sh` が終了コード0を返すこと。
2. `bash docs/scripts/check-design-doc-references.sh --self-test` が終了コード0を返すこと。
3. `bash generation-engine/scripts/tests/check-design-doc-references.sh --self-test` が終了コード0を返すこと。
4. `bash .claude/rules/always/tasks/instruction-format/check-instruction-format.sh` が不合格0件を返すこと。

**完了条件**: 上記4点の検収を満たすこと。

**反映先**: `docs/scripts/check-design-doc-references.sh`・`generation-engine/scripts/tests/check-design-doc-references.sh`・`docs/references/design-doc-reference-exclusions.json`

**優先度**: 中

**状態**: 完了（検収1〜4を実行し全件満たすことを確認した）

### 設計文書がスクリプトの行番号を参照してずれる

**現象**: `docs/design/`の設計文書に、このリポジトリ自身のスクリプトや文書を行番号で指す参照が77件あった。委任元の検出式で72件を確認し、広い検出式で単一行参照をさらに5件確認した。参照先へ行を追加または削除すると位置がずれ、説明が別の実装を指しても検知できなかった。

**修正内容**: 77件を行番号に依存しない「該当箇所」・関数名・節名の参照へ改めた。秒数・件数・版番号は変更していない。`docs/scripts/check-design-doc-line-refs.sh`を新設し、`docs/design/`のMarkdownに行番号参照があれば終了コード1を返す形にした。第1層が自己テストを拾う薄いラッパーも追加した。

**検収方法**: 1. `bash docs/scripts/check-design-doc-line-refs.sh`が終了コード0を返すこと。2. `bash docs/scripts/check-design-doc-line-refs.sh --self-test`が終了コード0を返すこと。3. `bash generation-engine/scripts/tests/check-design-doc-line-refs.sh`が終了コード0を返すこと。4. `bash .claude/rules/always/tasks/instruction-format/check-instruction-format.sh`が不合格0件を返すこと。

**完了条件**: 77件の位置依存参照が0件になり、検出器の通常実行・自己テスト・薄いラッパー・指示書形式検査がすべて合格すること。

**反映先**: `docs/design/`・`docs/scripts/check-design-doc-line-refs.sh`・`generation-engine/scripts/tests/check-design-doc-line-refs.sh`・`.claude/rules/scoped/portal/page-conventions/rule.md`

**優先度**: 高

**状態**: 完了（検収1 PASS: `bash docs/scripts/check-design-doc-line-refs.sh`を実行し終了コード0、行番号参照0件。検収2 PASS: `bash docs/scripts/check-design-doc-line-refs.sh --self-test`を実行し終了コード0、行番号参照を検出して秒数・件数・版番号を除外。検収3 PASS: `bash generation-engine/scripts/tests/check-design-doc-line-refs.sh`を実行し終了コード0、本体自己テストへの委譲を確認。検収4 PASS: `bash .claude/rules/always/tasks/instruction-format/check-instruction-format.sh`を実行し終了コード0、不合格0件。機械で確かめられない項目なし）

### 見本だけに廃止語が残る

**現象**: 改善課題1-246で廃止した語がテンプレートでは0件だが、再生成されなかった見本5件に15箇所残っている。

**修正内容**: 見本を現行テンプレートへ揃える。廃止語一覧を使い、見本と全種別のテンプレートを同時に走査する検査も追加する。

**検収方法**: 1. 通常検査が合格すること。2. 本体の自己テストが合格すること。3. 薄い入口が合格すること。4. 派生台帳検査が合格すること。5. 指示書形式検査の不合格が0件であること。

**完了条件**: 見本とテンプレートに現役の廃止語が無く、新設検査と既存検査がすべて合格すること。

**反映先**: `generation-engine/samples/`、`docs/scripts/`、`generation-engine/scripts/tests/`、`.claude/rules/scoped/portal/page-conventions/rule.md`

**優先度**: 高

**状態**: 完了。検収1 PASS: `bash docs/scripts/check-retired-terms-in-samples.sh`を実行し終了コード0、両ツリーの廃止語0件。検収2 PASS: `bash docs/scripts/check-retired-terms-in-samples.sh --self-test`を実行し終了コード0、6 PASS・0 FAIL。検収3 PASS: `bash generation-engine/scripts/tests/check-retired-terms-in-samples.sh`を実行し終了コード0、薄い入口から同じ6件を確認。検収4 PASS: `bash generation-engine/scripts/tests/check-derived-drift-ledger.sh`を実行し終了コード0、台帳一致。検収5 PASS: `bash .claude/rules/always/tasks/instruction-format/check-instruction-format.sh`を実行し終了コード0、不合格0件。機械で確かめられない項目なし。

### スキル間の相互参照が実在確認されていない

**現象**: `.claude/skills` のMarkdownに現れるスキル名候補と、対応する `SKILL.md` の実在を横断確認する検査が無かった。指定の抽出式では54件を検出し、実在50件、非実在4件だった。非実在4件は、旧名の記録2件、テストスクリプト名1件、別リポジトリへ配布するスキル名1件であり、切れた呼び出しではなかった。また、対象の `SKILL.md` は指定の52本ではなく実測53本だった。

**修正内容**: `docs/scripts/check-skill-cross-references.sh`を新設した。指定の抽出式に、実在する起動記述から確認した3接頭辞を補った。出現元と理由を持つ除外一覧を読み、除外に無い非実在参照を終了コード1にする。除外名が別の文書に現れた場合や、同じ文書へ実呼び出しが加わった場合も終了コード1にする。`--self-test`と、第1層向けの薄いラッパーも追加した。改名・統合・削除による切れた参照は0件だったため、既存スキル本文の参照変更は無い。

**検収方法**: 1. `bash docs/scripts/check-skill-cross-references.sh`が終了コード0を返すこと。2. `bash docs/scripts/check-skill-cross-references.sh --self-test`が終了コード0を返すこと。3. `bash generation-engine/scripts/tests/check-skill-cross-references.sh`が終了コード0を返すこと。4. `bash .claude/rules/always/tasks/instruction-format/check-instruction-format.sh`が不合格0件を返すこと。

**完了条件**: 理由付き除外に無い非実在参照が0件であり、上記4検査がすべて合格すること。

**反映先**: `docs/scripts/check-skill-cross-references.sh`・`generation-engine/scripts/tests/check-skill-cross-references.sh`・`docs/references/skill-cross-reference-exclusions.tsv`・`.claude/rules/scoped/portal/page-conventions/rule.md`・`docs/tasks/作業課題一覧.md`

**優先度**: 高

**状態**: 完了。指定式の実測は候補54件、実在50件、非実在4件だった。補完走査を含む通常検査は候補57件、実在53件、除外4件、不在0件で終了コード0。自己テストは10件すべて合格し、薄いラッパーも同じ10件で終了コード0。指示書形式検査は128件中不合格0件。
### 参照JSON同士のキー切れを検出する

**現象**: `delivery-payload/references/` の7定義は相互参照を持つが、横断検査が無かった。
2026-08-26に生成スクリプトの読取箇所を照合し、23対応を実測した。
対応は `reference-json-integrity.json` へ記録した。参照先不在は0件だった。

**対応一覧**:

| 参照元 | 参照先 | 対応 |
|---|---|---|
| portal-catalog | deliverable-inventory | blueprint kind ↔ item kind |
| portal-catalog | output-layout | defaultRoots のキー・同じキーと値 |
| portal-catalog | portal-catalog | discovery root・roots → discoveryRoots |
| deliverable-inventory | portal-catalog | dependsOnKind → blueprint kind |
| output-layout | output-layout | kindLabels のキー ↔ kindDirNames のキー |
| design-unit-layout | output-layout | kinds のキー・表示名 → kindLabels のキー・値 |
| doc-extraction | design-unit-layout | kinds・除外種別・別契約種別 → kinds |
| doc-extraction | output-layout | unitRootKey・directoryKey → layout のキー |
| doc-extraction | doc-extraction | testDocuments の文書名 ↔ documents の同じ役割と文書名 |
| doc-extraction | design-unit-layout | 画面専用テスト文書名 ↔ 画面専用テスト文書名 |
| design-unit-layout | design-unit-layout | sourcePhase・targetPhase → phases、excludedKinds → kinds |
| unit-axes | design-unit-layout | sourceKind の値域 ↔ kinds |
| deliverable-inventory | output-layout | manifestKey → layout のキー |
| deliverable-inventory | design-unit-layout | unitTestDocuments → test phase の文書名 |
| deliverable-inventory | design-unit-layout | screenOnlyTestDocuments ↔ 画面専用テスト文書名 |
| doc-extraction | design-unit-layout | 種別・phase・文書名 → 同じ種別・phase・文書名 |
| rule-taxonomy | portal-catalog | 子キー＋standard ↔ 規約kind、親子パス ↔ 規約dir |
| rule-taxonomy | portal-catalog | 親子パス＋rule.html ↔ 規約discovery glob |

複合キーと値の比較を分けて数えるため、表の18行が検査の23対応になる。
`blueprints[].dir` は配置キー名ではなく配置値を展開したパスである。
生成器は `defaultRoots` の同名キーを介して `layout` を読む。
このため、直接のキー参照としては数えない。

**修正内容**: 対応定義JSONを読み、参照先不在を数える検査と自己テストの薄い入口を新設する。定義を削除して通す修正は行わない。

**検収方法**:
1. `bash docs/scripts/check-reference-json-integrity.sh`が終了コード0を返すこと。
2. 同じ本体へ`--self-test`を渡すと終了コード0を返すこと。
3. `bash generation-engine/scripts/tests/check-reference-json-integrity.sh`が終了コード0を返すこと。
4. 指示書形式検査が不合格0件を返すこと。

**完了条件**: 23対応の不在が0件で、上記4点が合格すること。

**反映先**:
- `delivery-payload/references/reference-json-integrity.json`
- `docs/scripts/check-reference-json-integrity.sh`
- `generation-engine/scripts/tests/check-reference-json-integrity.sh`

### テンプレートの記入規則が生成手順へ届く保証がない

**現象**: `delivery-payload/templates/リバース検証/`のMarkdownを調べた。`INTRODUCTION_GUIDANCE`形式の記入規則は43件だった。`記入規則:`形式は0件だった。生成Skillには、冒頭案内の内容を実行する共通手順が0件だった。

**修正内容**: 対応を確定できた42件について、担当する17個のSkillへ記入規則の実行手順を追加した。テンプレートと担当Skillの対応を外部TSVへ記録した。対応の網羅とSkill内の手順を照合する検査、その自己テスト、薄い第1層入口を追加した。`詳細設計記述規約.md`は生成物ではなく検査入力としてのみ参照される。生成担当を確定できないため、理由付きの判定不能としてTSVへ記録した。

**検収方法**: 1. `bash docs/scripts/check-template-guidance-skill-steps.sh`が終了コード0を返すこと。2. 同じ本体の`--self-test`が終了コード0を返すこと。3. `bash generation-engine/scripts/tests/check-template-guidance-skill-steps.sh`が終了コード0を返すこと。4. `bash .claude/rules/always/tasks/instruction-format/check-instruction-format.sh`が不合格0件を返すこと。

**完了条件**: 対応を確定できた全テンプレートに手順があり、新設した3つの検査入口と指示書形式検査が合格すること。判定不能のテンプレートには調べた内容と理由が記録されていること。

**反映先**:

- `.claude/skills/`の担当17Skill
- `docs/scripts/check-template-guidance-skill-steps.sh`
- `generation-engine/scripts/tests/check-template-guidance-skill-steps.sh`
- `docs/references/template-guidance-skill-mapping.tsv`
- `.claude/rules/scoped/portal/page-conventions/rule.md`

**優先度**: 高

**状態**: 完了。23対応の参照先不在は0件だった。
`targetPhase: test`は、生成器が`kinds.*.phases.test`を読むため配置名の不在ではない。
screenも、`excluded`と`separateKindContracts`を生成器が別用途で読むため欠落ではない。
推測修正が必要な対応は0件だった。検収1〜4はすべて終了コード0だった。
**状態**: 完了。初回計測は記入規則43件、手順あり0件、手順なし42件、判定不能1件だった。修正後は手順あり42件、手順なし0件、判定不能1件である。通常検査は終了コード0、自己テストは6件中6件合格、薄い入口も6件中6件合格した。

### 結合テスト仕様書生成スキルの説明が形式に違反する

**現象**: `generating-integration-test-spec-for-reverse-docs`の説明には句点が3個と英字が含まれ、動詞で終わっていなかった。2026年8月26日10時55分38秒のコミット`6db15cd`で説明へ起動条件と対象外条件を加えた時から不合格だった。検査は自己テストを持ち、第1層の集約一覧へ既に載っていた。第1層は自己テストだけを実行するため、リポジトリ実体への通常検査を代替せず、不合格が残っていた。

**修正内容**: 説明を複数の設計単位をまたぐ結合テスト仕様書の生成という1文へ戻す。起動条件と単一設計単位を対象外とする条件は本文の使用タイミング節に保持する。

**検収方法**: `bash generation-engine/scripts/check-skill-frontmatter.sh`で合格53件かつ不合格0件を確認する。`bash generation-engine/scripts/check-skill-frontmatter.sh --self-test`で自己テストを確認する。`bash generation-engine/scripts/verification/run-layer-machine-checks.sh --list`で対象検査が一覧にあることを確認する。`bash .claude/rules/always/tasks/instruction-format/check-instruction-format.sh`で不合格0件を確認する。

**完了条件**: 説明が1文、動詞終わり、英字なしとなる。起動条件と対象外条件が本文に残る。通常検査と自己テストと指示書形式検査が合格する。第1層の集約一覧から対象検査を確認できる。

**反映先**: `.claude/skills/generating-integration-test-spec-for-reverse-docs/SKILL.md`へ反映する。
`docs/tasks/作業課題一覧.md`へ記録する。

**優先度**: 高

**状態**: 完了。通常検査は合格53件、不合格0件だった。自己テストは8件すべて合格した。第1層の集約一覧に対象検査があることを確認した。指示書形式検査は合格128件、不合格0件だった。

### `docs/scripts` の自己テストが第1層の集約から実行されていなかった

**現象**: `docs/scripts` のシェルスクリプト48本は第1層の走査対象外だった。対応ラッパーがあるものは15本だった。ラッパーが無いものは33本だった。そのうち24本は自己テストを持つが、第1層から実行されていなかった。実測すると`check-manifest-count-mismatch.sh`の自己テスト1件も不合格だった。

**修正内容**: 自己テストを持つ検査23本について、同名の薄いラッパーを`generation-engine/scripts/tests`へ追加する。本体の自己テストを呼び、終了コードをそのまま返す。`check-manifest-count-mismatch.sh`の合成入力には、前付けが指す実装ファイルを追加する。

**検収方法**: 新設ラッパー23本を`--self-test`付きで個別実行し、すべて終了コード0になることを確認する。指示書形式検査で不合格0件を確認する。`run-layer-machine-checks.sh`は実行しない。

**完了条件**: 新設ラッパー23本が第1層の走査条件を満たす。各ラッパーが本体の自己テストを実行する。各ラッパーが終了コード0で終わる。指示書形式検査が不合格0件で終わる。

**反映先**: `generation-engine/scripts/tests`へラッパー23本を追加する。`docs/scripts/check-manifest-count-mismatch.sh`へ自己テスト用の実装ファイルを追加する。`docs/tasks/作業課題一覧.md`へ調査結果と判断を記録する。

**優先度**: 高

**状態**: 完了。全48本のうち既存ラッパーあり15本、なし33本だった。ラッパーなしのうち自己テストあり24本、なし9本だった。自己テストあり24本から報告用1本を除き、23本を追加した。

自己テストを持たないため対象外としたものは9本である。`check-case49-orphan-html.sh`、`check-confirmation-ledger-current-template.sh`、`check-coverage-zero.sh`を対象外とした。`check-e2e-zero.sh`、`check-sample-placeholders.sh`、`check-sample-sections.sh`も対象外とした。`check-scaffold-phase-dirs.sh`、`check-screen-doc-template-guard.sh`、`check-sequence-kind-policy-test.sh`も対象外とした。いずれも実行時に固有の引数や入力を必要とし、`--self-test`を処理しない。

`report-progress.sh`も対象外とした。自己テストは持つが、進行状況の報告と状態集計が主目的である。合否を返す検査として第1層へ載せる対象ではない。

`docs/scripts`を第1層の走査対象へ直接追加しない。直接追加すると、自己テストの有無だけでは検査と報告用スクリプトを区別できない。既存のラッパー方式なら、載せる検査を明示できる。今回の23本を足しても、集約の探索責務を広げる必要はない。直接走査は対象選別の契約が別途定まった場合に再検討する。
