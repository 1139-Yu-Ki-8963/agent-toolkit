---
# 機能設計書テンプレート（unit_kind=feature）
# 構成要素の単位に依存しない。画面・API・実行ファイルのいずれで構成されていてもよい
feature_key: generating-external-list-for-reverse-docs
feature_id: feat-generating-external-list-for-reverse-docs
category: 生成
source_ref: generating-external-list-for-reverse-docs/SKILL.md
unit_kind: feature
chapter_map:
  - role: 機能概要
    section: 1
  - role: 機能範囲
    section: 2
  - role: 業務フロー
    section: 3
  - role: 業務ルール
    section: 4
  - role: 機能データ
    section: 5
  - role: 状態受け渡し
    section: 6
  - role: 呼び出し仕様
    section: 7
  - role: 機能エラー
    section: 8
  - role: 機能非機能要件
    section: 9
  - role: 機能共通準拠
    section: 10
  - role: 機能関連資料
    section: 11
---

# 外部連携の一覧を作る 機能設計書

**この文書の位置づけ**（別の言語で作り直す場合は、まず「仕様」から読む）

- 仕様: §1 機能概要、§2 機能の範囲、§3 業務フロー、§4 業務ルール、§5 データ、§6 構成要素間の状態受け渡し。§7 呼び出し仕様、§8 エラーと業務メッセージ、§9 非機能、§10 共通仕様への準拠、§11 関連資料
- 現行実装: 該当なし
- 仕様／現行実装（詳細は各節の太字1行を見る）: 該当なし

本書は集約設計書です。構成要素の個別仕様は各構成要素の設計書を正とし、本書は値を再掲せずパスで参照します。

## §1 機能概要

**この節の位置づけ: 仕様**

### 1.1 基本情報

| 項目 | 値 |
|---|---|
| 機能キー | generating-external-list-for-reverse-docs |
| 機能ID | feat-generating-external-list-for-reverse-docs |
| 大分類 | 生成 |
| 業務対象 | 既存コードを調べ、外部連携ごとの一覧ページ（外部連携一覧.html）を作ること |
| 確からしさ | high |
| 判定根拠 | 機能一覧マニフェストの sourceFile と `.claude/skills/generating-external-list-for-reverse-docs/SKILL.md` の記述が一致する |
| 境界の決定元 | build-target（Claude Code スキルのディレクトリ単位） |

### 1.2 業務上の位置づけ

対象コードベースをスタック調査→検出戦略の宣言→戦略に基づく抽出→整合検証の順で調べ、外部連携一覧.htmlを作ります。マニフェストスキーマ・整合検証・HTML生成は決定的スクリプトに固定し、抽出（外部連携境界の検出）だけをプロジェクトごとに可変な手作業とします。

### 1.3 利用者と権限

| 利用者区分 | この機能でできること | 参照先 |
|---|---|---|
| リバース設計フローの進行担当（AI エージェント） | 既存コードベースから外部連携一覧.htmlと永続マニフェストを作る | .claude/skills/generating-external-list-for-reverse-docs/SKILL.md |

## §2 機能の範囲

**この節の位置づけ: 仕様**

### 2.1 構成要素一覧

| 構成要素キー | 種別 | 識別子 | 個別設計書 |
|---|---|---|---|
| validate-manifest | バッチ（整合検証） | generation-engine/scripts/unit-list/validate-manifest.sh | docs/design/batches/batch-unit-list/detail-design/バッチ詳細設計書.md |
| build-unit-list | バッチ（HTML生成） | generation-engine/scripts/unit-list/build-unit-list.sh | docs/design/batches/batch-unit-list/detail-design/バッチ詳細設計書.md |
| extract-external-metadata | バッチ（メタデータ付与） | generation-engine/scripts/extract/extract-external-metadata.sh | docs/design/batches/batch-extract/detail-design/バッチ詳細設計書.md |
| detect-encoding | バッチ（エンコーディング判定・変換） | generation-engine/scripts/detect-encoding.sh | docs/design/batches/batch-root/detail-design/バッチ詳細設計書.md |

### 2.2 参照テーブル一覧

| テーブル | 作成 | 参照 | 更新 | 削除 | 参照先 |
|---|---|---|---|---|---|
| 該当なし | — | — | — | — | 本機能はデータベーステーブルを持ちません |

### 2.3 範囲外

外部連携以外の種類の一覧作成、設計書の雛形展開・生成・記入、コードと設計書を比べる検証は本機能の範囲外です。

## §3 業務フロー

**この節の位置づけ: 仕様**

### 3.1 正常系フロー

| 順 | 実行主体 | 構成要素 | 処理 | 次の分岐条件 |
|---|---|---|---|---|
| 1 | 進行担当 | 本機能（Phase 1） | スタック・外部連携規約を調査し、検出戦略宣言を作る | Phase 2 へ |
| 2 | 進行担当 | 本機能（Phase 2）／extract-external-metadata.sh | 戦略に基づきカスタム抽出を実行し、マニフェストJSONを作成後、メタデータを付与する | Phase 3 へ |
| 3 | 進行担当 | validate-manifest.sh | 拡張マニフェストの整合を検証する | 全項目PASSでPhase 4へ |
| 4 | 進行担当 | build-unit-list.sh | 外部連携一覧.htmlを生成する | 完了 |

### 3.2 分岐と例外フロー

| キー | 分岐条件 | 到達先 | 参照先 |
|---|---|---|---|
| 外部連携実在しない | 検出0件、かつ調査書が外部連携不在と判定 | status=NONE。外部連携一覧（該当なし）.mdを生成し正常終了する | .claude/skills/generating-external-list-for-reverse-docs/SKILL.md 出力先 |
| 検出失敗 | 検出0件、かつ調査書が外部連携実在と判定 | 停止する。status=ERROR | .claude/skills/generating-external-list-for-reverse-docs/SKILL.md 出力先 |
| 整合検証不合格 | validate-manifest.shがFAILを返す | 修正して再実行。3回失敗で抽出手順へ差し戻し | .claude/skills/generating-external-list-for-reverse-docs/SKILL.md Phase 3 |

### 3.3 前後の機能との関係

| 関係 | 相手の機能キー | 受け渡す情報 | 参照先 |
|---|---|---|---|
| 後続 | generating-external-basic-design-for-reverse-docs／generating-external-detail-design-for-reverse-docs | external-manifest.json・external-manifest.ext.json | .claude/skills/generating-external-basic-design-for-reverse-docs/SKILL.md |

## §4 業務ルール

**この節の位置づけ: 仕様**

### 4.1 業務判断ポイント

| キー | 判断内容 | 判断材料 | 参照先 |
|---|---|---|---|
| 0件時の分岐判定 | 該当なし（正常終了）か検出失敗（停止）か | survey_doc_pathの外部連携実在判定 | .claude/skills/generating-external-list-for-reverse-docs/SKILL.md 出力先 |

### 4.2 状態と遷移の制約

| 状態 | 遷移先 | 遷移条件 | 参照先 |
|---|---|---|---|
| 一時保存 | 永続化（output_dir/manifestsRoot/external-manifest.json） | 一時ファイル+renameによる原子的永続化の完了 | .claude/skills/generating-external-list-for-reverse-docs/SKILL.md Phase 2 |

### 4.3 計算規則

| キー | 算出対象 | 算式 | 参照先 |
|---|---|---|---|
| 該当なし | — | — | 本機能は数値の算出規則をSKILL.md本文に明記していません |

### 4.4 権限別の可否

| 利用者区分 | 可能な操作 | 不可の操作 | 参照先 |
|---|---|---|---|
| リバース設計フローの進行担当 | スクリプト経由でHTMLを決定的に生成する | 0件検出時の手動リストの聞き出し | .claude/skills/generating-external-list-for-reverse-docs/SKILL.md 重要な注意事項 |

## §5 データ

**この節の位置づけ: 仕様**

### 5.1 業務対象の定義

| 項目 | 意味 | 型 | 必須 | 参照先 |
|---|---|---|---|---|
| 外部連携一覧.html | 検出した外部連携の一覧ページ | HTML | 必須 | .claude/skills/generating-external-list-for-reverse-docs/SKILL.md 出力先 |
| external-manifest.json | 検出したユニットの生マニフェスト | JSON | 必須 | .claude/skills/generating-external-list-for-reverse-docs/SKILL.md 出力先 |
| external-manifest.ext.json | メタデータを付与した拡張マニフェスト | JSON | 必須 | .claude/skills/generating-external-list-for-reverse-docs/SKILL.md Phase 2 |

### 5.2 構成要素とテーブルの対応

| 構成要素キー | テーブル | 操作 | 参照先 |
|---|---|---|---|
| 該当なし | — | — | 本機能はテーブルを持ちません |

### 5.3 ライフサイクル

| 段階 | 生成・変更する構成要素 | 残る状態 | 参照先 |
|---|---|---|---|
| 検出直後 | 一時ディレクトリへマニフェストを保存する | 未永続化状態 | .claude/skills/generating-external-list-for-reverse-docs/SKILL.md Phase 2 |
| 検証合格後 | output_dir/manifestsRoot へ原子的に永続化する | 永続化済み状態 | .claude/skills/generating-external-list-for-reverse-docs/SKILL.md Phase 2 |

## §6 構成要素間の状態受け渡し

**この節の位置づけ: 仕様**

### 6.1 受け渡しパラメータ

| 送り元 | 受け先 | 項目 | 型 | 参照先 |
|---|---|---|---|---|
| 呼び出し元 | 本機能 | source_dir・output_dir | 文字列（パス） | .claude/skills/generating-external-list-for-reverse-docs/SKILL.md 使用タイミング |

### 6.2 共有ストア・共有領域

| 領域 | 書き込む構成要素 | 読む構成要素 | 参照先 |
|---|---|---|---|
| output_dir/manifestsRoot/external-manifest.ext.json | 本機能（Phase 2） | generating-external-basic-design-for-reverse-docs／generating-external-detail-design-for-reverse-docs（想定） | .claude/skills/generating-external-list-for-reverse-docs/SKILL.md Phase 2 |

### 6.3 セッション・実行コンテキスト

| 項目 | 保持期間 | 参照する構成要素 | 参照先 |
|---|---|---|---|
| 一時ディレクトリ | 検出結果の永続化前まで | 本機能（Phase 2） | .claude/skills/generating-external-list-for-reverse-docs/SKILL.md Phase 2 |

## §7 呼び出し仕様

**この節の位置づけ: 仕様**

### 7.1 機能内の呼び出し一覧

| 識別子 | 契機 | 呼び出す構成要素 | 個別設計書 |
|---|---|---|---|
| extract-external-metadata | Phase 2 メタデータ付与 | generation-engine/scripts/extract/extract-external-metadata.sh | docs/design/batches/batch-extract/detail-design/バッチ詳細設計書.md |
| validate-manifest | Phase 3 整合検証 | generation-engine/scripts/unit-list/validate-manifest.sh | docs/design/batches/batch-unit-list/detail-design/バッチ詳細設計書.md |
| build-unit-list | Phase 4 HTML生成 | generation-engine/scripts/unit-list/build-unit-list.sh | docs/design/batches/batch-unit-list/detail-design/バッチ詳細設計書.md |

### 7.2 呼び出し元との対応

| 呼び出し元 | 識別子 | 用途 | 参照先 |
|---|---|---|---|
| orchestrating-ai-development-setup（本リポジトリの18本には含まれない） | generating-external-list-for-reverse-docs | 外部連携一覧の生成 | .claude/skills/generating-external-list-for-reverse-docs/SKILL.md |

## §8 エラーと業務メッセージ

**この節の位置づけ: 仕様**

### 8.1 業務エラー一覧

| キー | 発生条件 | 利用者に見える結果 | 参照先 |
|---|---|---|---|
| ERROR | 外部連携が実在するはずなのに検出0件 | status=ERRORで停止し、手動リストは聞き出さない | .claude/skills/generating-external-list-for-reverse-docs/SKILL.md 出力先 |
| NONE | 外部連携を持たないプロジェクト | status=NONEで正常終了し、外部連携一覧（該当なし）.mdを生成する | .claude/skills/generating-external-list-for-reverse-docs/SKILL.md 出力先 |

### 8.2 メッセージ定義との対応

| メッセージID | 本文 | 使う構成要素 | 参照先 |
|---|---|---|---|
| 該当なし | — | — | 本機能はコマンドライン実行のスキルであり、画面へ表示する定義済みメッセージIDを持ちません |

## §9 非機能

**この節の位置づけ: 仕様**

### 9.1 件数と量の制限

| 対象 | 上限 | 参照先 |
|---|---|---|
| 整合検証の修正ループ | 3回 | .claude/skills/generating-external-list-for-reverse-docs/SKILL.md Phase 3 |

### 9.2 性能目安

| 対象 | 目安 | 参照先 |
|---|---|---|
| 該当なし | 実測値の記載はSKILL.md本文にありません | — |

### 9.3 同時実行の前提

| 対象 | 前提 | 参照先 |
|---|---|---|
| 該当なし | validate-manifest.sh・build-unit-list.sh は jq に依存する | .claude/skills/generating-external-list-for-reverse-docs/SKILL.md 予想を裏切る挙動 |

## §10 共通仕様への準拠

**この節の位置づけ: 仕様**

### 10.1 共通部品の利用

| 部品 | 使う構成要素 | 参照先 |
|---|---|---|
| generation-engine/scripts/unit-list/validate-manifest.sh | 本機能（Phase 3） | docs/design/batches/batch-unit-list/detail-design/バッチ詳細設計書.md |
| generation-engine/scripts/unit-list/build-unit-list.sh | 本機能（Phase 4） | docs/design/batches/batch-unit-list/detail-design/バッチ詳細設計書.md |

### 10.2 用語とラベル

| 用語 | 表示文言 | 定義元 |
|---|---|---|
| unit_kind: external | マニフェストの固定種別 | .claude/skills/generating-external-list-for-reverse-docs/SKILL.md |

### 10.3 表示・出力フォーマット

| 対象 | 書式 | 定義元 |
|---|---|---|
| マニフェストの配列キー | units | .claude/skills/generating-external-list-for-reverse-docs/SKILL.md |

## §11 関連資料

**この節の位置づけ: 仕様**

| 資料 | パス | 本書との関係 |
|---|---|---|
| スキル定義 | .claude/skills/generating-external-list-for-reverse-docs/SKILL.md | 本機能の実行手順そのもの |
| バッチ設計単位（unit-list群） | docs/design/batches/batch-unit-list/ | validate-manifest.sh・build-unit-list.sh の個別設計書 |
| バッチ設計単位（extract群） | docs/design/batches/batch-extract/ | extract-external-metadata.sh の個別設計書 |
| バッチ設計単位（root群） | docs/design/batches/batch-root/ | detect-encoding.sh の個別設計書 |
