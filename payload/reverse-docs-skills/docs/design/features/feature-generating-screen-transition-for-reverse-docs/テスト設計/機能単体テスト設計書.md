---
# 機能単体テスト設計書テンプレート（unit_kind=feature）
feature_key: generating-screen-transition-for-reverse-docs
feature_id: feat-generating-screen-transition-for-reverse-docs
category: 生成
source_ref: .claude/skills/generating-screen-transition-for-reverse-docs/SKILL.md
unit_kind: feature
status: draft
---

# 画面遷移図の書き出し 機能単体テスト設計書

**この文書の位置づけ**（別の言語で作り直す場合は、まず「仕様」から読む）

- 仕様: §1 テスト観点、§2 テストケース一覧、§3 入力条件、§4 期待結果、§5 異常系、§6 境界値、§7 網羅基準、§8 前提条件と終了条件
- 現行実装: 該当なし
- 仕様／現行実装（詳細は各節の太字1行を見る）: 該当なし

本書は関数・メソッド単位の内部処理を検証します。観点と具体的なケースを分けず、1文書で管理します。対応する設計書は `機能設計書.md` です。

## 本書が検証するもの

結合は2段に分けて定義します。1つの設計単位の中で構成要素が結合した状態を内部結合、複数の設計単位をまたぐ連携を外部結合と呼びます。単位ごとの文書は内部結合までを担い、プロジェクト全体の文書が外部結合を担います。

| 段 | 検証する状態 | 対応する設計書 | 文書 |
|---|---|---|---|
| 単体 | 1つの関数・メソッドの入出力 | 機能設計書 | 機能単体テスト設計書（本書） |
| 内部結合 | 1つの機能の中で関数・メソッドが結合した状態（外部の振る舞い） | 機能設計書 | 機能結合テスト設計書 |
| 外部結合 | 複数の画面・機能・APIをまたぐ連携 | 複数の設計書 | docs/test-cases/結合テスト仕様書.md |

## テスト対象

対象は関数・メソッド単位の内部処理とする。

## テストの粒度と自動化の方針

反復可能なケースは自動化し、実装後のテスト参照をケースに記録する。単位配下へ結合テスト文書を生成しない。

## 本書が扱わない範囲

複数の画面・機能・APIにまたがる結合テストはプロジェクト全体の `docs/test-cases/結合テスト仕様書.md` が担う。

## §1 テスト観点

**この節の位置づけ: 仕様**

| キー | 観点 | 由来する基本設計書の章 | 由来する詳細設計書の章 |
|---|---|---|---|
| 対象関数該当なし | 本機能は Claude Code スキルの Phase/Step 手順として実行される bash/Read/Write 手順であり、関数・メソッド単位に分解した詳細設計を持たない（design-unit-layout.json の feature.phases.detail は空） | §3 業務フロー | 該当なし |

### 根拠

.claude/skills/generating-screen-transition-for-reverse-docs/SKILL.md に定義されている。generation-engine/scripts/detail-pages/等の生成エンジンスクリプトを呼ぶ手順として記述されている。独自定義の関数・メソッドを持たない。スクリプトの関数単位の検証は詳細設計書（docs/design/batches/batch-detail-pages/detail-design/バッチ詳細設計書.md）の単体テスト設計書が担う。

## §2 テストケース一覧

**この節の位置づけ: 仕様**

| キー | 番号 | 対応観点キー | 入力・操作 | 期待結果 |
|---|---|---|---|---|
| 対象関数該当なし | 1 | 対象関数該当なし | — | 該当なし（§1参照）。実行可能な検証は生成エンジンスクリプト側の単体テスト設計書に記載する |

## §3 入力条件

**この節の位置づけ: 仕様**

| キー | データ | 用途 |
|---|---|---|
| 対象関数該当なし | — | 該当なし（§1参照） |

## §4 期待結果

**この節の位置づけ: 仕様**

期待結果は観測可能な値・状態・表示で記載する。本機能は対象関数を持たないため、期待結果の記載対象は無い（§1参照）。

## §5 異常系

**この節の位置づけ: 仕様**

| 観点のキー | 条件 | 期待結果 |
|---|---|---|
| 対象関数該当なし | — | 該当なし（§1参照） |

## §6 境界値

**この節の位置づけ: 仕様**

| 観点のキー | 境界 | 期待結果 |
|---|---|---|
| 対象関数該当なし | — | 該当なし（§1参照） |

## §7 網羅基準

**この節の位置づけ: 仕様**

設計書に定義された分岐・状態・制約をすべて観点へ対応付ける。本機能は対象関数を持たないため、網羅基準の適用対象は無い（§1参照）。

## §8 前提条件と終了条件

**この節の位置づけ: 仕様**

| 区分 | 条件 |
|---|---|
| 前提条件 | 本書は対象関数を持たないため、前提条件の記載対象は無い |
| 終了条件 | 該当なし |
