---
# バッチ詳細設計書テンプレート（unit_kind=batch・1バッチ1枚）
batch_key: detail-pages
batch_id: batch-detail-pages
batch_name: 詳細ページ
# batch_trigger_type の値域: scheduled（時刻起動）/ triggered（外部トリガー起動）
batch_trigger_type: triggered
source_ref: generation-engine/scripts/detail-pages/build-detail-page.sh
unit_kind: batch
status: authored
---
# 詳細ページ バッチ実装記録

| 節 | 内容 | 読み手へのお願い |
|---|---|---|
| §1 疑似コード | 現行実装の分岐と繰り返しを日本語で示します。 | 作り直す際に引き継ぐかどうかを判断してください。 |
| §2 データ定義 | 現行実装が扱うデータ構造を示します。 | データの持ち方が今後も妥当かを判断してください。 |
| §3 実装契約 | ファイル・関数・依存関係を示します。 | 依存の妥当性を判断してください。 |

**この文書の位置づけ**（別の言語で作り直す場合は、まず「仕様」から読む）

- 仕様: 該当なし
- 現行実装: §1 疑似コード、§2 データ定義、§3 実装契約
- 仕様／現行実装（詳細は各節の太字1行を見る）: 該当なし

本書は、このバッチの今の作りをそのまま記録した文書です。作り直す際に引き継がない情報だけを置きます。業務の決まり・外部との契約・満たすべき性質はバッチ詳細設計書を参照してください。

現行実装には、次のような素直でない作りが含まれます。もとになるデータの検証処理は、1つでも不合格が見つかった時点で止まらず、全観点を最後まで確認してから合否をまとめて返します。埋め込むデータの組み立ては、表示の仕組みを乗っ取る恐れのある文字を、単純な変換ではなく専用の変換で無害化します。用語辞書の投影は、同じ入力を検証と変換の両方で1回だけ読み、確認した内容と実際に使う内容が食い違わないようにします。画面遷移図の橋渡しは、もとの画面一覧が今回のデータと対応しているかを照合値で確かめてから、前回の引き継ぎ情報を使うかどうかを決めます。

## §1 疑似コード

**この節の位置づけ: 現行実装。本節は現在の実装の作りをそのまま記録した節です。作り直す前に、この節に書いた作りをどう扱うかを決める必要がある**

```
build_detail_page を実行する

　　もし 種類・もとになるデータ・出力先の引数が揃っていないならば

　　　　エラーを標準エラー出力へ書き、処理を終える

　　validate_page_data を呼び、もとになるデータを検証する

　　もし 検証が不合格ならば

　　　　観点ごとの合否を書き、処理を終える

　　もし もとになるデータの種類が指定した種類と一致しないならば

　　　　エラーを書き、処理を終える

　　build_json_for_embed を呼び、埋め込み用に安全な形へ変換する
　　ひな形へ変換したデータを埋め込む
　　出力先とは別の場所へいったん書き出し、書き終えてから決まった名前へ確定させる

処理を終える
```

## §2 データ定義

**この節の位置づけ: 現行実装。本節は現在の実装の作りをそのまま記録した節です。作り直す前に、この節に書いた作りをどう扱うかを決める必要がある**

### 2.1 扱うデータ構造

| 名前 | 型 | 有効な範囲 | 寿命 |
|---|---|---|---|
| もとになるデータ | 構造化データ | 種類ごとの必須項目を満たす集合 | 1回の実行の間だけ |
| 検証観点ごとの合否 | 判定結果の並び | 9つの観点それぞれの合格・不合格 | 1回の実行の間だけ |
| 前回の引き継ぎ情報（画面遷移図） | 過去に検出済みの追加情報 | 対応関係が保たれている場合だけ引き継ぐ | 対応関係が崩れると破棄する |

### 2.2 コミットの単位

| 対象 | コミットの単位 | 件数の目安 |
|---|---|---|
| 表示資料 | 1回の実行で1件を書き出す | 1件 |

### 2.3 排他の制御

| 対象 | 排他の方式 | 取得と解放の箇所 |
|---|---|---|
| 出力先の資料 | 出力先と同じ場所に一時ファイルを作り、書き終えてから確定させる | 書き出しの開始時に取得し、確定時に解放する |

### 2.4 件数と実行時間の目安

| 対象 | 件数 | 実行時間 |
|---|---|---|
| 検証観点 | 9観点 | 1件あたり数秒程度 |

## §3 実装契約

**この節の位置づけ: 現行実装。本節は現在の実装の作りをそのまま記録した節です。作り直す前に、この節に書いた作りをどう扱うかを決める必要がある**

### 3.1 ファイルと関数

| ファイル | 関数 | 役割 | 参照先 |
|---|---|---|---|
| generation-engine/scripts/detail-pages/build-detail-page.sh | build_detail_page | 共通ビルダー本体の入口 | 該当なし |
| generation-engine/scripts/detail-pages/build-detail-page.sh | build_json_for_embed | 埋め込みデータの安全な組み立て | 該当なし |
| generation-engine/scripts/detail-pages/validate-page-data.sh | validate_page_data | もとになるデータの検証本体 | 該当なし |
| generation-engine/scripts/detail-pages/build-detail-pages-from-screen-manifest.sh | build_detail_pages_from_screen_manifest | 画面遷移図の橋渡し本体 | 該当なし |
| generation-engine/scripts/detail-pages/build-detail-pages-from-screen-manifest.sh | manifest_hash | マニフェストの照合値算出 | 該当なし |
| generation-engine/scripts/detail-pages/check-screen-transition-manifest-alignment.sh | check_screen_transition_manifest_alignment | 3資産の対応関係確認 | 該当なし |
| generation-engine/scripts/detail-pages/project-semantic-glossary.py | project_semantic_glossary | 用語辞書の投影本体 | 該当なし |

### 3.2 型定義

型を明示的に定義する実装ではないため、該当なしとします。

| 型名 | フィールド | 型 | 参照先 |
|---|---|---|---|

### 3.3 依存

| 依存先 | 用途 | 参照先 |
|---|---|---|
| render-template.sh | ひな形へのマーカー置換を行う共通処理 | generation-engine/scripts/render-template.sh |
| shell-injection.sh | 共通の表示枠を注入する共通処理 | generation-engine/scripts/shell-injection.sh |
| 用語集の検証処理 | 承認済みであることを再確認するために呼ぶ | generation-engine/scripts/glossary/validate-semantic-glossary.sh |
