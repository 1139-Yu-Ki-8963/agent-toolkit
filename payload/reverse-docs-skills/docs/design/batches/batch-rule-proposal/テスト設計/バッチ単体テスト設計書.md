---
# バッチ単体テスト設計書テンプレート（unit_kind=batch）
batch_key: rule-proposal
batch_id: batch-rule-proposal
batch_name: rule-proposal
source_ref: generation-engine/scripts/rule-proposal/build-rule-proposal.sh
unit_kind: batch
status: authored
---

# rule-proposal バッチ単体テスト設計書

**この文書の位置づけ**（別の言語で作り直す場合は、まず「仕様」から読む）

- 仕様: §1 テスト観点、§2 テストケース一覧、§3 入力条件、§4 期待結果、§5 異常系、§6 境界値、§7 網羅基準、§8 前提条件と終了条件
- 現行実装: 該当なし
- 仕様／現行実装（詳細は各節の太字1行を見る）: 該当なし

本書は関数・メソッド単位の内部処理を検証します。観点と具体的なケースを分けず、1文書で管理します。対応する設計書は `バッチ詳細設計書.md` です。

## 本書が検証するもの

結合は2段に分けて定義します。1つの設計単位の中で構成要素が結合した状態を内部結合、複数の設計単位をまたぐ連携を外部結合と呼びます。単位ごとの文書は内部結合までを担い、プロジェクト全体の文書が外部結合を担います。

| 段 | 検証する状態 | 対応する設計書 | 文書 |
|---|---|---|---|
| 単体 | 1つの関数・メソッドの入出力 | バッチ詳細設計書 | バッチ単体テスト設計書（本書） |
| 内部結合 | 1つのバッチの中で関数・メソッドが結合した状態（外部の振る舞い） | バッチ基本設計書 | バッチ結合テスト設計書 |
| 外部結合 | 複数の画面・機能・APIをまたぐ連携 | 複数の設計書 | docs/test-cases/結合テスト仕様書.md |

## テスト対象

対象は関数・メソッド単位の内部処理とする。`build_rule_proposal`・`run_static_verify` の2関数を対象とする。

## テストの粒度と自動化の方針

反復可能なケースは自動化し、実装後のテスト参照をケースに記録する。単位配下へ結合テスト文書を生成しない。

## 本書が扱わない範囲

複数の画面・機能・APIにまたがる結合テストはプロジェクト全体の `docs/test-cases/結合テスト仕様書.md` が担う。1つのバッチ内の外部から見た振る舞いはバッチ結合テスト設計書が担う。

## §1 テスト観点

**この節の位置づけ: 仕様**

§1は正常・異常・境界を含む全観点の一覧です。
§5と§6は、§1のうち異常と境界の観点を列ごとに詳しく述べる部分集合です。
§5と§6では新しい観点キーを作らず、§1と同じ観点キーを使います。
実行する全ケースは§2だけに記載し、§1の全観点へ1件以上のケースを対応させます。
実行するテストの件数は§2のデータ行数だけで数えます。

| キー | 観点 | 由来する基本設計書の章 | 由来する詳細設計書の章 |
|---|---|---|---|
| build_rule_proposal-正常終了 | 入力JSONとテンプレートから規約提案HTMLを組み立て、終了コード0を返す | §1 外部仕様 | §2 処理の定義 |
| build_rule_proposal-入力不在 | 入力JSONが不在の場合に終了コード1を返す | §5 エラーと例外 | §5 エラー処理 |
| run_static_verify-道具あり | 静的検査の道具が存在する場合に検査を実行し合否を返す | 該当なし | §2 処理の定義 |
| run_static_verify-道具なし | 静的検査の道具が存在しない場合に警告のみで処理を継続する | 該当なし | §3 ロジック |

## §2 テストケース一覧

**この節の位置づけ: 仕様**

| キー | 番号 | 対応観点キー | 入力・操作 | 期待結果 |
|---|---|---|---|---|
| build_rule_proposal-正常終了 | 1 | build_rule_proposal-正常終了 | 正しい入力JSON・出力先パスを渡す | 出力先に規約提案HTMLが書かれ、終了コード0を返す |
| build_rule_proposal-入力不在 | 2 | build_rule_proposal-入力不在 | 実在しない入力JSONパスを渡す | 出力先にファイルが作られず、終了コード1を返す |
| run_static_verify-道具あり | 3 | run_static_verify-道具あり | 検査の道具が存在する環境で、検査に違反しない出力を渡す | 検査に合格し、呼び出し元へ0を返す |
| run_static_verify-道具なし | 4 | run_static_verify-道具なし | 検査の道具が存在しない環境で実行する | 警告を出し、呼び出し元へ0を返す |

## §3 入力条件

**この節の位置づけ: 仕様**

| キー | データ | 用途 |
|---|---|---|
| build_rule_proposal-正常終了 | 章・カテゴリ・観測事項・提案文が揃った入力JSON | 正常系の入力 |
| build_rule_proposal-入力不在 | 実在しないファイルパス | 異常系の入力 |
| run_static_verify-道具あり | 検査の道具一式が配置された実行環境 | 道具ありの条件 |
| run_static_verify-道具なし | 検査の道具を持たない実行環境 | 道具なしの条件 |

## §4 期待結果

**この節の位置づけ: 仕様**

期待結果は観測可能な値・状態・表示で記載する。

## §5 異常系

**この節の位置づけ: 仕様**

| 観点のキー | 条件 | 期待結果 |
|---|---|---|
| build_rule_proposal-入力不在 | 入力JSONが実在しない | 終了コード1を返し、出力先にファイルを作らない |

## §6 境界値

**この節の位置づけ: 仕様**

| 観点のキー | 境界 | 期待結果 |
|---|---|---|
| build_rule_proposal-正常終了 | 入力JSONの章の件数が0件 | 章が0件でも処理を完了し、内容が空の文書を出力する |

## §7 網羅基準

**この節の位置づけ: 仕様**

設計書に定義された分岐・状態・制約をすべて観点へ対応付ける。

## §8 前提条件と終了条件

**この節の位置づけ: 仕様**

| 区分 | 条件 |
|---|---|
| 前提条件 | テンプレート一式が実在すること |
| 終了条件 | 出力先へ結果が書かれているか、終了コードが確定していること |
