---
# バッチ詳細設計書テンプレート（unit_kind=batch・1バッチ1枚）
batch_key: rule-proposal
batch_id: batch-rule-proposal
batch_name: rule-proposal
# batch_trigger_type の値域: scheduled（時刻起動）/ triggered（外部トリガー起動）
batch_trigger_type: triggered
source_ref: generation-engine/scripts/rule-proposal/build-rule-proposal.sh
unit_kind: batch
status: authored
---
# rule-proposal バッチ実装記録

| 節 | 内容 | 読み手へのお願い |
|---|---|---|
| §1 疑似コード | 分岐と繰り返しの構造を日本語で示します。 | 実際の分岐条件と食い違わないかを確認してください。 |
| §2 データ定義 | 扱うデータ構造と件数の目安を示します。 | 実測値が古くなっていないかを確認してください。 |
| §3 実装契約 | ファイルと関数、依存先を示します。 | 参照先の実在を確認してください。 |

**この文書の位置づけ**（別の言語で作り直す場合は、まず「仕様」から読む）

- 仕様: 該当なし
- 現行実装: §1 疑似コード、§2 データ定義、§3 実装契約
- 仕様／現行実装（詳細は各節の太字1行を見る）: 該当なし

本書は、このバッチの今の作りをそのまま記録した文書です。作り直す際に引き継がない情報だけを置きます。業務の決まり・外部との契約・満たすべき性質は バッチ詳細設計書 を参照してください。

## §1 疑似コード

**この節の位置づけ: 現行実装。本節は現在の実装の作りをそのまま記録した節です。作り直す前に、この節に書いた作りをどう扱うかを決める必要がある**

```
build_rule_proposal(入力JSONの位置, 出力HTMLの位置)
  もし 入力JSONが実在しないならば

    エラーを報告して終わる

  もし テンプレートが実在しないならば

    エラーを報告して終わる

  入力JSONを読み込む
  テンプレートへ章・カテゴリ・観測事項・提案文を差し込み、規約提案HTMLを組み立てる
  出力HTMLの位置へ書き出す

  もし 静的検査の道具と実行系が実行環境に存在するならば

    run_static_verify(出力HTMLの位置) を呼ぶ
    もし 検査が不合格ならば

      エラーを報告して終わる

  終わる
```

## §2 データ定義

**この節の位置づけ: 現行実装。本節は現在の実装の作りをそのまま記録した節です。作り直す前に、この節に書いた作りをどう扱うかを決める必要がある**

### 2.1 扱うデータ構造

| 名前 | 型 | 有効な範囲 | 寿命 |
|---|---|---|---|
| 規約提案の内容 | JSONオブジェクト | 入力JSON1件分 | 実行1回分 |
| 規約提案の文書 | HTML文字列 | 組み立て結果1件分 | 出力先へ書き出すまで |

### 2.2 コミットの単位

| 対象 | コミットの単位 | 件数の目安 |
|---|---|---|
| 出力HTML | 1回の実行で1ファイル | 1件 |

### 2.3 排他の制御

| 対象 | 排他の方式 | 取得と解放の箇所 |
|---|---|---|
| 出力先ファイル | 排他制御を行わない（出力先が異なれば衝突しない前提） | 該当なし |

### 2.4 件数と実行時間の目安

| 対象 | 件数 | 実行時間 |
|---|---|---|
| 入力の章・観測事項・提案文 | 入力JSONの規模に依存 | 入力の規模に比例する短時間 |

## §3 実装契約

**この節の位置づけ: 現行実装。本節は現在の実装の作りをそのまま記録した節です。作り直す前に、この節に書いた作りをどう扱うかを決める必要がある**

### 3.1 ファイルと関数

| ファイル | 関数 | 役割 | 参照先 |
|---|---|---|---|
| generation-engine/scripts/rule-proposal/build-rule-proposal.sh | build_rule_proposal | 入力JSONから規約提案HTMLを組み立てる | 該当箇所 |
| generation-engine/scripts/rule-proposal/build-rule-proposal.sh | run_static_verify | 外部の静的検査を任意で実行する | 該当箇所 |

### 3.2 型定義

| 型名 | フィールド | 型 | 参照先 |
|---|---|---|---|
| （該当なし。動的型付けのシェルスクリプトであり明示的な型定義を持たない） | — | — | — |

### 3.3 依存

| 依存先 | 用途 | 参照先 |
|---|---|---|
| generation-engine/scripts/render-template.sh | テンプレート文字列の展開 | 該当箇所 |
| delivery-payload/templates/rule-proposal/rule-proposal-template.html | 組み立ての土台となるテンプレート本体 | 該当箇所 |
| $HOME/agent-home/skills/reviewing-explanatory-html/scripts/verify-html-static.mjs | 生成後の静的検査（実行環境に存在する場合のみ） | 該当箇所 |
