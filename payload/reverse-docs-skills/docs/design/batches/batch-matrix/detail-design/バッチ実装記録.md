---
# バッチ詳細設計書テンプレート（unit_kind=batch・1バッチ1枚）
batch_key: matrix
batch_id: batch-matrix
batch_name: マトリクス
# batch_trigger_type の値域: scheduled（時刻起動）/ triggered（外部トリガー起動）
batch_trigger_type: triggered
source_ref: generation-engine/scripts/matrix/build-matrix-pages.sh
unit_kind: batch
status: authored
---
# マトリクス バッチ実装記録

| 節 | 内容 | 読み手へのお願い |
|---|---|---|
| §1 疑似コード | 現行実装の分岐と繰り返しを日本語で示します。 | 作り直す際に引き継ぐかどうかを判断してください。 |
| §2 データ定義 | 現行実装が扱うデータ構造を示します。 | データの持ち方が今後も妥当かを判断してください。 |
| §3 実装契約 | ファイル・関数・依存関係を示します。 | 依存の妥当性を判断してください。 |

**この文書の位置づけ**（別の言語で作り直す場合は、まず「仕様」から読む）

- 仕様: 該当なし
- 現行実装: §1 疑似コード、§2 データ定義、§3 実装契約
- 仕様／現行実装（詳細は各節の太字1行を見る）: 該当なし

本書は、このバッチの今の作りをそのまま記録した文書です。作り直す際に引き継がない情報だけを置きます。業務の決まり・外部との契約・満たすべき性質はバッチ詳細設計書を参照してください。

## §1 疑似コード

**この節の位置づけ: 現行実装。本節は現在の実装の作りをそのまま記録した節です。作り直す前に、この節に書いた作りをどう扱うかを決める必要がある**

```
build_matrix_pages を実行する

　　もし 対応する種類・入力データ・出力先の引数が揃っていないならば

　　　　エラーを標準エラー出力へ書き、処理を終える

　　もし 対応する種類が 確認が必要な事項 ではないならば

　　　　もし 入力データが 必須項目 を満たさないならば

　　　　　　remove_stale_output を呼び、既存の資料を取り除く
　　　　　　処理を終える

　　　　もし 入力データの件数が 0件 ならば

　　　　　　remove_stale_output を呼び、既存の資料を取り除く
　　　　　　処理を終える

　　render_page を呼び、ひな形へ入力データを埋め込む
　　組み立てた資料を出力先へ書き出す
処理を終える
```

## §2 データ定義

**この節の位置づけ: 現行実装。本節は現在の実装の作りをそのまま記録した節です。作り直す前に、この節に書いた作りをどう扱うかを決める必要がある**

### 2.1 扱うデータ構造

| 名前 | 型 | 有効な範囲 | 寿命 |
|---|---|---|---|
| 対応する種類 | 6値のいずれかを表す文字列 | 権限と画面・権限と機能・データと機能・追跡・確認が必要な事項・AI運用設定 | 1回の実行の間だけ |
| 入力データ | 構造化データ | 対応する種類ごとの必須項目を持つ集合 | 1回の実行の間だけ |
| 表示資料 | 単独で開ける表示資料 | 出力先に作成される1件 | 次に作り直すまで |

### 2.2 コミットの単位

| 対象 | コミットの単位 | 件数の目安 |
|---|---|---|
| 表示資料 | 1回の実行で1件の資料を書き出す | 1件 |

### 2.3 排他の制御

| 対象 | 排他の方式 | 取得と解放の箇所 |
|---|---|---|
| 出力先の資料 | 呼び出し元（生成の一連の流れ）が同時実行を避ける運用で制御する | 本処理自身は排他の取得・解放を行わない |

### 2.4 件数と実行時間の目安

| 対象 | 件数 | 実行時間 |
|---|---|---|
| 対応する種類 | 6種類 | 1種類あたり数秒程度 |

## §3 実装契約

**この節の位置づけ: 現行実装。本節は現在の実装の作りをそのまま記録した節です。作り直す前に、この節に書いた作りをどう扱うかを決める必要がある**

### 3.1 ファイルと関数

| ファイル | 関数 | 役割 | 参照先 |
|---|---|---|---|
| generation-engine/scripts/matrix/build-matrix-pages.sh | build_matrix_pages | 全体の処理を束ねる入口 | 該当なし |
| generation-engine/scripts/matrix/build-matrix-pages.sh | check_required_keys | 必須項目の充足を判定する | 該当なし |
| generation-engine/scripts/matrix/build-matrix-pages.sh | render_page | ひな形へ入力データを埋め込む | 該当なし |
| generation-engine/scripts/matrix/build-matrix-pages.sh | remove_stale_output | 既存の古い資料を取り除く | 該当なし |

### 3.2 型定義

型を明示的に定義する実装ではないため、該当なしとします。

| 型名 | フィールド | 型 | 参照先 |
|---|---|---|---|

### 3.3 依存

| 依存先 | 用途 | 参照先 |
|---|---|---|
| render-template.sh | ひな形へのマーカー置換を行う共通処理 | generation-engine/scripts/render-template.sh |
| shell-injection.sh | 共通の表示枠（サイドバー等）を注入する共通処理 | generation-engine/scripts/shell-injection.sh |
| extract群の各データ導出処理 | 対応表・確認が必要な事項のもとになるデータを作る前工程 | generation-engine/scripts/extract/ |
