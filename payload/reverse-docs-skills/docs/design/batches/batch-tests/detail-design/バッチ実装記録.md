---
# バッチ詳細設計書テンプレート（unit_kind=batch・1バッチ1枚）
batch_key: tests
batch_id: batch-tests
batch_name: tests
# batch_trigger_type の値域: scheduled（時刻起動）/ triggered（外部トリガー起動）
batch_trigger_type: triggered
source_ref: generation-engine/scripts/verification/run-layer-machine-checks.sh
unit_kind: batch
status: authored
---
# tests バッチ実装記録

| 節 | 内容 | 読み手へのお願い |
|---|---|---|
| §1 疑似コード | 検査全体の実行の流れを日本語で示します。 | 実際の分岐条件と食い違わないかを確認してください。 |
| §2 データ定義 | 扱うデータ構造と件数の目安を示します。 | 実測値が古くなっていないかを確認してください。 |
| §3 実装契約 | ファイルと関数、依存先を示します。 | 参照先の実在を確認してください。 |

**この文書の位置づけ**（別の言語で作り直す場合は、まず「仕様」から読む）

- 仕様: 該当なし
- 現行実装: §1 疑似コード、§2 データ定義、§3 実装契約
- 仕様／現行実装（詳細は各節の太字1行を見る）: 該当なし

本書は、このバッチの今の作りをそのまま記録した文書です。作り直す際に引き継がない情報だけを置きます。業務の決まり・外部との契約・満たすべき性質は バッチ詳細設計書 を参照してください。

## §1 疑似コード

**この節の位置づけ: 現行実装。本節は現在の実装の作りをそのまま記録した節です。作り直す前に、この節に書いた作りをどう扱うかを決める必要がある**

```
run_all_checks(対象システムのパス)
  検査を実行できるファイルを機械的に列挙する
  列挙した対象ごとに繰り返す

    もし 一時領域を要し、かつ作成できないならば

      当該対象を判定不能として記録する
      次の対象へ進む

    対象を実行し、合否または判定不能を受け取る
    受け取った結果を蓄積する

  蓄積した結果から、対象・成功・失敗・判定不能・打ち切りを集計する
  集計結果を報告する
```

## §2 データ定義

**この節の位置づけ: 現行実装。本節は現在の実装の作りをそのまま記録した節です。作り直す前に、この節に書いた作りをどう扱うかを決める必要がある**

### 2.1 扱うデータ構造

| 名前 | 型 | 有効な範囲 | 寿命 |
|---|---|---|---|
| 検査対象の一覧 | 一覧 | 実行1回分 | 集計が終わるまで |
| 検査結果 | 一覧（合否・判定不能を含む） | 検査対象1件につき1件 | 実行結果として出力するまで |

### 2.2 コミットの単位

| 対象 | コミットの単位 | 件数の目安 |
|---|---|---|
| 検査結果の出力 | 実行1回で一括出力 | 検査対象の件数分 |

### 2.3 排他の制御

| 対象 | 排他の方式 | 取得と解放の箇所 |
|---|---|---|
| 検査対象ファイル | 排他制御を行わない（読み取り専用の走査が中心） | 該当なし |

### 2.4 件数と実行時間の目安

| 対象 | 件数 | 実行時間 |
|---|---|---|
| 検査対象（generation-engine/scripts/tests/配下） | 数十本規模 | 対象の件数・種類に比例する時間 |

## §3 実装契約

**この節の位置づけ: 現行実装。本節は現在の実装の作りをそのまま記録した節です。作り直す前に、この節に書いた作りをどう扱うかを決める必要がある**

### 3.1 ファイルと関数

| ファイル | 関数 | 役割 | 参照先 |
|---|---|---|---|
| generation-engine/scripts/tests/check-argjson-unbounded-value.sh | （ファイル単位） | 可変長の値の安全でない渡し方を検査する | 該当箇所 |
| generation-engine/scripts/tests/check-portal-catalog.sh | （ファイル単位） | 分類定義の構造・比較結果を検査する | 該当箇所 |
| generation-engine/scripts/tests/unit-list/test-rebuild-screen-derived-pages.sh | （ファイル単位） | 画面一覧から派生する成果物一式を再生成し検証する | 該当箇所 |
| generation-engine/scripts/tests/lib/find-cached-browser.cjs | findCachedBrowser | キャッシュ済みのブラウザ実行ファイルを探す | 該当箇所 |
| generation-engine/scripts/tests/check-improvement-ledger.test.mjs | （ファイル単位） | 改善内容の台帳の構造・件数を検査する | 該当箇所 |
| generation-engine/scripts/tests/test-non-applicable-density.sh | （ファイル単位） | 非該当の根拠の記入有無を検査する | 該当箇所 |

### 3.2 型定義

| 型名 | フィールド | 型 | 参照先 |
|---|---|---|---|
| （該当なし。動的型付けのシェルスクリプト・Node.jsスクリプトであり明示的な型定義を持たない） | — | — | — |

### 3.3 依存

| 依存先 | 用途 | 参照先 |
|---|---|---|
| generation-engine/scripts/output-layout.sh | 配置解決（複数の静的整合検査が使用） | 該当箇所 |
| generation-engine/scripts/unit-axes.sh | 分類軸解決（複数の静的整合検査が使用） | 該当箇所 |
| generation-engine/samples/ | 実描画検査・生成フロー往復試験のフィクスチャ | 該当箇所 |
