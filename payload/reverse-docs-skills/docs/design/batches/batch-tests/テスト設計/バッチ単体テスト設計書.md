---
# バッチ単体テスト設計書テンプレート（unit_kind=batch）
batch_key: tests
batch_id: batch-tests
batch_name: tests
source_ref: generation-engine/scripts/verification/run-layer-machine-checks.sh
unit_kind: batch
status: authored
---

# tests バッチ単体テスト設計書

**この文書の位置づけ**（別の言語で作り直す場合は、まず「仕様」から読む）

- 仕様: §1 テスト観点、§2 テストケース一覧、§3 入力条件、§4 期待結果、§5 異常系、§6 境界値、§7 網羅基準、§8 前提条件と終了条件
- 現行実装: 該当なし
- 仕様／現行実装（詳細は各節の太字1行を見る）: 該当なし

本書は関数・メソッド単位の内部処理を検証します。観点と具体的なケースを分けず、1文書で管理します。対応する設計書は `バッチ詳細設計書.md` です。

## 本書が検証するもの

結合は2段に分けて定義します。1つの設計単位の中で構成要素が結合した状態を内部結合、複数の設計単位をまたぐ連携を外部結合と呼びます。単位ごとの文書は内部結合までを担い、プロジェクト全体の文書が外部結合を担います。

| 段 | 検証する状態 | 対応する設計書 | 文書 |
|---|---|---|---|
| 単体 | 1つの関数・メソッドの入出力 | バッチ詳細設計書 | バッチ単体テスト設計書（本書） |
| 内部結合 | 1つのバッチの中で関数・メソッドが結合した状態（外部の振る舞い） | バッチ基本設計書 | バッチ結合テスト設計書 |
| 外部結合 | 複数の画面・機能・APIをまたぐ連携 | 複数の設計書 | docs/test-cases/結合テスト仕様書.md |

## テスト対象

対象は関数・メソッド単位の内部処理とする。§1構成要素に挙げた6要素を代表として対象とする。

## テストの粒度と自動化の方針

反復可能なケースは自動化し、実装後のテスト参照をケースに記録する。単位配下へ結合テスト文書を生成しない。

## 本書が扱わない範囲

複数の画面・機能・APIにまたがる結合テストはプロジェクト全体の `docs/test-cases/結合テスト仕様書.md` が担う。1つのバッチ内の外部から見た振る舞いはバッチ結合テスト設計書が担う。

## §1 テスト観点

**この節の位置づけ: 仕様**

§1は正常・異常・境界を含む全観点の一覧です。
§5と§6は、§1のうち異常と境界の観点を列ごとに詳しく述べる部分集合です。
§5と§6では新しい観点キーを作らず、§1と同じ観点キーを使います。
実行する全ケースは§2だけに記載し、§1の全観点へ1件以上のケースを対応させます。
実行するテストの件数は§2のデータ行数だけで数えます。

| キー | 観点 | 由来する基本設計書の章 | 由来する詳細設計書の章 |
|---|---|---|---|
| argjson検査-許可リスト外検出 | 許可リストに無い可変長の値の渡し方を検出する | §5 エラーと例外 | §2 処理の定義 |
| portal-catalog-判定不能区別 | 一時領域の作成に失敗した場合を判定不能として扱う | §2 業務仕様 | §5 エラー処理 |
| rebuild-作業場所解決 | 一時的な作業場所を実体の経路へ解決してから使う | §1 外部仕様 | §3 ロジック |
| find-cached-browser-未検出時 | キャッシュ済みのブラウザが見つからない場合に空文字を返す | §5 エラーと例外 | §4 入出力の値 |
| improvement-ledger-下限確認 | 台帳の件数が下限を下回らないことを確認する | §2 業務仕様 | §2 処理の定義 |
| non-applicable-density-誤検出防止 | ロケールの明示により誤検出を防ぐ | §1 外部仕様 | §3 ロジック |

## §2 テストケース一覧

**この節の位置づけ: 仕様**

| キー | 番号 | 対応観点キー | 入力・操作 | 期待結果 |
|---|---|---|---|---|
| argjson検査-許可リスト外検出 | 1 | argjson検査-許可リスト外検出 | 許可リストに無い渡し方を含むファイルを渡す | 該当箇所が一覧として報告される |
| portal-catalog-判定不能区別 | 2 | portal-catalog-判定不能区別 | 一時領域を作成できない状態で自己確認を実行する | 判定不能として報告され、不合格とは区別される |
| rebuild-作業場所解決 | 3 | rebuild-作業場所解決 | 別名（シンボリックリンク）を経由する一時領域で実行する | 実体の経路へ解決され、書き込み許可検査に阻まれない |
| find-cached-browser-未検出時 | 4 | find-cached-browser-未検出時 | ブラウザがキャッシュされていない環境で呼び出す | 空文字列が返る |
| improvement-ledger-下限確認 | 5 | improvement-ledger-下限確認 | 実在の台帳に対して検査を実行する | 件数が定めた下限以上であることが確認される |
| non-applicable-density-誤検出防止 | 6 | non-applicable-density-誤検出防止 | 正しく記入された非該当行を含むサンプルを検査する | 誤って未記入と判定されない |

## §3 入力条件

**この節の位置づけ: 仕様**

| キー | データ | 用途 |
|---|---|---|
| argjson検査-許可リスト外検出 | 許可リストに無い渡し方を含む合成ファイル | 異常系の入力 |
| portal-catalog-判定不能区別 | 一時領域の作成を拒む実行環境 | 判定不能確認用の入力 |
| rebuild-作業場所解決 | 別名を経由する一時領域 | 境界確認用の入力 |
| find-cached-browser-未検出時 | ブラウザ実行ファイルを持たない実行環境 | 異常系の入力 |
| improvement-ledger-下限確認 | 実在の台帳ファイル | 正常系の入力 |
| non-applicable-density-誤検出防止 | 正しく記入された非該当行を含むサンプル | 正常系の入力 |

## §4 期待結果

**この節の位置づけ: 仕様**

期待結果は観測可能な値・状態・表示で記載する。

## §5 異常系

**この節の位置づけ: 仕様**

| 観点のキー | 条件 | 期待結果 |
|---|---|---|
| portal-catalog-判定不能区別 | 一時領域が作成できない | 判定不能として報告され、処理自体は異常終了しない |
| find-cached-browser-未検出時 | ブラウザがキャッシュされていない | 空文字列を返し、呼び出し元がその旨を扱えるようにする |

## §6 境界値

**この節の位置づけ: 仕様**

| 観点のキー | 境界 | 期待結果 |
|---|---|---|
| improvement-ledger-下限確認 | 台帳の件数が定めた下限と一致する | 下限と一致する場合は合格として扱われる |

## §7 網羅基準

**この節の位置づけ: 仕様**

設計書に定義された分岐・状態・制約をすべて観点へ対応付ける。

## §8 前提条件と終了条件

**この節の位置づけ: 仕様**

| 区分 | 条件 |
|---|---|
| 前提条件 | 検査対象のファイルが実在すること |
| 終了条件 | 各要素の検査結果が確定していること |
