---
# バッチ詳細設計書テンプレート（unit_kind=batch・1バッチ1枚）
batch_key: verification
batch_id: batch-verification
batch_name: verification
# batch_trigger_type の値域: scheduled（時刻起動）/ triggered（外部トリガー起動）
batch_trigger_type: triggered
source_ref: generation-engine/scripts/verification/run-verification-loop.sh
unit_kind: batch
status: authored
---
# verification バッチ実装記録

| 節 | 内容 | 読み手へのお願い |
|---|---|---|
| §1 疑似コード | 全体の統合の流れを日本語で示します。 | 実際の分岐条件と食い違わないかを確認してください。 |
| §2 データ定義 | 扱うデータ構造と件数の目安を示します。 | 実測値が古くなっていないかを確認してください。 |
| §3 実装契約 | ファイルと関数、依存先を示します。 | 参照先の実在を確認してください。 |

**この文書の位置づけ**（別の言語で作り直す場合は、まず「仕様」から読む）

- 仕様: 該当なし
- 現行実装: §1 疑似コード、§2 データ定義、§3 実装契約
- 仕様／現行実装（詳細は各節の太字1行を見る）: 該当なし

本書は、このバッチの今の作りをそのまま記録した文書です。作り直す際に引き継がない情報だけを置きます。業務の決まり・外部との契約・満たすべき性質は バッチ詳細設計書 を参照してください。

## §1 疑似コード

**この節の位置づけ: 現行実装。本節は現在の実装の作りをそのまま記録した節です。作り直す前に、この節に書いた作りをどう扱うかを決める必要がある**

```
run_verification_loop(対象システムのパス)
  版（バージョン）を取得する
  もし 第1層を省略しないならば

    run_layer_machine_checks(対象システムのパス) を呼ぶ

  使い捨ての出力先を2つ用意する
  1つ目の出力先へ run_layer_full_pipeline(疑似入力, 1つ目) を呼ぶ
  2つ目の出力先へ run_layer_full_pipeline(疑似入力, 2つ目) を呼ぶ
  check_coverage(1つ目) を呼ぶ
  check_self_contained() を呼ぶ
  check_reproducible(1つ目, 2つ目) を呼ぶ
  check_sound(1つ目) を呼ぶ
  もし 記録を省略しないならば

    record_verification_result(版, 各判定の結果) を呼ぶ
    compare_with_previous(実行記録) を呼ぶ

  2つの出力先を片付ける
  全体の合否・判定不能を確定して報告する
```

## §2 データ定義

**この節の位置づけ: 現行実装。本節は現在の実装の作りをそのまま記録した節です。作り直す前に、この節に書いた作りをどう扱うかを決める必要がある**

### 2.1 扱うデータ構造

| 名前 | 型 | 有効な範囲 | 寿命 |
|---|---|---|---|
| 使い捨ての出力先 | ディレクトリ | 検証実行1回につき2件 | 検証終了まで |
| 判定結果 | 一覧（合否・判定不能を含む） | 4判定分 | 実行記録へ追記するまで |
| 実行記録 | 台帳（版付きの検証結果の集まり） | 恒久 | 検証運用を続ける限り |

### 2.2 コミットの単位

| 対象 | コミットの単位 | 件数の目安 |
|---|---|---|
| 実行記録 | 検証1回につき1件追記 | 検証の実行回数分 |

### 2.3 排他の制御

| 対象 | 排他の方式 | 取得と解放の箇所 |
|---|---|---|
| 使い捨ての作業領域 | 検証実行ごとに独立した領域を発番する（排他制御自体は行わない） | 用意・片付けの各段 |

### 2.4 件数と実行時間の目安

| 対象 | 件数 | 実行時間 |
|---|---|---|
| 第1層の対象 | 数十本〜百本規模 | 対象の件数・種類に比例する時間 |
| 第3層の段 | 10段 | 生成連鎖全体で相応の時間を要する |

## §3 実装契約

**この節の位置づけ: 現行実装。本節は現在の実装の作りをそのまま記録した節です。作り直す前に、この節に書いた作りをどう扱うかを決める必要がある**

### 3.1 ファイルと関数

| ファイル | 関数 | 役割 | 参照先 |
|---|---|---|---|
| generation-engine/scripts/verification/run-verification-loop.sh | （ファイル単位） | 検証の全段を1本で回す統合入口 | 該当箇所 |
| generation-engine/scripts/verification/run-layer-machine-checks.sh | run_one | 対象1件の自己確認を実行する | 該当箇所 |
| generation-engine/scripts/verification/run-layer-full-pipeline.sh | （ファイル単位） | 疑似入力から成果物一式を生成する | 該当箇所 |
| generation-engine/scripts/verification/verification-env.sh | verification_env_setup / verification_env_teardown | 使い捨ての作業領域を発番・削除する | 該当箇所 |

### 3.2 型定義

| 型名 | フィールド | 型 | 参照先 |
|---|---|---|---|
| （該当なし。動的型付けのシェルスクリプトであり明示的な型定義を持たない） | — | — | — |

### 3.3 依存

| 依存先 | 用途 | 参照先 |
|---|---|---|
| generation-engine/scripts/output-layout.sh | 出力配置パスの宣言解決 | 該当箇所 |
| delivery-payload/references/portal-catalog.json | 網羅の判定の分母の定義元 | 該当箇所 |
| delivery-payload/templates/リバース検証/ | 疑似入力の雛形 | 該当箇所 |
