---
# バッチ単体テスト設計書テンプレート（unit_kind=batch）
batch_key: unit-list
batch_id: batch-unit-list
batch_name: unit-list
source_ref: generation-engine/scripts/unit-list/detect-screens.sh
unit_kind: batch
status: authored
---

# unit-list バッチ単体テスト設計書

**この文書の位置づけ**（別の言語で作り直す場合は、まず「仕様」から読む）

- 仕様: §1 テスト観点、§2 テストケース一覧、§3 入力条件、§4 期待結果、§5 異常系、§6 境界値、§7 網羅基準、§8 前提条件と終了条件
- 現行実装: 該当なし
- 仕様／現行実装（詳細は各節の太字1行を見る）: 該当なし

本書は関数・メソッド単位の内部処理を検証します。観点と具体的なケースを分けず、1文書で管理します。対応する設計書は `バッチ詳細設計書.md` です。

## 本書が検証するもの

結合は2段に分けて定義します。1つの設計単位の中で構成要素が結合した状態を内部結合、複数の設計単位をまたぐ連携を外部結合と呼びます。単位ごとの文書は内部結合までを担い、プロジェクト全体の文書が外部結合を担います。

| 段 | 検証する状態 | 対応する設計書 | 文書 |
|---|---|---|---|
| 単体 | 1つの関数・メソッドの入出力 | バッチ詳細設計書 | バッチ単体テスト設計書（本書） |
| 内部結合 | 1つのバッチの中で関数・メソッドが結合した状態（外部の振る舞い） | バッチ基本設計書 | バッチ結合テスト設計書 |
| 外部結合 | 複数の画面・機能・APIをまたぐ連携 | 複数の設計書 | docs/test-cases/結合テスト仕様書.md |

## テスト対象

対象は関数・メソッド単位の内部処理とする。§1構成要素に挙げた7要素を代表として対象とする。

## テストの粒度と自動化の方針

反復可能なケースは自動化し、実装後のテスト参照をケースに記録する。単位配下へ結合テスト文書を生成しない。

## 本書が扱わない範囲

複数の画面・機能・APIにまたがる結合テストはプロジェクト全体の `docs/test-cases/結合テスト仕様書.md` が担う。1つのバッチ内の外部から見た振る舞いはバッチ結合テスト設計書が担う。

## §1 テスト観点

**この節の位置づけ: 仕様**

§1は正常・異常・境界を含む全観点の一覧です。
§5と§6は、§1のうち異常と境界の観点を列ごとに詳しく述べる部分集合です。
§5と§6では新しい観点キーを作らず、§1と同じ観点キーを使います。
実行する全ケースは§2だけに記載し、§1の全観点へ1件以上のケースを対応させます。
実行するテストの件数は§2のデータ行数だけで数えます。

| キー | 観点 | 由来する基本設計書の章 | 由来する詳細設計書の章 |
|---|---|---|---|
| detect-親画面解決 | 日本語を含む画面名の親子関係を正しく解決する | §1 外部仕様 | §3 ロジック |
| build-委譲判定 | 種別が画面・機能の場合に専用処理へ委譲する | §2 業務仕様 | §3 ロジック |
| validate-検証失敗検出 | 必須項目が欠けた元データを不合格として検出する | §5 エラーと例外 | §2 処理の定義 |
| rebuild-失敗時復元 | 生成連鎖の途中で失敗した場合に元の状態へ復元する | §5 エラーと例外 | §2 処理の定義 |
| restore-埋め込み復元 | 一覧に埋め込まれた元データを正しく取り出す | §1 外部仕様 | §2 処理の定義 |

## §2 テストケース一覧

**この節の位置づけ: 仕様**

| キー | 番号 | 対応観点キー | 入力・操作 | 期待結果 |
|---|---|---|---|---|
| detect-親画面解決 | 1 | detect-親画面解決 | 日本語の画面名を持つ複数画面を渡す | 親子関係が正しく解決される |
| build-委譲判定 | 2 | build-委譲判定 | 種別に画面または機能を指定する | 対応する専用処理へ委譲される |
| validate-検証失敗検出 | 3 | validate-検証失敗検出 | 必須項目が欠けた元データを渡す | 不合格として検出される |
| rebuild-失敗時復元 | 4 | rebuild-失敗時復元 | 生成連鎖の途中で障害を注入する | 元の状態へ復元される |
| restore-埋め込み復元 | 5 | restore-埋め込み復元 | 元データが埋め込まれた一覧を渡す | 埋め込みブロックが正しく取り出される |

## §3 入力条件

**この節の位置づけ: 仕様**

| キー | データ | 用途 |
|---|---|---|
| detect-親画面解決 | 日本語の画面名を持つ複数画面 | 正常系の入力 |
| build-委譲判定 | 種別「画面」または「機能」を指定した引数 | 分岐確認用の入力 |
| validate-検証失敗検出 | 必須項目が欠けた元データ | 異常系の入力 |
| rebuild-失敗時復元 | 途中失敗を注入する実行環境 | 異常系の入力 |
| restore-埋め込み復元 | 埋め込みブロックを持つ一覧HTML | 正常系の入力 |

## §4 期待結果

**この節の位置づけ: 仕様**

期待結果は観測可能な値・状態・表示で記載する。

## §5 異常系

**この節の位置づけ: 仕様**

| 観点のキー | 条件 | 期待結果 |
|---|---|---|
| validate-検証失敗検出 | 元データの必須項目が欠けている | 不合格として検出され、一覧生成へ進まない |
| rebuild-失敗時復元 | 生成連鎖の途中で失敗する | 退避しておいた元の内容へ復元される |

## §6 境界値

**この節の位置づけ: 仕様**

| 観点のキー | 境界 | 期待結果 |
|---|---|---|
| detect-親画面解決 | 検出した画面の件数が1件 | 1件だけでも親子解決の処理が正しく完了する |

## §7 網羅基準

**この節の位置づけ: 仕様**

設計書に定義された分岐・状態・制約をすべて観点へ対応付ける。

## §8 前提条件と終了条件

**この節の位置づけ: 仕様**

| 区分 | 条件 |
|---|---|
| 前提条件 | 対象の入力（原本コード・元データ・一覧）が実在すること |
| 終了条件 | 各要素の処理結果が確定していること |
