---
# バッチ詳細設計書テンプレート（unit_kind=batch・1バッチ1枚）
batch_key: root
batch_id: batch-root
batch_name: ルート直下
# batch_trigger_type の値域: scheduled（時刻起動）/ triggered（外部トリガー起動）
batch_trigger_type: triggered
source_ref: generation-engine/scripts/build-portal.sh
unit_kind: batch
status: authored
---
# ルート直下 バッチ実装記録

| 節 | 内容 | 読み手へのお願い |
|---|---|---|
| §1 疑似コード | 現行実装の分岐と繰り返しを日本語で示します。 | 作り直す際に引き継ぐかどうかを判断してください。 |
| §2 データ定義 | 現行実装が扱うデータ構造を示します。 | データの持ち方が今後も妥当かを判断してください。 |
| §3 実装契約 | ファイル・関数・依存関係を示します。 | 依存の妥当性を判断してください。 |

**この文書の位置づけ**（別の言語で作り直す場合は、まず「仕様」から読む）

- 仕様: 該当なし
- 現行実装: §1 疑似コード、§2 データ定義、§3 実装契約
- 仕様／現行実装（詳細は各節の太字1行を見る）: 該当なし

本書は、このバッチの今の作りをそのまま記録した文書です。作り直す際に引き継がない情報だけを置きます。業務の決まり・外部との契約・満たすべき性質はバッチ詳細設計書を参照してください。

現行実装には次の素直でない作りが含まれます。一時領域を作る箇所は、実行環境の標準機能がそのまま返す場所を使わず、実際の物理的な場所へ解決してから使います。標準機能がそのまま返す場所は、環境によっては別名を経由しており、後段の安全確認で拒まれることがあるためです。表示場所一式を作る処理は所要時間が長いことがあらかじめ分かっており、これは短縮の対象とはしません。実行環境の変数名の一部に全角文字を続けて書くと、変数名の解釈を誤ることがあるため、変数名を明確に区切る書き方を使います。生成済みの表示資料を後始末する際は、対応表にある資料や別の生成処理が作った資料を誤って消さないよう、複数の手がかりを組み合わせて判定します。日本語を含む対象を走査する際は、環境のロケール設定に左右されない書き方、または左右されてよい書き方を、走査の性質に応じて使い分けます。表示場所の入口を作り直す処理は、渡す場所の情報を文字列へ直接つなげず、実行環境の変数として明示的に渡します。複数の処理の間で共有する一覧は、判定結果を表す出力とは別に、一時的な記録として扱います。後始末の予約は、値を渡す時点で固定してから登録します。

## §1 疑似コード

**この節の位置づけ: 現行実装。本節は現在の実装の作りをそのまま記録した節です。作り直す前に、この節に書いた作りをどう扱うかを決める必要がある**

```
build_portal を実行する

　　もし 対象リポジトリ・出力先の引数が揃っていないならば

　　　　エラーを標準エラー出力へ書き、処理を終える

　　種類ごとに、次を繰り返す

　　　　もし その種類のもとになるデータが準備済みでないならば

　　　　　　その種類を飛ばす

　　　　もし 準備済みならば

　　　　　　その種類の表示資料を組み立てる

　　対象リポジトリのAI運用の設定を抽出する
　　抽出結果から専用の表示資料を組み立てる
　　全種類の表示資料をまとめ、表示場所一式を組み立てる

処理を終える
```

## §2 データ定義

**この節の位置づけ: 現行実装。本節は現在の実装の作りをそのまま記録した節です。作り直す前に、この節に書いた作りをどう扱うかを決める必要がある**

### 2.1 扱うデータ構造

| 名前 | 型 | 有効な範囲 | 寿命 |
|---|---|---|---|
| 生成物のハッシュ台帳 | 構造化データ | 生成物ごとの内容の指紋 | 次に記録し直すまで |
| 表示場所の入口 | 表示資料 | もとになるデータへの案内一式 | 次に作り直すまで |
| 影響を受ける成果物ID一覧 | 文字列の並び | 1回の実行の間の中間記録 | 1回の実行の間だけ |

### 2.2 コミットの単位

| 対象 | コミットの単位 | 件数の目安 |
|---|---|---|
| 表示場所一式 | 1回の実行で全体を作り切る | 1件 |

### 2.3 排他の制御

| 対象 | 排他の方式 | 取得と解放の箇所 |
|---|---|---|
| 出力先の資料一式 | 呼び出し元が同時実行を避ける運用で制御する | 本処理自身は排他の取得・解放を行わない |

### 2.4 件数と実行時間の目安

| 対象 | 件数 | 実行時間 |
|---|---|---|
| 表示場所一式の生成 | 種類の数に応じた件数 | 実測で長時間（十数分規模）かかることが分かっている |

## §3 実装契約

**この節の位置づけ: 現行実装。本節は現在の実装の作りをそのまま記録した節です。作り直す前に、この節に書いた作りをどう扱うかを決める必要がある**

### 3.1 ファイルと関数

| ファイル | 関数 | 役割 | 参照先 |
|---|---|---|---|
| generation-engine/scripts/build-portal.sh | build_portal | 表示場所一式の生成本体 | 該当なし |
| generation-engine/scripts/build-portal.sh | create_physical_tmpdir | 一時領域を物理的な場所へ解決する | 該当なし |
| generation-engine/scripts/check-derived-drift.sh | check_derived_drift | 生成物の手作業書き換え検知 | 該当なし |
| generation-engine/scripts/check-portal-entry-freshness.sh | check_portal_entry_freshness | 表示場所入口の鮮度確認 | 該当なし |
| generation-engine/scripts/audit-consistency.sh | audit_consistency | 設計文書の内部整合性確認 | 該当なし |
| generation-engine/scripts/prefill-design-from-facts.sh | prefill_design_from_facts | 確定済み情報の機械転記 | 該当なし |
| generation-engine/scripts/add-sync-entry.sh | add_sync_entry | 配布対応表への登録 | 該当なし |

### 3.2 型定義

型を明示的に定義する実装ではないため、該当なしとします。

| 型名 | フィールド | 型 | 参照先 |
|---|---|---|---|

### 3.3 依存

| 依存先 | 用途 | 参照先 |
|---|---|---|
| render-template.sh | ひな形へのマーカー置換を行う共通処理 | generation-engine/scripts/render-template.sh |
| output-layout.sh | 出力配置の解決を行う共通処理 | generation-engine/scripts/output-layout.sh |
| portal-catalog.mjs | 表示場所のカタログ検証と描画を行う処理 | generation-engine/scripts/portal-catalog.mjs |
