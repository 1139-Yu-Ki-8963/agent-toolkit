---
# テーブル結合テスト設計書テンプレート（unit_kind=table・1テーブル1枚）
table_key: portal-catalog-legacy-golden
table_id: table-portal-catalog-legacy-golden
table_name: portal-catalog-legacy-golden
source_ref: delivery-payload/references/portal-catalog-legacy-golden.json
unit_kind: table
status: authored
---

# ポータルカタログの旧版基準 テーブル結合テスト設計書

**この文書の位置づけ**（別の言語で作り直す場合は、まず「仕様」から読む）

- 仕様: §1 テスト観点、§2 テストケース一覧、§3 入力条件、§4 期待結果、§5 異常系、§6 境界値、§7 網羅基準、§8 前提条件と終了条件、§9 関連資料
- 現行実装: 該当なし
- 仕様／現行実装（詳細は各節の太字1行を見る）: 該当なし

## 本書が検証するもの

結合は2段に分けて定義します。1つの設計単位の中で構成要素が結合した状態を内部結合、複数の設計単位をまたぐ連携を外部結合と呼びます。単位ごとの文書は内部結合までを担い、プロジェクト全体の文書が外部結合を担います。

| 段 | 検証する状態 | 対応する設計書 | 文書 |
|---|---|---|---|
| 単体 | 1つの関数・メソッドの入出力 | テーブル定義書 | テーブル単体テスト設計書 |
| 内部結合 | 1つの定義の中で項目が結合した状態（外部の振る舞い） | 論理データモデル | テーブル結合テスト設計書（本書） |
| 外部結合 | 複数の画面・機能・APIをまたぐ連携 | 複数の設計書 | docs/test-cases/結合テスト仕様書.md |

## テスト対象

| 対象 | 確認する内容 | 根拠となる基本設計書の章 |
|---|---|---|
| delivery-payload/references/portal-catalog-legacy-golden.json 全体 | 捕捉時点のカテゴリ・カード一覧が業務仕様（論理データモデル §2・§4）の整合性の条件を満たしていること | §2・§4 |

## テストの粒度と自動化の方針

| 項目 | 方針 |
|---|---|
| テストの粒度 | 定義ファイル全体を1件として、現行の抽出結果との差分の有無を確認する。 |
| 自動化 | 対応する検査スクリプト（check-portal-catalog.sh）で自動化する。 |

## 本書が扱わない範囲

| 対象外の事項 | 対象外とする理由 | 代わりに確認する資料または試験 |
|---|---|---|
| 現行の portal-catalog.json 自体の妥当性 | 現行定義の妥当性は table-portal-catalog の責務であるため | table-portal-catalog のテスト設計書 |

## §1 テスト観点

**この節の位置づけ: 仕様**

§1は正常・異常・境界を含む全観点の一覧です。
§5と§6は、§1のうち異常と境界の観点を列ごとに詳しく述べる部分集合です。
§5と§6では新しい観点キーを作らず、§1と同じ観点キーを使います。
実行する全ケースは§2だけに記載し、§1の全観点へ1件以上のケースを対応させます。
実行するテストの件数は§2のデータ行数だけで数えます。

観点は論理データモデルのデータ仕様・エラーと例外の各章から起こす。

| キー | 観点 | 由来する基本設計書の章 |
|---|---|---|
| `portal-catalog-legacy-golden-id-整合` | [].id が一意であること | §2・§4 |
| `portal-catalog-legacy-golden-差分-整合` | 現行の抽出結果との差分が0件であること | §2・§4 |

## §2 テストケース一覧

**この節の位置づけ: 仕様**

| キー | 番号 | ケースの名前 | 対応する観点のキー | 区分 |
|---|---|---|---|---|
| `portal-catalog-legacy-golden-id-確認` | 1 | カテゴリ識別子の一意性確認 | `portal-catalog-legacy-golden-id-整合` | 正常 |
| `portal-catalog-legacy-golden-差分-確認` | 2 | 現行抽出結果との差分なし確認 | `portal-catalog-legacy-golden-差分-整合` | 正常 |

## §3 入力条件

**この節の位置づけ: 仕様**

| ケースのキー | 入力の項目 | 値 |
|---|---|---|
| `portal-catalog-legacy-golden-id-確認` | カテゴリ識別子一覧 | delivery-payload/references/portal-catalog-legacy-golden.json に定義された実際の値 |
| `portal-catalog-legacy-golden-差分-確認` | 現行 portal-catalog.json から抽出した結果 | generation-engine/scripts/portal-catalog.mjs extract の実行結果 |

## §4 期待結果

**この節の位置づけ: 仕様**

| ケースのキー | 期待する結果 | 確認の方法 |
|---|---|---|
| `portal-catalog-legacy-golden-id-確認` | [].id が一意であること | 対応する検査スクリプト（check-portal-catalog.sh）の実行結果で確認する |
| `portal-catalog-legacy-golden-差分-確認` | 現行抽出結果と本基準の差分が0件であること | 対応する検査スクリプトの実行結果で確認する |

## §5 異常系

**この節の位置づけ: 仕様**

| 観点のキー | 発生させる条件 | 期待する振る舞い | 由来する基本設計書の章 |
|---|---|---|---|
| `portal-catalog-legacy-golden-差分検出-対応` | 現行抽出結果と本基準に差分がある | 対応する検査スクリプトが不合格として検出する | §2.4 |

## §6 境界値

**この節の位置づけ: 仕様**

| 観点のキー | 対象の項目 | 境界の値 | 境界の直前と直後の扱い |
|---|---|---|---|
| `portal-catalog-legacy-golden-差分件数-境界` | 現行抽出結果との差分件数 | 0件と1件以上の境界 | 0件時は合格、1件以上時は不合格として検出する |

## §7 網羅基準

**この節の位置づけ: 仕様**

| 観点 | 基準 |
|---|---|
| 条件の網羅 | §1に列挙した観点をすべて§2で1件以上確認する |
| 分岐の網羅 | 識別子の一意性・差分検出の2条件を確認する |
| 境界の網羅 | 差分0件と1件以上の両方を確認する |

## §8 前提条件と終了条件

**この節の位置づけ: 仕様**

| 区分 | 内容 |
|---|---|
| 実行の前提条件 | delivery-payload/references/portal-catalog-legacy-golden.json が実在し、読み取り権限があること |
| 終了の条件 | 現行の portal-catalog.json から生成したポータルトップHTMLが本基準と一致すること |

## §9 関連資料

**この節の位置づけ: 仕様**

他文書の所在はこの節へまとめる。確定できなかった事項は推測で埋めず、空欄のままとする。

| 資料 | パス | 本書との関係 |
|---|---|---|
| 論理データモデル | `../basic-design/論理データモデル.md` | 本書が検証する仕様の根拠 |
| テーブル定義書 | `../detail-design/テーブル定義書.md` | 本書が対応する実装レベルの設計書 |
