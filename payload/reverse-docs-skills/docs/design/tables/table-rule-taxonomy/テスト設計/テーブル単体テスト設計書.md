---
# テーブル単体テスト設計書テンプレート（unit_kind=table）
table_key: rule-taxonomy
table_id: table-rule-taxonomy
table_name: rule-taxonomy
source_ref: delivery-payload/references/rule-taxonomy.json
unit_kind: table
status: authored
---

# 規約分類の定義 テーブル単体テスト設計書

**この文書の位置づけ**（別の言語で作り直す場合は、まず「仕様」から読む）

- 仕様: §1 テスト観点、§2 テストケース一覧、§3 入力条件、§4 期待結果、§5 異常系、§6 境界値、§7 網羅基準、§8 前提条件と終了条件
- 現行実装: 該当なし
- 仕様／現行実装（詳細は各節の太字1行を見る）: 該当なし

本書は関数・メソッド単位の内部処理を検証します。観点と具体的なケースを分けず、1文書で管理します。対応する設計書は `テーブル定義書.md` です。

## 本書が検証するもの

結合は2段に分けて定義します。1つの設計単位の中で構成要素が結合した状態を内部結合、複数の設計単位をまたぐ連携を外部結合と呼びます。単位ごとの文書は内部結合までを担い、プロジェクト全体の文書が外部結合を担います。

| 段 | 検証する状態 | 対応する設計書 | 文書 |
|---|---|---|---|
| 単体 | 1つの関数・メソッドの入出力 | テーブル定義書 | テーブル単体テスト設計書（本書） |
| 内部結合 | 1つの定義の中で項目が結合した状態（外部の振る舞い） | 論理データモデル | テーブル結合テスト設計書 |
| 外部結合 | 複数の画面・機能・APIをまたぐ連携 | 複数の設計書 | docs/test-cases/結合テスト仕様書.md |

## テスト対象

対象は本定義を読み込む各スクリプトの、項目単位の読み取り処理とする。

## テストの粒度と自動化の方針

反復可能なケースは自動化し、実装後のテスト参照をケースに記録する。単位配下へ結合テスト文書を生成しない。

## 本書が扱わない範囲

複数の画面・機能・APIにまたがる結合テストはプロジェクト全体の `docs/test-cases/結合テスト仕様書.md` が担う。

## §1 テスト観点

**この節の位置づけ: 仕様**

§1は正常・異常・境界を含む全観点の一覧です。
§5と§6は、§1のうち異常と境界の観点を列ごとに詳しく述べる部分集合です。
§5と§6では新しい観点キーを作らず、§1と同じ観点キーを使います。
実行する全ケースは§2だけに記載し、§1の全観点へ1件以上のケースを対応させます。
実行するテストの件数は§2のデータ行数だけで数えます。

| キー | 観点 | 由来する基本設計書の章 | 由来する詳細設計書の章 |
|---|---|---|---|
| `rule-taxonomy-parents-必須` | 項目 parents[].key の記入規則を守っていること（親カテゴリ識別子） | 該当なし | §1・§4 |
| `rule-taxonomy-children-必須` | 項目 parents[].children[].key の記入規則を守っていること（子カテゴリ識別子） | 該当なし | §1・§4 |
| `rule-taxonomy-checker-必須` | 項目 children[].checker の記入規則を守っていること（toolDefinedがtrueなら空でない） | 該当なし | §1・§4 |
| `rule-taxonomy-scope-必須` | 項目 children[].scope の記入規則を守っていること（適用範囲） | 該当なし | §1・§4 |
| `rule-taxonomy-構文不正` | JSON構文が誤っている場合に読み込み元が中断すること | §5 | §5 |

## §2 テストケース一覧

**この節の位置づけ: 仕様**

| キー | 番号 | 対応観点キー | 入力・操作 | 期待結果 |
|---|---|---|---|---|
| `rule-taxonomy-parents-必須` | 1 | `rule-taxonomy-parents-必須` | delivery-payload/references/rule-taxonomy.json の parents[].key を読む | 親カテゴリ識別子 に沿った値が得られる |
| `rule-taxonomy-children-必須` | 2 | `rule-taxonomy-children-必須` | delivery-payload/references/rule-taxonomy.json の parents[].children[].key を読む | 子カテゴリ識別子 に沿った値が得られる |
| `rule-taxonomy-checker-必須` | 3 | `rule-taxonomy-checker-必須` | delivery-payload/references/rule-taxonomy.json の children[].checker を読む | 機械検査の検査名 に沿った値が得られる |
| `rule-taxonomy-scope-必須` | 4 | `rule-taxonomy-scope-必須` | delivery-payload/references/rule-taxonomy.json の children[].scope を読む | 適用範囲 に沿った値が得られる |
| `rule-taxonomy-構文不正` | 5 | `rule-taxonomy-構文不正` | delivery-payload/references/rule-taxonomy.json の構文を意図的に壊して読み込む | 読み込み元スクリプトが処理を中断しエラーを通知する |

## §3 入力条件

**この節の位置づけ: 仕様**

| キー | データ | 用途 |
|---|---|---|
| `rule-taxonomy-parents-必須` | delivery-payload/references/rule-taxonomy.json の parents[].key | 親カテゴリ識別子 |
| `rule-taxonomy-children-必須` | delivery-payload/references/rule-taxonomy.json の parents[].children[].key | 子カテゴリ識別子 |
| `rule-taxonomy-checker-必須` | delivery-payload/references/rule-taxonomy.json の children[].checker | 機械検査の検査名 |
| `rule-taxonomy-scope-必須` | delivery-payload/references/rule-taxonomy.json の children[].scope | 適用範囲 |

## §4 期待結果

**この節の位置づけ: 仕様**

期待結果は観測可能な値・状態・表示で記載する。各キーの必須項目は値が取得できること、任意項目は値が無くても読み込み元が既定値または対象外として扱えることを期待結果とする。

## §5 異常系

**この節の位置づけ: 仕様**

| 観点のキー | 条件 | 期待結果 |
|---|---|---|
| `rule-taxonomy-構文不正` | JSON構文が誤っている | 読み込み元スクリプトが処理を中断しエラーを通知する |

## §6 境界値

**この節の位置づけ: 仕様**

| 観点のキー | 境界 | 期待結果 |
|---|---|---|
| `rule-taxonomy-toolDefined-境界` | toolDefined が false（機械検査なし）と true（機械検査あり）の境界 | false時はcheckerを要求せず、true時はcheckerの実在を確認する |

## §7 網羅基準

**この節の位置づけ: 仕様**

設計書に定義された分岐・状態・制約をすべて観点へ対応付ける。本定義は分岐を持たないため、項目の必須・任意の区分と構文エラー時の扱いを網羅の基準とする。

## §8 前提条件と終了条件

**この節の位置づけ: 仕様**

| 区分 | 条件 |
|---|---|
| 前提条件 | delivery-payload/references/rule-taxonomy.json が実在し、読み取り権限があること |
| 終了条件 | 読み込み元スクリプトが本定義の各項目へ正しくアクセスできる状態になっていること |
