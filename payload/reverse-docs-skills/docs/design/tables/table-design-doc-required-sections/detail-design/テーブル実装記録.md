---
# テーブル定義書テンプレート（unit_kind=table・1テーブル1枚）
table_key: design-doc-required-sections
table_id: table-design-doc-required-sections
table_name: design-doc-required-sections
table_subkind: table
source_ref: delivery-payload/references/design-doc-required-sections.json
unit_kind: table
status: authored
---
# 設計文書の必須節定義 テーブル実装記録

**この文書の位置づけ**（別の言語で作り直す場合は、まず「仕様」から読む）

- 仕様: 該当なし
- 現行実装: §1 疑似コード、§2 データ定義、§3 実装契約
- 仕様／現行実装（詳細は各節の太字1行を見る）: 該当なし

本書は、この定義ファイルの今の作りをそのまま記録した文書です。作り直す際に引き継がない情報だけを置きます。業務の決まり・外部との契約・満たすべき性質はテーブル定義書を参照してください。

## §1 疑似コード

**この節の位置づけ: 現行実装。本節は現在の実装の作りをそのまま記録した節です。作り直す前に、この節に書いた作りをどう扱うかを決める必要がある**

読み込み元スクリプトは次の手順でこの定義を扱います。

1. delivery-payload/references/design-doc-required-sections.json をファイルとして読み込む。
2. JSON として解析する。解析に失敗した場合は処理を中断しエラーを通知する。
3. 必要な項目（documentTypes.<kind>.<doc>.requiredSections[]）を取り出す。additionalAllowance.description, additionalAllowance.projectFile 等も同様に取り出す。
4. 取り出した値を用いて、呼び出し元固有の判定または生成処理を行う。

## §2 データ定義

**この節の位置づけ: 現行実装。本節は現在の実装の作りをそのまま記録した節です。作り直す前に、この節に書いた作りをどう扱うかを決める必要がある**

### 2.1 カラム定義

| 物理名 | 論理名 | 型 | 長さ | 必須 | 既定値 | 制約 |
|---|---|---|---|---|---|---|
| documentTypes.<kind>.<doc>.requiredSections[] | 必須節見出しの並び | VARCHAR | 制限を設けない | 必須 | なし | 本領域は全項目非該当（根拠: JSON 定義ファイルのため） |
| additionalAllowance.description | 追加許容の説明 | TEXT | 制限を設けない | 任意 | なし | 本領域は全項目非該当（根拠: JSON 定義ファイルのため） |
| additionalAllowance.projectFile | 対象プロジェクト側の受け口ファイルパス | VARCHAR | 制限を設けない | 任意 | なし | 本領域は全項目非該当（根拠: JSON 定義ファイルのため） |

### 2.2 索引

本定義はJSONファイルであり、データベースの索引に相当する仕組みを持ちません。

| 索引名 | 対象カラム | 一意性 |
|---|---|---|

### 2.3 外部キー

| カラム | 参照先のテーブル | 参照先のカラム | 削除時の動作 | 関連の種別 |
|---|---|---|---|---|
| 種別 | 参照先は外部キー（design-unit-layout の種別） | 参照先の定義ファイル | 指定しない | 多対一 |

### 2.4 整合性のチェック点

| 対象 | チェックの内容 | 実施の箇所 |
|---|---|---|
| 本定義を読む各スクリプト | JSON構文と必須項目の実在 | 各スクリプトの読み込み処理・自己テスト |

## §3 実装契約

**この節の位置づけ: 現行実装。本節は現在の実装の作りをそのまま記録した節です。作り直す前に、この節に書いた作りをどう扱うかを決める必要がある**

### 3.1 ファイルと関数

| ファイル | 関数 | 役割 | 参照先 |
|---|---|---|---|

### 3.2 型定義

| 型名 | フィールド | 型 | 参照先 |
|---|---|---|---|

### 3.3 依存

| 依存先 | 用途 | 参照先 |
|---|---|---|
| delivery-payload/references/design-doc-required-sections.json | 本定義の実体 | delivery-payload/references/design-doc-required-sections.json |
