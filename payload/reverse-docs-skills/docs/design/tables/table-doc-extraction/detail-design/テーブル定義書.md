---
# テーブル定義書テンプレート（unit_kind=table・1テーブル1枚）
table_key: doc-extraction
table_id: table-doc-extraction
table_name: doc-extraction
table_subkind: table
source_ref: delivery-payload/references/doc-extraction.json
unit_kind: table
status: authored
---

# 設計文書からの抽出定義 テーブル定義書

**この文書の位置づけ**（別の言語で作り直す場合は、まず「仕様」から読む）

- 仕様: §1 構成要素、§2 処理の定義、§3 ロジック、§4 入出力の値、§5 エラー処理、§6 関連資料
- 現行実装: 該当なし
- 仕様／現行実装（詳細は各節の太字1行を見る）: 該当なし

## §1 構成要素

**この節の位置づけ: 仕様**

処理を担う単位を列挙する。本定義は JSON の設定データであり、クラスを持たないため、JSON のトップレベル項目の単位で記す。

| 要素名 | 種別 | 可視性 | 所在 |
|---|---|---|---|
| doc-extraction.excluded.screen | 設定項目 | 公開 | delivery-payload/references/doc-extraction.json |
| doc-extraction.kinds.<kind>.unitRootKey | 設定項目 | 公開 | delivery-payload/references/doc-extraction.json |
| doc-extraction.kinds.<kind>.docFileName | 設定項目 | 公開 | delivery-payload/references/doc-extraction.json |
| doc-extraction.kinds.<kind>.mapping | 設定項目 | 公開 | delivery-payload/references/doc-extraction.json |
| doc-extraction.kinds.<kind>.identifier | 設定項目 | 公開 | delivery-payload/references/doc-extraction.json |
| doc-extraction.kinds.<kind>.unresolvable | 設定項目 | 公開 | delivery-payload/references/doc-extraction.json |
| doc-extraction.sectionAutomation.kinds | 設定項目 | 公開 | delivery-payload/references/doc-extraction.json |

## §2 処理の定義

**この節の位置づけ: 仕様**

| 要素名 | 処理の概要 | 前提条件 | 事後条件 | 送出する例外 |
|---|---|---|---|---|
| doc-extraction.json | 設計文書（詳細設計書のfrontmatter）から一覧マニフェストを機械的に組み立てる際の、種別ごとの抽出規則を定めた定義です。 | ファイルが正しいJSON構文であること | 読み込み元スクリプトが本定義の値へアクセスできる | JSON構文が誤っている場合に読み込み元でパースエラーが発生する |

## §3 ロジック

**この節の位置づけ: 仕様**

### 3.1 分岐

| キー | 条件 | 真の場合 | 偽の場合 |
|---|---|---|---|
| 分岐-なし | 設定データの定義であり手続きの分岐を持たない | 該当なし | 該当なし |

### 3.2 繰り返し

| キー | 対象 | 継続条件 | 打ち切りの条件 |
|---|---|---|---|
| 繰り返し-なし | 設定データの定義であり繰り返しを持たない | 該当なし | 該当なし |

### 3.3 判定表

| キー | 入力の組み合わせ | 結果 |
|---|---|---|
| 判定表-なし | 設定データの定義であり入力の組み合わせによる分岐を持たない | 該当なし |

## §4 入出力の値

**この節の位置づけ: 仕様**

<!-- 区分列は 入力 / 出力 に加えて 戻り値 も表す。戻り値を記録する場合は 区分=戻り値 の行を追加する -->

| 要素名 | 区分 | 名前 | 型 | 有効な範囲 | 空値の許容 | 初期値 | 桁と精度 |
|---|---|---|---|---|---|---|---|
| doc-extraction.excluded.screen | 入力 | excluded.screen | TEXT | 制限を設けない | 許容しない | なし | 本領域は全項目非該当（根拠: JSON 定義ファイルのため） |
| doc-extraction.kinds.<kind>.unitRootKey | 入力 | kinds.<kind>.unitRootKey | VARCHAR | 制限を設けない | 許容しない | なし | 本領域は全項目非該当（根拠: JSON 定義ファイルのため） |
| doc-extraction.kinds.<kind>.docFileName | 入力 | kinds.<kind>.docFileName | VARCHAR | 制限を設けない | 許容しない | なし | 本領域は全項目非該当（根拠: JSON 定義ファイルのため） |
| doc-extraction.kinds.<kind>.mapping | 入力 | kinds.<kind>.mapping | オブジェクト | 制限を設けない | 許容しない | なし | 本領域は全項目非該当（根拠: JSON 定義ファイルのため） |
| doc-extraction.kinds.<kind>.identifier | 入力 | kinds.<kind>.identifier | VARCHAR | 制限を設けない | 許容しない | なし | 本領域は全項目非該当（根拠: JSON 定義ファイルのため） |
| doc-extraction.kinds.<kind>.unresolvable | 入力 | kinds.<kind>.unresolvable | オブジェクト | 制限を設けない | 許容する | なし | 本領域は全項目非該当（根拠: JSON 定義ファイルのため） |
| doc-extraction.sectionAutomation.kinds | 入力 | sectionAutomation.kinds | オブジェクト | 制限を設けない | 許容する | なし | 本領域は全項目非該当（根拠: JSON 定義ファイルのため） |

## §5 エラー処理

**この節の位置づけ: 仕様**

| キー | 送出または捕捉の箇所 | エラーコード | リトライの判定 | ログの出力点 |
|---|---|---|---|---|
| 構文-不正 | 読み込み元スクリプトによるJSONパース時 | 読み込みライブラリが返す構文エラー | 再実行しない | 読み込み元スクリプトの標準エラー出力 |
| 必須項目-欠落 | 読み込み元スクリプトによる項目参照時 | 読み込み元スクリプトが定める既定の代替値またはエラー | 再実行しない | 読み込み元スクリプトの標準エラー出力 |

## §6 関連資料

**この節の位置づけ: 仕様**

他文書の所在はこの節へまとめる。確定できなかった事項は推測で埋めず、空欄のままとする。

| 資料 | パス | 本書との関係 |
|---|---|---|
| 論理データモデル | `../basic-design/論理データモデル.md` | 本書の業務レベルの対応文書 |
