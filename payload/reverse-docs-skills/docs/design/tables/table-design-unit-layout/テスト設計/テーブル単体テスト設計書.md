---
# テーブル単体テスト設計書テンプレート（unit_kind=table）
table_key: design-unit-layout
table_id: table-design-unit-layout
table_name: design-unit-layout
source_ref: delivery-payload/references/design-unit-layout.json
unit_kind: table
status: authored
---

# 設計単位の配置定義 テーブル単体テスト設計書

**この文書の位置づけ**（別の言語で作り直す場合は、まず「仕様」から読む）

- 仕様: §1 テスト観点、§2 テストケース一覧、§3 入力条件、§4 期待結果、§5 異常系、§6 境界値、§7 網羅基準、§8 前提条件と終了条件
- 現行実装: 該当なし
- 仕様／現行実装（詳細は各節の太字1行を見る）: 該当なし

本書は関数・メソッド単位の内部処理を検証します。観点と具体的なケースを分けず、1文書で管理します。対応する設計書は `テーブル定義書.md` です。

## 本書が検証するもの

結合は2段に分けて定義します。1つの設計単位の中で構成要素が結合した状態を内部結合、複数の設計単位をまたぐ連携を外部結合と呼びます。単位ごとの文書は内部結合までを担い、プロジェクト全体の文書が外部結合を担います。

| 段 | 検証する状態 | 対応する設計書 | 文書 |
|---|---|---|---|
| 単体 | 1つの関数・メソッドの入出力 | テーブル定義書 | テーブル単体テスト設計書（本書） |
| 内部結合 | 1つの定義の中で項目が結合した状態（外部の振る舞い） | 論理データモデル | テーブル結合テスト設計書 |
| 外部結合 | 複数の画面・機能・APIをまたぐ連携 | 複数の設計書 | docs/test-cases/結合テスト仕様書.md |

## テスト対象

対象は本定義を読み込む各スクリプトの、項目単位の読み取り処理とする。

## テストの粒度と自動化の方針

反復可能なケースは自動化し、実装後のテスト参照をケースに記録する。単位配下へ結合テスト文書を生成しない。

## 本書が扱わない範囲

複数の画面・機能・APIにまたがる結合テストはプロジェクト全体の `docs/test-cases/結合テスト仕様書.md` が担う。

## §1 テスト観点

**この節の位置づけ: 仕様**

§1は正常・異常・境界を含む全観点の一覧です。
§5と§6は、§1のうち異常と境界の観点を列ごとに詳しく述べる部分集合です。
§5と§6では新しい観点キーを作らず、§1と同じ観点キーを使います。
実行する全ケースは§2だけに記載し、§1の全観点へ1件以上のケースを対応させます。
実行するテストの件数は§2のデータ行数だけで数えます。

| キー | 観点 | 由来する基本設計書の章 | 由来する詳細設計書の章 |
|---|---|---|---|
| `design-unit-layout-label-必須` | 項目 kinds.<kind>.label の記入規則を守っていること（種別ラベル） | 該当なし | §1・§4 |
| `design-unit-layout-token-必須` | 項目 kinds.<kind>.token の記入規則を守っていること（識別トークン） | 該当なし | §1・§4 |
| `design-unit-layout-basic-必須` | 項目 kinds.<kind>.phases.basic[] の記入規則を守っていること（基本設計のファイル一覧） | 該当なし | §1・§4 |
| `design-unit-layout-detail-必須` | 項目 kinds.<kind>.phases.detail[] の記入規則を守っていること（詳細設計のファイル一覧） | 該当なし | §1・§4 |
| `design-unit-layout-test-必須` | 項目 kinds.<kind>.phases.test[] の記入規則を守っていること（テスト設計のファイル一覧） | 該当なし | §1・§4 |
| `design-unit-layout-testDocumentCount-任意` | 項目 kinds.<kind>.testDocumentCount の記入規則を守っていること（テスト文書数） | 該当なし | §1・§4 |
| `design-unit-layout-screenOnlyTestDocuments-任意` | 項目 kinds.<kind>.screenOnlyTestDocuments[] の記入規則を守っていること（画面のみのテスト文書名） | 該当なし | §1・§4 |
| `design-unit-layout-sourcePhase／targetPhase-必須` | 項目 generationRules.<rule>.sourcePhase／targetPhase の記入規則を守っていること（後追い生成の元phaseと対象phase） | 該当なし | §1・§4 |
| `design-unit-layout-excludedKinds-任意` | 項目 generationRules.<rule>.excludedKinds[] の記入規則を守っていること（後追い生成の対象外種別） | 該当なし | §1・§4 |
| `design-unit-layout-構文不正` | JSON構文が誤っている場合に読み込み元が中断すること | §5 | §5 |

## §2 テストケース一覧

**この節の位置づけ: 仕様**

| キー | 番号 | 対応観点キー | 入力・操作 | 期待結果 |
|---|---|---|---|---|
| `design-unit-layout-label-必須` | 1 | `design-unit-layout-label-必須` | delivery-payload/references/design-unit-layout.json の kinds.<kind>.label を読む | 種別ラベル に沿った値が得られる |
| `design-unit-layout-token-必須` | 2 | `design-unit-layout-token-必須` | delivery-payload/references/design-unit-layout.json の kinds.<kind>.token を読む | 識別トークン に沿った値が得られる |
| `design-unit-layout-basic-必須` | 3 | `design-unit-layout-basic-必須` | delivery-payload/references/design-unit-layout.json の kinds.<kind>.phases.basic[] を読む | 基本設計のファイル一覧 に沿った値が得られる |
| `design-unit-layout-detail-必須` | 4 | `design-unit-layout-detail-必須` | delivery-payload/references/design-unit-layout.json の kinds.<kind>.phases.detail[] を読む | 詳細設計のファイル一覧 に沿った値が得られる |
| `design-unit-layout-test-必須` | 5 | `design-unit-layout-test-必須` | delivery-payload/references/design-unit-layout.json の kinds.<kind>.phases.test[] を読む | テスト設計のファイル一覧 に沿った値が得られる |
| `design-unit-layout-testDocumentCount-任意` | 6 | `design-unit-layout-testDocumentCount-任意` | delivery-payload/references/design-unit-layout.json の kinds.<kind>.testDocumentCount を読む | テスト文書数 に沿った値が得られる |
| `design-unit-layout-screenOnlyTestDocuments-任意` | 7 | `design-unit-layout-screenOnlyTestDocuments-任意` | delivery-payload/references/design-unit-layout.json の kinds.<kind>.screenOnlyTestDocuments[] を読む | 画面のみのテスト文書名 に沿った値が得られる |
| `design-unit-layout-sourcePhase／targetPhase-必須` | 8 | `design-unit-layout-sourcePhase／targetPhase-必須` | delivery-payload/references/design-unit-layout.json の generationRules.<rule>.sourcePhase／targetPhase を読む | 後追い生成の元phaseと対象phase に沿った値が得られる |
| `design-unit-layout-excludedKinds-任意` | 9 | `design-unit-layout-excludedKinds-任意` | delivery-payload/references/design-unit-layout.json の generationRules.<rule>.excludedKinds[] を読む | 後追い生成の対象外種別 に沿った値が得られる |
| `design-unit-layout-構文不正` | 10 | `design-unit-layout-構文不正` | delivery-payload/references/design-unit-layout.json の構文を意図的に壊して読み込む | 読み込み元スクリプトが処理を中断しエラーを通知する |

## §3 入力条件

**この節の位置づけ: 仕様**

| キー | データ | 用途 |
|---|---|---|
| `design-unit-layout-label-必須` | delivery-payload/references/design-unit-layout.json の kinds.<kind>.label | 種別ラベル |
| `design-unit-layout-token-必須` | delivery-payload/references/design-unit-layout.json の kinds.<kind>.token | 識別トークン |
| `design-unit-layout-basic-必須` | delivery-payload/references/design-unit-layout.json の kinds.<kind>.phases.basic[] | 基本設計のファイル一覧 |
| `design-unit-layout-detail-必須` | delivery-payload/references/design-unit-layout.json の kinds.<kind>.phases.detail[] | 詳細設計のファイル一覧 |
| `design-unit-layout-test-必須` | delivery-payload/references/design-unit-layout.json の kinds.<kind>.phases.test[] | テスト設計のファイル一覧 |
| `design-unit-layout-testDocumentCount-任意` | delivery-payload/references/design-unit-layout.json の kinds.<kind>.testDocumentCount | テスト文書数 |
| `design-unit-layout-screenOnlyTestDocuments-任意` | delivery-payload/references/design-unit-layout.json の kinds.<kind>.screenOnlyTestDocuments[] | 画面のみのテスト文書名 |
| `design-unit-layout-sourcePhase／targetPhase-必須` | delivery-payload/references/design-unit-layout.json の generationRules.<rule>.sourcePhase／targetPhase | 後追い生成の元phaseと対象phase |
| `design-unit-layout-excludedKinds-任意` | delivery-payload/references/design-unit-layout.json の generationRules.<rule>.excludedKinds[] | 後追い生成の対象外種別 |

## §4 期待結果

**この節の位置づけ: 仕様**

期待結果は観測可能な値・状態・表示で記載する。各キーの必須項目は値が取得できること、任意項目は値が無くても読み込み元が既定値または対象外として扱えることを期待結果とする。

## §5 異常系

**この節の位置づけ: 仕様**

| 観点のキー | 条件 | 期待結果 |
|---|---|---|
| `design-unit-layout-構文不正` | JSON構文が誤っている | 読み込み元スクリプトが処理を中断しエラーを通知する |

## §6 境界値

**この節の位置づけ: 仕様**

| 観点のキー | 境界 | 期待結果 |
|---|---|---|
| `design-unit-layout-任意項目-未設定` | 任意項目が未設定（null または省略） | 読み込み元スクリプトが既定値または対象外として扱う |

## §7 網羅基準

**この節の位置づけ: 仕様**

設計書に定義された分岐・状態・制約をすべて観点へ対応付ける。本定義は分岐を持たないため、項目の必須・任意の区分と構文エラー時の扱いを網羅の基準とする。

## §8 前提条件と終了条件

**この節の位置づけ: 仕様**

| 区分 | 条件 |
|---|---|
| 前提条件 | delivery-payload/references/design-unit-layout.json が実在し、読み取り権限があること |
| 終了条件 | 読み込み元スクリプトが本定義の各項目へ正しくアクセスできる状態になっていること |
