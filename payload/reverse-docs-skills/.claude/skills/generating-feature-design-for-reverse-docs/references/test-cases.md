# generating-feature-design-for-reverse-docs テストケース

観点キーは連番禁止。内容を要約した意味語で付ける（番号からは情報を得られないため）。

| 観点キー | 前提 | 操作 | 期待結果 | 機械検証 |
|---|---|---|---|---|
| 引数-必須3件の検収 | 必須3引数（feature_manifest_path/output_dir/template_root）のいずれかが欠けている | Phase 1 Step 1を実行する | status=ERRORで停止する | 手動（合成フィクスチャを要する） |
| テンプレート参照-実在 | SKILL.mdが作成済みである | `grep -c 'リバース検証/機能/機能設計書.md' SKILL.md`を実行する | 1以上の値が返る | `grep -c 'リバース検証/機能/機能設計書.md' SKILL.md` が 1 以上 |
| 集約-共通処理の一箇所記述 | 複数の構成要素が同じ内部ヘルパーを呼ぶマニフェストがある | Phase 2 Step 2およびPhase 3 Step 2を実行する | 機能設計書の§7.1に1回だけ記述され、個別設計書へ重複記載されない | 手動（合成フィクスチャを要する） |
| 集約-共有状態の一箇所記述 | 複数の構成要素が同じ記憶域を介して連携するマニフェストがある | Phase 2 Step 2およびPhase 3 Step 2を実行する | §6.2に1回だけ記述される | 手動（合成フィクスチャを要する） |
| 章完備-11章と付録 | 設計書が生成済みである | 生成した設計書に対し`grep -c '^## §'`を実行する | 生成した設計書に §1 から §11 が存在し、付録 A・B も存在する | 検査1（`grep -c '^## §'` が 11、かつ `grep -c '^## 関連資料\|^## 章マップ'` が 2） |
| 全章充足-空章の要確認回送 | 記入できなかった章がある設計書がある | Phase 4 検査2を実行する | 記入できなかった章に対応する項目が§11要確認事項へ回っている。どちらも満たさない空章があれば不合格となる | 検査2 |
| プレースホルダ-残存なし | テンプレートを展開した設計書がある | Phase 4 検査4を実行する | `FEATUREKEY`等のプレースホルダが本文に残っていないことを確認し、残存があれば不合格となる | 検査4 |
| 個別設計書不在-中断しない | 構成要素の個別設計書が1枚も無いマニフェストがある | Phase 2 Step 3およびPhase 3を実行する | status=ERRORにならず、構成要素一覧と要確認事項だけを持つ設計書が生成される | 手動（合成フィクスチャを要する） |
| 出力先-識別子のフォールバック | `unitId` が空の機能がある | Phase 3 Step 1を実行する | `status=DONE`となり、`unitKey` 由来のディレクトリへ出力される | 手動（合成フィクスチャを要する） |
| 出力先-識別子の優先順 | `unitId` が埋まった機能がある | Phase 3 Step 1を実行する | 従来どおり `unitId` 由来のディレクトリへ出力される | 手動（合成フィクスチャを要する） |
| 出力先-識別子双方空で中断 | `unitId` と `unitKey` の双方が空の機能がある | Phase 3 Step 1を実行する | `status=ERROR`となる | 手動（合成フィクスチャを要する） |

## 機械検証との対応

- 機械検証が「手動」の行は、guide.html の検証状況へ手動確認を記録する
- self-test に対応ケースを追加したら、この表の機械検証列を同じコミットで更新する
