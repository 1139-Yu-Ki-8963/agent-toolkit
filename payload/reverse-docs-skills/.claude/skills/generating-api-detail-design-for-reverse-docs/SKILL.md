---
name: generating-api-detail-design-for-reverse-docs
日本語名: 接続窓口の詳細設計書を作る
description: "接続窓口ごとに実装コードから詳細設計書を作る。"
invocation: generating-api-detail-design-for-reverse-docs
type: transform
allowed-tools: [AskUserQuestion, Bash, Glob, Grep, Read, TaskCreate, TaskUpdate, Write]
---

## いつ使うか

接続窓口の詳細設計書を作りたいとき、呼び出し口ごとの設計書を書き起こしたいとき、接続窓口の仕様を書き起こしたいとき。

## いつ使わないか

接続窓口の一覧を作るとき（→generating-api-list-for-reverse-docs が担当）、複数の機能をまとめた集約設計書を作るとき、画面の詳細設計書を作るとき（→generating-reverse-detailed-design が担当）。

# 正本: reverse-docs-skills

本スキルが生成する納品物は顧客提示の文書である。自由記述の本文（要約・説明文）は敬体（です・ます）で書く。記入規則・検証記録・作業記録は常体でもよい（`delivery-payload/references/設計書様式.md` §8）。

# API詳細設計書の生成スキル

工程全体は orchestrating-ai-development-setup が案内する。本スキルは API 1 本ごとの詳細設計書の執筆のみを担い、単独起動できる。起動引数は api_manifest_path・source_dir・output_dir・template_root の 4 つで、これらを渡せば動く。`unit_kind` は **api 固定** であり、引数では受け取らない。

## 情報源と原本を見ない検証の扱い

画面詳細設計書（generating-reverse-detailed-design）は確定済み facts だけを情報源とし、原本コードの読解を禁じる。本スキルはこの制約を採らない。API 単位の facts を供給する経路が存在しないためである。

したがって本スキルは **原本だけを読み取る種類** である。情報源は次の 2 つに限る。

1. API 一覧の拡張マニフェスト（`api-manifest.ext.json`。`method`・`authRequired`・`ioSummary` を含む。`authRequired` が欠落している場合、それはルーター登録時に一括付与される認証依存（`include_router` の `dependencies` 引数等）を抽出器が検出できないことによる欠落であり「認証不要」を意味しない。原本のルーター登録箇所を確認してから記述する）
2. マニフェストの `files` が指す原本コード

この帰結として、本スキルが生成した設計書は往復検証（設計書だけからコードを再生成して原本と突き合わせる検証）の対象にならない。設計書と原本が同じ情報源を共有しており、突合が独立した検証にならないためである。品質は §検証 の 8 つの機械検査で担保する。

原本から確定できない型・制約・既定値を推測で埋めない原則は維持する。ただし、テンプレートの 12.5「設計判断とその理由」に限り、実装構造から導いた選択理由を `推定（実装構造）` と明示して記録できる。推定を観測事実として扱わない。

原本コードの位置は納品物の本文へ記録しない。API詳細設計書の本文には対象コードへの参照や対象コードのファイル名を書かず、行番号、`file:line`、ファイル名を省いた括弧書き、文中の行番号も、表・疑似コード・説明文のいずれにも書かない。疑似コードの行末へ対象コードの位置を注記することも禁止する。参照先を記入する場合は、本書の節番号または関数単位の契約への文書内参照に限定する。前付けの `source_ref` に代表ファイルのリポジトリルート相対パス1件を記録する場合だけを例外とする。

API単体テスト設計書と機能設計書を含むすべての納品物で、対象コードの位置を本文や別資料へ記録しない。バッチ詳細設計書、帳票詳細設計書、外部連携詳細設計書など、コードを直接読んで作る設計書にも同じ非記録原則を適用する。各前付けで定義された `source_ref` は、この非記録原則の対象外とする。

## 言語プロファイル

対象言語ごとに、呼び出し境界の読み取り方を定めたプロファイルを持つ。Phase 1 はプロファイルを選択し、どのプロファイルにも該当しない言語だけを中断する。「未対応なら中断」という判定は採らない。

実装済みのプロファイルは次の 2 つである。

| プロファイル | 対象 | 拡張子 | エンドポイントの所在 | 引数の所在 | 応答の所在 |
|---|---|---|---|---|---|
| `python` | FastAPI 等の HTTP フレームワーク | `.py` | デコレータ（`@app.get` 等）とルーター登録 | 関数シグネチャの型注釈と `Query`・`Path`・`Body` の宣言 | 戻り値の型注釈と `response_model` |
| `perl` | HTTP エンドポイントを持つ Perl 実装 | `.pl`・`.pm`・`.cgi` | ルーティング定義表（ディスパッチ表・ルートのハッシュ等）とサブルーチン名の対応 | `param` による読み出し箇所と、引数ハッシュの展開箇所 | 応答を組み立てる箇所（描画呼び出し・標準出力・ハッシュの返却） |

### プロファイルの選択

`api-manifest.ext.json` の `units[].files` の拡張子を集計し、最多の拡張子が属するプロファイルを選ぶ。同数の場合は、対象ユニット数の多い側のプロファイルを選ぶ。それでも決まらない場合は AskUserQuestion で確認する。

どのプロファイルの拡張子にも該当しない場合だけ `status=ERROR` を返して中断し、hint に「該当する言語プロファイルなし」と、集計した拡張子の内訳を記録する。

選んだプロファイルは返却ブロックの `language_profile` に記録する。

### perl プロファイルの読み取り規約

- エンドポイントとサブルーチンの対応は、ルーティング定義表を正とする。サブルーチン名からの推測で対応を決めない
- 1 つのモジュールに複数のエンドポイントが同居する構成を前提とする。ファイル単位ではなくサブルーチン単位で §9 実装契約を書く
- 引数は宣言的な型を持たないため、読み出し箇所から名前を採る。型は原本から確定できる場合のみ書き、確定できない型は要確認事項一覧へ記録する
- 共通処理を担う内部ヘルパーが複数のエンドポイントから呼ばれる場合、§4.2 内部呼び出しへ記録する。ヘルパー自体を独立したエンドポイントとして数えない
- `files` が入口モジュール 1 本のみを指し、その中にディスパッチ表がある場合は、表から当該エンドポイントのハンドラを引き、ハンドラの実装モジュールまで読む。入口モジュールの記述だけで §2 から §9 を埋めない
- ハンドラの実装モジュールが `files` に含まれていない場合も読解対象にする。あわせて `files` の不足を要確認事項一覧へ記録し、一覧生成側の再実行が要ることを残す

### 設計判断の読み取り規約

- コードコメントを `観測（コードコメント）` に分類できるのは、コメントが現在の実装上の選択と理由を因果関係で直接結び付け、そのコメントが説明する処理を特定できる場合だけとする
- TODO、過去の経緯だけを述べるコメント、却下済み案の説明、別処理を説明する近接コメントは観測から除外する
- 観測として記録するのは「原本コメントがその理由を述べている」という事実である。コメントの主張が現在も妥当であるとは断定しない
- コメントに理由が無く、実装構造から理由を導ける場合は `推定（実装構造）` とする。確からしさは `medium` または `low` に限り、推定に `high` を使わない
- 選ばなかった選択肢と不採用理由を原本から読めない場合は、両欄を `不明（実装に記述なし）` とする。もっともらしい代替案を補わない
- 設計判断または選択理由を観測も推定もできない場合は 12.5 に行を作らず、要確認事項一覧へ移す
- 原本コードの位置は設計書本文にも別資料にも書かない。前付けの `source_ref` だけは代表ファイルのリポジトリルート相対パス1件を記録する

## 使用タイミング

- API 一覧が生成済みで、エンドポイント単位の詳細設計書を作りたいとき
- 前提: `<output_dir>/<manifestsRoot>/api-manifest.ext.json` が生成済みであること（`manifestsRoot` は output-layout の物理配置キー。既定値 `docs/manifests`）
- 起動引数:
  - `api_manifest_path`（必須。API 一覧の拡張マニフェストの絶対パス）
  - `source_dir`（必須。原本コードのルート）
  - `output_dir`（必須。設計書の出力先ルート）
  - `template_root`（必須。テンプレート群のルート。実体は `delivery-payload/templates/`）
  - `unit_keys`（任意。対象を絞る場合のマニフェスト上のキー配列。省略時は全件）
  - `common_docs_root`（任意。プロジェクト共通文書のルート。メッセージ定義との突合に使う）

## 出力

| 項目 | API詳細設計書 | API単体テスト設計書 |
|---|---|---|
| 出力フォルダ | `<output_dir>/<apiUnitRoot>/api-<API 識別子>/<unitPhaseDirNames.detail>/` | `<output_dir>/<apiUnitRoot>/api-<API 識別子>/<unitTestDesignDir>/` |
| 出力ファイル | `API詳細設計書.md` | `API単体テスト設計書.md` |
| テンプレート | `<template_root>/リバース検証/API/API詳細設計書.md` | `<template_root>/リバース検証/API/API単体テスト設計書.md` |

`apiUnitRoot` は output-layout の物理配置キー（既定値 `docs/design/apis`）。表示用 `kindLabels.api` は path に使わない。`unitPhaseDirNames.basic`・`unitPhaseDirNames.detail` は output-layout の `unitPhaseDirNames.basic`・`.detail` の値（既定値 `basic-design`・`detail-design`）であり、scaffold-design-unit.sh が実際に展開する配置フォルダ名と一致させる（1-210）。

API単位のテスト文書は `<unitTestDesignDir>/API結合テスト設計書.md`（外部契約）と `API単体テスト設計書.md`（関数・メソッド単位）の2件である。本スキルが執筆するのは後者だけとし、前者はAPI基本設計スキルが基本設計書を根拠に執筆する。単位配下に結合テスト文書は生成しない。

テンプレートの各章は表の列見出しと記入規則だけを持つ。値の欄は執筆時に埋める。

### テンプレート正本契約

API詳細設計書は、テンプレートを出力先へ複製してから値だけを埋める。見出しや表を記憶から組み立て直さない。現行テンプレートの `##` 見出しは、次の14見出しをこの順序で持つ。課題1-201記載時の「§13 要確認事項一覧・関連資料（付録A）」は、課題1-223で「§13 関連資料」へ統合済みであるため、現行テンプレートを正とする。

1. `§1 API概要`
2. `§2 リクエスト`
3. `§3 レスポンス`
4. `§4 処理フロー`
5. `§5 ロジック`
6. `§6 疑似コード`
7. `§7 データアクセス`
8. `§8 データ定義`
9. `§9 業務ルールとバリデーション`
10. `§10 エラー`
11. `§11 非機能`
12. `§12 実装契約`
13. `§13 関連資料`
14. 前付けの `chapter_map`（章の役割キーと §番号の対応。本文の節ではなく機械向けの対応表）

表の列見出しは次の契約をそのまま保つ。列を省略・統合・改名・並べ替えない。該当データが無い場合も列見出しと区切り行を残し、データ行を0件にする。

| 小節 | 列見出し（左から右） |
|---|---|
| 1.1 基本情報 | 項目 / 値 |
| 2.1 パスパラメータ | 名前 / 型 / 必須 / 有効な範囲 / NULL許容 / 初期値 / 桁と精度 / 制約 |
| 2.2 クエリパラメータ | 名前 / 型 / 必須 / 有効な範囲 / NULL許容 / 既定値 / 桁と精度 / 制約 |
| 2.3 ヘッダ | 名前 / 必須 / 有効な範囲 / NULL許容 / 初期値 / 桁と精度 / 用途 |
| 2.4 ボディ | 項目 / 型 / 必須 / 有効な範囲 / NULL許容 / 初期値 / 桁と精度 / 制約 |
| 3.1 正常系の構造 | 項目 / 型 / 常に存在 / 有効な範囲 / NULL許容 / 初期値 / 桁と精度 / 意味 |
| 3.2 ステータスコード一覧 | コード / 発生条件 / 本文の形 |
| 3.3 空・境界の扱い | 状況 / 返す内容 |
| 4.1 処理順序 | 順 / 処理 / 呼び出す関数 |
| 4.2 内部呼び出し | 呼び出し先 / 引数 / 戻り値 |
| 4.3 外部呼び出し | 呼び出し先 / 契機 / 失敗時の扱い |
| 5.1 分岐 | キー / 条件 / 真の場合 / 偽の場合 |
| 5.2 繰り返し | キー / 対象 / 継続条件 / 打ち切りの条件 |
| 5.3 判定表 | キー / 入力の組み合わせ / 結果 |
| 7.1 参照テーブル | テーブル / 取得条件 |
| 7.2 更新テーブル | テーブル / 操作 / 更新条件 |
| 7.3 トランザクション境界 | 範囲 / 確定の契機 / 巻き戻しの条件 |
| 8.1 内部データ構造 | 名前 / 型 / 構成要素 / 組み立てる箇所 / 用途 / 有効な範囲 / 寿命 |
| 9.1 入力検査 | 対象 / 検査内容 / 違反時の応答 |
| 9.2 業務判断 | キー / 判断内容 / 判断材料 |
| 9.3 権限 | 利用者区分 / 可否 / 判定材料 |
| 10.1 エラー一覧 | キー / 発生条件 / 応答コード / 利用者に見える結果 |
| 10.2 メッセージ定義との対応 | メッセージID / 本文 / 使う箇所 |
| 11.1 件数と量の制限 | 対象 / 上限 |
| 11.2 タイムアウトと再試行 | 対象 / 方針 |
| 11.3 冪等性 | 観点 / 性質 |
| 12.1 関数と役割 | 関数 / 役割 |
| 12.2 関数単位の契約 | 関数 / 公開範囲 / 引数 / 戻り値 / 前提条件 / 副作用 / 例外・エラー |
| 12.3 型定義 | 型名 / フィールド / 型 |
| 12.4 依存 | 依存先 / 用途 |
| 12.5 設計判断とその理由 | 判断キー / 設計判断 / 選択理由 / 記述区分 / 判断材料 / 選ばなかった選択肢 / 不採用理由 / 確からしさ |
| §13 関連資料 | 資料 / パス / 本書との関係 |
| 前付け `chapter_map` | 章の役割キー / §（本文には表示しない） |

`<API 識別子>` はマニフェストの `unitId` を使う。`unitId` が空（`null` または空文字）の場合は `unitKey` を使う。両方が空の場合だけ当該ユニットを生成せず、`status=ERROR` で中断して hint に「出力先の識別子が確定しない」と当該ユニットの `identifier` を記録する。

実マニフェストでは `unitId` が全件空のことがある。一覧生成側が `unitId` を非必須フィールドとして扱い、実装にも `null` を入れる分岐があるためである。合成フィクスチャでは埋まっていることが多く、この状態は実データでしか現れない。

`unitKey` をディレクトリ名に使う場合、パス区切り（`/`）と制御文字をそのまま使わない。含む場合はハイフンへ置き換え、実行結果へ置換前後を記録する。

## 進捗管理（必須手順）

スキル開始時に `TaskCreate` で Phase 1〜4 のタスクを登録する。各 Phase 開始時に該当タスクを `in_progress` に、完了時に `completed` へ `TaskUpdate` で更新する。Phase 4 から Phase 3 へ差し戻す場合は該当タスクを `in_progress` に戻す。実行環境に TaskCreate/TaskUpdate が存在しない場合は、出力先ディレクトリ内のタスク台帳ファイル（`task-ledger.md`）で同等の Phase 遷移記録を代替する。

## Phase 1: 起動引数の確認と対象確定

## Step 1-1: 起動引数の確認と対象確定

**使用ツール**: Bash, Read, AskUserQuestion, TaskCreate, TaskUpdate

- **Step 1**: 必須 4 引数の実在を確認する（`test -f` / `test -d`）。いずれかが欠ける場合は `status=ERROR` で停止する。完了条件: 4 引数すべてが実在する
- **Step 2**: `api_manifest_path` を Read し、`unitKind` が `api` であること、`units` が 1 件以上あることを確認する。完了条件: マニフェストが確認済み
- **Step 3**: 言語プロファイルを選択する。「言語プロファイル」節の選択規則に従い `python` または `perl` を確定する。どちらの拡張子にも該当しない場合だけ `status=ERROR` で停止し、hint に「該当する言語プロファイルなし」と拡張子の内訳を記録する。完了条件: プロファイルが 1 つ確定している、または該当なしとして停止している
- **Step 4**: 対象ユニットを確定する。`unit_keys` が渡されていればその集合、無ければ次の規則で選ぶ。`kind` が `endpoint` または `dispatch-entry` のユニットを対象に含める。`entrypoint` は、同じ実行ファイルに `dispatch-entry` が 1 件でも存在する場合は対象外とし、存在しない場合だけ対象に含める。`middleware` と `unresolved` は常に対象外とする。完了条件: 対象ユニットの一覧が確定済み

**完了**: 4 引数が実在し、マニフェストが確認済みで、言語プロファイルが確定し、対象ユニットが確定している

## Phase 2: 原本読解

## Step 2-1: 実装契約の機械抽出

**使用ツール**: Bash

対象ユニット全件のループに入る前に、1 回だけ実行する。

```bash
bash generation-engine/scripts/extract/build-implementation-contract-section.sh \
  <api_manifest_path> <source_dir> \
  <output_dir>/<manifestsRoot>/api-manifest.impl-contract.json
```

命名規約の定義（接頭辞と型の対応表）が対象プロジェクトにあれば `--naming-convention <path>` を足す。無ければ足さない。足さない場合、型はすべて「判定不能」になる。

出力に `detectionSummary.diagnostics.implementationContract` がある。ここに抽出できた割合（`readableArguments`・`readableReturns`）が記録される。この割合が既定の閾値（80%）を下回る場合、Step 2-2 の Step 2 で原本を読解して補う対象が多いことを示す。

**完了**: 拡張マニフェスト（`api-manifest.impl-contract.json`）が生成済み

## Step 2-2: ユニットごとの原本読解

**使用ツール**: Read, Grep, Glob

対象ユニット 1 件につき次を行う。

- **Step 1**: マニフェストの `files` が指すファイルを Read する。`sourceFile` を起点に、そこから呼ばれる関数の定義元まで追う。追跡は 2 段までとし、それ以上は要確認事項一覧へ記録する。`files` と `sourceFile` が入口モジュール 1 本へ集約されている場合は、そのモジュール内のディスパッチ表から当該エンドポイントのハンドラを引く。引いたハンドラの実装モジュールを読解対象へ加える。この解決は追跡の 2 段とは別枠で数え、ハンドラへ到達した地点を 1 段目とする。完了条件: 読解対象のファイル一覧が確定済み
- **Step 2**: テンプレートの §2 から §11 と §12.3〜§12.5 に対応する事実を原本から抽出する。§12.1・§12.2 は、Step 2-1 の拡張マニフェストが持つ当該ユニットの `implementationContract` を第一の情報源として記入する。`implementationContract` が付いていない関数、または `readableArguments`・`readableReturn` が偽の関数だけ、原本を読解して補う。読み取りは Phase 1 で選んだプロファイルの読み取り規約に従う。対象コードの位置は記入材料として保持しない。確定できない項目は空欄のまま要確認事項一覧へ回す。完了条件: 全章の本文材料が揃っている
- **Step 3**: メッセージ定義書が `common_docs_root` にあれば、§10.2 のメッセージ ID を突合する。無ければ §10.2 は空欄とし、その旨を要確認事項一覧へ記録する。完了条件: メッセージの突合が済んでいる
- **Step 4**: 「設計判断の読み取り規約」に従い、現在の実装上の選択と理由を直接結び付けるコードコメント、および理由を推定できる実装構造を抽出する。TODO・過去の経緯・却下済み案・別処理のコメントは除外する。原本位置は記録しない。完了条件: 12.5 の記入材料が区分付きで揃っている、または要確認事項が確定している

**完了**: 対象ユニット全件について、本文材料と要確認事項が揃っている

## Phase 3: 執筆

### テンプレートの配置

執筆の前に、テンプレートを出力先へ配置する。手作業で Read と Write を行わず、次のスクリプトを呼ぶ。

```bash
bash generation-engine/scripts/scaffold-design-unit.sh api detail <output_dir> <識別子> <表示名> <template_root>
bash generation-engine/scripts/scaffold-design-unit.sh api test <output_dir> <識別子> <表示名> <template_root>
```

`<kind>` と `<phase>` は本スキルの担当に固定する。配置済みのファイルへ記入する形で執筆する。

配置後は「テンプレート正本契約」の全見出し・全表の列見出し・区切り行を固定部分として扱う。執筆で変更してよいのは、プレースホルダ、説明本文、表のデータ行だけである。値が不明でも列自体を削らず、空欄または要確認事項一覧への回送で表現する。

API単体テスト設計書では、§1を正常・異常・境界を含む全観点の一覧とする。
§5と§6には、§1のうち異常・境界の観点を§1と同じ観点キーで詳述する。
§5と§6では新しいケースを定義せず、§1の全観点に対応する実行ケースを§2だけへ記載する。
実行するテストの件数は、§2のデータ行数だけで数える。

配置に失敗した場合は執筆へ進まず、`status=ERROR` を返す。

### 表題の形式と前付けとの関係

本文冒頭の `# ` 見出し（表題）は「<対象名> <役割名>」の形式とする。役割名はファイル名から拡張子を除いた文字列と完全に一致させ、記入時に書き換えない。対象名（表示名）トークンは雛形の展開時に自動置換済みであり、記入時に対象名部分を書き換えない。対象名の末尾が役割名の先頭と同じ語で終わる場合は、対象名側から重複する語を落とし、同じ語が2回続けて現れないようにする（例: 対象名「注文API」＋ 役割名「API詳細設計書」→ 表題「注文API詳細設計書」）。

前付け（frontmatter）の識別名（`<種別>KEY`・`<種別>ID` 等）と本文の表題は、一致を求めない。前付けはマニフェストの識別子をそのまま使う機械可読の値であり、表題は対象の表示名を使う人が読む見出しである。両者が異なる文字列になっても構わない。

API詳細設計書の前付けは、次の 7 鍵を canonical な完全な集合とする。

| 鍵 | 値の決め方 |
|---|---|
| `api_key` | マニフェストの `unitKey` を使う |
| `api_id` | マニフェストの `unitId` を使う。空の場合も鍵を残して値を空欄とし、要確認事項一覧へ未確定事項として記録する |
| `method` | マニフェストの `method` を使う |
| `path` | マニフェストに独立した `path` があれば使う。無ければ `identifier` の先頭にある HTTP メソッドとそれに続く空白を除き、後半を path として決定的に分離する |
| `feature_key` | マニフェストに `featureKey` があれば使う。無ければ鍵を残して値を空欄とし、要確認事項一覧へ未確定事項として記録する |
| `source_ref` | マニフェストの `sourceFile` を使う |
| `unit_kind` | 固定値 `api` を使う |

この集合へ `title`・`login_required`・`category`・`auth`・`note` を追加せず、生成物にも残さない。

#### source_ref の代表ファイル選定契約

- 1ファイルの場合: `source_ref` は、該当実装ファイルのリポジトリルート相対パス1件とする。
- 複数ファイルの場合: HTTP要求を最初に受ける代表ファイルを、ルーター、コントローラー、ハンドラーの順で1件選ぶ。同じ優先順位の候補が複数あるときは、マニフェストの `sourceFile` と一致するものを選ぶ。代表ファイルを起点に原本の呼び出し関係をたどり、複数モジュールの関数契約は本文へ記録する。
- 実装が0件の場合: `source_ref` の鍵を残して値を空欄にする。未検出の事実は、確認事項質問票（`confirmation-survey`）の入力へ記録する。`confirmation-item-ledger` を納品物として復活させない。

いずれの場合も絶対パスと行番号を含めない。

`delivery-payload/references/doc-extraction.json` の宣言に従い、実際の読取処理である `generation-engine/scripts/portal-input/build-manifests-from-docs.sh` は `api_key`・`api_id`・`source_ref`・`method`・`path` を読む。少なくとも `api_key`・`source_ref`・`method`・`path` が揃った文書を `kind=endpoint` と判定する。`unitId`（`api_id`）は欠落時に代替可能であるため、この判定条件には含めない。`generation-engine/scripts/build-portal.sh --build-manifests-from-docs` がこの読取経路を起動する。`unit_kind` と `feature_key` は現行の抽出器が参照する鍵ではないが、canonical 集合の一部として保持する。

## Step 3-1: テンプレートの展開と記入

**使用ツール**: Bash, Read, Write

- **Step 1**: `<template_root>/リバース検証/API/API詳細設計書.md` を Read する。読み込んだ内容を `<output_dir>/<apiUnitRoot>/api-<API 識別子>/<unitPhaseDirNames.detail>/API詳細設計書.md` へ書き出し、以後はその複製へ記入する。見出し・表の列見出し・区切り行を再構築しない。`<API 識別子>` の決め方は「出力」節の規約に従う。中間ディレクトリが無ければ作成する。frontmatter は canonical な 7 鍵だけを保持し、`APIKEY` は `unitKey`、`APIID` は `unitId`、`METHOD` は `method`、`FEATUREKEY` は `featureKey`、`SOURCEREF` は `sourceFile` で置換する。`PATH` はマニフェストに独立した `path` があればその値で置換し、無ければ `identifier` の先頭にある HTTP メソッドとそれに続く空白を除いた後半で置換する。`unitId` または `featureKey` が無い場合も対応する鍵を残して値を空欄とし、その旨を要確認事項一覧へ記録する。`unit_kind` は `api` のまま保持する。完了条件: 全対象ユニットの設計書が配置済み
- **Step 2**: Phase 2 で得た本文材料を各章へ記入する。§12.1・§12.2 は Step 2-1 の拡張マニフェストが持つ `subroutines`・`functions` 配列の値をそのまま表へ写す。ただし共通基盤の関数（`基盤設計.md` §7 に載る、複数の設計単位から呼ばれる関数）は §12.2 へ写さず、§12.4 依存へ「基盤設計 共通関数の契約 <関数名>」と本 API が渡す値・受け取る値の使い方だけを書く（契約は基盤設計が唯一の正。改善課題1-275）。読解で補った関数だけ、読解の結果を記入する。12.5 以外は原本に無い値を書かず、推測で埋めない。12.5 は「設計判断の読み取り規約」に従い、`観測（コードコメント）` と `推定（実装構造）` を区別する。選ばなかった選択肢または不採用理由を読めない場合は `不明（実装に記述なし）` とする。表、疑似コード、説明文を含む本文全体に、対象コードへの参照や対象コードのファイル名を記入しない。行番号、`file:line`、ファイル名を省いた括弧書き、文中の行番号も記入しない。疑似コード行末の位置注記も禁止する。完了条件: §1 から §12 と 12.5 がコード位置を含まず記入済み
- **Step 3**: 空欄のまま残した項目を要確認事項一覧へ列挙する。対象コードのファイルパスと行番号は記録しない。完了条件: 未確定事項が要確認事項一覧へ集約済み
- **Step 4**: API詳細設計書の§12.2 関数単位の契約・§5 ロジック・§10 エラーから関数・メソッド単位の観点を起こし、`<unitTestDesignDir>/API単体テスト設計書.md` へ記入する。各観点とケースには関数・メソッド名と由来する詳細設計書の章を記録する。HTTPステータスを検証する外部契約の観点はAPI結合テスト設計書へ残し、本書へ混在させない。完了条件: 関数・メソッド単位の観点が全対象関数を覆う

**完了**: 全対象ユニットのAPI詳細設計書とAPI単体テスト設計書が記入済みで、未確定事項が要確認事項一覧へ集約されている

## Phase 4: 検証

## Step 4-1: 機械検査

**使用ツール**: Bash

次の 8 つを実行する。1 つでも不合格なら Phase 3 へ戻る。上限 3 回で収束しなければ `status=ERROR` とする。

- **検査1 章の完備**: 生成した各設計書に §1 から §13 が存在し、あわせて前付けに `chapter_map` が存在する。§ の章は `grep -c '^## §'` が 13 を返すことで確認し、§13 が関連資料であることと、`grep -c '^chapter_map:'` が 1 を返し `grep -c '^## 章マップ'` が 0 を返すことを確認する
- **検査2 詳細設計記述規約**: `node generation-engine/scripts/tests/check-detailed-design-conventions.cjs --self-test` を実行し、テンプレート正本に対象コードへの参照・対象コードのファイル名・行番号がなく、参照先が文書内参照だけに限定されていることを確認する。終了コード0だけを合格とする
- **検査3 プレースホルダの残存**: `APIKEY`・`APIID`・`METHOD`・`PATH`・`FEATUREKEY`・`SOURCEREF` が本文に残っていないことを確認する
- **検査4 配置と前付けの検査**: `bash generation-engine/scripts/tests/check-detail-design-frontmatter-keys.sh --check-api-template` と `bash generation-engine/scripts/tests/check-detail-design-frontmatter-keys.sh --check-source-ref-contract` を実行する。続けて `bash generation-engine/scripts/scaffold-design-unit.sh --verify api detail <output_dir> <識別子>` と `bash generation-engine/scripts/scaffold-design-unit.sh --verify api test <output_dir> <識別子>` を実行し、詳細設計書・API結合テスト設計書・API単体テスト設計書の必須ファイル・トークン・章・配置を確認する。詳細設計書の frontmatter は `delivery-payload/references/detail-design-frontmatter-keys.json` の鍵集合と欠落・余剰なく完全一致しなければ生成完了として扱わない
- **検査5 設計判断と本文位置情報の検査**: `node generation-engine/scripts/validate-api-design-decisions.mjs <生成したAPI詳細設計書...>` を実行する。12.5「設計判断とその理由」の存在、記述区分、推定の確からしさ、原本から読めない選択肢の表記を検査する。本文の表・疑似コード・説明文も検査する。対象コードのファイル名、行番号、`file:line`、ファイル名を省いた括弧書き、文中の行番号が0件でなければならない。終了コード0だけを合格とする。データ行を作れない判断は要確認事項一覧へ記録する
- **検査6 単体テスト粒度の検査**: API単体テスト設計書の§1と§2の全データ行に関数・メソッド名があり、`HTTPステータス` が0件であることを確認する
- **検査7 単体テスト配置の検査**: API単体テスト設計書が`<unitTestDesignDir>`にだけ存在し、`<unitPhaseDirNames.basic>/`と`<unitPhaseDirNames.detail>/`にテスト設計書が存在しないことを確認する
- **検査8 テンプレート一致の検査**: `bash generation-engine/scripts/tests/check-design-doc-section-consistency.sh <output_dir>` を実行する。API詳細設計書について、現行テンプレートの全 `##` 見出しの名前・順序・件数と、全表の所属節・小節・列見出し・順序・件数が完全一致することを確認する。§2 リクエストと §3 レスポンスも例外にせず、列の省略を不合格にする

**完了**: 8 検査すべてが合格している

## 返却

本スキルは orchestrating-ai-development-setup の契約に準拠する。完了時に以下を返す。

- `status`: `DONE | ERROR`
- `artifacts`: 生成または更新した API詳細設計書.md・API単体テスト設計書.md のパスの配列
- `unit_kind`: `api`（固定値）
- `language_profile`: 選択した言語プロファイル（`python` または `perl`）
- `authored_count`: 生成した設計書の枚数
- `unresolved_count`: 全単位の要確認事項一覧に記録された確定できなかった事項の合計件数

## ツールリファレンス

| ツール | 用途 |
|---|---|
| Read | マニフェスト・原本コード・テンプレート・メッセージ定義書の参照 |
| Grep/Glob | 呼び出し先の定義元の追跡、プレースホルダ残存の確認 |
| Bash | 対象コード位置の非記録と章の完備の確認 |
| Write | 設計書の書き出し |
| AskUserQuestion | Phase 1 の言語判定が割れた場合の確認 |
| TaskCreate/TaskUpdate | Phase 1〜4 の進捗管理 |

## 重要な注意事項

- 原本に無い値を書かない。型・制約・既定値を推測で補わない
- 12.5 の推定は設計理由だけを対象とする。仕様値、型、制約、既定値を推定する例外ではない
- 根拠のない項目は空欄のままとし、要確認事項一覧へ回す。空欄を埋めるために推測しない
- 詳細設計書本文にコード位置を書かない。対象コードのファイル名、行番号、`file:line`、ファイル名を省いた括弧書き、文中の行番号は、表・疑似コード・説明文のすべてで禁止する
- 疑似コードの各行末へ対象コードの位置をコメントや括弧で注記しない
- 本スキルの成果物は API詳細設計書.md、API単体テスト設計書.md。一覧・マニフェストの生成・更新は行わない
- 往復検証の対象外である。この設計書だけからコードを再生成して原本と突き合わせる検証は行わない
- `middleware` と `unresolved` のユニットは対象外とする。呼び出し境界として確定していないものに実装契約は書けない
- ディスパッチ表を持つ構成では、業務単位は `dispatch-entry` として記録される。この場合に `endpoint` だけを対象にすると生成物が 0 件になる。対象選別の規則は Phase 1 Step 4 が正である

## 予想を裏切る挙動

- 実装済みのプロファイルは python と perl の 2 つ。判定は `files` の拡張子の最多で行うため、多言語混在のリポジトリでは意図しないプロファイルが選ばれうる。その場合は `unit_keys` で同一言語のユニットだけに絞って起動する
- 呼び出し追跡は 2 段まで。深い委譲構造を持つ実装では、末端の処理を要確認事項一覧へ記録する
- 本スキルは原本を見ない検証ではない。画面詳細設計書と同じ品質保証を期待しない
- 実マニフェストでは `unitId` が全件空のことがある。その場合の出力先は `unitKey` 由来になるため、合成フィクスチャで確認した出力先と実データでの出力先が一致しないことがある
- ディスパッチ表を持つ構成では、`files` が入口モジュール 1 本だけを指し、`confidence` が全件 `low` になることがある。この状態でも中断せず、ディスパッチ表を解いて実装本体まで読む
- 非 HTTP 系のマニフェストには `kind` が `endpoint` のユニットが 1 件も無いことがある。業務単位が `dispatch-entry` として記録されるためで、この場合も対象として扱う

## 完了条件

| Phase | 完了条件 |
|---|---|
| Phase 1 | 4 引数が実在し、マニフェストが確認済みで、言語プロファイルが確定し、対象ユニットが確定している |
| Phase 2 | 対象ユニット全件について、本文材料と要確認事項が揃っている |
| Phase 3 | 全対象ユニットのAPI詳細設計書とAPI単体テスト設計書が記入済みで、未確定事項が要確認事項一覧へ集約されている |
| Phase 4 | 8 検査すべてが合格している |
| **Goal** | 対象エンドポイントごとに、コード位置を含まない実装契約と区分付きの設計判断を持つ詳細設計書、および関数・メソッド単位のAPI単体テスト設計書が生成されている |

## 関連

- `.claude/skills/generating-api-list-for-reverse-docs/SKILL.md` — 入力となるマニフェストの生成元
- `.claude/skills/generating-reverse-detailed-design/SKILL.md` — 画面単位の詳細設計書（原本を見ない検証型。本スキルとは情報源の扱いが異なる）
- `delivery-payload/references/chapter-map.md` — 章の役割キーの対応表
- `delivery-payload/references/納品物フォルダ体系.md` — 成果物の配置

## 設計判断

### test-api-design-decisions.sh

**必要性**: 改善課題1-194の形式を繰り返し検証するため、コードコメントを持つ合成フィクスチャからAPI詳細設計書の「設計判断とその理由」を埋め、テンプレート、scaffold、専用validatorの連携を一気通貫で実行する。生成Skillの分類規約、正常な表形式、推定へ`high`を指定した場合、判断理由を本文・要確認事項一覧の双方へ残さなかった場合を検査する。白紙状態の実行者によるSkill実行は、受け入れ時の実機確認として別に行う。

**代替案を採用しなかった理由**: 一時ディレクトリでの単発生成は回帰検知にならず、`scaffold-design-unit.sh --self-test`はテンプレートの複製可否だけを検証して§12.5の形式を検証しない。専用validatorの単独実行ではテンプレート展開との連携を検査できない。このリポジトリにはMakefileや`package.json` scriptsによる同等のテスト定義基盤もない。

**保守責任者**: 人手（`reverse-docs-skills`保守者）。API詳細設計書の12.5、記述区分、またはscaffoldの配置契約を変更した場合、本テストと合成フィクスチャを同時に更新する。

**廃棄条件**: コードコメントから設計判断を生成する決定的な生成器が同等の正常系・異常系テストを内包した時、または改善課題1-194の記録要件を廃止した時。

### check-design-doc-section-consistency.sh のテンプレート一致検査

**必要性**: 必須節の存在検査だけでは、全件が同じ短縮版の節構成を持つ場合や、§2・§3 の表から列が一律に欠ける場合を検出できない。生成のたびに現行テンプレートの全見出しと全表列を同じ基準で比較し、3件以上でも繰り返せる機械検査が必要である。

**代替案を採用しなかった理由**: 生成スキル本文へ固定した一覧だけでは実行時の逸脱を止められず、`scaffold-design-unit.sh --verify` は配置・frontmatter・必須トークンを対象として全表列を比較しない。新しい検査スクリプトへ分けると、既に第1層の検査から呼ばれている節検査と走査・判定責務が重複するため、既存検査を拡張する。

**保守責任者**: 人手（reverse-docs-skills 保守者）。API詳細設計書テンプレートの `##` 見出しまたは表の列を変更した場合、検査はテンプレートから実行時に抽出するため固定値の更新は不要だが、本スキルの「テンプレート正本契約」と test-cases.md を同時に更新する。

**廃棄条件**: API詳細設計書の生成が決定的なテンプレートエンジンへ移り、全見出し・全表列の保持を同等以上の検査で保証するようになった時。
## テンプレート記入規則の実行

<!-- TEMPLATE_GUIDANCE_EXECUTION -->

使用する各 Markdown テンプレートを Read する。`<!-- 記入規則: ... -->` と `<!-- INTRODUCTION_GUIDANCE ... -->` の指示を本文生成の手順として実行する。冒頭案内は `delivery-payload/references/設計書様式.md` の §9 に従う。複数節では「節｜内容｜読み手へのお願い」の3列表を本文の節ごとに1行ずつ作る。作る側の判断理由・保管方法・作業経緯・文書作成方針・件数内訳は冒頭案内へ書かない。自由記述は敬体で書く。指示を反映した後、記入規則の HTML コメントは生成文書から除去する。
