# generating-api-detail-design-for-reverse-docs テストケース

観点キーは連番禁止。内容を要約した意味語で付ける（番号からは情報を得られないため）。

| 観点キー | 前提 | 操作 | 期待結果 | 機械検証 |
|---|---|---|---|---|
| 引数-必須4件の検収 | 必須4引数（api_manifest_path/source_dir/output_dir/template_root）のいずれかが欠けている | Phase 1 Step 1を実行する | status=ERRORで停止する | 手動（対象コードベースを要する） |
| 言語判定-該当なしの中断 | どの言語プロファイルの拡張子にも該当しない対象がある | Phase 1 Step 3で言語プロファイルを選択する | status=ERRORとなり、hintに「該当する言語プロファイルなし」と拡張子の内訳が記録される | 手動（対象コードベースを要する） |
| 対象選別-middleware除外 | `kind`が`middleware`と`unresolved`のユニットを含むマニフェストがある | Phase 1 Step 4で対象ユニットを確定する | `middleware`と`unresolved`のユニットが対象から除外される | 手動（対象コードベースを要する） |
| 章完備-13章と章マップ | 設計書が生成済みである | 生成した設計書に対し`grep -c '^## §'`を実行する | 生成した設計書に §1 から §13 と章マップ（付録B）が存在する | 検査1（`grep -c '^## §'` が 13、かつ `grep -c '^## 章マップ'` が 1） |
| テンプレート一致-合成3件 | 版管理外の一時領域へscaffoldしたAPI詳細設計書が3件ある | `bash generation-engine/scripts/tests/check-design-doc-section-consistency.sh --self-test`を実行する | 3件すべてで現行テンプレートの全`##`見出しと全表列が一致し、節名変更と§2の列省略をそれぞれ不合格にする | self-test「課題1-201」7ケース |
| 根拠実在-file:lineの照合 | 設計書に`file:line`形式の根拠が記入済みである | Phase 4 検査2を実行する | 記入した根拠のファイルが`source_dir`配下に実在しない場合は不合格となる | 検査2 |
| プレースホルダ-残存なし | テンプレートを展開した設計書がある | Phase 4 検査3を実行する | `APIKEY`等のプレースホルダが本文に残っていないことを確認し、残存があれば不合格となる | 検査3 |
| 空欄処理-要確認事項への回送 | 原本から根拠を付けられない項目がある | Phase 2 Step 2およびPhase 3 Step 3を実行する | 推測で埋めず、空欄のまま要確認事項一覧へ列挙される | 手動（対象コードベースを要する） |
| 追跡上限-2段で打ち切り | 呼び出し先の定義元を追跡すると2段を超える委譲構造がある | Phase 2 Step 1で呼び出し追跡を行う | 2段を超える追跡を行わず、要確認事項一覧へ記録される | 手動（対象コードベースを要する） |
| 言語プロファイル-perl選択 | `.pm`が最多の対象マニフェストがある | Phase 1 Step 3で言語プロファイルを選択する | `perl`プロファイルが選ばれ、status=DONEとなり設計書が生成される | 手動（合成フィクスチャを要する） |
| 言語プロファイル-python据え置き | `.py`が最多の対象マニフェストがある | Phase 1 Step 3で言語プロファイルを選択する | 従来どおり`python`が選ばれ、生成結果が変化しない | 手動（合成フィクスチャを要する） |
| 言語プロファイル-返却記録 | 設計書生成が完了している | 返却ブロックを確認する | `language_profile`に選択したプロファイル名が入る | 手動（合成フィクスチャを要する） |
| 出力先-識別子のフォールバック | `unitId`が全件空のマニフェストがある | Phase 3 Step 1で出力先を決定する | `status=DONE`となり、`unitKey`由来のディレクトリへ出力される | 手動（合成フィクスチャを要する） |
| 出力先-識別子の優先順 | `unitId`が埋まったマニフェストがある | Phase 3 Step 1で出力先を決定する | 従来どおり`unitId`由来のディレクトリへ出力され、出力先が変化しない | 手動（合成フィクスチャを要する） |
| 出力先-識別子双方空で中断 | `unitId`と`unitKey`の双方が空のユニットがある | Phase 3 Step 1で出力先を決定する | `status=ERROR`となり、hintに「出力先の識別子が確定しない」が入る | 手動（合成フィクスチャを要する） |
| ディスパッチ解決-ハンドラ到達 | `files`が入口モジュールのみを指す構成がある | Phase 2 Step 1で読解対象を確定する | ディスパッチ表を解いてハンドラの実装モジュールを読み、根拠がハンドラのモジュールを指す | 手動（合成フィクスチャを要する） |
| ディスパッチ解決-files不足の記録 | ハンドラの実装モジュールが`files`に含まれていない | Phase 2 Step 1で読解対象を確定する | その不足が要確認事項一覧へ記録される | 手動（合成フィクスチャを要する） |
| 対象選別-dispatch-entryの採用 | `endpoint`が0件で`dispatch-entry`のみのマニフェストがある | Phase 1 Step 4で対象ユニットを確定する | `dispatch-entry`が対象になり生成物が0件にならない | 手動（合成フィクスチャを要する） |
| 対象選別-entrypointの除外 | 同じ実行ファイルに`dispatch-entry`がある`entrypoint`がある | Phase 1 Step 4で対象ユニットを確定する | 当該`entrypoint`が対象から外れ、二重に設計書が作られない | 手動（合成フィクスチャを要する） |
| 設計判断-コメントの観測 | 現在の実装上の選択と理由を直接結び付けるコメントを持つ合成フィクスチャがある | `bash generation-engine/scripts/tests/test-api-design-decisions.sh`を実行する | 12.5に設計判断と理由が入り、記述区分が`観測（コードコメント）`になる | self-test（テンプレート・scaffold・validator連携）および実機（白紙状態の実行者） |
| 設計判断-構造からの推定 | 理由を述べるコメントは無いが、選択理由を導ける実装構造を持つ合成フィクスチャがある | スキルを白紙状態の実行者が起動する | 12.5の記述区分が`推定（実装構造）`になり、確からしさが`medium`または`low`になる | 実機（構造だけの合成フィクスチャ） |
| 設計判断-理由なしの回送 | コメントにも実装構造にも選択理由が無い合成フィクスチャがある | スキルを白紙状態の実行者が起動する | 12.5に理由を捏造せず、理由を確定できない事項が要確認事項一覧へ入る | 実機（理由なしの合成フィクスチャ） |
| 設計判断-選択肢不明の明示 | 設計判断と理由は読めるが、選ばなかった選択肢を読めない合成フィクスチャがある | 12.5を執筆する | 選ばなかった選択肢と不採用理由が`不明（原本に記述なし）`になる | 設計判断の検査 |

## 機械検証との対応

- 機械検証が「手動」の行は、guide.html の検証状況へ手動確認を記録する
- self-test に対応ケースを追加したら、この表の機械検証列を同じコミットで更新する
