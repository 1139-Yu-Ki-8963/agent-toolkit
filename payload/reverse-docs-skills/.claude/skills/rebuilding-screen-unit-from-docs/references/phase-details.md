# Phase 詳細（P3〜P7 の詳細手順・6 計測の算出式）

`SKILL.md` の P3・P4・P5・P6 で参照する詳細手順。SKILL.md 本体には要約のみを置き、具体的な算出方法・prompt 骨格・書式はここに集約する。

章の役割キー → 節キーワード対応表は本ファイルでは複製しない。正本は起動引数 chapter_map_path（管理者が配布。実体: `~/reverse-docs-skills/shared/references/chapter-map.md`）であり、本スキルもそちらを参照する。

## スクリプト入力契約

### `scripts/measure-file-diff.sh <generated-file> <original-file>`

- **標準出力**（key=value を 1 行ずつ・機械可読）:
  ```
  import_diff_lines=<整数>
  style_diff_lines=<整数>
  total_diff_lines=<整数>
  substantive_diff_lines=<整数>
  verdict=PASS|FAIL
  ```
- **verdict の判定式**: `import_diff_lines == 0 && style_diff_lines == 0 && substantive_diff_lines <= 20` のとき `PASS`、それ以外 `FAIL`
- **対象外**: 「単体テスト仕様の検査」（禁止パターン・import・コンポーネント/API 利用）は本スクリプトの対象外。P5 でメインが別途実施する
- **引数不足・ファイル不在**: stderr にメッセージを出して `exit 1`。正常時は `exit 0`

### 4 計測の算出方法（heuristic）

本表は概要説明であり、正本は `measure-file-diff.sh` 本体（ヘッダコメントおよび `STYLE_PATTERN`）である。スクリプトの実装が変更された場合、本表よりスクリプト本体を正とする。

| 計測 | 算出方法 |
|---|---|
| import diff | 両ファイルから `^\s*import ` 行を抽出→ソート→`diff` の差分行数（`^[<>]` 行の数） |
| style diff | `className="..."` / `style={{...}}` 等の属性値部分のみを `grep -oE` で抽出（行全体ではなく属性値のみが対象）・16 進カラーコード（`#[0-9a-fA-F]{3,8}`）・`px`/`rem`/`em`/`vh`/`vw` の直後が区切り文字（`;` `,` `)` `}` `]` クォート・空白・行末のいずれか）の場合のみ数値を抽出→ソート→diff の差分行数 |
| 全体 diff | `diff <original> <generated>` の差分行数（参考値。合格判定には使わない） |
| 実質 diff | コメント行（`^\s*//`・`^\s*/\*`・`^\s*\*`）と空行を除外した上での diff 差分行数 |

### 非 JSX ファイル（hook / util 等）での縮退

対象ファイルが JSX を持たないロジックフック・ユーティリティ（`.ts` 等）の場合、style diff と P6 の差分 4 分類のうち JSX 分類は構造的に 0 に縮退しやすい。この場合は logic 分類（データフロー §6・API通信 §7）を主軸に解釈する。`style_diff_lines=0` は縮退による 0 であり、それだけで合格を意味するのではなく、実質 diff のうち logic に起因する差分を重視して判定する。

## P3: 単体テスト仕様作成の詳細

1. 単体テスト観点表の全行から、対象ファイル（対象の関数・フック・コンポーネント名）に対応する行を抽出する
2. 抽出した観点ごとに RED のテストケースを書く（1 行 = 1 つ以上のテスト）
3. 対象ファイルに対応する観点が 1 件も無い場合、意味キー規約（連番禁止・内容要約キー）に沿って観点表へ最低限の行を追記してから RED を書く
4. 本フェーズでは元コード（原本参照コミットの内容）を一切参照しない。観点表・設計書の該当章のみを根拠にする
5. 実測が要る断定（初期表示値・DOM 順・位置・レイアウト等）は本スキルでは確定せず `[画面単位検証で実測]` プレースホルダに残す。本スキルで確定するのは静的に決定可能な宣言的契約（型・import・export・Props 名・API 契約形・ロジック構造・定数値）に限る

テストファイルの配置先は、対象プロジェクトの既存規約（`プロジェクト共通/規約/コーディング規約.md`等）に従う。規約が無い場合は対象ファイルと同一の慣習的配置（隣接する `.test.ts(x)` または `__tests__/`）とする。実行は P5 で対象プロジェクトが既に持つテストランナー（`package.json` の test スクリプト等）を用いる。本スキル専用の新規テスト実行基盤は導入しない。

target_repo_path 上のこの配置は P4〜P5 実行時の一時コピーに過ぎない。収束後の最終版は P7 で設計書リポジトリ側へ正本として保存する（詳細は本ファイル末尾「P7: テストコード保存の詳細」を参照）。テンプレート（scaffold-screen.sh）には `テストコード/` ディレクトリを scaffold しない。

## P4: サブエージェント委任の prompt 骨格

`worker-sonnet` へ 1 エージェント = 1 ファイルで委任する。prompt には次の要素のみを含める。

```
## 対象
<対象ファイルパス>（新規作成 or 白紙化済みファイルの再作成）

## 対象設計書の該当章（抜粋）
<画面構造 / データフロー / API通信 / イベント処理 / 領域別仕様 / 定数設定値 / エラーハンドリング
 等、対象ファイルに関係する章のみを抜粋。設計書全文は渡さない>

## 共通部品（該当する場合のみ）
<DESIGN.md の関連スタイル定義 / 共通設計書の関連コンポーネント仕様 / メッセージ定義書の関連文言>

## 単体テスト観点表（該当行）+ P3 で書いたテスト
<該当行の抜粋 + P3 で書いたテストコード全文>

## 禁止事項（厳守）
- 元コード（オリジナル環境・原本参照コミット）は一切 Read しない。パス・内容ともに提示されていない
- 上記の設計書抜粋・共通部品・テストのみを根拠に実装する
- import/export/Props 名は設計書の記載と完全一致させる（省略形・別名禁止）
```

**期待返却値**: 実装ファイル 1 件の内容・変更内容の要約・P3 テストのローカル実行可否

**新規サブエージェント起動の徹底**: ループの各周（P6→P2 再投入後の P4 再実行時）で、必ず新規に `worker-sonnet` を起動する。前周の会話・生成物の文脈を prompt に含めない（同一エージェントへの追加指示は禁止）。

## P5: 差分比較の詳細手順

1. `git show <原本参照コミット>:<対象ファイルパス> > $CLAUDE_JOB_DIR/tmp/<basename>.original`（`$CLAUDE_JOB_DIR` 未設定時は `${TMPDIR:-/tmp}/claude-job-<session>/` にフォールバック）で原本を一時ファイルへ抽出する
   `<session>` はセッション ID が取得できる場合はそれを用い、取得できない場合は並列実行時の衝突を避けるため `$$`（プロセス ID）等の一意な値を用いてよい。
2. P3 のテストを P4 の生成物に対して実行する（禁止パターン検査・import 検査・コンポーネント/API 利用検査を含む）。`vitest run --ui=false` で実行する。全項目合格が必須
3. `scripts/measure-file-diff.sh <generated-file> <original-tmp-file>` を実行し、4 計測（import diff / style diff / 全体 diff / 実質 diff）を省略なく取得する
4. verdict=FAIL の場合、差分の内容（import/style/substantive のどの行が差分か）を P6 の分類の根拠として記録する
5. `measure-file-diff.sh` の verdict は生シグナルである。差分のうち `[画面単位検証で実測]` 委譲事実（初期表示値・DOM 順・位置・レイアウト起因）に該当する行は画面単位検証への委譲として分類し収束判定から除外し、静的契約差分（import・型・export・Props 名・定数・ロジック）のみで PASS/FAIL を判定する
6. **第 6 計測（原本での P3 テスト実行）**: 手順 1 で原本を一時抽出できた時点で、生成物だけでなく原本にも P3 テストを実行する。
   1. `cp $CLAUDE_JOB_DIR/tmp/<basename>.original <target_repo_path>/<対象ファイルパス>` で原本内容を target_repo_path の対象パスへ一時配置する
   2. `vitest run --ui=false` を実行し、結果（PASS/FAIL 件数・失敗ケース名）を記録する
   3. 実行結果を記録した直後に、`cp <generated-file> <target_repo_path>/<対象ファイルパス>` で対象パスを P4 の生成物へ **必ず復元する**。手順 2 の vitest 実行がエラー・タイムアウトで異常終了した場合も、この復元だけは省略せず必ず実行する（finally 相当。復元漏れは重大違反であり、target_repo_path に原本が残ったまま次フェーズへ進んではならない）
   4. 得られるのは PASS/FAIL の実行結果のみであり、原本の字面読み禁止（P2〜P5 のカンニング防止層 1）は本手順でも維持する
7. 第 6 計測の結果を次のとおり判定へ組み込む: 生成物 green かつ原本 green は収束判定へ進む。生成物 green かつ原本 red は P3 テストの推測誤りとして P6 で観点表・設計書を修正し P2 へ再投入する。原本 red が原本自体のバグ疑いの場合は P6 の自動再投入をせずユーザーへ判断を仰ぐ

## P6: 差分分類 → 設計書修正先マップ

| 差分種別 | 判定条件 | 修正先 |
|---|---|---|
| import | `import_diff_lines > 0` | 実装契約章の依存（既定 §15.3） |
| style | `style_diff_lines > 0` | `DESIGN.md`（画面構造章のスタイル適用パターン経由、既定 §3.6） |
| logic | `substantive_diff_lines` の内容がデータフロー・API 呼び出し条件に起因 | データフロー（既定 §6）・API通信（既定 §7） |
| JSX | `substantive_diff_lines` の内容が JSX 構造・領域仕様に起因 | 画面構造（既定 §3）・領域別仕様（既定 §9） |

logic と JSX の切り分けは目視判断による。1 件の実質 diff が複数分類にまたがる場合、両方の修正先に記載する。

**画面単位検証への委譲差分の扱い**: `[画面単位検証で実測]` に該当する実測断定差分（初期表示値・DOM 順・位置・レイアウト）は確定修正せず、`[画面単位検証で実測]` プレースホルダのまま P7 へ引き継ぐ。収束判定の分母からも除外する。

### 修正順序

1. 単体テスト観点表に不足していた観点（境界値・異常系等）を追記する
2. 設計書の該当章に記載を追加する
3. verdict=FAIL が続く場合は P2（白紙化）へ再投入する（原本参照コミットは再取得不要）

## 修正指示書・検証記録の書式

配置先は `<画面ディレクトリ>/検証記録/単体-<対象ファイル basename>/<timestamp>/修正指示書.md`。記載項目は次のとおり。

- 対象ファイルパス・収束/未収束の判定（単体テスト仕様の検査結果 + 6 計測の verdict で行う）
- 「根拠となる証跡パス」には P5 の 6 計測出力全文（key=value。第 6 計測は原本 PASS/FAIL 件数）を参照パスとして記載する
- P6 の差分 4 分類（import/style/logic/JSX）ごとの修正内容
- NG が 0 件（収束）の場合は「NG なし」と明記し、表は省略する

## P7: テストコード保存の詳細

収束確定時、P3〜P6 で収束した最終テストコードを設計書リポジトリの `<画面ディレクトリ>/テスト項目書/テストコード/単体/<対象ファイル basename>/` へ正本として保存する。検証記録（`検証記録/単体-<対象ファイル basename>/<timestamp>/`）とは別枠であり、設計書・観点表と同格の納品物として扱う。

1. target_repo_path 上の一時コピー（P3 で配置したテストファイル）の内容をそのまま設計書リポジトリ側の上記パスへコピーする
2. target_repo_path 上の一時コピーは本フェーズ終了時に撤去し、target_repo_path には残さない
3. 設計書リポジトリ側の保存先に同名の既存テストファイルが存在する場合は上書きしない。既存テストの扱い（共存させるか置換するか）を返却ブロックの hint に記録し管理者へ申し送る
4. `scripts/check-viewpoint-coverage.sh <単体テスト観点表> <テストコード>` を実行し、exit 0（観点網羅ゲート通過）であることを確認する。exit 1 の場合は未言及の観点を補って再実行する
5. 返却ブロックの拡張フィールド `saved_test_paths` に、本手順で保存したテストコードのパス一覧を記載する

## レガシー資産の移行（テンプレ構造への無損失移行）

新規スクリプトは用意しない。既存資産をテンプレ構造へ移行する際は from-zero（SKILL.md P1 参照）に準じ、以下を手順として使う。

1. 旧資産の見出し一覧を確認する: `grep -n '^#' <旧資産>.md`
2. 旧資産の非見出し内容を抽出・整列する: `grep -v '^#\|^$\|^---' <旧資産>.md | sort > /tmp/legacy-content.txt`
3. 16章の役割キー（画面詳細設計書.md の「## 章マップ」参照）に沿って内容を意味的に割り当てて記述する。既存の16章に対応先が無い内容は、削除せず該当章の末尾に追記する（無損失を優先する）
4. 移行後の新設計書一式に対して同じ抽出を行い突合する: `cat <新設計書> <新観点表> ... | grep -v '^#\|^$\|^---' | sort > /tmp/migrated-content.txt && diff /tmp/legacy-content.txt /tmp/migrated-content.txt`。差分は「移行漏れ」か「意図的な言い換え」かを目視確認する
5. 起動引数 audit_script_path を Bash 実行する（`bash <audit_script_path> <画面ディレクトリ>`）し、移行後の構造的健全性（章マップ・キー突合・プレースホルダ）を確認する
