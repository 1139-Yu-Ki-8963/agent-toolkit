---
doc_id: coding-standards
type: coding-standards
status: draft   # draft | traced（全節が実測値で埋まった状態）
updated: <YYYY-MM-DD>
---

# コーディング規約（リバース版）

本書は既存コードから抽出したコーディングスタイル・フォーマットルールの記録である。あるべきスタイルを新たに設計するのではなく、既存コードに実際に適用されているルールのみを記載する。

## インデント・フォーマット

| 項目 | 実測値 | 抽出元 |
|---|---|---|
| インデント | `<タブ/スペースN個>` | `<lint設定ファイルパスまたは実測>` |
| 行末セミコロン | `<あり/なし>` | `<抽出元>` |
| 引用符 | `<シングル/ダブル>` | `<抽出元>` |
| 行の最大長 | `<N文字>` | `<抽出元>` |

## lint・フォーマッタ設定

| ツール | 設定ファイル | 主要ルール |
|---|---|---|
| `<ESLint/Prettier等>` | `<設定ファイルパス>` | `<主要な有効ルール>` |

## import 順序

| 順序 | カテゴリ | 例 |
|---|---|---|
| 1 | `<外部ライブラリ/内部モジュール等>` | `<例>` |

## コメント規約

| 項目 | 実測パターン |
|---|---|
| 関数コメント | `<JSDoc/なし等>` |
| TODO 記法 | `<TODO: / FIXME: 等>` |

## AI設定資産への変換

| 規約キー | 対象パス | 強制区分 | 規約要点 | 違反時手順 |
|---|---|---|---|---|
| `coding-format` | `**/*` | `advisory` | 実測したインデント、セミコロン、引用符、最大行長に合わせる | 抽出元を再確認し、実測値に合うよう整形する |
| `coding-lint-formatter` | `**/*` | `advisory` | 実在するlint・formatter設定と有効ルールに従う | 設定fileの実行commandで修正し、違反が消えたことを確認する |
| `coding-import-order` | `**/*` | `advisory` | 実測したimport category順を維持する | importを実測順へ並べ直してlintを再実行する |
| `coding-comments` | `**/*` | `advisory` | 関数commentと作業注記を実測記法に合わせる | commentを実測記法へ直し、抽出元との不一致を解消する |
