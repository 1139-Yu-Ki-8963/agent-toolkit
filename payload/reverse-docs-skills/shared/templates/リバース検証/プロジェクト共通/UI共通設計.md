---
doc_id: ui-common-design
type: ui-common-design
status: draft   # draft | traced（全節が実測値で埋まった状態）
updated: <YYYY-MM-DD>
---

# UI共通設計書（リバース版）

本書は複数のレガシー画面から抽出した、UI の設計方針を上位から俯瞰する記録である。あるべき UI 設計を新たに構想するのではなく、既存コードに実際に存在する共通方針のみを記載する。個々の状態・操作規則の詳細は `./共通設計書.md`、数値・色・フォントは `./DESIGN.md` を正とし、本書はそれらの要約・俯瞰に徹する。

## 本書に書かないもの

| 書かない内容 | 正の置き場 |
|---|---|
| 共通状態・操作規則の詳細（loading/empty/error 等） | `./共通設計書.md`（§1〜§3） |
| 数値・色・フォント（共通） | `./DESIGN.md` |
| 数値・色・フォント（画面固有差分） | 各画面の `../画面/詳細設計/DESIGN.md` |
| UI 文言 | `./メッセージ定義書.md` |
| コンポーネント実装規則（props 命名・分割方針等） | `./規約/コンポーネント設計規約.md` |
| 画面固有のレイアウト | 各画面詳細設計書 |

---

## §1 デザインシステム（実測）

| 項目 | 内容 | 根拠パス |
|---|---|---|
| 使用ライブラリ・デザインシステム | `<実測: UIライブラリ名。独自実装のみの場合は「実在しない（理由: 独自実装のコンポーネントのみ）」>` | `<実測: 根拠パス（package.json等）>` |
| バージョン | `<実測>` | `<実測: 根拠パス>` |

---

## §2 共通コンポーネント一覧（実測）

<!-- 複数画面で横断的に使用されているコンポーネントのみを記載する。1画面限定のコンポーネントは書かない -->

| コンポーネント | 用途 | 使用画面数（頻度） | 根拠パス |
|---|---|---|---|
| `<実測: コンポーネント名>` | `<実測: 用途>` | `<実測: N/M 形式>` | `<実測: 根拠パス>` |

---

## §3 レイアウト方針（実測）

<!-- 個別のレイアウト構造ではなく、複数画面で共通して観測された原則のみを記載する。詳細な構造原則は ./共通設計書.md §3 を参照 -->

| 要素 | 実装済みの原則 | 根拠パス |
|---|---|---|
| グリッドシステム | `<実測: 使用有無・カラム数等>` | `<実測: 根拠パス>` |
| ブレークポイント | `<実測: レスポンシブ分岐点>` | `<実測: 根拠パス>` |

---

## §4 テーマ・スタイル管理（実測）

| 項目 | 内容 | 根拠パス |
|---|---|---|
| CSS 設計方針 | `<実測: CSS Modules/styled-components/Tailwind等>` | `<実測: 根拠パス>` |
| テーマ切替の有無 | `<実測 または 実在しない（理由: …）>` | `<実測: 根拠パス。実在しない場合は「-」>` |
| レスポンシブ対応方針 | `<実測>` | `<実測: 根拠パス>` |

---

## §5 アクセシビリティ方針（実測）

| 項目 | 内容 | 根拠パス |
|---|---|---|
| WAI-ARIA 対応 | `<実測: aria-* 属性の使用状況 または 実在しない（理由: …）>` | `<実測: 根拠パス>` |
| キーボード操作対応 | `<実測: 共通設計書.md §2 のフォーカス移動規則と重複させず要約のみ>` | `<実測: 根拠パス>` |

---

## traced の条件

- 全節の実測値が既存コードまたは `./共通設計書.md`・`./DESIGN.md` からの要約で埋まっていること
- コード内に実在しない項目は「実在しない（理由: …）」で埋めること（空欄・省略は禁止）
