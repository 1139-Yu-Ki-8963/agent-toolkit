---
doc_id: naming-conventions
type: naming-conventions
status: draft
updated: <YYYY-MM-DD>
---

# 命名規約（リバース版）

本書は既存コードから抽出した命名パターンの記録である。

## ファイル名

| 対象 | パターン | 例 |
|---|---|---|
| コンポーネント | `<PascalCase/kebab-case等>` | `<実例>` |
| フック | `<use + PascalCase等>` | `<実例>` |
| ユーティリティ | `<camelCase等>` | `<実例>` |
| テストファイル | `<.test.ts/.spec.ts等>` | `<実例>` |
| スタイルファイル | `<.module.css/.styled.ts等>` | `<実例>` |

## 変数・関数名

| 対象 | パターン | 例 |
|---|---|---|
| ローカル変数 | `<camelCase等>` | `<実例>` |
| 定数 | `<UPPER_SNAKE等>` | `<実例>` |
| コンポーネント Props 型 | `<ComponentNameProps等>` | `<実例>` |
| イベントハンドラ | `<handle + 動詞等>` | `<実例>` |

## CSS クラス名

| パターン | 例 |
|---|---|
| `<BEM/CSS Modules/Tailwind等>` | `<実例>` |

## AI設定資産への変換

| 規約キー | 対象パス | 強制区分 | 規約要点 | 違反時手順 |
|---|---|---|---|---|
| `naming-files` | `**/*` | `advisory` | component、hook、utility、test、styleのfile名を実測patternに合わせる | 対象fileと参照を実測patternへrenameする |
| `naming-symbols` | `**/*` | `advisory` | 変数、関数、定数、Props型、event handlerを実測patternに合わせる | symbolと全参照を実測patternへrenameしてtestする |
| `naming-css-classes` | `**/*` | `advisory` | CSS class名を実測patternに合わせる | class定義と参照を同時に直し、表示を再確認する |
