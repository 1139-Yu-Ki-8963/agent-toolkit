---
doc_id: "common/アーキテクチャ調査書"
type: architecture-survey
status: draft   # draft | traced（全節が実測値で埋まった状態）
updated: <実測: YYYY-MM-DD>
---

# アーキテクチャ調査書（リバース版）

本書は対象リポジトリを決定的コマンド（`find`/`grep`/`ls`）のみで走査し、後続工程（共通採録・facts抽出・詳細設計執筆）が前提とする技術スタック・ディレクトリ責務・ユニット種別の実在有無を確定した記録である。根拠のない記入での埋め合わせは行わない。判定不能・実在しない項目は「実在しない（理由: …）」で必ず埋める（空欄・省略は禁止）。

## §1 調査メタ

| 項目 | 内容 |
|---|---|
| 対象リポジトリ | `<実測: target_repo_path>` |
| 対象ブランチ | `<実測: target_branch。未指定なら「未指定」>` |
| 参照コミット（source_ref） | `<実測: source_ref。未指定なら「未指定」>` |
| 調査実施日 | `<実測: YYYY-MM-DD>` |

### 実行した調査コマンド一覧

<!-- Phase 2〜3 で実行した find/grep/ls をすべて記録する。再現可能性の担保。 -->

| コマンド | 目的 |
|---|---|
| `<実測: 実行コマンド>` | `<実測: 何を確認するためか>` |

---

## §2 技術スタック

| 項目 | 内容 | 根拠パス |
|---|---|---|
| 言語・ランタイム | `<実測>` | `<実測: 根拠パス>` |
| フレームワーク | `<実測>` | `<実測: 根拠パス>` |
| パッケージマネージャ | `<実測>` | `<実測: 根拠パス（lockファイル等）>` |
| ルーティングライブラリ | `<実測>` | `<実測: 根拠パス>` |

---

## §3 ビルドと起動

| 項目 | 内容 | 根拠パス |
|---|---|---|
| ビルドコマンド | `<実測>` | `<実測: 根拠パス>` |
| 起動コマンド（開発） | `<実測>` | `<実測: 根拠パス>` |
| 起動コマンド（本番） | `<実測>` | `<実測: 根拠パス>` |
| 環境変数定義の所在 | `<実測>` | `<実測: 根拠パス>` |

---

## §4 ディレクトリ責務マップ

<!-- 決定的走査（find/ls）で確認した実在ディレクトリのみを記載する。根拠のないディレクトリ追加は禁止。 -->
<!-- 例外: 複数ディレクトリから import・参照される共有ファイル（共通スタイル定数・テーマ定義ファイル等、単一ディレクトリの責務に還元できないファイル）に限り、
     ディレクトリ列に当該ファイルの相対パスをそのまま記載してよい。この場合、責務列は「共有ファイル（<N>ディレクトリから参照）」の形式で書き、
     根拠パス列に検出に使った決定的コマンドと実測件数を記載する。該当する共有ファイルが無いプロジェクトでは本行を追加しない（空行・プレースホルダのまま残すことを禁止する）。 -->

| ディレクトリ | 責務 | 根拠パス |
|---|---|---|
| `<実測: ディレクトリパス>` | `<実測: 責務の要約>` | `<実測: 根拠パス>` |

---

## §5 エントリポイントとルーティング

| 項目 | 内容 | 根拠パス |
|---|---|---|
| エントリポイント | `<実測>` | `<実測: 根拠パス>` |
| ルーティング定義の所在 | `<実測>` | `<実測: 根拠パス>` |
| ルーティング方式 | `<実測>` | `<実測: 根拠パス>` |

---

## §6 ユニット種別判定

<!-- 画面・API・テーブル・バッチ・帳票・外部連携の6種別すべてに判定行が必須。空欄・省略禁止。 -->
<!-- 判定語は「実在する」または「実在しない（理由: …）」のいずれかで、種別名と同一行に記載する（機械ゲートが同一行検索するため）。 -->

| 種別 | 実在判定 | 検出手がかり | 根拠パス |
|---|---|---|---|
| 画面 | `<実測: 実在する または 実在しない（理由: …）>` | `<実測: 再現可能なgrep/findパターン>` | `<実測: 根拠パス。実在しない場合は「-」>` |
| API | `<実測: 実在する または 実在しない（理由: …）>` | `<実測: 再現可能なgrep/findパターン>` | `<実測: 根拠パス。実在しない場合は「-」>` |
| テーブル | `<実測: 実在する または 実在しない（理由: …）>` | `<実測: 再現可能なgrep/findパターン>` | `<実測: 根拠パス。実在しない場合は「-」>` |
| バッチ | `<実測: 実在する または 実在しない（理由: …）>` | `<実測: 再現可能なgrep/findパターン>` | `<実測: 根拠パス。実在しない場合は「-」>` |
| 帳票 | `<実測: 実在する または 実在しない（理由: …）>` | `<実測: 再現可能なgrep/findパターン>` | `<実測: 根拠パス。実在しない場合は「-」>` |
| 外部連携 | `<実測: 実在する または 実在しない（理由: …）>` | `<実測: 再現可能なgrep/findパターン>` | `<実測: 根拠パス。実在しない場合は「-」>` |

キーは連番禁止。内容を要約した意味語で付ける（番号からは情報を得られないため）。

---

## §7 判定不能と実在しない項目の理由

<!-- §6で「実在しない」とした種別、および技術スタック等で判定不能だった項目の理由を集約する。 -->

| キー | 内容 | 理由 |
|---|---|---|
| `<実測: 対象-要約>` | `<実測: 判定不能または実在しない項目>` | `<実測: 理由>` |

---

## §8 後続工程への申し送り

<!-- 共通採録スキル・facts抽出スキル・詳細設計執筆スキルが本調査書を読む際に留意すべき事項。 -->

| キー | 内容 |
|---|---|
| `<実測: 申し送り-要約>` | `<実測: 申し送り内容>` |
