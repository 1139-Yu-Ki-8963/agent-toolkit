---
screenId: screen-order-list
screenName: 注文一覧
type: unit-test
---

# 注文一覧 単体テスト仕様書

## テスト項目

| キー | 分類 | テスト内容 | 前提条件 | 操作手順 | 期待結果 | 優先度 |
|---|---|---|---|---|---|---|
| 検索-受注番号絞り込み | 入力検証 | 受注番号を入力して検索した場合、該当する注文のみ一覧表示される | 複数件の注文が登録済み | 受注番号を入力し検索実行ボタンを押す | 入力した受注番号に一致する注文のみ表示される | 高 |
| 検索-期間範囲逆転エラー | 入力検証 | 開始日が終了日より後の場合、範囲エラーが表示される | 注文一覧画面表示中 | 開始日に終了日より後の日付を入力し検索を実行する | MSG-ORDER-001の範囲エラーがインライン表示される | 高 |
| 表示-キャンセル行強調 | 表示条件 | ステータスがキャンセルの注文行がmuted-rowパターンで表示される | キャンセル状態の注文を含む一覧を表示 | 画面を表示する | キャンセル行が薄いグレーで表示される | 中 |
| 出力-権限制御 | 表示条件 | 利用者が経理権限を持たない場合CSV出力ボタンが表示されない | 経理権限を持たない利用者でログイン済み | 画面を表示する | CSV出力ボタンが表示されない | 高 |
| 出力-上限件数超過案内 | 入力検証 | 絞り込み結果が出力上限5000件を超える場合、絞り込みを促す案内が表示される | 検索結果が5000件を超える状態 | CSV出力ボタンを押す | 上限超過の案内メッセージが表示される | 中 |
| 一覧-初期ローディング遷移 | 状態遷移 | 初期表示中はスケルトン表示になりAPI成功後にreadyへ遷移する | 注文検索API呼び出し中 | 画面を表示する | スケルトン表示後に一覧テーブルへ切り替わる | 中 |
| 一覧-空状態表示 | 状態遷移 | 検索結果が0件の場合、空状態メッセージが表示される | 該当する注文が0件の検索条件 | 該当0件になる条件で検索を実行する | 空状態メッセージが表示される | 中 |
