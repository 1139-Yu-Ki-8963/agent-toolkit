---
doc_id: unit-test-screen-order-detail
type: unit-test-sheet
status: draft   # draft | traced（全行にテスト参照が付いた状態）
target_screen: screen-order-detail
source_design: ./画面詳細設計書.md
updated: 2026-07-24
---

# 注文詳細 単体テスト観点表（reverse-docs 版）

本書は関数・フック・コンポーネント単位（small）の「何を検証すべきか（観点）」の正である。TDD 二重ループの**内側ループ**（red → green → refactor）の RED 起点として使う。具体的なテストケース（入力値・アサーション）は管理しない — テストコードが正。

reverse-docs 版の判定基準は project-docs 版と異なる。**正は「既存挙動の忠実再現」であり、新規開発における「あるべき仕様」ではない。** 旧設計書に書かれた意図とコードの実際の挙動が食い違う場合も、三点照合（旧設計書×コード×有識者判定）で検証済みの実挙動を観点として記載する。

運用: 往復検証（rebuilding-code-from-docs スキル）の Phase 2 では、画面詳細設計書 §2 機能一覧表の各機能キーが本書または結合テスト観点表のいずれかに出現するかをキー集合で突合する（件数比較ではない）。観点表側にしか無いキーは §2 の記載漏れとして内部矛盾に扱う。

## テストサイズ対応表

| サイズ | 実行範囲 | 例 | 置き場 |
|---|---|---|---|
| small | 1 プロセス内で完結 | vitest・コンポーネントテスト | 単体テスト観点表（+ 結合表の一部） |
| medium | 1 マシン内（実ブラウザ・ローカル実 DB 可） | Playwright での単画面ブラウザ検証 | 結合テスト観点表 |
| large | 複数システム・実環境・画面横断 | 画面をまたぐユーザージャーニー | 往復検証のスコープ外（単一画面内の操作シナリオは操作シナリオ仕様書.md） |

所属は実行技術ではなく、対象が 1 画面に閉じるかで決める。

## 本書に書かないもの

| 書かない内容 | 正の置き場 |
|---|---|
| 具体的な入力値・手順・アサーション | テストコード |
| 画面内の結合・ブラウザ検証（medium）・受け入れ条件 | 結合テスト観点表 |
| 画面をまたぐシナリオ（large） | 往復検証のスコープ外（単一画面内の操作シナリオは操作シナリオ仕様書.md） |
| lint・静的解析 | Phase 7 完了チェック / hooks |
| UI の見た目 | モック・DESIGN.md |
| 三点照合そのものの手順・判定記録 | 往復検証レポート（rebuilding-code-from-docs スキル成果物） |

## 観点の導出元マップ

| 設計書セクション | 導出する観点の種類 |
|---|---|
| §4 業務ルール | 出荷済・キャンセル閲覧専用モードの観点 |
| §5 状態管理 | 次ステータス算出・編集可否判定の観点 |
| §11 エラーハンドリング | 更新備考・遷移不可選択・競合検出の観点 |
| §12 画面遷移仕様 | 一覧へ戻る遷移方式の観点 |

## 観点表

> 対象は既存コードなので実装済みの関数・フック名で書く（リバース時点で実体が存在する）。
> キーは連番禁止。内容を要約した意味語で付ける（番号からは情報を得られないため）。
> 乖離分類は三点照合で発見した乖離のみ記入する。値は「該当なし」「記録されなかった仕様変更」「生きているバグ」「文書の腐敗」のいずれか。

| キー | 対象 | 観点 | 由来 § | 区分 | 乖離分類 | テスト参照（実装後） |
|---|---|---|---|---|---|---|
| 遷移可否-次ステータス算出 | availableNextStatuses | order.status を起点に遷移可能な次ステータス一覧を算出する | §5 | 正常 | 該当なし | |
| 編集可否-ステータス判定 | isEditable | order.status が下書きまたは確定のとき true になる | §5 | 正常 | 該当なし | |
| 入力検証-備考文字数上限 | 更新備考文字数チェック | 更新備考が200文字を超える場合、形式エラーとする | §11 | 境界 | 該当なし | |
| 入力検証-遷移不可選択 | 更新後ステータス選択チェック | 現在のステータスから遷移不可な区分を選択した場合エラーとする | §11 | 異常 | 該当なし | |
| モード-出荷済閲覧専用 | 閲覧専用モード切替ロジック | ステータスが出荷済またはキャンセルの場合、キャンセルへの更新操作を無効化する | §4 | 正常 | 該当なし | |
| 遷移-一覧へback | 一覧へ戻る遷移制御 | 一覧へ戻るボタン押下時、back 方式で直前の検索条件を維持し遷移する | §12 | 正常 | 該当なし | |
| 競合検出-再取得トリガー | 更新競合処理 | ステータス更新APIが409を返した場合、競合エラー表示後に最新状態を再取得する | §11 | 異常 | 該当なし | |

## TDD 内側ループ規則

- 本書は TDD 二重ループの**内側ループ**を担う（外側ループは結合テスト観点表の受け入れ ◯ 観点）
- 観点表を上から順に RED（テスト先行）で書く
- 1 行 = 1 つ以上のテストに対応する
- GREEN 後にテスト参照列を記入する
- 三点照合で新たな乖離を発見したら行を追加し、乖離分類を記入してから RED を書く
- 全行にテスト参照が付いたら frontmatter の `status: traced` に更新する

## 自動化宣言

単体（small）は全観点を自動化する。自動化できない観点が出た場合は結合テスト観点表の手動観点へ移す。本書は手動観点の枠を持たない。

## 乖離分類の定義

| 分類 | 定義 | 本書での扱い |
|---|---|---|
| 記録されなかった仕様変更 | 旧設計書に反映されないまま業務判断でコードが変更された | 実挙動を観点として採用する |
| 生きているバグ | コードの不具合だが実運用で依存されている | 有識者判定で「忠実再現」対象と確定した場合のみ観点として採用する |
| 文書の腐敗 | 旧設計書の記述が古いだけで実挙動・仕様意図とも現状が正しい | 実挙動を観点として採用する |

## 他ドキュメントとの連動

| 連動する情報 | 定義権 | 参照方向 |
|---|---|---|
| 観点キー | 本書 | テストコードがコメントで参照する |
| 受け入れ条件（medium） | 結合テスト観点表 | 本書は参照のみ。重複させない |
| 業務ルール・状態定義 | 画面詳細設計書 §4〜§6・§11・§12 | 本書の導出元マップが使用する |
| 乖離分類の確定根拠 | 三点照合（旧設計書×コード×有識者判定） | 本書の乖離分類列が参照する |
