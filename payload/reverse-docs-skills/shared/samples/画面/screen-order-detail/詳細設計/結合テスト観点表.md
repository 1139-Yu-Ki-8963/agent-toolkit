---
doc_id: integration-test-screen-order-detail
type: integration-test-sheet
status: draft   # draft | traced（全行にテスト参照が付いた状態）
target_screen: screen-order-detail
source_design: ./画面詳細設計書.md
updated: 2026-07-24
---

# 注文詳細 結合テスト観点表（reverse-docs 版）

本書は画面内の結合（領域間連携・API 連携・状態切替・権限別表示）と単画面ブラウザ検証（medium）の観点の正であり、**この画面の受け入れ条件の正**でもある。自動テストの具体的な手順・アサーションはテストコードが正。手動観点のみ本書が手順を持つ。

reverse-docs 版の判定基準は project-docs 版と異なる。**正は「既存挙動の忠実再現」であり、新規開発における「あるべき仕様」ではない。** 旧設計書に書かれた意図とコードの実際の挙動が食い違う場合も、三点照合（旧設計書×コード×有識者判定）で検証済みの実挙動を受け入れ条件として記載する。

運用: 往復検証（rebuilding-code-from-docs スキル）の Phase 2 では、画面詳細設計書 §2 機能一覧表の各機能キーが単体テスト観点表または本書のいずれかに出現するかをキー集合で突合する（件数比較ではない）。観点表側にしか無いキーは §2 の記載漏れとして内部矛盾に扱う。

## テストサイズ対応表

| サイズ | 実行範囲 | 例 | 置き場 |
|---|---|---|---|
| small | 1 プロセス内で完結 | vitest・コンポーネントテスト | 単体テスト観点表（+ 結合表の一部） |
| medium | 1 マシン内（実ブラウザ・ローカル実 DB 可） | Playwright での単画面ブラウザ検証 | 結合テスト観点表 |
| large | 複数システム・実環境・画面横断 | 画面をまたぐユーザージャーニー | 往復検証のスコープ外（単一画面内の操作シナリオは操作シナリオ仕様書.md） |

所属は実行技術ではなく、対象が 1 画面に閉じるかで決める。

## 本書に書かないもの

| 書かない内容 | 正の置き場 |
|---|---|
| 関数・フック単位（small）の検証 | 単体テスト観点表 |
| 画面をまたぐシナリオ（large） | 往復検証のスコープ外（単一画面内の操作シナリオは操作シナリオ仕様書.md） |
| 自動テストの具体的な手順・アサーション | テストコード |
| lint・静的解析 | Phase 7 完了チェック / hooks |
| A-B 突合・DOM 常在・L1〜L5 検証の手順・判定記録 | 手順・判定記録は往復検証レポート（観点は本書の往復検証観点表） |

## 観点の導出元マップ

| 設計書セクション | 導出する観点の種類 |
|---|---|
| §2 機能一覧 / §9 領域別仕様 | 注文一覧との遷移引き継ぎ観点 |
| §4 業務ルール | 権限×ステータス更新の結合観点 |
| §5 状態管理 / §6 データフロー | ステータス更新履歴反映の状態切替観点 |
| §12 画面遷移仕様 | 一覧への復帰時のデータ反映観点 |
| §13 非機能要件 | 商品明細の件数上限観点 |
| §14 共通仕様への準拠 | キャンセル理由必須化の要確認事項の観点 |

## 観点表

> 実施方法: コード結合（small）= プロセス内の結合テスト / ブラウザ（medium）= Playwright 等での単画面ブラウザ検証 / 手動 = 自動化しない観点。
> キーは連番禁止。内容を要約した意味語で付ける（番号からは情報を得られないため）。
> 乖離分類は三点照合で発見した乖離のみ記入する。値は「該当なし」「記録されなかった仕様変更」「生きているバグ」「文書の腐敗」のいずれか。

| キー | 観点 | 由来 § | 区分 | 受け入れ | 実施方法 | 乖離分類 | テスト参照/手動参照 |
|---|---|---|---|---|---|---|---|
| 一覧-詳細引継 | 注文一覧の行クリックから受注番号を引き継いで詳細画面が表示される | §9 | 正常 | ◯ | ブラウザ（medium） | 該当なし | |
| 権限-出荷担当者更新可 | 出荷担当者・受注担当者ともにステータス更新実行ボタンが表示される | §4 | 正常 | ◯ | ブラウザ（medium） | 該当なし | |
| 確定操作-確認ダイアログ経由 | ステータス更新は確認ダイアログでの承認を経てAPIが呼ばれる | §4 | 正常 | ◯ | ブラウザ（medium） | 該当なし | |
| 更新api-履歴反映 | ステータス更新API成功時に statusHistory へ新しい履歴が追記され新しい順で表示される | §6 | 正常 | ◯ | コード結合（small） | 該当なし | |
| 詳細-一覧反映 | ステータス更新完了後、一覧へ戻る操作で更新結果が反映された一覧が再表示される | §12 | 正常 | ◯ | ブラウザ（medium） | 該当なし | |
| 件数上限-スクロール表示 | 商品明細が100件を超える場合、明細テーブル内でスクロール表示になる | §13 | 境界 | | 手動 | 該当なし | 明細上限-手動確認 |
| 逸脱-キャンセル理由未確定 | キャンセルへの更新時に備考入力を必須化すべきという要確認事項が実装に反映されていない | §14 | 正常 | | 手動 | 記録されなかった仕様変更 | キャンセル理由-手動確認 |

## 手動観点表

> 手動観点は項目レベルの手順を持つ唯一の場所である。自動化が困難な観点のみここに置く。
> キーは連番禁止。内容を要約した意味語で付ける（番号からは情報を得られないため）。

| キー | 観点キー | 手順 | 期待結果 | 最終確認日 |
|---|---|---|---|---|
| 明細上限-手動確認 | 件数上限-スクロール表示 | 商品明細101件の注文詳細を表示する | 明細テーブル内でスクロールバーが表示され全件がスクロールで閲覧できる | <YYYY-MM-DD> |
| キャンセル理由-手動確認 | 逸脱-キャンセル理由未確定 | キャンセルへステータス更新する操作を備考未入力で実行する | 備考未入力でも更新が成立する（現行実装の実挙動） | <YYYY-MM-DD> |

## 往復検証観点表

> `references/ng-classification.md` 表 B の失敗クラス（意味キー）10 種と 1:1 対応する。区分は 操作反映（ユーザー操作後の画面反映）/ DOM常在（絶対配置要素の常時マウント等、DOM 構造の恒常性）/ A-B突合（オリジナルコード環境とリバースコード環境の機械的一致）のいずれか。検証層は syncing-reverse-env の動的比較 L1〜L5（L1 起動 / L2 構造スナップショット / L3 画素比較 / L4 コンソールエラー集合 / L5 操作シーケンス突合 = 操作シナリオ仕様書.md の operations を実行し操作後の状態を突合）のいずれか。
> キーは連番禁止。内容を要約した意味語で付ける（番号からは情報を得られないため）。
> L5（操作シーケンス突合）の観点キーは、`操作シナリオ仕様書.md` のシナリオ一覧表「対応往復検証観点キー」列と 1:1 で対応する。操作手順を追記する際は、対応するキーを必ずこの表に先に用意すること。

| キー | 対応失敗クラス | 区分 | 観点 | 検証層 | 受け入れ | テスト参照/検証記録参照 |
|---|---|---|---|---|---|---|
| export名-完全一致 | export-import-型不一致 | A-B突合 | OrderDetailPage・OrderItemTable・StatusUpdatePanel・useOrderDetailState の export 名がオリジナルコード環境と完全一致すること | L1 | ◯ | |
| 状態初期値-AB一致 | 状態変数欠落・初期値差 | A-B突合 | selectedNextStatus（空文字）・isSubmitting（偽）の初期値がオリジナルコード環境と一致すること | L2 | ◯ | |
| 座標制御-常時マウント | 表示制御方式差（常時マウント vs 条件レンダー） | DOM常在 | StatusUpdatePanel の確認ダイアログは DOM 常時マウント・条件レンダリング禁止 | L2 | ◯ | |
| 操作後-画面反映 | イベント処理挙動差 | 操作反映 | ステータス更新実行後に statusHistory 表示へ即座に反映されること | L5 | ◯ | |
| スタイル数値-L3閾値内 | スタイル数値差 | A-B突合 | status-badge・disabled-panel のスタイル数値が画素比較の閾値内に収まること | L3 | ◯ | |
| 文言-メッセージID一致 | 文言差 | A-B突合 | 遷移不可な更新操作・更新時の競合検出の表示文言が MSG-ORDER-004 / MSG-ORDER-005 と一致すること | L2 | ◯ | |
| API呼出-条件と型一致 | API呼び出し条件・型差 | 操作反映 | 注文ステータス更新（PATCH）の呼び出し契機・型が設計書と一致し、コンソールエラーを出さないこと | L4 | ◯ | |
| 遷移方式-パラメータ一致 | 遷移方式・パラメータ差 | 操作反映 | 一覧へ戻る操作が back 方式で直前の検索条件を維持することが一致すること | L5 | ◯ | |
| 定数値-AB一致 | 定数値差 | A-B突合 | STATUS_TRANSITION_MAP・ORDER_STATUS の定数値がオリジナルコード環境と一致すること | L2 | ◯ | |
| 空エラー状態-表示一致 | 空状態・エラー状態差 | DOM常在 | 該当注文が存在しない場合の空状態メッセージ表示がオリジナルコード環境と一致すること | L2 | ◯ | |

## 乖離分類の定義

| 分類 | 定義 | 本書での扱い |
|---|---|---|
| 記録されなかった仕様変更 | 旧設計書に反映されないまま業務判断でコードが変更された | 実挙動を受け入れ条件として採用する |
| 生きているバグ | コードの不具合だが実運用で依存されている | 有識者判定で「忠実再現」対象と確定した場合のみ受け入れ条件として採用する |
| 文書の腐敗 | 旧設計書の記述が古いだけで実挙動・仕様意図とも現状が正しい | 実挙動を受け入れ条件として採用する |

## 二重ループ・受け入れ規則

- 受け入れ ◯ の観点は **Phase 6 開始時に失敗するテストとして先行作成する（TDD 二重ループの外側ループ）**
- 内側ループ（単体テスト観点表）の消化が進むことで外側ループが green に近づく
- 受け入れ ◯ が全て green（自動）または確認済み（手動）になったとき画面完成とみなす
- 受け入れ ◯ は「既存挙動を忠実に再現できている」と業務語で言い切れる観点に限る
- 受け入れ ◯ の全 green と手動観点の確認済みが PR マージの条件
- 全行にテスト参照または手動参照が付いたら frontmatter の `status: traced` に更新する

## 他ドキュメントとの連動

| 連動する情報 | 定義権 | 参照方向 |
|---|---|---|
| 観点キー | 本書 | テストコードと手動観点表が参照する |
| small 単体の内側ループ | 単体テスト観点表 | 本書の外側ループが green になる前提 |
| 領域名・状態名 | 画面詳細設計書 §9 / §5 | 本書の観点の文言に使用する |
| 遷移条件・文脈 | 画面詳細設計書 §12 | 遷移と文脈引き継ぎ観点の由来 |
| large シナリオ | 往復検証のスコープ外（対応テンプレートなし） | 単一画面内の操作シナリオは操作シナリオ仕様書.md を参照 |
| 乖離分類の確定根拠 | 三点照合（旧設計書×コード×有識者判定） | 本書の乖離分類列が参照する |
| L5 観点キー | 本書（往復検証観点表） | `操作シナリオ仕様書.md` のシナリオ一覧表「対応往復検証観点キー」列から参照される |
