---
doc_id: unit-test-spec-screen-order-detail
type: unit-test-spec
status: draft   # draft | implemented（テストコードが実装され、本書の各ケースがテストコードから参照される状態）
target_screen: screen-order-detail
source_design: ../詳細設計/画面詳細設計書.md
unit_test_sheet: ../詳細設計/単体テスト観点表.md
updated: 2026-07-24
---

# 注文詳細 単体テスト仕様書

本書は `../詳細設計/単体テスト観点表.md` の各観点キーに対応する、具体的な入力値・手順・期待結果（アサーション）を記述する。観点表は「何を検証するか」の正であり、本書は Phase 4（設計書からのTDD実装）の SPEC ステップで実際にテストコードへ落とし込む前の、具体的ケースの設計時契約である。

**実装後はテストコードが正になる**（§15 実装契約と同じ扱い）。実装後、本書の各行は「実装後のテストファイル参照」列にファイルパスを記入し、以後の変更はテストコード側で行う。本書の入力値・アサーションを実装後に手で書き換えてはならない（乖離があれば観点表の乖離分類で処理する）。

## 本書に書かないもの

| 書かない内容 | 正の置き場 |
|---|---|
| 抽象的な観点（何を検証すべきかの一般論） | `../詳細設計/単体テスト観点表.md` |
| 実装後のテストコードの変更管理 | テストコード自体 |
| 複数画面にまたがる結合的な検証 | `../詳細設計/結合テスト観点表.md` + `結合テスト仕様書.md` |

## テストケース一覧

<!-- キーは連番禁止。単体テスト観点表.mdの観点キーと1:1または1:多で対応させる -->

| キー | 対応観点キー | 入力値 | 期待結果（アサーション） | 実装後のテストファイル参照 |
|---|---|---|---|---|
| 下書き明細あり-確定含む | 遷移可否-次ステータス算出 | `order.status: "draft"`, 明細1件以上 | `availableNextStatuses` に `"confirmed"` が含まれる | |
| 下書き明細0件-確定を含まない | 遷移可否-次ステータス算出 | `order.status: "draft"`, 明細0件 | `availableNextStatuses` に `"confirmed"` が含まれず更新実行時にエラーが表示される | |
| 下書き確定-編集可判定true | 編集可否-ステータス判定 | `order.status: "draft"` または `"confirmed"` | `isEditable` が `true` を返す | |
| 出荷済-編集可判定false | 編集可否-ステータス判定 | `order.status: "shipped"` | `isEditable` が `false` を返す | |
| 備考201文字-形式エラー | 入力検証-備考文字数上限 | `updateComment` に201文字を入力 | 形式エラーが表示され更新が実行されない | |
| 出荷済-キャンセル選択肢除外 | 入力検証-遷移不可選択 | `order.status: "shipped"` | ステータス更新の選択肢に `"cancelled"` が含まれない | |
| キャンセル済-キャンセル選択肢除外 | モード-出荷済閲覧専用 | `order.status: "cancelled"` | キャンセルへの更新操作が無効化され選択肢から除外される | |
| 一覧へ戻る-back方式で条件維持 | 遷移-一覧へback | 「一覧へ戻る」ボタン押下 | back 方式で遷移し直前の検索条件が維持される | |
| 更新競合409-再取得トリガー | 競合検出-再取得トリガー | ステータス更新APIレスポンス `409` | `MSG-ORDER-005` が表示され最新状態が再取得される | |
