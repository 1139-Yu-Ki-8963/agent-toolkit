---
doc_id: unit-test-spec-screen-home
type: unit-test-spec
status: draft   # draft | implemented（テストコードが実装され、本書の各ケースがテストコードから参照される状態）
target_screen: screen-home
source_design: ../詳細設計/画面詳細設計書.md
unit_test_sheet: ../詳細設計/単体テスト観点表.md
updated: 2026-07-24
---

# ホーム 単体テスト仕様書

本書は `../詳細設計/単体テスト観点表.md` の各観点キーに対応する、具体的な入力値・手順・期待結果（アサーション）を記述する。観点表は「何を検証するか」の正であり、本書は Phase 4（設計書からのTDD実装）の SPEC ステップで実際にテストコードへ落とし込む前の、具体的ケースの設計時契約である。

**実装後はテストコードが正になる**（§15 実装契約と同じ扱い）。実装後、本書の各行は「実装後のテストファイル参照」列にファイルパスを記入し、以後の変更はテストコード側で行う。本書の入力値・アサーションを実装後に手で書き換えてはならない（乖離があれば観点表の乖離分類で処理する）。

## 本書に書かないもの

| 書かない内容 | 正の置き場 |
|---|---|
| 抽象的な観点（何を検証すべきかの一般論） | `../詳細設計/単体テスト観点表.md` |
| 実装後のテストコードの変更管理 | テストコード自体 |
| 複数画面にまたがる結合的な検証 | `../詳細設計/結合テスト観点表.md` + `結合テスト仕様書.md` |

## テストケース一覧

<!-- キーは連番禁止。単体テスト観点表.mdの観点キーと1:1または1:多で対応させる -->

| キー | 対応観点キー | 入力値 | 期待結果（アサーション） | 実装後のテストファイル参照 |
|---|---|---|---|---|
| 管理者権限-メニュー項目含有 | 権限-管理者メニュー表示 | `userRole: "admin"` | `visibleMenuItems` に管理系メニュー項目（例: 売上ダッシュボード）が含まれる | |
| 一般権限-メニュー項目除外 | 権限-一般メニュー除外 | `userRole: "general"` | `visibleMenuItems` に管理系メニュー項目が含まれない | |
| 未読1件以上-判定true | 通知-未読判定成立 | `unreadCount: 1` | `hasUnread` が `true` を返す | |
| 未読0件-判定false | 通知-未読判定不成立 | `unreadCount: 0` | `hasUnread` が `false` を返す | |
| 既読化-件数1減算 | 通知-既読化減算 | `unreadCount: 3`, 既読化対象 `notificationId: "N-101"` | `markNotificationAsRead("N-101")` 呼び出し後 `unreadCount` が `2` になる | |
| 通知取得失敗-トースト表示 | エラー-通知取得失敗表示 | 通知一覧API応答: `500` | `MSG-COMMON-500` のトーストが表示される | |
| メニュー押下-業務画面push | 遷移-サイドメニュー押下 | サイドメニュー項目「注文一覧」をクリック | `/orders` へ push 方式で遷移する | |
