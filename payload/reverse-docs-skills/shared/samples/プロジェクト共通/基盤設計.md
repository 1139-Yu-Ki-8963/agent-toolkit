---
doc_id: infrastructure-design
type: infrastructure-design
status: current
updated: 2026-07-23
---

# 基盤設計書

本書はシステムの基盤構成を整理した設計書である。フレームワーク・ビルド・デプロイ・認証の各構成を記載する。

## 本書に書かないもの

| 書かない内容 | 正の置き場 |
|---|---|
| 技術スタック・ビルドコマンドの詳細 | `./アーキテクチャ調査書.md`（§2〜§3） |
| ディレクトリ・ファイルの命名規則 | `./規約/ディレクトリ構成規約.md` |
| コーディングスタイル | `./規約/コーディング規約.md` |
| 画面固有のビルド差分 | 各画面詳細設計書 |

---

## §1 フレームワーク構成

| 項目 | 内容 | 根拠パス |
|---|---|---|
| フレームワーク | フロントエンド: React 18 + Vite / バックエンド: Node.js（Express） | `package.json` |
| バージョン | React 18.2.0 / Express 4.19.2 | `package-lock.json` |
| 選定理由 | 記載なし | - |

---

## §2 ビルド設定

| 項目 | 内容 | 根拠パス |
|---|---|---|
| ビルドツール | Vite | `vite.config.ts` |
| 環境別設定差分 | `.env.development` / `.env.production` で API ベース URL を切替 | `.env.production` |

---

## §3 デプロイ構成

| 項目 | 内容 | 根拠パス |
|---|---|---|
| デプロイ先 | AWS ECS Fargate（本番）/ AWS ECS（検証環境） | `infra/ecs-task-definition.json` |
| CI/CD パイプライン | GitHub Actions によるビルド → ECR プッシュ → ECS デプロイ | `.github/workflows/deploy.yml` |

---

## §4 ディレクトリ構成方針

| ディレクトリ | 構成意図 | 根拠パス |
|---|---|---|
| `src/pages` | 画面単位のルートコンポーネントを配置 | `src/pages/` |
| `src/components/common` | 画面横断で使用する共通コンポーネント | `src/components/common/` |
| `src/stores` | Zustand によるドメイン別状態管理ストア | `src/stores/` |
| `server/routes` | Express のリソース別ルーティング定義 | `server/routes/` |
| `server/batch` | 日次・月次で実行されるバッチ処理 | `server/batch/` |
| `server/webhooks` | 決済・配送等の外部サービスからの Webhook 受信処理 | `server/webhooks/` |

---

## §5 環境変数管理

| 項目 | 内容 | 根拠パス |
|---|---|---|
| 環境変数定義の所在 | `.env.development` / `.env.production` / `.env.test` の 3 ファイルに分離 | `.env.production` |
| 環境別の分離方式 | Vite の `import.meta.env` 経由で読み込み、ビルド時にモード別ファイルを注入 | `vite.config.ts` |

---

## §6 認証セッション・ストレージ方式

| 項目 | 方式 | 保存先（cookie/localStorage/sessionStorage/メモリ） | 有効期限・失効契機 | 根拠パス |
|---|---|---|---|---|
| アクセストークン | Zustand ストア（`authStore`）でメモリ保持。リロードで消失 | メモリ | 有効期限 15 分。リフレッシュ失敗時に失効 | `src/stores/authStore.ts` |
| リフレッシュトークン | HTTPOnly Cookie で保持 | cookie | 有効期限 14 日。ログアウト操作時にサーバー側で失効 | `server/routes/auth.ts` |
| テーマ設定 | localStorage（キー: `theme-preference`） | localStorage | 有効期限なし（明示的な変更まで保持） | `src/contexts/ThemeContext.tsx` |
| 下書き自動保存 | localStorage（キー: `draft-<screenId>`） | localStorage | 保存後 24 時間で自動破棄 | `src/hooks/useDraftAutosave.ts` |

