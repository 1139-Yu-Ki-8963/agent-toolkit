# リバース工程設計

## 出自

本文書は `スキル実装計画.md` が「Phase/Step×スキル対応の正本」として参照する文書である。旧版が欠落したため、2026-07-08 に既存スキル群（`.claude/skills/` 配下）と `納品物フォルダ体系.md` から再構成した。以降、新規スキルの追加・Phase/Step構成の変更時は本文書を更新する。

## Phase/Step 対応表

| Phase | Step | 内容 | 担当スキル |
|---|---|---|---|
| Phase 1 準備 | Step 1 | 対象リポジトリと output_dir の解決 | orchestrating-reverse-docs-flow |
| Phase 1 準備 | Step 2 | スコープ確認（唯一の人間介在点） | orchestrating-reverse-docs-flow |
| Phase 2 アーキテクチャ調査 | Step 3 | 前提確認とテンプレ展開 | surveying-architecture-for-reverse-docs |
| Phase 2 アーキテクチャ調査 | Step 4 | 決定的走査 | surveying-architecture-for-reverse-docs |
| Phase 2 アーキテクチャ調査 | Step 5 | 調査書執筆 | surveying-architecture-for-reverse-docs |
| Phase 2 アーキテクチャ調査 | Step 6 | 機械ゲート（check-architecture-survey.sh） | surveying-architecture-for-reverse-docs |
| Phase 2 アーキテクチャ調査 | Step 7 | 調査確定の返却 | surveying-architecture-for-reverse-docs |
| Phase 2 アーキテクチャ調査 | Step 8 | 差し戻し（下流工程で検出手がかり欠落を検知したら mode=revise で Step 5 へ再入） | orchestrating-reverse-docs-flow → surveying-architecture-for-reverse-docs |
| Phase 3 目録 | Step 9 | 一覧生成（種別ループ。「一覧生成の種別ループ」節を参照） | 種別別の一覧生成スキル6本（generating-screen-list-for-reverse-docs ほか5本） |
| Phase 3 目録 | Step 10 | 対象外種別と派生一覧の確定 | orchestrating-reverse-docs-flow |
| Phase 3 目録 | Step 11 | ポータル・任意基盤ページ・任意図の確定 | orchestrating-reverse-docs-flow / 各生成スキル |
| Phase 4 共通採録 | Step 12 | 層化サンプリング | generating-reverse-common-docs |
| Phase 4 共通採録 | Step 13 | 規約4種の採録 | generating-reverse-common-docs |
| Phase 4 共通採録 | Step 14 | 共通設計書・メッセージ定義書・DESIGN.md の採録 | generating-reverse-common-docs |
| Phase 4 共通採録 | Step 15 | 機械ゲート（check-common-docs.sh） | generating-reverse-common-docs |
| Phase 4 共通採録 | Step 16 | v0確定 | generating-reverse-common-docs |
| Phase 5 ユニット処理 | Step 17 | 対象画面の選定（開通有無は静的リバースの阻害条件にしない） | orchestrating-reverse-docs-flow |
| Phase 5 ユニット処理 | Step 18 | facts抽出 | extracting-unit-facts-from-code |
| Phase 5 ユニット処理 | Step 19 | 独立再計数ゲート | extracting-unit-facts-from-code |
| Phase 5 ユニット処理 | Step 20 | 封印 | extracting-unit-facts-from-code |
| Phase 5 ユニット処理 | Step 21 | 再現性検証 | extracting-unit-facts-from-code |
| Phase 5 ユニット処理 | Step 22 | テンプレート展開とfacts読込 | generating-reverse-basic-design |
| Phase 5 ユニット処理 | Step 23 | facts→業務語彙への転記 | generating-reverse-basic-design |
| Phase 5 ユニット処理 | Step 24 | 実装用語混入検査 | generating-reverse-basic-design |
| Phase 5 ユニット処理 | Step 25 | 封印検証とfacts読込 | generating-reverse-detailed-design |
| Phase 5 ユニット処理 | Step 26 | 執筆転記 | generating-reverse-detailed-design |
| Phase 5 ユニット処理 | Step 27 | 機械ゲート（check-fact-coverage.sh と audit-consistency.sh） | generating-reverse-detailed-design |
| Phase 5 ユニット処理 | Step 28 | 執筆完了の返却 | generating-reverse-detailed-design |
| Phase 6 往復検証 | Step 29 | `single-pass` / `iterative` のみ、必要時の画面開通（dynamic-only）→環境setup→ファイル単位検証→環境同期→再実装→比較 | unlocking-reverse-target-screens / syncing-reverse-env / rebuilding-screen-unit-from-docs / rebuilding-code-from-docs（mode=implement） |
| Phase 7 判定・確定 | Step 30 | judge（判定）・基準タグ本番更新。NG3系統への自動差し戻しは iterative 指定時のみ | rebuilding-code-from-docs（mode=judge） / syncing-reverse-env / orchestrating-reverse-docs-flow |

`verification_mode=docs-only` は Step 28 の詳細設計 AUTHORED 直後に、verification_url の有無を問わず静的完了として終端する。Step 29〜30 と統括 Phase 7〜11 の動的工程は常にスキップする。

キーは連番禁止。Phase/Step番号は構造上の位置番号であり管理ID用途ではないため本表の対象外（`~/.claude/rules/always/naming/semantic-key/rule.md` の「章・節の構造番号」に該当）。

## 一覧生成の種別ループ（Phase 3 / Step 9）

一覧生成工程は、アーキテクチャ調査書の `unit_kinds_present` にある全種別について、種別別の一覧生成スキルを起動する。種別とスキルの対応は下表のとおり。

| unit_kind | スキル名 | ラベル | 出力 | 配列キー |
|---|---|---|---|---|
| screen | generating-screen-list-for-reverse-docs | 画面 | 画面一覧/画面一覧.html | screens（後方互換） |
| api | generating-api-list-for-reverse-docs | API | API一覧/API一覧.html | units |
| table | generating-table-list-for-reverse-docs | テーブル | テーブル一覧/テーブル一覧.html | units |
| batch | generating-batch-list-for-reverse-docs | バッチ | バッチ一覧/バッチ一覧.html | units |
| report | generating-report-list-for-reverse-docs | 帳票 | 帳票一覧/帳票一覧.html | units |
| external | generating-external-list-for-reverse-docs | 外部連携 | 外部連携一覧/外部連携一覧.html | units |

facts抽出以降の工程（ユニット反復〜往復検証）は screen のみ実装済み。screen 以外の種別は、一覧確立をもって「後続未対応」の終端状態として記録する。

管理者（orchestrating-reverse-docs-flow）の最終報告には、全6種別それぞれについて次の3値のいずれかを必ず含める。

| 値 | 意味 |
|---|---|
| 生成済み | 実在し一覧化済み（screen のみ後続工程へ進む） |
| 対象外 | excluded-kinds.json に記録された、対象リポジトリに実在しない種別 |
| 後続未対応 | 実在し一覧化済みだが facts 抽出に進めない種別 |

## 計測キーの意味併記規約

L1〜L5 は機械キーとして config・スクリプト・返却ブロックに存続させる。人が読む文書では初出時に必ず意味を併記する。

| 機械キー | 意味 |
|---|---|
| L1 | 描画到達 |
| L2' | 内容一致 |
| L2 | ARIA一致 |
| L3 | 画素差分 |
| L4 | console一致 |
| L5 | 操作一致 |

## 15状態の判定順

統括は次の意味語キーを上から順に必ず判定する。任意状態をスキップする場合も、データ源未整備などの根拠を記録してから次へ進む。

| 順序 | 状態キー | 意味 |
|---|---|---|
| 1 | アーキ未調査 | アーキテクチャ調査書または機械ゲートが未確定 |
| 2 | 一覧未生成 | 実在種別の一覧または対象外種別の記録が未確定 |
| 3 | 共通未採録 | プロジェクト共通10文書が未確定 |
| 4 | ポータル未生成 | output_dir 直下のポータルが未生成 |
| 5 | 基盤ページ未生成（任意） | 基盤ページ群が未生成 |
| 6 | 状態遷移図未生成（任意） | 状態遷移図が未生成 |
| 7 | シーケンス図未生成（任意） | 対象画面のシーケンス図が未生成 |
| 8 | 事実未封印 | facts が未抽出・未封印 |
| 9 | 基本設計未著述 | 画面基本設計書が未著述 |
| 10 | 設計書未著述 | 画面詳細設計書が未著述または facts と不整合 |
| 11 | 画面未開通 | 実レンダリング確認済み URL が未確定 |
| 12 | ファイル単位未検証 | 着手済みのファイル単位検証が再現一致していない |
| 13 | 基準未確立 | baseline_tag が未確立 |
| 14 | 往復未検証 | judge の直近結果が PASS でない |
| 15 | 検証完了 | judge の直近結果が PASS |

静的工程は標準ユニットでは 8→9/10 の facts→基本設計・詳細設計を開通前に完了する。大規模ユニットでは 8→10（詳細設計パス1）→9+10（基本設計・観点表・テスト仕様書のパス2）の順に完了し、パス1未完了でパス2へ進まない。`docs-only` は著述モードごとの静的完了status検収直後に必ず終端し、11〜15 を判定しない。`single-pass` / `iterative` は静的完了後、必要な場合だけ 11 の開通を行い、12〜15 へ進む。

## NG帰着3系統

判定（Phase 7 / Step 30）でFAILした場合、原因を以下の3系統いずれかに帰着させ、該当スキルの改訂へ差し戻す。

| 系統 | 原因 | 差し戻し先 |
|---|---|---|
| (a) 執筆規律不足 | 詳細設計書の執筆規律・転記精度に起因する不一致 | generating-reverse-detailed-design の執筆規律 reference を改訂 |
| (b) facts欠落 | 事実抽出プロファイルが対象コードの挙動を捕捉できていない | extracting-unit-facts-from-code の抽出プロファイルを改訂 |
| (c) 共通文書欠落 | 共通設計書・規約4種等のプロジェクト共通文書に該当挙動の記載が無い | generating-reverse-common-docs を mode=append で起動 |

## 責務確定

工程をまたぐ責務のうち、解釈が分かれていた5点を工程設計の観点から次のとおり確定した。キーは連番禁止。内容を要約した意味語で付ける（番号からは情報を得られないため）。

| 確定事項 | 結論 |
|---|---|
| scaffold正本 | scaffold-screen.sh の正本は `shared/scripts/scaffold-screen.sh` の1本のみ。generating-reverse-detailed-design と rebuilding-screen-unit-from-docs は起動引数 `scaffold_script_path`（管理者が解決して渡す。`audit_script_path` と同型）で受け取って実行する |
| 単体テスト正本 | 単体テストコードの生産者は rebuilding-screen-unit-from-docs のみ。rebuilding-code-from-docs が `saved_test_paths` 不在時に観点表から自作するテストは正規の代替経路だが検証用の一時テストであり、テストコード正本ディレクトリ（`<画面ディレクトリ>/テスト項目書/テストコード/単体/`）への保存を禁止する |
| e2e作成実行分担 | E2E（RT-/SM-/IT-/CMP- 系）テストの作成と基準実測は rebuilding-code-from-docs（mode=implement）が担い、実行は syncing-reverse-env の dynamic 検査が担う（`compare_request.scenarios_ready` / syncing の `scenarios` 引数に整合）。未解決の設計課題ではない |
| 比較結果の解釈権 | compare_result.status について、syncing-reverse-env は計測事実（static_diff / dynamic / env_check）の報告者であり、往復検証の PASS/FAIL の意味解釈は judge（rebuilding-code-from-docs mode=judge）が単独で担う |
| 開通の直接起動 | unlocking-reverse-target-screens の `standalone` は syncing-reverse-env（mode=registry）を直接起動して基準まで単独完走する正式仕様である。統括の静的著述後経路は `dynamic-only` を使い、URL・frontmatter 補完後の基準確立を後続工程へ委ねる |

## 段階計画（Cycle 0〜4）

種別横断の拡張は以下の Cycle で進める。Cycle 番号は章・節と同じ構造上の位置番号である。各 Cycle の合格条件は既存原則を踏襲する: 異種プロジェクトでスキル無改造成立・決定的出力のみで検収。

| Cycle | 状態 | 内容 |
|---|---|---|
| Cycle 0 | 完了 | 一覧スキル6分割・契約明文化・責務確定・README/全体ガイド整備 |
| Cycle 1 | 未着手 | api 縦貫。extracting-unit-facts-from-code の profile=api 追加・facts-schema 拡張 → generating-reverse-detailed-design の api 章マップ → 画面レンダリング比較に代わる検証方式（スキーマ差分・HTTP応答突合）の設計 |
| Cycle 2 | 未着手 | table / batch。table はスキーマ静的比較、batch は実行契約の facts |
| Cycle 3 | 未着手 | report / external。帳票レイアウト・外部連携契約 |
| Cycle 4 | 未着手 | 上位抽象化スキル。基本設計・要件定義文書群（`納品物フォルダ体系.md` の未実装担当分） |

基盤設計.md・UI共通設計.md・データ設計.mdの3文書は、当初 Cycle 4（上位抽象化スキル）の担当分として計画していたが、generating-reverse-common-docs の Phase 4（共通文書採録）へ責務を移管し実装済みとした（`納品物フォルダ体系.md` 参照）。Cycle 4 の残担当は機能要件一覧・帳票要件・バッチ要件・外部連携要件・ビジネス概要の5文書、およびバッチ/帳票/外部連携の各基本設計書である。

## バージョニングの一般方針

テンプレート・スキーマ変更時の既存成果物との互換性方針を以下のとおり定める。

- 既存生成済み設計書は再生成時に新テンプレートを適用する。手動追記部分（§16 要確認事項一覧・レビューコメント等、著述工程の機械転記対象外の記述）は差分マージで保持し、機械転記の上書きで消さない
- テンプレート・スキーマのフィールド追加・削除・リネーム時は、既存成果物の移行手順を本文書の「改訂記録」に記録する

## 改訂記録

- 2026-07-08: 旧版欠落により既存スキル群と `納品物フォルダ体系.md` から再構成（「出自」を参照）
- 2026-07-10: generating-unit-list-for-reverse-docs（unit_kind 引数方式の単一スキル）を種別別の6スキル（「一覧生成の種別ループ」の表を参照）へ分割。Phase/Step 対応表の Step 9 を種別ループへ更新し、「一覧生成の種別ループ」「責務確定」「段階計画（Cycle 0〜4）」の各節を新設
- 2026-07-12: 基本設計書生成スキル generating-reverse-basic-design を新規実装（従来「子スキルなし・現時点で未実装」だった基本設計未著述状態を解消）。Phase/Step 対応表に Step 22-24（テンプレート展開とfacts読込／facts→業務語彙への転記／実装用語混入検査）を新設し、旧 Step 22-27（detailed-design・rebuilding・judge）を Step 25-30 へ繰り下げ。旧番号を参照していた `generating-reverse-common-docs/SKILL.md`・`extracting-unit-facts-from-code/SKILL.md`・`スキル実装計画.md` の Step 27 参照を Step 30 へ追従済み
- 2026-07-13: 複雑度プロファイリング（`shared/scripts/unit-list/detect-screens.sh` の `--profile` サブコマンド）を新規実装。Phase 3 / Step 9（一覧生成の種別ループ）の screen 系スキルに複雑度層別サンプリング（S/M/L/XL）が加わり、`スキル実装計画.md`「複雑度プロファイリング（第9回追加）」節に実装物・検証結果を記載
- 2026-07-24: テスト基盤整備を実施。E2E 責務分担は既に確定済みであり（未解決の設計課題ではない）、新工程の追加は不要と判断した。本セッションで整備した内容は以下のとおり: (1) テスト基盤調査をアーキテクチャ調査書の§9に追加, (2) 画面一覧マニフェストに `existingTestCount` フィールドを追加, (3) テストケースを md 方式（`テスト項目書/単体テスト仕様書.md` 等）に統一し、HTML 生成スキルは不要とした, (4) テスト仕様書 md のポータルビューア連携を `build-portal.sh` に追加（doc-nav のテストケースタブが `テスト項目書/単体テスト仕様書.html` を参照）

## 関連

- `スキル実装計画.md` — Stage別の実装順序・完了条件・検証想定の正本
- `納品物フォルダ体系.md` — 成果物の置き場（`<output_dir>` 配下構成）の正本
- `.claude/skills/orchestrating-reverse-docs-flow/references/contract.md` — 返却ブロック契約・args仕様・状態判定表の正本
