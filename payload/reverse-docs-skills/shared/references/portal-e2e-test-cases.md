# ポータル E2E テストケースカタログ

## 目的と実行方法

ポータル全画面（トップ・一覧 7 種・交差ビュー 4 種・AI設定資産・画面遷移図・技術スタック・環境構築手順・用語辞書・プロジェクト共通 3 種の計 20 ページ。ER図.html は対象外）の操作品質を回帰検証する。ページ追加・テンプレート改修のたびに本カタログのケースを通し、導線切れ・操作機能の退行を検出する。

実行方法は 2 種を併用する。

| 実行方法 | 内容 | 対象ケース |
|---|---|---|
| 静的 | `shared/scripts/test-e2e-portal.sh` を実行し、生成済み HTML への grep / jq 検査で DOM 構造・リンク・CSS 契約を機械検証する | 検証方法 = 静的 |
| ブラウザ | 本カタログのケースを Playwright 等で手動・機械実行し、クリック・入力・遷移の実挙動を検証する | 検証方法 = ブラウザ |

## テストケース表

記入規則: ケースキーは連番禁止。内容を要約した意味語で付ける（番号からは情報を得られないため）。

| ケースキー | 対象ページ | 操作 | 期待結果 | 検証方法 |
|---|---|---|---|---|
| 導線-トップカード開通 | index.html | カテゴリカード・導線カードの遷移先パスを列挙する | 全遷移先の HTML ファイルが実在する（リンク切れなし） | 静的 |
| 戻るリンク-一覧階層 | 一覧 7 種 | 戻るリンクの `href` を検査する | `../../index.html`（ポータルトップ）に解決される | 静的 |
| 戻るリンク-下層ページ | 交差ビュー 4 種・AI設定資産・プロジェクト共通 3 種 | 戻るリンク・ブランドリンクの `href` を検査する | 相対パスがポータルトップ index.html の実在パスに解決される | 静的 |
| 埋め込みデータ-JSON妥当 | 全 20 ページ（`page-data` / `*-manifest` を持つページ） | 埋め込み `<script>` の JSON を jq でパースする | パースエラーなし。必須キーが存在する | 静的 |
| テーマ-トグル設置 | 全 20 ページ | `id="theme-toggle"` の存在を検査する | 全ページにテーマ切替ボタンが設置されている | 静的 |
| コピー-全セル付与 | 一覧 7 種・交差ビュー 4 種 | データセルへのコピー機構（`copy-btn` 生成コード）を検査する | 全データセルにコピー操作が付与される実装が存在する | 静的 |
| 縦書き-正立表示 | 権限画面マトリクス・権限機能マトリクス | 列ヘッダの縦書き CSS を検査する | `writing-mode: vertical-rl` と `text-orientation: mixed` が併記され、和文が正立で表示される | 静的 |
| 行高-1行固定 | 一覧 7 種 | 一覧テーブル `td` の CSS を検査する | `white-space: nowrap` + `overflow: hidden` + `text-overflow: ellipsis` により行が 1 行高に固定される | 静的 |
| セクション-AI設定資産4種 | AI設定資産 | rules / skills / subagents / hooks の各セクション要素を検査する | `section-rules`・`section-skills`・`section-subagents`・`section-hooks` と対応 4 テーブルが存在する | 静的 |
| 検索-部分一致絞り込み | 一覧 7 種・交差ビュー 4 種・AI設定資産・用語辞書 | 検索ボックスに既存データの部分文字列を入力する | 一致行のみ表示され、非一致行が消える。入力クリアで全行復帰する | ブラウザ |
| q受け口-画面一覧 | 画面一覧（代表。実装は一覧 7 種共通） | `?q=<語>` 付き URL で開く | 検索ボックスに初期値が入り、絞り込み済みの状態で表示される | ブラウザ |
| チップ-区分トグル | 一覧 7 種・用語辞書 | 区分チップをクリックし、再クリックで解除する | クリックで該当区分のみに絞り込まれ、解除・クリアで全行復帰する | ブラウザ |
| 行展開-クリック開閉 | 一覧 7 種 | 一覧の行をクリックし、再クリックする | 詳細行（row-detail）が開閉する。他行の表示は崩れない | ブラウザ |
| csv-出力ボタン | 一覧 7 種 | CSV 出力ボタンをクリックする | 表示中の絞り込み結果と同内容の CSV がダウンロードされる | ブラウザ |
| ソート-列ヘッダ | 一覧 7 種 | 列ヘッダをクリックし、再クリックする | 昇順・降順が交互に切り替わり、行順が追従する | ブラウザ |
| ページネーション-50件境界 | 一覧 7 種 | 51 件以上のデータで次ページ・最終ページへ移動する | 1 ページ 50 件で分割され、先頭・前・次・末尾の各ボタンと件数表示が整合する。50 件以下ではページ送りが不活性となる | ブラウザ |
| テーマ-切替永続 | 全 20 ページ（代表: index.html） | テーマ切替ボタンをクリックし、リロード・別ページ遷移する | `localStorage`（キー `rd-portal-theme`）に保存され、リロード後・遷移先でもテーマが維持される | ブラウザ |
| 相互参照-画面から関連API | 画面一覧 | 行展開内の関連 API リンクをクリックする | API一覧の該当ユニットへ遷移する（`relatedApis` の unitKey と一致） | ブラウザ |
| ロールチップ-列絞り込み | 権限画面マトリクス・権限機能マトリクス | ロールチップをクリックし、解除する | 該当ロール列のみに絞り込まれ、解除で全列復帰する | ブラウザ |
| モード切替-追跡可能性2モード | 追跡可能性 | 画面起点・テーブル起点のモードタブを切り替える | 2 モードの表が排他的に切り替わり、アクティブタブの見た目が追従する | ブラウザ |
| モード切替-遷移図3モード | 画面遷移図 | 画面別・動線別・未接続のモードを切り替える | 3 モードの表示が排他的に切り替わり、各モードで対応する図・一覧が描画される | ブラウザ |
| マークダウン-共通文書描画 | プロジェクト共通 3 種 | ページを開く | 埋め込み Markdown（`doc-md`）が `doc-content` に見出し・表つきで描画される | ブラウザ |

## カバレッジ表（対象ページ×共通操作）

各ページにどのケースが適用されるかの対応表。全 20 ページ共通の 3 ケース（埋め込みデータ-JSON妥当・テーマ-トグル設置・テーマ-切替永続）は表から省き、ページ固有の適用ケースのみを列挙する。

| 対象ページ | 適用ケースキー（全ページ共通 3 ケースを除く） |
|---|---|
| index.html（ポータルトップ） | 導線-トップカード開通 |
| 一覧 7 種（画面/API/テーブル/バッチ/帳票/外部連携/機能） | 戻るリンク-一覧階層・コピー-全セル付与・行高-1行固定・検索-部分一致絞り込み・チップ-区分トグル・行展開-クリック開閉・csv-出力ボタン・ソート-列ヘッダ・ページネーション-50件境界 |
| 画面一覧（一覧共通ケースに追加） | q受け口-画面一覧・相互参照-画面から関連API |
| 権限画面マトリクス・権限機能マトリクス | 戻るリンク-下層ページ・コピー-全セル付与・検索-部分一致絞り込み・縦書き-正立表示・ロールチップ-列絞り込み |
| CRUD図 | 戻るリンク-下層ページ・コピー-全セル付与・検索-部分一致絞り込み |
| 追跡可能性 | 戻るリンク-下層ページ・コピー-全セル付与・検索-部分一致絞り込み・モード切替-追跡可能性2モード |
| AI設定資産 | 戻るリンク-下層ページ・検索-部分一致絞り込み・セクション-AI設定資産4種 |
| 画面遷移図 | モード切替-遷移図3モード |
| 技術スタック・環境構築手順 | （全ページ共通 3 ケースのみ） |
| 用語辞書 | 検索-部分一致絞り込み・チップ-区分トグル |
| プロジェクト共通 3 種（コーディング規約/共通設計書/命名規約） | 戻るリンク-下層ページ・マークダウン-共通文書描画 |

## 設計判断

### test-e2e-portal.sh

- 配置: shared/scripts/test-e2e-portal.sh
- 必要性: ポータル全 20 ページの回帰検査は、リンク解決・埋め込み JSON 妥当性・CSS 契約（縦書き・行高）・DOM 構造の 20 超の検査を 20 ページへ掛け合わせる作業であり、毎回手動 grep では再現不能。本カタログの E2E ケースのうち静的検証部（検証方法 = 静的）を機械化し、同一入力から同一判定を返す回帰エンジンとして固定する
- 代替案を採用しなかった理由:
  - Bash ツール直叩き: 20 超の検査 × 20 ページの組み立てを毎回行うのは非現実的で、検査条件が実行ごとにぶれて回帰検証が成立しない
  - 既存 Makefile 拡張 / package.json scripts 追加: 本リポジトリにビルド設定が存在せず、新規導入は本検査専用の依存を増やすだけになる
- 保守責任者: 人手（ユーザー）。ページ・機能を追加した場合は本カタログのテストケース表・カバレッジ表と本スクリプトの検査を同時に追従させる
- 廃棄条件: ポータル生成が別基盤に移行した時
