# 章役割キー→§→節キーワードの対応表

章役割キー→§→節キーワードの対応表。本リポジトリの shared/ 配下で正本を持ち、rebuilding-screen-unit-from-docs / rebuilding-code-from-docs へ chapter_map_path として配布する。

## 章の役割キー → 節キーワード対応表（正本）

章の特定は設計書の章マップ表（役割キー列）で行う。`shared/scripts/audit-consistency.sh` は章マップ表から役割キー→§番号を解決し、以下の節キーワードで二次解決する。本表が節キーワード対応の正本であり、SKILL.md・ng-classification.md・report-format.md・guide.html はここへのポインタとする。

| 役割キー | 節キーワード（既定 §） |
|---|---|
| 機能一覧 | 機能一覧表（既定 §2） |
| 画面構造 | スタイル適用パターン（既定 §3.6） |
| 状態管理 | メイン画面の状態変数（既定 §5.3）・サブコンポーネントの状態変数（既定 §5.4） |
| データフロー | 空状態（既定 §6.6） |
| API通信 | 各 API のリクエスト・レスポンス型（既定 §7.2） |
| イベント処理 | イベント処理挙動（既定 §8） |
| 領域別仕様 | 項目定義・イベント挙動（既定 §9） |
| 定数設定値 | 定数値（既定 §10） |
| エラーハンドリング | メッセージ定義・空状態エラー状態（既定 §11.2/§11） |
| 画面遷移 | 遷移方式・パラメータ（既定 §12） |
| 実装契約 | ファイル分割（既定 §15.1）・型定義（既定 §15.2）・依存（既定 §15.3）・スコープ分類（既定 §15.5） |
