# gold-standard 実証記録

## 合成原本コード

| ファイル | 行数 | 役割 |
|---|---|---|
| ItemListPage.tsx | 197 | メイン画面コンポーネント（一覧表示・検索・追加・削除） |
| ItemListColumns.tsx | 64 | カラム定義・ソート関数・styled コンポーネント |
| ItemListPage.gql.ts | 47 | GraphQL クエリ/ミューテーション定義・型定義 |

10構文パターン網羅: テーブル表示 / フォーム入力 / API呼出し / エラー処理 / インラインsx+ネストセレクタ / ローカル型定義 / enum引数 / useEffect / import type / 画面遷移

## facts 抽出結果

- 12分類すべてにセクションあり（measurement_pending は「該当なし」で items 空・reason 記載）
- 全アイテム数: 約70件
- evidence 形式: 全件 `<ファイル名>:<行番号>` 準拠

## 設計書著述

docs/facts.yml から著述。全章を著述で埋め、実測委譲0件。

## 検証スクリプト結果

- `backtest-facts-against-gold.sh --self-test`: 全ケース PASS
- `check-doc-coverage-against-gold.sh --self-test`: 全ケース PASS
- gold 自己突合: coverage=100.0（第1引数=第2引数で検証）

## 備考

本 gold-standard は合成コード（プロジェクト非依存の語彙のみ使用）に対する正解基準であり、実プロジェクトの設計書の直接的なテンプレートではない。用途は3つ: (1) バックテスト（スキーマ検証） (2) カバレッジ受入判定 (3) 著述見本（書式のみ参照、値の転写禁止）。
