# 体系の再設計

このリポジトリの skill・要件・設計を組み直すための設計資料。決めたこと、その理由、退けた案、まだ決めていないこと、改善事項を1つのファイルで追う。要件の定義は `docs/design/AI駆動開発の導入支援の要件.md` である。

## 決定事項

| 決定 | 内容 | 採用した理由 | 退けた案 |
|---|---|---|---|
| 業務の柱 | 業務は調査・基盤一式の作成・引き渡しの3段階で進める。調査は人の手作業であり、このリポジトリは担わない | 参照スライドの3区分のうち、調査は見積もり前の判断であり支援ツール化の対象ではないため | 調査を支援ツールの柱に含める案 |
| 納品物 | 設計書・規約、各AIツールの設定、開発フローの3つ。用語は規約（業務の言葉の決まり）に含める | 用語が無いと開発が回らない根拠が無く、配る規約の1件として既に存在するため | 用語を独立した納品物にする案 |
| AIツール名 | 要件・納品物にツール名を書かない | 未対応のツール（GitHub Copilot）を約束することになるため。現場の環境の事実としてだけ制約に残す | 納品物にツール名を列挙する案 |
| リポジトリの担当 | セットアップと運用。セットアップの中に納品物セットアップ・リバースと、任意のポータル・反復リバース検証を置く | 業務の柱と支援ツールの単位は粒度が違う。単独で使いたい単位（リバースだけ等）を独立させる必要があるため | 業務の柱をそのままフォルダにする案 |
| 独立単位 | setup / reverse / verify / portal / operate の5つ。skill 名の接頭辞で表し、大分類（セットアップ／運用）は宣言に持つ | Claude Code が skill を見つけるのは `.claude/skills/` の1階層だけで、その下に柱のフォルダを作れないため | 最上位フォルダで柱を切る案 |
| 独立の条件 | 他の単位の skill 名を本文に書かない。受け渡しは出力ファイル経由だけ。共有部品は単位ごとに持つ | 単位を単独で取り出して動かすため | 共有部品を1か所に置き全単位が参照する案 |
| 機能の単位 | 1 skill = 1 機能。フォルダの中に SKILL.md・scripts・templates・tests・samples をすべて持つ | 追加が1フォルダで完結する。今は1納品物で8か所を直す必要があり、どこを直すか分からない | 種別（画面・API…）をフォルダの階層にする案 |
| 機能の宣言 | SKILL.md の先頭に unit・category・kind・inputs・outputs・requires・acceptance を書く。統括は宣言を読んで inputs と outputs の依存で順序を決め、skill 名を直書きしない。requires には同じ単位の機能名だけを書ける | 機能を足したとき統括を編集しなくてよいため。現在の統括は43本の skill 名を直書きしている | 統括の Step 本文で呼ぶ現行方式 |
| 統括の実装 | `setup-orchestrating-units` の `plan-setup.sh` が機能の宣言（requires と inputs・outputs の重なり）から実行順を導き、`STEP <n> <機能名>` として返す。統括の手順書は計画の出力から名前を読み、機能名を直書きしない。入出力の重なりはパターンの固定部分の接頭辞一致で判定する近似であり、外部入力は表に載せて判断を読む側に委ねる | 機能を足したとき統括を編集しなくてよいため。旧の統括は 43 本の名前を直書きしていた | 統括の Step 本文で名前を呼ぶ旧の形 |
| 合格の集計 | 機能は tests が全部通ること。単位は配下の機能がすべて合格。要件は受入基準を担う単位が合格 | 何をクリアすればよいかが各階層で機械的に決まる。テスト設計を機能ごとの表として書ける | 階層ごとに別の基準を持つ案 |
| 欠落の検査 | テストを持たない機能・どの機能にも属さないテスト・2単位に属する機能・統括が直書きする機能名・他の単位の名前を持つ機能の5つを0件で要求する | 追跡の鎖が切れた箇所を機械で見つけるため | 目視レビューだけに頼る案 |
| 共有の定義 | output-layout・portal-catalog などの定義は `docs/` に1つだけ置き、各単位へ配布時に複製する。一致は検査で見張る | 4単位から読まれる定義を手で5か所同期する形を避けるため。定義と生成物の分け方の決まりに沿う | 各単位に手で写す案 |
| ポータル | 任意。第一段階（AIが読む定義）だけで開発フローが回る。ポータルは第一段階の出力だけを入力にする。portal 以外の4単位の SKILL.md・scripts・tests に portal の語が現れないことを欠落の検査で見張る | 業務の3段階のどこにもポータルは必須として現れないため | ポータルを納品物に含める案 |
| 確認事項一覧 | 1行1事項。単位（画面・API等）はキー列。確認事項・規約提案の採否・用語の承認の3種類を1枚に載せる。Markdown を定義とし、各行に反映先を持つ | 顧客が確認する資産を1つにする。1単位に複数の事項があるため1行1単位にはしない。単位ごとの台帳とポータルの質問票に定義が散っている状態を解消する | 1行1単位の案、単位ごとの台帳を定義にする案、ポータルの質問票を定義にする案 |
| ユーザーへの提示 | スライドを見せる前に観点表と判定役を通し、タイトル・副題・読み手と結論の3行だけで提示する | 長文の提示が読まれずに差し戻された実例による。規約 `generating-explanation-html-slides` の「ユーザー提示」節に定めた | 構成の解説や検査結果を添える提示 |
| ルート構成 | ルート直下は `docs/`・`.claude/`・`delivery-payload/`・`ai-work/` の4つ。定義・機能・配布の3層と運用の記録に対応する。`generation-engine/` は解体し、scripts と samples は各機能フォルダへ、単位共有の部品は `delivery-payload/<単位>/shared/` へ移す。`ai-work/` はこの体系を作り直す作業の計画・作業指示書・完了記録を置き、先方リポジトリの `ai-work/` と同じ形にする | 層とフォルダを1対1にし、置き場に迷わないため。作業の計画を旧リポジトリの指示書に置いたままでは、旧は不具合修正だけを行う決定と矛盾するため | `generation-engine/` を残して中を単位で切る案、作業の計画を旧リポジトリの `docs/tasks/` に置き続ける案 |
| skill の定義と派生 | `docs/skills/<単位>-<機能>/` を定義とし、`.claude/skills/` へ機械で派生させる。派生の仕組みは新リポジトリの最初の機能とする | 納品先に求める「定義は docs、派生は生成物」の仕組みを自分自身にも持つため。持たなければ納品先に同じ運用を求める根拠が無い | `.claude/skills/` を定義にする案（編集のたびの派生は不要だが、納品先と仕組みが食い違う） |
| 規約の定義の形 | front matter に `docType: rule \| context` と任意の `derivedPath` を持つ。`context` は説明文書（project-context 等）で、規則の表の検査を飛ばし本文だけを派生する。`derivedPath` は共通の hook が固定の場所で読む派生物のために派生先を上書きする。生成器はリポジトリのルート・分類定義・checker の置き場をすべて引数と規約フォルダから解決し、自分の位置に依存しない | 旧の生成器を新しい置き場で動かしたところ、置き場依存のパス解決と規則表前提の検査で止まったため | 説明文書を規約の外に置く案（共通 hook の固定パスへ派生できない） |
| project-context の置き場 | 配置先でも自分のリポジトリでも `docs/rules/agent-operations/project-context/rule.md` を定義とし、`docType: context` と `derivedPath` で共通の hook が読む固定の場所へ派生させる。`flow-values.yml` は規約ではないため配置が直接書く | 配置が派生物を直接書くと、定義から派生する形と矛盾し、ずれ検査が常に「余分」を報告するため。親は分類定義にある `agent-operations` に揃え、自分専用の親を作らない | 配置が `.claude/rules` を直接書く旧の形、自分専用の親 `repository-operations` を持つ案 |
| category の値 | 宣言の category は支援ツールの3本柱に対応する `survey`・`setup`・`operate` の3値。unit が operate なら category は operate。それ以外の unit は3値のどれでもよい（例: 納品先で回すポータル保守は unit=portal・category=operate） | 要件の合格を柱ごとに集計するには、機能が柱のどれを担うかを宣言に持つ必要があるため。2値では調査を支える柱を表せなかった | category を setup／operate の2値にする当初の案 |
| 合格の集計の実装 | `setup-checking-acceptance` が機能（tests全通過）→単位（配下すべて合格）→要件（柱ごとにcategory一致の機能がすべて合格し、必須単位に機能がある）を1コマンドで判定し、欠落5つを0件で要求する。柱と単位の対応は `docs/design/requirement-pillars.json` に置く。tests1本の上限は300秒で、超過は不合格ではなく未確認として扱う | 何をクリアすればよいかを各階層で機械的に決めるため。上限超過を不合格に数えると、実行環境の都合を成果物の欠陥と読み違える | 階層ごとに別の基準を持つ案、上限超過を不合格に数える案 |
| skill の派生の形 | `docs/skills/<名前>/` から `tests/` と `samples/` を除く全ファイルを `.claude/skills/<名前>/` へ複製する。SKILL.md は front matter の直後に生成物の印を入れる。派生の前に定義の形（宣言の鍵・名前と接頭辞と unit の一致・requires の同一単位・tests の実在・他の単位の名前の不在）を検査する | 実行時に要るのは手順とスクリプトだけで、検査と見本は定義側にあればよいため。欠落の検査を派生の入口に置けば、検査を通らない機能は Claude Code に読まれない | 派生せず `.claude/skills` を定義にする案 |
| 新リポジトリ | `ai-driven-development-setup` を新設し、そこへ新構造を作る。旧リポジトリ（reverse-docs-skills）は配布済みの納品物の不具合修正だけを行い、新しい機能を足さない | 旧の規約・hook・公開の受け口が現構造に結び付いており、その場で組み替えると作業のたびに止まるため。移行の期間に `generation-engine/` が残ることも、ルート直下の許可リストと両立しない | 同じリポジトリの中で横に作る案 |
| 移行の進め方 | 単位ごとに新リポジトリへ作る。合格条件に「旧リポジトリの見本と同じ入力から同じ出力が出る」を含める。旧の検査は写さず、機能ごとの tests へ振り直す。単位が合格するごとに旧の対応する skill を廃止する | 一気に移すと合格を判定できない期間が長くなるため | 全部書いてから一気に移す案 |
| このリポジトリ自身の定義と生成物 | 規約は `docs/rules/` を定義とし `.claude/rules/` へ派生させる。skill も同じ仕組みで派生させる | 納品先と同じ仕組みを自分自身が持つため | `.claude/` を定義にする案 |
| 公開 | リポジトリ自体は GitHub に置かない（remote を持たない）。必要な内容だけを agent-toolkit の `payload/ai-driven-development-setup` へ同期して公開する。公開対象は `.claude/skills`（派生）・`delivery-payload`・`README.md` とし、`docs/` は配らない。受け口 `.claude/rules/always/publish/publish-values.txt` は hook が固定の場所で読むため `.claude/` に直接置く。同期定義への登録は最初の配布物ができた時に行う | 旧リポジトリと同じ運用にし、定義と設計資料を外へ出さないため | GitHub に非公開リポジトリを作る案 |
| skill の名前 | 名前は `<単位>-<作業>-<対象>` の 3 語だけで組む。種別は `kind`、文脈は `unit`・`category` が表し、名前に入れない。共有部品は `<単位>-shared`。規約 `docs/rules/agent-operations/skill-naming/` が定義し、検査は機能の派生の入口から呼ぶ | 名前で単位をグループ分けしているため、形が崩れると欠落の検査と統括の計画が崩れる。旧の名前は文脈と種別の語を重ねており、束ねる粒度と合わない | 名前の形を validate の実装だけに持ち規約を書かない案 |
| 規約と検査の 1 対 1 | 配る規約の定義は `templates/rules/tool-defined/<親>/<子>/` に rule.md・checker・checker の test を同じフォルダで持つ。横断の検査 5 本は規約に起こし（27 → 32 件）、横断の枠は無くす。検査は宣言と実在の 1 対 1（宣言の無い checker・1 つの checker を 2 規約が宣言・test の欠落）を不合格にする | 共通の決まり「規約のフォルダに、その規約を守らせる hook を 1 対 1 で置く」を定義側にも適用するため。定義と配置先で置き場が違うと、どの検査がどの規約のものかを追えない | checker を 1 フォルダにまとめる旧の置き場、1 規約に複数の checker を許す案 |
| リバースの流れ | 第一フェーズ（AI が読む: 領域の切り出し → 領域ごとの調査 → 要件定義書 → 共通設計文書 → 検査と出し直し → 範囲の承認）と第二フェーズ（道標の検出条件を実行: 一覧化 → 要件定義書の裏付け → 基盤文書 → 事実の取り出し → 基本設計と単体テスト設計 → 完了判定 → 共通処理の詳細設計 → 詳細設計 → テスト設計 → 規約提案 → 確認事項一覧 → 提示 → 完了報告）の 19 工程。定義は `docs/design/リバースの流れの設計.md` | 知らないリポジトリに既製の検索パターンを当てられないため、AI が読んで対象に合った検出条件を組み、それを機械で実行して件数で確かめる 2 段にした。実案件のコードで第一フェーズを通し、4 種別で実測が候補数と一致した | 一覧を先に機械で作り、その後に要件を書く旧の順序（言語ごとの走査スクリプトを前提にする） |
| 要件定義書の組み込み | 含める。第一フェーズで AI が現状の要件として書き、第二フェーズの一覧で裏付ける。様式は規約「設計書の書き方の決まり」の 6 内容に節を 1 対 1 で対応させた新しいもの | 基本設計書は要件定義書で確定した要求を実現する文書であり、要件なしに書き始めない。旧の 5 文書の様式は規約の内容を持っていなかった | 含めない（当初の既定）。一覧と基本設計書と現場の回答から起こす 4 段の順序 |
| 機能の粒度 | 作業ごとに 1 機能。種別は `kind` の引数で受ける | 種別ごとに機能を分けると同じ作業が 7 つに割れ、統括と検査が増える | 種別ごとに機能を分ける |
| 単位共有の部品の置き場 | `docs/skills/<単位>-shared/` に置き、配布時に `delivery-payload/<単位>/shared/` へ複製する | 単位を単独で取り出して動かすため、共有は単位の中に閉じる | 全単位が 1 か所を参照する |
| 人に聞く場面 | 範囲の承認・確認事項の提示・完了の受け入れの 3 回。中間で人を待たず、既定を置いて進む | 無人で走らせるため。判定を人に頼ると基準がぶれる | 基本設計の完了時に人へ確認する |
| python 専用の事実抽出の経路 | 持ち込まない。言語ごとの走査や抽出の規則は支援ツール側に持たず、第一フェーズが対象を読んで検出条件と取り出しの規則を組む | 言語ごとの経路を支援ツールが持つと、知らない対象で合わない | 言語ごとの経路を機能に持つ |
| 検証の出力先 | 対象リポジトリの外（`ai-output/<先方の名前>/<実行の識別子>/`）。確認事項・承認の記録・事実・記録を置く | 先方リポジトリには AI が読む定義だけを置き、回答の途中の状態を混ぜない | 対象リポジトリの中に置く |
| 確認事項一覧のキー | 内容を要約した意味語のキー。連番は使わない | 規約が連番を禁じている | 連番の番号 |
| 公開の段階 | 納品物の段階で切る。公開1〜3 | 機能の途中で止めると完了の判定ができないため | 機能の途中で止める案 |
| 出力の範囲 | 統括の前提の確認で3値（全部・基本設計まで・一覧まで）を選ぶ | 一覧までや基本設計までの試作を、人に聞かずに打ち切れるようにするため | 常に全部を実行する案 |
| 要件定義書の裏付けを独立の機能にする | reverse-checking-requirements として工程2-2に置く | 工程2-2の役割を機能の宣言と1対1にするため | 一覧化の機能に含める案 |

## まだ決めていないこと

| 論点 | 選択肢 | 既定（決まるまでの扱い） |
|---|---|---|
| 様式外の内容の扱い | 現場の判断が要るものは確認事項へ、繰り返し現れる種類はテンプレート改訂候補へ、単発は該当する節へ様式外の印を付けて書く。様式外事項の一覧を出力に加える／扱いを定めない | 定めない。決まれば reverse の出力と検査に加える |
| 既存53本の skill の対応 | 改名・統合・新設・廃止の対応表（提案済み。5単位の接頭辞へ振り直しが要る） | 対応表を確定してから移行する |

## 改善事項

調査（2026-09-02）で確かめた事実に基づく。場所はファイル名と行番号で示す。

| 改善事項 | 場所 | 直し方 | 対応する決定 |
|---|---|---|---|
| 反復検証の一気通貫がポータル生成を内包する | generation-engine/scripts/verification/run-layer-full-pipeline.sh:1094-1100 | ポータル段を選択式にする | ポータル |
| 健全性判定がポータルの規約検査へ委譲し、網羅の分母にポータル入口を含む | verification/check-sound.sh:124-139、verification/check-coverage.sh:183 | ポータルを選んだ時だけ分母と検査に含める | ポータル |
| 開発フロー Phase 5 がポータル再生成の成功を統合条件にする | delivery-payload/templates/delivered-skills/dev-flow/SKILL.md:205 | ポータルがある場合だけ再生成する。設計書の値の同期検査は残す | ポータル |
| 用語の公開がポータル専用 HTML の投影を呼ぶ | .claude/skills/managing-semantic-glossary/SKILL.md:107-122 | 投影の段を portal 側の skill へ移す | 独立単位 |
| 規約配布が生成エンジン全体を丸ごと複製する | generation-engine/scripts/rules/deploy-generation-engine.sh:184-190 | 単位ごとの配布に分ける | 独立単位 |
| 納品物一覧が全単位の出力の有無で状態を判定する | generation-engine/scripts/build-deliverable-inventory.sh:71-72,580 | どの単位にも属さない報告として統括の側に置く | 独立単位 |
| 統括が43本の skill 名を直書きしている | .claude/skills/orchestrating-ai-development-setup/SKILL.md | 宣言を読む統括に作り直す | 機能の宣言 |
| 被参照0の skill が4本ある | generating-requirement-definitions、generating-rule-proposals、generating-sequence-diagram、prioritizing-improvement-tasks-from-images | 前3本は単位へ組み込む。最後の1本はこのリポジトリ自身の運用として残す | 機能の単位 |
| 定義 JSON 22件を手で同期している | delivery-payload/references/*.json | 各機能の宣言から生成する派生物にする | 共有の定義 |
| 共有部品を35ファイルが読み、定義2件を50・25ファイルが読む | generation-engine/scripts/output-layout.sh、delivery-payload/references/output-layout.json、portal-catalog.json | 定義は1つ、配布時に複製 | 共有の定義 |
| 質問票がポータルの HTML としてだけ出る | generation-engine/scripts/extract/build-confirmation-survey-data.sh、matrix/confirmation-survey-template.html | 確認事項一覧を Markdown で reverse の出力にする | 確認事項一覧 |
| 単位ごとの要確認事項台帳とポータルの質問票に定義が散る | 要確認事項台帳.json（設計単位ごと） | 確認事項一覧を定義にし、台帳は廃止するか一覧から導く | 確認事項一覧 |
| 納品スキル・agent の配布が規約の配置と同じスクリプトに同居していた | 旧 generation-engine/scripts/rules/scaffold-rule-definitions.sh の `--with-skills` | 運用（operate）単位の機能 `operate-installing-skills` として別に作る | 機能の単位 |
| 配る規約の本文 3 件に textlint の指摘が 4 件残る（destructive-operation-safety の冗長表現・document-writing の助詞の重複と 1 文 109 字・scalability の助詞の重複） | docs/skills/setup-scaffolding-rules/templates/rules/tool-defined/ の該当 rule.md | 本文を直し、配置される規約全件に textlint を通す検査を配置の tests に足す | 規約と検査の 1 対 1 |

## 実践で見つかった欠点と不具合

旧リポジトリの規約派生の仕組みを新リポジトリへそのまま持ち込み、このリポジトリ自身の規約で動かして確かめた（2026-09-02）。同日、規約の配置（setup-scaffolding-rules）を旧から持ち込み、空の対象へ27規約を配置して派生を通す実践でも欠点と不具合を見つけた。

| 事象 | 場所 | 原因 | 扱い |
|---|---|---|---|
| `build-derived-rules.sh --self-test` が終了コード1で止まり、ケース8以降の結果が1行も出力されない | docs/skills/setup-deriving-rules/scripts/build-derived-rules.sh の deploy_rule_scripts 関数（694-736行目） | 4本目のスクリプト resolve-applicable-rules.sh を複製していない。旧リポジトリでは delivery-payload/templates/rules/checkers/resolve-applicable-rules.sh から配備するが、新リポジトリにはこの置き場が無く、SCRIPT_DIR への自己参照フォールバックも無いため deploy_rule_scripts が return 1 し、set -euo pipefail により自己テスト全体が即座に終了する | 対応済み（resolve-applicable-rules.sh を docs/skills/setup-deriving-rules/scripts/ へ複製し、4本とも同じフォルダから複製する形に統一した） |
| REPO_ROOT・TAXONOMY_JSON・CHECKERS_DIR が本来のリポジトリルートではなく docs/ を指す | docs/skills/setup-deriving-rules/scripts/validate-rule-definitions.sh 52-54行目 | REPO_ROOT の算出が `SCRIPT_DIR/../../..` の固定3階層上昇。旧リポジトリでは generation-engine/scripts/rules/ が3階層でルートに届いたが、新リポジトリの置き場 docs/skills/setup-deriving-rules/scripts/ は4階層深い。実測: `validate-rule-definitions.sh docs/rules` の実行で「参照したパス」に `.../docs/delivery-payload/references/rule-taxonomy.json` が表示され、本来の `delivery-payload/references/rule-taxonomy.json` を指していないことを確認した | 対応済み（REPO_ROOT・TAXONOMY_JSON・CHECKERS_DIR の固定パス算出を廃し、分類定義は任意引数 --taxonomy で受け、checker本体は docs/rules のルート配下を走査して求める形にした） |
| `rule-taxonomy.json` が無い場合、checker宣言の検査は行われず [UNKNOWN] を出すだけで通過する | 同上 | 新リポジトリはこの段階で rule-taxonomy.json を持たない。パス誤りとは独立に、そもそも定義が無い | 対応済み（--taxonomy 未指定は判定不能を表す [UNKNOWN] ではなく、対象が無いことを表す [SKIP] を1行返して通す形にした） |
| `docs/rules/repository-operations/project-context/rule.md` が validate-rule-definitions.sh で不合格になる | 検査「このプロジェクトの規則-存在」「規則-検査列」 | project-context/rule.md は旧リポジトリの `規則 \| 内容 \| 検査` 形式の規則表を持たない、概要・技術スタック・設定索引を書いた説明文書である。validate-rule-definitions.sh は全規約がこの規則表形式を持つことを前提にしており、説明文書型の規約を許容しない | 対応済み（front matter に docType: rule／context を追加し、context の規約は規則の表を前提にする3検査を飛ばす形にした。project-context/rule.md に docType: context を設定して合格させた） |
| `build-derived-rules.sh`（dry-run・--apply とも）が検査不合格の project-context を1件だけ飛ばして続行し、対象規約が他に無いため「生成 0 件」で終了コード1になる | docs/skills/setup-deriving-rules/scripts/build-derived-rules.sh のケース19相当の挙動 | 上記の project-context 不合格が唯一の入力規約であるため、件単位のスキップ機構が働いても生成対象が0件になる | 対応済み（project-context/rule.md が合格するようになったことで解消した。build-derived-rules.sh --apply を実行し、.claude/rules/・.cursor/rules/・AGENTS.md 等が1件生成されることを確認した） |
| `check-rule-drift.sh <リポジトリのルート>` という1引数呼び出しは使い方エラー（終了コード2）になる | docs/skills/setup-deriving-rules/scripts/check-rule-drift.sh の usage 出力 | このスクリプトは `<docs/rules のルート> <出力先リポジトリルート>` の2引数を要求する。作業指示書の手順4の呼び出し例（1引数）は実際の引数契約と食い違っていた | 対応済み（SKILL.md の手順4を実際の2引数契約 `<docs/rules のルート> <出力先リポジトリルート>` に合わせて修正した） |
| `validate_one_skill` が検査を最後まで実行せず終了コード1で止まり、不合格の内訳が1件も出力されない | docs/skills/setup-deriving-skills/scripts/validate-skill-definitions.sh の scan_leaks_in_content 呼び出し | `leaks="$(scan_leaks_in_content ...)"` の右辺のパイプ（grep \| while read）が一致0件のとき pipefail により終了コード1を返し、代入文自体の終了コードになる。set -euo pipefail の下ではこの代入文の失敗で関数呼び出し元へ即座に抜ける | 対応済み（代入の末尾へ `|| true` を付け、一致0件を失敗として扱わない形にした） |
| validate-skill-definitions.sh 自身に対して「他単位-名前混入」検査を実行すると、self-test内のフィクスチャ文字列（他単位を模した機能名）が自分自身のscripts配下の文字列として検出され不合格になる | docs/skills/setup-deriving-skills/scripts/validate-skill-definitions.sh の self_test 内フィクスチャ | 検査対象の機能名文字列をテストコード内へ直接literalで書くと、検査自身がそのソースコードを走査した際に本物の混入と区別できない | 対応済み（フィクスチャの文字列をハイフンを変数経由で組み立てる形にし、ソース上に連続した一致文字列が現れないようにした） |
| 規約の配置（scaffold-rule-definitions.sh）から `--with-skills`（納品スキル3本の配布・agentのrule-reviewer.md配布・generation-engine一式の配布）を除去した | 旧 generation-engine/scripts/rules/scaffold-rule-definitions.sh の deliver_skills（859行目）・deliver_skill_file（903行目）・deliver_agent（963行目）・run_scaffold内のWITH_SKILLS分岐（805-822行目）・main()の--with-skills引数解析（2144行目）。自己テストのケース6（1247行目）・6a（1284行目）・6b（1313行目）・7（1327行目）も同時に除去した | 規約の配置は規約定義一式の配布だけに絞る方針であり、納品スキル・agent・生成エンジンの配布は別の関心事であるため | 対応済み（除去し、改善事項へ運用（operate）単位の機能として別に作る旨を追記した） |
| `check-customer-facing-adequacy.test.sh --self-test` の「追加回帰2」が不合格になる | docs/skills/setup-scaffolding-rules/templates/rules/checkers/check-customer-facing-adequacy.sh（同フォルダ相対で `../../../references/customer-facing-terms.json` を参照） | 旧リポジトリの delivery-payload/references/customer-facing-terms.json を前提にしており、本移設ではこの参照定義ファイルを持ち込んでいないため実在しない | 対応済み（customer-facing-terms.json・document-style-exclusions.json をcheckersと同じフォルダへ複製し、参照パスをSCRIPT_DIR基準へ直した） |
| 空の対象へ配置した27規約に対し validate-rule-definitions.sh --taxonomy を実行すると「宣言したcheckerが実在しない」で5件不合格になる（check-document-style-register.sh・check-customer-facing-adequacy.sh・check-instruction-writing.sh・check-code-line-number-reference.sh・check-portal-unmaintainable-content.sh） | docs/skills/setup-scaffolding-rules/scripts/scaffold-rule-definitions.sh の run_scaffold（.parents[].children[].checker だけを配備し、.crossCuttingChecks[].checker を配備しない） | 旧版のrun_scaffoldは元々crossCuttingChecksの配備ロジックを持たず、この欠落は移設前から存在した。setup-deriving-rulesのvalidate-rule-definitions.shがcrossCuttingChecksの宣言網羅を--taxonomyで検査するようになったことで初めて表面化した | 対応済み（run_scaffoldにcrossCuttingChecks[]の配備ループを足し、ai-config-asset-management/へ集約して置く形にした） |
| 同じ検査で「宣言の無いchecker: check-rule-drift.sh」も不合格になる | --deploy-rule-scriptsがdocs/rules/agent-operations/ai-config-asset-management/へ配備する4本のうちcheck-rule-drift.shが、同フォルダの規約が宣言する検査（check-ai-config-derivative-manual-edit.sh）と同じ命名規則（check-*.sh）に一致する | validate-rule-definitions.shのchecker宣言検査はdocs/rules/<親>/<子>/直下のcheck-*.shを機械的に走査するため、規約の検査本体ではない配備物（診断ツール）が同じ命名規則を満たすと誤って未宣言のcheckerとして検出される | 対応済み（validate-rule-definitions.shのactual走査からDEPLOYED_TOOL_NAMES（派生のスクリプト4本）を除外する形にした） |
| check-rule-drift.shの実行で「.claude/rules/always/project-context/rule.md」がADDEDとして検出される | docs/skills/setup-scaffolding-rules/scripts/scaffold-rule-definitions.sh の build_project_context_rule_md（docs/rulesの27子カテゴリとは別に直接.claude/rules/へ書く） | この規約はdocs/rules/<親>/<子>/rule.mdという通常の定義単位を経由せず、実装フローのゲートのため直接.claude/rules/へ書かれる特別な1ファイルであり、build-derived-rules.shが再生成する対象に含まれない | 対応済み（project-context/rule.mdをdocs/rules/agent-operations/project-context/へ定義として持たせ、.claude/rules/側はbuild-derived-rules.shが派生させる形にした） |
| scaffold-rule-definitions.shのヘッダコメントに書いた移設元の参照が、validate-skill-definitions.shの「他単位-名前混入」検査で誤検出された | docs/skills/setup-scaffolding-rules/scripts/scaffold-rule-definitions.sh 6行目 | 検査は `\b(setup\|reverse\|verify\|portal\|operate)-[a-z0-9-]+` という正規表現で他単位名らしき文字列を機械的に検出するため、リポジトリ名「reverse-docs-skills」の「reverse-」がこのパターンに一致した | 対応済み（コメントを「旧リポジトリの」という表現へ書き換え、リポジトリ名の直書きを避けた） |
| scaffold-rule-definitions.shの自己テストからケース9c（enforcement節の三者一致）・ケース13（納品サンプルへの禁止語検査）を除去した | 旧 generation-engine/scripts/rules/scaffold-rule-definitions.sh 1141行目（サンプルfind）・1943行目（ケース13） | どちらもgeneration-engine/samples/docs/rules（納品済みの完成サンプル）を前提にしており、本移設ではこのサンプル資産を持ち込んでいないため | 対応済み（該当ブロックを除去し、コメントで対象外の理由を明記した） |
| output-layout.sh・rule-scope-overrides.shのうちoutput-layout.shだけを持ち込まなかった | 旧 generation-engine/scripts/output-layout.sh 28行目（OUTPUT_LAYOUT_DEFAULT="$OUTPUT_LAYOUT_DIR/../../delivery-payload/references/output-layout.json"）・26行目（SAFE_WRITE_PATH_LIB既定パス） | output-layout.shは自身の既定宣言ファイルの置き場を固定の相対段数で前提にしており、この構成は新リポジトリのdocs/skills/配下には存在しない。加えてlib/safe-write-path.cjsという追加の依存も伴う | 未対応（resolve_output_layout等は未定義のままとし、呼び出し側の既存フェイルセーフにより常に「未解析」を返す形にとどめた。空の対象への配置では実際のマニフェストも存在しないため実害は無いが、対象なし／観測ありの判定を要する場合は別途持ち込みが要る） |
| check-customer-facing-adequacy.test.shの「追加回帰9」が、配置後の再配置先で実行すると実物テンプレートディレクトリ不在で不合格になる | docs/skills/setup-scaffolding-rules/templates/rules/checkers/check-customer-facing-adequacy.sh（SCRIPT_DIRの2階層上のリバース検証を参照） | リバース検証テンプレート48件がtemplates直下に置かれ、checker自身のフォルダから2階層上という固定の相対段数を前提にしていた。crossCuttingChecksのcheckerをdocs/rules/agent-operations/ai-config-asset-management/へ再配置すると、この相対段数が別の場所を指してしまう | 対応済み（リバース検証ディレクトリをtemplates/rules/checkers/直下へ移し、参照をSCRIPT_DIR直下へ直した。crossCuttingChecksの配備ループが、checkerと同じ配置先へこのディレクトリごと複製する） |
| scaffold-rule-definitions.shへ足した新しいコメント（checker名の一部を含む語）が、validate-skill-definitions.shの「他単位-名前混入」検査で誤検出された | docs/skills/setup-scaffolding-rules/scripts/scaffold-rule-definitions.sh のcrossCuttingChecks配備ループ付近のコメント | 検査は他単位名らしき接頭辞を機械的に検出するため、checker名の一部の語がこのパターンに一致した | 対応済み（コメントからchecker名の直書きを外し、他単位接頭辞に一致しない言い回しへ書き換えた） |
| check-acceptance.sh Wの実行で3機能（setup-checking-acceptance・setup-deriving-rules・setup-scaffolding-rules）が未確認になった | docs/skills/setup-deriving-rules/tests/test-self-tests.sh・docs/skills/setup-scaffolding-rules/tests/test-scaffold-and-derive.sh・test-self-tests.sh・docs/skills/setup-checking-acceptance/tests/test-acceptance-on-self.sh | 調査を兼ねて複数の check-acceptance.sh を同時に背景実行しており、負荷平均が30を超える状態だった。各testsを1本ずつ time bash <tests> で計測すると、setup-deriving-rulesのtest-self-tests.shが786秒（4検査PASS）だった。負荷が下がった後に同じtestsを再計測すると24秒で終わった。setup-scaffolding-rulesのtest-scaffold-and-derive.shは260秒（8検査PASS）、test-self-tests.shは26秒（32検査PASS）だった。setup-checking-acceptanceのtest-self-tests.shは10秒、内側で全体をもう一度走査するtest-acceptance-on-self.shは79秒だった。いずれも300秒の上限を実際には超えていない。300秒に一度でも達したのはsetup-deriving-rulesの初回786秒だけであり、これは負荷平均30超の下での計測だった | 対象外（check-acceptance.shの上限の測り方に不具合はない。原因は自分が同時に投入した複数の背景実行によるCPU競合であり、tests自体の遅さではない。負荷を下げてcheck-acceptance.sh Wを実行し直すと4機能とも合格・単位setupも合格・終了コード0になることを確認した。testsを短くする、または上限を見直す必要は無いと判断する） |
| skill の名前の決まり（agent-operations/skill-naming）を新設し検査を配線すると、`setup-orchestrating` が単位・作業だけの2語で対象を持たず、名前の決まりの1件目の規則（3語以上）に不合格になる | docs/skills/setup-orchestrating-units（旧 setup-orchestrating。フォルダ名・SKILL.mdのname/invocation）。検査本体はdocs/rules/agent-operations/skill-naming/check-skill-naming.sh。配線先はdocs/skills/setup-deriving-skills/scripts/validate-skill-definitions.sh | `setup-orchestrating` は全体を統括する機能であり、特定の対象を持たなかった。他の4機能（setup-checking-acceptance・setup-deriving-rules・setup-deriving-skills・setup-scaffolding-rules）はいずれも3語以上で名前の決まりに合格する | 対応済み（選択肢A（改名）を採用した。委任元の判断により `setup-orchestrating` を `setup-orchestrating-units`（対象＝単位。単位を統括する）へ改名した。フォルダ・SKILL.mdのname/invocation・自己参照・本文書・旧skillの対応表.mdの言及を置き換え、名前の決まりの規約文言は変えていない） |
| `check-skill-naming.sh` を機能の派生の検査（CLI引数でルートを渡す形）から呼ぶよう配線したが、規約自身がcheckable:trueとして派生させるとPreToolUse(Write\|Edit\|MultiEdit)のhookとしても登録され、hook経路では引数無し・標準入力にJSON（tool_input.file_path等）が渡る。CLI専用の実装のままではhook経由の呼び出しで対象ルートを求められない | docs/rules/agent-operations/skill-naming/check-skill-naming.sh | 検査本体の設計時点でhook経由の呼び出し方（標準入力からのJSON受け取り）を想定しておらず、引数の有無だけで判定を分けていた | 対応済み（引数が無く標準入力がJSONのときはtool_input.file_pathからdocs/skillsのルートを求めて検査し、file_pathがdocs/skills/配下でなければ何もせず終了コード0、不合格ならhookの作法（終了コード2とstderrへのメッセージ）で止める形にした。自己テストへhook経路の3ケース（合格・不合格・対象外）を足した） |
| 派生のスクリプト4本の除外がbasenameの一致だけで判定されており、配備先（agent-operations/ai-config-asset-management/）以外の規約フォルダへ同名ファイルが混入しても除外され、checker-専有・宣言網羅の検査が機能しなくなる | docs/skills/setup-deriving-rules/scripts/validate-rule-definitions.sh のDEPLOYED_TOOL_NAMES・is_deployed_tool_name | 除外の判定がbasenameの一致だけを見ており、配備先フォルダに実在するかどうかを確かめていなかった | 対応済み（is_deployed_tool_dirを新設し、basenameの一致に加えて配備先フォルダ（agent-operations/ai-config-asset-management/）への実在も確かめる形にした。自己テストのtool-exclusionを配備先へ置く形へ書き換え、配備先以外へ混入した場合は検出するtool-exclusion-scopeを追加した） |
| docs/skills/setup-scaffolding-rules/scripts/validate-rule-definitions.shという、checker-専有・checker-一意の検査を持たない旧版の複製が配置側に残ったまま、どこからも参照されずに放置されていた | docs/skills/setup-scaffolding-rules/scripts/validate-rule-definitions.sh | scaffold-rule-definitions.shが同フォルダのvalidate-rule-definitions.shをsourceしていたため、docs/skills/setup-deriving-rules/scripts/側の最新版と乖離した複製が配置側にも残っていた | 対応済み（scaffold-rule-definitions.shの2箇所の参照をdocs/skills/setup-deriving-rules/scripts/validate-rule-definitions.shへ向け直し、配置側の未参照の複製を削除した） |

## 経緯

- 2026-09-02: 参照スライド「AI駆動開発基盤納品・3区分」を起点に、業務の要件から定義し直した。業務要件と支援ツールの要件をスライド（ai-slide-library、PR #96・#97）と要件文書に起こした。skill・スクリプトの依存を調査し、5単位への分離が可能であることと、結合箇所を確かめた
- 2026-09-02: ルート構成（3ディレクトリ）・skill の定義と派生・新リポジトリ `ai-driven-development-setup` の新設・移行の進め方を決めた
- 2026-09-02: 新リポジトリ `ai-driven-development-setup` をローカルに作成した。公開方針を決めた（GitHub に置かず agent-toolkit へ同期）
- 2026-09-02: 実践で見つかった欠点と不具合の6件すべてを直した。パス解決を引数と規約フォルダに限る形へ改めた。front matter に docType・derivedPath を足した。project-context/rule.md を派生で作り直した。setup-deriving-rules の4本の自己テストが通ることを確かめた。docs/rules に対する検査・生成・ずれ検知の実行も、すべて終了コード0になることを確認した
- 2026-09-02: 機能の派生（setup-deriving-skills）を作り、2機能を .claude/skills へ派生させた
- 2026-09-02: 規約の配置（setup-scaffolding-rules）を旧から持ち込み、空の対象へ27規約を配置して派生を通した
- 2026-09-02: 空の対象で配置 → 検査 → 派生 → ずれ検査が 0 で通る状態を作り、setup-scaffolding-rules の tests に固定した
- 2026-09-03: 合格の集計（setup-checking-acceptance）を作り、category を3値にした
- 2026-09-03: skill の名前の決まりを規約として新設し、機能の派生の検査から呼ぶ形にした
- 2026-09-03: 配る規約の定義を規約と検査の 1 対 1 のフォルダへ揃え、横断の検査 5 本を規約に起こした
- 2026-09-03: 作業の計画と作業指示書を `ai-work/todo/` に置くことにし、ルート直下を 4 つにした。旧リポジトリの指示書「新リポジトリへ体系を作り直す指示書」の内容を `ai-work/todo/体系の作り直しの計画.md` へ移した
- 2026-09-03: リバースの流れを第一フェーズ（AI が読んで検出条件を組む）と第二フェーズ（検出条件を実行する）の 19 工程に設計した。批判的レビュー 2 人の所見 27 件を取り込み、実案件のコードで第一フェーズを承認用の要約まで通した。一覧化の実測は 4 種別で候補数と一致し、表は削除を辿る仕組みの不足で不一致だった。反映事項を設計と手順書に取り込んだ
