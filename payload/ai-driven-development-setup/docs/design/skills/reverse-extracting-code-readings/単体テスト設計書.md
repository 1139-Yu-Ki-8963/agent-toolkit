# コードの読み取り結果を取り出す（reverse-extracting-code-readings）の単体テスト設計書

**この文書の位置づけ**（別の言語で作り直す場合は、まず「仕様」から読む）

- 仕様: §1 テスト観点、§2 テストケース一覧、§3 入力条件、§4 期待結果、§5 異常系、§6 境界値、§7 網羅基準、§8 前提条件と終了条件
- 現行実装: 該当なし

## 本書が検証するもの

現場コードで機能を走らせたとき、読み取り結果の各項目がどんな値になるかを検証する。試験用の対象コードは同梱せず、検証者が用意した現場コードで確かめる。

## テスト対象

| スクリプト | 自己テストの実行 | ケース数 |
|---|---|---|
| scripts/extract-code-readings.sh | bash scripts/extract-code-readings.sh --self-test | 65 |
| scripts/compare-code-readings.sh | bash scripts/compare-code-readings.sh --self-test | 8 |

## テストの粒度と自動化の方針

| 項目 | 方針 |
|---|---|
| テストの粒度 | 現場コードに機能を1回走らせ、読み取り結果の値が期待どおりかを人とAIが確認する。機械での自動実行は行わない。 |
| 自動化 | 検査スクリプトの分岐と終了コードの自動確認は「自己テスト」（§7の末尾）が別に担う。 |

## 本書が扱わない範囲

| 対象外の事項 | 対象外とする理由 | 代わりに確認する資料または試験 |
|---|---|---|
| 複数機能をまたぐ結合 | 単位ごとの文書は結合テストを扱わないため | docs/design/skills/reverse-orchestrating-flow/ |
| 検査スクリプトの分岐・終了コードの網羅 | 現場コードでの期待値の確認とは別の観点のため | §7網羅基準の末尾の自己テストの表 |

## §1 テスト観点

**この節の位置づけ: 現場コード**

| キー | 機能 | 観点 | 由来する詳細設計書の章 |
|---|---|---|---|
| 読み取り結果-経路 | extract-code-readings.sh | 現場コードの接続窓口の経路を取り出すと、全単位の「経路」に値が入り、単位ごとに値が混ざらない | 詳細設計書 §2 処理の定義 |
| 読み取り結果-列と型の対応 | extract-code-readings.sh | 現場コードのテーブル定義から列と型を取り出すと、列の件数と型の並びが対応する | 詳細設計書 §2 処理の定義 |
| 読み取り結果-2回一致 | extract-code-readings.sh | 同じ現場コードを2回取り出しても値に差分が出ない | 詳細設計書 §2 処理の定義 |
| 読み取り結果-規則の不備 | extract-code-readings.sh | 調査と検出条件の定義書の取り出しの規則が規定の形でない項目を検出し、他の項目は続行する | 詳細設計書 §2 処理の定義 |
| 読み取り結果-AIで埋める | extract-code-readings.sh | 規則で表せない項目をAIの読み取りで埋め、決まらない項目は確認事項に残す | 詳細設計書 §2 処理の定義 |
| 読み取り結果-非UTF8 | extract-code-readings.sh | 現場コードが非UTF-8でも変換して取り出し、値が文字化けしない | 詳細設計書 §2 処理の定義 |
| 読み取り結果-見本合格 | extract-code-readings.sh | 見本の対象に対し`--run`を実行すると終了コード0になる | 詳細設計書 §2 処理の定義 |
| 読み取り結果-単位別ファイル実在 | extract-code-readings.sh | 単位ごとの読み取り結果ファイルが実在する | 詳細設計書 §2 処理の定義 |
| 読み取り結果-入力項目捕捉 | extract-code-readings.sh | 一致条件の捕捉から入力項目の値が正しく入る | 詳細設計書 §3 ロジック |
| 読み取り結果-パイプエスケープ捕捉 | extract-code-readings.sh | エスケープしたパイプを含む正規表現でも捕捉できる | 詳細設計書 §3 ロジック |
| 読み取り結果-AI出所記録 | extract-code-readings.sh | AIの読み取りで埋めた項目は出所が「AI」になる | 詳細設計書 §2 処理の定義 |
| 読み取り結果-呼ぶ接続窓口未計上 | extract-code-readings.sh | 呼ぶ接続窓口が特定できない項目は「未」に載る | 詳細設計書 §2 処理の定義 |
| 読み取り結果-0件空配列 | extract-code-readings.sh | 一致が0件だった項目は値が空配列になる | 詳細設計書 §3 ロジック |
| 読み取り結果-0件未計上 | extract-code-readings.sh | 一致が0件だった項目は「未」に載る | 詳細設計書 §2 処理の定義 |
| 読み取り結果-run値取り込み | extract-code-readings.sh | 取り出した実行にrun.jsonの値（対象・出力先・識別子・コミット）が入る | 詳細設計書 §4 入出力の値 |
| 読み取り結果-未計上判定 | extract-code-readings.sh | 「未」の項目を持つ単位は読み取り結果の当該欄が「未」になる | 詳細設計書 §2 処理の定義 |
| 読み取り結果-集計単位数 | extract-code-readings.sh | 集計の単位数が実測の単位数と一致する | 詳細設計書 §4 入出力の値 |
| 読み取り結果-集計規則無し項目列挙 | extract-code-readings.sh | 規則の無い項目が集計の「規則の無い項目」欄に列挙される | 詳細設計書 §4 入出力の値 |
| 読み取り結果-属するファイル不在検知 | extract-code-readings.sh | 一覧の属するファイルが実在しないと終了コード1になる | 詳細設計書 §5 エラー処理 |
| 読み取り結果-検証一致判定 | extract-code-readings.sh | `--verify`で既存の読み取り結果と一致すれば終了コード0で、未の項目数を別行で出す | 詳細設計書 §2 処理の定義 |
| 読み取り結果-検証不一致判定 | extract-code-readings.sh | `--verify`で既存の読み取り結果と不一致なら終了コード1になる | 詳細設計書 §5 エラー処理 |
| 使い方-誤り | extract-code-readings.sh / compare-code-readings.sh | 必須引数（run・kind・種別・定義書・規則）を欠く使い方は終了コード2で判定不能になる | 詳細設計書 §5 エラー処理 |
| 読み取り結果-対象外種別飛ばし | extract-code-readings.sh | 到達範囲が対象外の種別は取り出しを飛ばし、対象外の表示が出る | 詳細設計書 §2 処理の定義 |
| 読み取り結果-非UTF8変換判定 | extract-code-readings.sh | EUC-JPのファイルを変換して読み、日本語の値を正しく捕捉する | 詳細設計書 §3 ロジック |
| 読み取り結果-単位区間分離 | extract-code-readings.sh | 複数単位の区間が正しく分かれ、他単位の値と混ざらない | 詳細設計書 §3 ロジック |
| 読み取り結果-属するファイルglob展開 | extract-code-readings.sh | 属するファイルのglobが複数ファイルへ展開され両方捕捉する。0件一致は警告に留め終了コード0 | 詳細設計書 §3 ロジック |
| 読み取り結果-捕捉取りこぼし許容 | extract-code-readings.sh | 捕捉の取りこぼしがあっても終了コード0で続行する | 詳細設計書 §3 ロジック |
| 読み取り結果-空文字捕捉扱い | extract-code-readings.sh | 空文字列の捕捉を値として扱う規則が、standaloneの捕捉も含め正しく反映される | 詳細設計書 §3 ロジック |
| 読み取り結果-規則解釈不能 | extract-code-readings.sh | 規則を解釈できない項目があると、`--verify`の有無にかかわらず終了コードが0以外になる（改善指示書第1回1-8） | 詳細設計書 §5 エラー処理 |
| 比較-一致判定 | compare-code-readings.sh | 値が一致する（順序違いを含む）2つの読み取り結果は終了コード0になる | 詳細設計書 §2 処理の定義 |
| 比較-値不一致判定 | compare-code-readings.sh | 値が不一致な読み取り結果は終了コード1で、値-不一致のメッセージが出る | 詳細設計書 §5 エラー処理 |
| 比較-片方単位無し判定 | compare-code-readings.sh | 片方にしか無い単位があると終了コード1で、単位-不在Bのメッセージが出る | 詳細設計書 §5 エラー処理 |
| 比較-集計対象外 | compare-code-readings.sh | 集計.jsonは比較の対象外として終了コード0になる | 詳細設計書 §2 処理の定義 |
| 範囲-単位の定義の終端 | extract-code-readings.sh | 範囲が単位の定義のとき、終端は開始行以降で最初に開く丸・角・波括弧（3種まとめて1つの残高）からの残高が0に戻る行になる。行の末尾に1個以上並ぶ`\r`を除いてから扱う（CRLFのファイルでも空行を空行として判定するため）。残高が0に戻った直後が`;`または`,`ならその行で終える。`;`・`,`のいずれでもなく行が終わるときは、残高を0に戻した閉じ括弧が丸括弧`)`のときに限り、探索範囲内の次の空白でない行の先頭が波括弧`{`かどうかで先読みし、波括弧なら終端にせず数え続け、そうでなければその行で終える（次の行の先頭が丸括弧`(`・角括弧`[`では続行しない）。閉じ括弧が波括弧`}`・角括弧`]`のときは、次の行が波括弧`{`で始まっていても先読みせずその行で終える（オブジェクトを並べる様式で隣のオブジェクトを取り込まない）。次の文字が`;`・`,`のいずれでも行末でもなければ数え続ける。探索は一覧の次の単位の直前までに限る。壊れた定義・ネストした括弧の閉じ行・1行定義・次行で開く定義・同一行で閉じてすぐ開く定義・関数シグネチャ形・波括弧が次行の定義・丸括弧で始まる別の文が次行に続く定義・括弧で囲まない定義（def）・defの次行が丸括弧で始まるタプル代入・デコレータ行から始まる定義・波括弧閉じの次行が波括弧の定義・CRLFの空行を挟む定義・カンマで閉じた呼び出し式・CRLFの空行に`\r`が2個続く定義でも、一覧から除外した定義の内容を隣の単位が取り込まない。範囲内で閉じない場合は従来の終端に落ちる。既知の限界は6つある。(1)本体を括弧で囲まない定義は開始行だけになる（次行が`{`なら続行する）。(2)デコレータ・注釈の行から始まる定義は、デコレータの括弧が閉じる行までになり、本体は取れない。戻り値の型が`[]`で終わる署名（TypeScriptの`function f(): string[]`など）の次行に`{`を置く定義は先読みで救えず開始行だけになる。(3)署名と`{`の間のコメント行があると開始行だけになる。(4)文字列・コメント中の括弧も数えるため終端が早まる・伸びることがある。(5)切り出しは行単位のため、次の単位が同じ行から始まると従来の終端に落ちる（行粒度）。(6)メソッド連鎖（`.get(…)`を次の行に続ける書き方）は最初の呼び出しの括弧が閉じた行で終端になり、続く連鎖を取れない。開始行を連鎖の全体を含む文にする検出条件を書く | 詳細設計書 §3 ロジック |

## §2 テストケース一覧

**この節の位置づけ: 現場コード**

| キー | 番号 | 機能 | ケースの名前 | 対応する観点のキー | 区分 | 前提（現場コードの状態） | 操作 | 期待結果 | 自己テストのケース名 |
|---|---|---|---|---|---|---|---|---|---|
| 読み取り結果-経路 | 1 | extract-code-readings.sh | 接続窓口の経路を取り出す | 読み取り結果-経路 | レビュー | 現場コードに接続窓口の定義行がある | `--run`付きで実行する | 全単位の「経路」に値が入り、同一ファイル内の別単位の値が混ざらない | （レビュー） |
| 読み取り結果-列と型の対応 | 2 | extract-code-readings.sh | テーブル定義の列と型を取り出す | 読み取り結果-列と型の対応 | レビュー | 現場コードのテーブル定義に列がS個ある | `--run`付きで実行する | 「列」がS件で、型が列と同じ並びで入る | （レビュー） |
| 読み取り結果-2回一致 | 3 | extract-code-readings.sh | 同じ対象を2回取り出す | 読み取り結果-2回一致 | レビュー | 前回取り出した現場コードと同じ対象 | `--verify`付きで実行する | 差分が0件 | （レビュー） |
| 読み取り結果-規則の不備 | 4 | extract-code-readings.sh | 規則の2列目が規定外の項目がある | 読み取り結果-規則の不備 | レビュー | 調査と検出条件の定義書の項目の2列目が規定の形でない | `--run`付きで実行する | 「規則の無い項目」に列挙され、標準出力に警告が出て終了コードが0以外 | （レビュー） |
| 読み取り結果-AIで埋める | 5 | extract-code-readings.sh | 規則で表せない項目をAIの読み取りで埋める | 読み取り結果-AIで埋める | レビュー | 現場コードに規則で表せない項目がある | AIの読み取りを実行する | 2回一致した値の出所がAIになり、決まらない項目は確認事項に残る | （レビュー） |
| 読み取り結果-非UTF8 | 6 | extract-code-readings.sh | 非UTF-8の対象コードを取り出す | 読み取り結果-非UTF8 | レビュー | 現場コードにEUC-JP等のファイルがある | `--run`付きで実行する | 変換して取り出せ、値が文字化けしない | （レビュー） |
| 読み取り結果-検証時の規則の不備 | 7 | extract-code-readings.sh | 検証時に規則を解釈できない項目がある | 読み取り結果-規則の不備 | レビュー | 調査と検出条件の定義書の項目の2列目が規定の形でなく、既存の読み取り結果がある | `--verify`付きで実行する | 比較自体は一致していても終了コードが0以外になる | （レビュー） |
| 読み取り結果-見本合格-終了コード | 8 | extract-code-readings.sh | 見本の対象で終了コード0 | 読み取り結果-見本合格 | 正常 | 見本の対象コード | `extract-code-readings.sh --run`を実行する | 終了コード0 | scripts/extract-code-readings.sh 合格-見本: 終了コード0 |
| 読み取り結果-単位別ファイル実在-OrderList | 9 | extract-code-readings.sh | OrderListの読み取り結果ファイルがある | 読み取り結果-単位別ファイル実在 | 正常 | 同上 | 同上 | OrderListの読み取り結果ファイルが実在する | scripts/extract-code-readings.sh OrderListの読み取り結果ファイルがある |
| 読み取り結果-入力項目捕捉-orderId | 10 | extract-code-readings.sh | 入力項目の値がorderId | 読み取り結果-入力項目捕捉 | 正常 | 同上 | 同上 | 入力項目の値が「orderId」になる | scripts/extract-code-readings.sh 入力項目の値がorderId |
| 読み取り結果-パイプエスケープ捕捉-Submit | 11 | extract-code-readings.sh | エスケープしたパイプでSubmitを捕捉 | 読み取り結果-パイプエスケープ捕捉 | 正常 | 規則にエスケープしたパイプを含む正規表現を持つ対象 | 同上 | Submitの値が正しく捕捉される | scripts/extract-code-readings.sh エスケープしたパイプを含む正規表現でSubmitを捕捉 |
| 読み取り結果-AI出所記録-AI | 12 | extract-code-readings.sh | AIの読み取りの項目は出所がAI | 読み取り結果-AI出所記録 | 正常 | AIの読み取りの項目を持つ対象 | 同上 | 当該項目の出所が「AI」になる | scripts/extract-code-readings.sh AIの読み取りの項目は出所がAI |
| 読み取り結果-呼ぶ接続窓口未計上-未 | 13 | extract-code-readings.sh | 呼ぶ接続窓口は未に載る | 読み取り結果-呼ぶ接続窓口未計上 | 正常 | 呼ぶ接続窓口が特定できない項目を持つ対象 | 同上 | 当該項目が「未」に載る | scripts/extract-code-readings.sh 呼ぶ接続窓口は未に載る |
| 読み取り結果-0件空配列-値 | 14 | extract-code-readings.sh | 0件だった項目は値が空配列 | 読み取り結果-0件空配列 | 正常 | 一致が0件の項目を持つ対象 | 同上 | 当該項目の値が空配列になる | scripts/extract-code-readings.sh 0件だった項目は値が空配列 |
| 読み取り結果-0件未計上-未 | 15 | extract-code-readings.sh | 0件だった項目は未に載る | 読み取り結果-0件未計上 | 正常 | 同上 | 同上 | 当該項目が「未」に載る | scripts/extract-code-readings.sh 0件だった項目は未に載る |
| 読み取り結果-run値取り込み-run | 16 | extract-code-readings.sh | 取り出した実行にrun.jsonの値が入る | 読み取り結果-run値取り込み | 正常 | run.jsonを伴う対象 | 同上 | 取り出した実行にrun.jsonの値が入る | scripts/extract-code-readings.sh 取り出した実行にrun.jsonの値が入る |
| 読み取り結果-未計上判定-単位 | 17 | extract-code-readings.sh | 未が有る単位は読み取り結果=未 | 読み取り結果-未計上判定 | 正常 | 未計上の項目を持つ単位がある対象 | 同上 | 当該単位の読み取り結果欄が「未」 | scripts/extract-code-readings.sh 未が有る単位は読み取り結果=未 |
| 読み取り結果-未計上判定-OrderList | 18 | extract-code-readings.sh | OrderListも呼ぶ接続窓口がAI項目のため未 | 読み取り結果-未計上判定 | 正常 | 同上（OrderListが該当） | 同上 | OrderListの読み取り結果欄も「未」 | scripts/extract-code-readings.sh 未が有る単位は読み取り結果=未（OrderListも呼ぶ接続窓口がAI項目のため未） |
| 読み取り結果-集計単位数-2 | 19 | extract-code-readings.sh | 集計の単位数が2 | 読み取り結果-集計単位数 | 正常 | 単位が2件の対象 | 同上 | 集計の単位数が2になる | scripts/extract-code-readings.sh 集計の単位数が2 |
| 読み取り結果-集計規則無し項目列挙-接続窓口 | 20 | extract-code-readings.sh | 集計の規則の無い項目に呼ぶ接続窓口を含む | 読み取り結果-集計規則無し項目列挙 | 正常 | 呼ぶ接続窓口の規則が無い対象 | 同上 | 集計の「規則の無い項目」に呼ぶ接続窓口が列挙される | scripts/extract-code-readings.sh 集計の規則の無い項目に呼ぶ接続窓口を含む |
| 読み取り結果-属するファイル不在検知-終了コード | 21 | extract-code-readings.sh | 属するファイルが不在だと終了コード1 | 読み取り結果-属するファイル不在検知 | 異常 | 一覧の属するファイルが実在しない対象 | 同上 | 終了コード1 | scripts/extract-code-readings.sh 不合格-属するファイル不在: 終了コード1 |
| 読み取り結果-検証一致判定-終了コード | 22 | extract-code-readings.sh | 検証一致で終了コード0 | 読み取り結果-検証一致判定 | 正常 | 既存の読み取り結果と一致する対象 | `--verify`付きで実行する | 終了コード0 | scripts/extract-code-readings.sh 検証-一致: 終了コード0 |
| 読み取り結果-検証一致判定-未件数別行 | 23 | extract-code-readings.sh | 未の項目数を別行で出す | 読み取り結果-検証一致判定 | 正常 | 同上（未が有っても合格） | 同上 | 未の項目数が別行で出て合格する | scripts/extract-code-readings.sh 検証-一致: 未の項目数を別行で出す（未が有っても合格） |
| 読み取り結果-検証不一致判定-終了コード | 24 | extract-code-readings.sh | 検証不一致で終了コード1 | 読み取り結果-検証不一致判定 | 異常 | 既存の読み取り結果と不一致な対象 | `--verify`付きで実行する | 終了コード1 | scripts/extract-code-readings.sh 検証-不一致: 終了コード1 |
| 使い方-誤り-run無し | 25 | extract-code-readings.sh | run引数無しは終了コード2 | 使い方-誤り | 異常 | run引数を渡さない | 引数無しで実行する | 終了コード2 | scripts/extract-code-readings.sh 使い方誤り-run無し: 終了コード2 |
| 使い方-誤り-kind無し | 26 | extract-code-readings.sh | kind引数無しは終了コード2 | 使い方-誤り | 異常 | kind引数を渡さない | 同上 | 終了コード2 | scripts/extract-code-readings.sh 使い方誤り-kind無し: 終了コード2 |
| 使い方-誤り-種別不正 | 27 | extract-code-readings.sh | 種別が不正だと終了コード2 | 使い方-誤り | 異常 | 定義されていない種別を渡す | 同上 | 終了コード2 | scripts/extract-code-readings.sh 使い方誤り-種別不正: 終了コード2 |
| 使い方-誤り-定義書不在 | 28 | extract-code-readings.sh | 定義書が無いと終了コード2 | 使い方-誤り | 異常 | 調査と検出条件の定義書が存在しない | 同上 | 終了コード2 | scripts/extract-code-readings.sh 使い方誤り-調査と検出条件の定義書不在: 終了コード2 |
| 使い方-誤り-規則不在 | 29 | extract-code-readings.sh | api用の規則の表が無いと終了コード2 | 使い方-誤り | 異常 | api用の取り出しの規則表が定義書に無い | 同上 | 終了コード2 | scripts/extract-code-readings.sh 使い方誤り-規則不在(api用の表が無い): 終了コード2 |
| 読み取り結果-対象外種別飛ばし-終了コード | 30 | extract-code-readings.sh | 対象外種別は飛ばしても終了コード0 | 読み取り結果-対象外種別飛ばし | 正常 | 到達範囲が対象外の種別を含む対象 | `--run`付きで実行する | 終了コード0 | scripts/extract-code-readings.sh 対象外-種別は飛ばす: 終了コード0 |
| 読み取り結果-対象外種別飛ばし-表示 | 31 | extract-code-readings.sh | 対象外の表示が出る | 読み取り結果-対象外種別飛ばし | 正常 | 同上 | 同上 | 標準出力に対象外の表示が出る | scripts/extract-code-readings.sh 対象外-種別は飛ばす: 対象外の表示が出る |
| 読み取り結果-非UTF8変換判定-終了コード | 32 | extract-code-readings.sh | EUC-JPを変換して終了コード0 | 読み取り結果-非UTF8変換判定 | 境界 | EUC-JPのファイルがある対象 | `--run`付きで実行する | 終了コード0 | scripts/extract-code-readings.sh 文字コード-EUC-JP: 終了コード0 |
| 読み取り結果-非UTF8変換判定-受注一覧捕捉 | 33 | extract-code-readings.sh | EUC-JPを変換して受注一覧を捕捉 | 読み取り結果-非UTF8変換判定 | 境界 | 同上 | 同上 | 「受注一覧」の値が文字化けせず捕捉される | scripts/extract-code-readings.sh 文字コード-EUC-JP: EUC-JPを変換して受注一覧を捕捉 |
| 読み取り結果-単位区間分離-終了コード | 34 | extract-code-readings.sh | 単位の区間分離で終了コード0 | 読み取り結果-単位区間分離 | 正常 | 1ファイルに複数単位（combo-1・combo-2）がある対象 | `--run`付きで実行する | 終了コード0 | scripts/extract-code-readings.sh 単位の区間: 終了コード0 |
| 読み取り結果-単位区間分離-combo1 | 35 | extract-code-readings.sh | combo-1はfieldAだけを持つ | 読み取り結果-単位区間分離 | 正常 | 同上 | 同上 | combo-1の値がfieldAだけで、fieldBと混ざらない | scripts/extract-code-readings.sh 単位の区間: combo-1はfieldAだけ（fieldBと混ざらない） |
| 読み取り結果-単位区間分離-combo2 | 36 | extract-code-readings.sh | combo-2はfieldBだけを持つ | 読み取り結果-単位区間分離 | 正常 | 同上 | 同上 | combo-2の値がfieldBだけで、fieldAと混ざらない | scripts/extract-code-readings.sh 単位の区間: combo-2はfieldBだけ（fieldAと混ざらない） |
| 読み取り結果-属するファイルglob展開-終了コード | 37 | extract-code-readings.sh | globが0件一致でも終了コード0 | 読み取り結果-属するファイルglob展開 | 境界 | 属するファイルがglobで複数一致する対象 | `--run`付きで実行する | 終了コード0（0件一致は警告のみ） | scripts/extract-code-readings.sh 属するファイルglob: 終了コード0（0件一致は警告のみ） |
| 読み取り結果-属するファイルglob展開-両方捕捉 | 38 | extract-code-readings.sh | 複数ファイルへの展開結果を両方捕捉 | 読み取り結果-属するファイルglob展開 | 正常 | 同上 | 同上 | 展開された複数ファイルの値を両方捕捉する | scripts/extract-code-readings.sh 属するファイルglob: 複数ファイルへの展開結果を両方捕捉 |
| 読み取り結果-属するファイルglob展開-0件警告 | 39 | extract-code-readings.sh | 0件一致は警告メッセージを出す | 読み取り結果-属するファイルglob展開 | 境界 | globが0件しか一致しない対象 | 同上 | 標準出力に警告メッセージが出る | scripts/extract-code-readings.sh 属するファイルglob: 0件一致は警告メッセージを出す |
| 読み取り結果-捕捉取りこぼし許容-終了コード | 40 | extract-code-readings.sh | 捕捉の取りこぼしがあっても終了コード0 | 読み取り結果-捕捉取りこぼし許容 | 正常 | 一部の捕捉が取りこぼされる対象 | `--run`付きで実行する | 終了コード0 | scripts/extract-code-readings.sh 捕捉の取りこぼし: 終了コード0 |
| 読み取り結果-空文字捕捉扱い-値として含める | 41 | extract-code-readings.sh | 空文字列の捕捉を値として含める | 読み取り結果-空文字捕捉扱い | 正常 | 空文字列を捕捉する規則を持つ対象 | 同上 | 空文字列が値の配列に含まれる | scripts/extract-code-readings.sh 捕捉: 空文字列の捕捉を値として含める |
| 読み取り結果-空文字捕捉扱い-standalone非空 | 42 | extract-code-readings.sh | 一致全体と同じ捕捉でもstandaloneが空にならない | 読み取り結果-空文字捕捉扱い | 正常 | 同上 | 同上 | standaloneの値が空にならない | scripts/extract-code-readings.sh 捕捉: 一致全体と同じ捕捉でも値を採る（standaloneが空にならない） |
| 読み取り結果-空文字捕捉扱い-常時空文字 | 43 | extract-code-readings.sh | 常に空文字列しか捕捉しない項目も値[""]になる | 読み取り結果-空文字捕捉扱い | 正常 | 同上 | 同上 | 当該項目の値が`[""]`になる | scripts/extract-code-readings.sh 捕捉: 常に空文字列しか捕捉しない項目も値[""]になる |
| 読み取り結果-空文字捕捉扱い-未に非計上 | 44 | extract-code-readings.sh | 空文字列の値がある項目は未に載らない | 読み取り結果-空文字捕捉扱い | 正常 | 同上 | 同上 | 当該項目が「未」に載らない | scripts/extract-code-readings.sh 捕捉: 空文字列の値がある項目は未に載らない |
| 読み取り結果-規則解釈不能-終了コード | 45 | extract-code-readings.sh | 規則を解釈できないと終了コードが0以外 | 読み取り結果-規則解釈不能 | 異常 | 取り出しの規則が規定の形でない項目がある対象 | `--run`付きで実行する | 終了コードが0以外 | scripts/extract-code-readings.sh 規則-解釈不能: 終了コードが0以外 |
| 読み取り結果-規則解釈不能-集計列挙 | 46 | extract-code-readings.sh | 規則の無い項目が集計に列挙される | 読み取り結果-規則解釈不能 | 異常 | 同上 | 同上 | 集計の「規則の無い項目」に当該項目が列挙される | scripts/extract-code-readings.sh 規則-解釈不能: 集計の規則の無い項目に列挙される |
| 読み取り結果-規則解釈不能-警告 | 47 | extract-code-readings.sh | 標準出力に警告が出る | 読み取り結果-規則解釈不能 | 異常 | 同上 | 同上 | 標準出力に警告が出る | scripts/extract-code-readings.sh 規則-解釈不能: 標準出力に警告が出る |
| 読み取り結果-規則解釈不能-他項目継続 | 48 | extract-code-readings.sh | 規定の形の他項目は従来どおり値が埋まる | 読み取り結果-規則解釈不能 | 異常 | 同上（規則が規定の形の項目も併存） | 同上 | 規定の形の項目は従来どおり値が埋まる | scripts/extract-code-readings.sh 規則-解釈不能: 規定の形の項目は従来どおり値が埋まる |
| 読み取り結果-規則解釈不能-verify終了コード | 49 | extract-code-readings.sh | verifyでも終了コードが0以外（改善指示書第1回1-8） | 読み取り結果-規則解釈不能 | 異常 | 同上の対象に既存の読み取り結果がある | `--verify`付きで実行する | 終了コードが0以外 | scripts/extract-code-readings.sh 規則-解釈不能: --verify でも終了コードが0以外 |
| 読み取り結果-規則解釈不能-verify比較一致 | 50 | extract-code-readings.sh | verifyの比較自体は一致する | 読み取り結果-規則解釈不能 | 異常 | 同上 | 同上 | 規則が解釈できる項目同士の比較は一致する | scripts/extract-code-readings.sh 規則-解釈不能: --verify の比較自体は一致する |
| 読み取り結果-規則解釈不能-verify理由出力 | 51 | extract-code-readings.sh | verifyで取り出し不合格の理由を標準エラーへ出す | 読み取り結果-規則解釈不能 | 異常 | 同上 | 同上 | 標準エラーに取り出し不合格の理由が出る | scripts/extract-code-readings.sh 規則-解釈不能: --verify で取り出し不合格の理由を標準エラーへ出す |
| 比較-一致判定-終了コード | 52 | compare-code-readings.sh | 値が一致すると終了コード0 | 比較-一致判定 | 正常 | 値が一致する2つの読み取り結果 | `compare-code-readings.sh <A> <B>`を実行する | 終了コード0 | scripts/compare-code-readings.sh 一致: 終了コード0 |
| 比較-一致判定-順序違い | 53 | compare-code-readings.sh | 値の順序違いは一致扱い | 比較-一致判定 | 正常 | 配列の値の順序だけが異なる2つの読み取り結果 | 同上 | 終了コード0（一致扱い） | scripts/compare-code-readings.sh 値の順序違いは一致扱い: 終了コード0 |
| 比較-値不一致判定-終了コード | 54 | compare-code-readings.sh | 値が不一致だと終了コード1 | 比較-値不一致判定 | 異常 | 値が異なる2つの読み取り結果 | 同上 | 終了コード1 | scripts/compare-code-readings.sh 値の不一致: 終了コード1 |
| 比較-値不一致判定-メッセージ | 55 | compare-code-readings.sh | 値-不一致のメッセージが出る | 比較-値不一致判定 | 異常 | 同上 | 同上 | 標準エラーに「値-不一致」を含むメッセージが出る | scripts/compare-code-readings.sh 値の不一致: メッセージに値-不一致 |
| 比較-片方単位無し判定-終了コード | 56 | compare-code-readings.sh | 片方にしか無い単位があると終了コード1 | 比較-片方単位無し判定 | 異常 | Bにしか無い単位がある2つの読み取り結果 | 同上 | 終了コード1 | scripts/compare-code-readings.sh 片方にしか単位が無い: 終了コード1 |
| 比較-片方単位無し判定-メッセージ | 57 | compare-code-readings.sh | 単位-不在Bのメッセージが出る | 比較-片方単位無し判定 | 異常 | 同上 | 同上 | 標準エラーに「単位-不在B」を含むメッセージが出る | scripts/compare-code-readings.sh 片方にしか単位が無い: メッセージに単位-不在B |
| 比較-集計対象外-終了コード | 58 | compare-code-readings.sh | 集計.jsonは比較対象外で終了コード0 | 比較-集計対象外 | 正常 | 集計.jsonを含む2つのディレクトリ | 同上 | 終了コード0（集計.jsonは比較しない） | scripts/compare-code-readings.sh 集計.jsonは比較対象外: 終了コード0 |
| 使い方-誤り-比較引数不足 | 59 | compare-code-readings.sh | 引数が不足すると終了コード2 | 使い方-誤り | 異常 | 比較対象の引数を渡さない | 引数無しで実行する | 終了コード2 | scripts/compare-code-readings.sh 使い方誤り: 終了コード2 |
| 範囲-単位の定義の終端-除外非混入 | 60 | extract-code-readings.sh | 単位の定義は除外された定義を取り込まない | 範囲-単位の定義の終端 | 正常 | 一覧から除外した定義（廃止されたもの）が対象単位と次の単位の間にある対象 | `--run`付きで実行する | 対象単位の値に除外された定義の内容が含まれない（列がid,amountだけ） | scripts/extract-code-readings.sh 単位の定義: 除外された定義を取り込まない（列がid,amountだけ） |
| 範囲-単位の定義の終端-単位は対比で混入 | 61 | extract-code-readings.sh | 単位（従来の終端）は除外された定義を取り込む | 範囲-単位の定義の終端 | 正常 | 同上 | 同上 | 単位の値に除外された定義の内容（notes）が混入する（対比） | scripts/extract-code-readings.sh 単位: 除外された定義を取り込む（対比。notesが混入する） |
| 範囲-単位の定義の終端-括弧付き正規化 | 62 | extract-code-readings.sh | 範囲「単位の定義（補足）」も単位の定義として扱う | 範囲-単位の定義の終端 | 境界 | 同上 | 同上 | 単位の定義と同じ結果（列がid,amountだけ）になる | scripts/extract-code-readings.sh 単位の定義: 括弧付きの値「単位の定義（補足）」も単位の定義に正規化される |
| 範囲-単位の定義の終端-閉じ括弧無し | 63 | extract-code-readings.sh | 閉じ括弧が無い壊れた定義は従来の終端に落ちる | 範囲-単位の定義の終端 | 異常 | 定義の括弧が閉じないまま次の単位の定義が始まる対象 | `--run`付きで実行する | 終了コード0で、値が従来の終端（次の単位の直前の行）までの内容になる | scripts/extract-code-readings.sh 単位の定義: 閉じ括弧が無い壊れた定義は従来の終端に落ちる |
| 範囲-単位の定義の終端-字下げ閉じ括弧 | 64 | extract-code-readings.sh | 字下げされた閉じ括弧の行を終端にする | 範囲-単位の定義の終端 | 境界 | 閉じ括弧の行が字下げされ、その直後に除外された定義の内容が続く対象 | `--run`付きで実行する | 終端が字下げされた閉じ括弧の行になり、直後の内容を取り込まない | scripts/extract-code-readings.sh 単位の定義: 字下げされた閉じ括弧の行を終端にする |
| 範囲-単位の定義の終端-次単位境界 | 65 | extract-code-readings.sh | 閉じ無しの定義の直後に閉じ括弧を持つ次の単位がある | 範囲-単位の定義の終端 | 異常 | 定義が閉じないまま、閉じ括弧を持つ次の単位（一覧に載る）が続く対象 | `--run`付きで実行する | 次の単位の閉じ括弧を終端に採らず、従来の終端（次の単位の直前の行）に落ちる（値がidだけ）。単位（対比）も同じ値になる | scripts/extract-code-readings.sh 単位の定義: 次の単位の閉じ括弧を誤って終端に採らない（単位も対比で同じ値） |
| 範囲-単位の定義の終端-ネスト括弧 | 66 | extract-code-readings.sh | ネストした括弧の閉じ行（`),`）を含む複数行の定義 | 範囲-単位の定義の終端 | 境界 | 定義の途中に別の開き括弧とその閉じ行（`),`）が入れ子で現れる対象 | `--run`付きで実行する | ネストした閉じ行では終端にせず、定義の最後（対応が0に戻る行）まで取る | scripts/extract-code-readings.sh 単位の定義: ネストした括弧の閉じ行では終わらず定義の最後まで取る |
| 範囲-単位の定義の終端-1行定義 | 67 | extract-code-readings.sh | 開始行の中で開いて閉じる1行定義 | 範囲-単位の定義の終端 | 境界 | 定義の開始と閉じが同じ行にあり、直後に別の単位が続く対象 | `--run`付きで実行する | 終端が開始行自身になり、次の単位の内容を取り込まない | scripts/extract-code-readings.sh 単位の定義: 1行定義はその行だけを終端にする |
| 範囲-単位の定義の終端-次行開始括弧 | 68 | extract-code-readings.sh | 定義を開く文の行に開き括弧が無く次行以降で開く | 範囲-単位の定義の終端 | 境界 | 開始行にテーブル名だけがあり、次の行で開き括弧が始まり、その後に除外した定義が続く対象 | `--run`付きで実行する | 次行以降で最初に開いた括弧から残高を数え、除外した定義の内容を取り込まない（値がid,amountだけ） | scripts/extract-code-readings.sh 単位の定義: 開始行に開き括弧が無く次行以降で開いても除外定義を取り込まない |
| 範囲-単位の定義の終端-同一行閉じ開き | 69 | extract-code-readings.sh | 残高が0に戻った直後が`;`で、同じ行に次の定義の開きが続く | 範囲-単位の定義の終端 | 境界 | `); CREATE TABLE zombi (`のように、閉じ括弧の直後に`;`と次の定義の開きが同じ行に続く対象 | `--run`付きで実行する | `;`の位置でその行を終端にし、同じ行に続く次の定義の内容も次行以降の内容も取り込まない（値がid,amountだけ） | scripts/extract-code-readings.sh 単位の定義: 残高0直後が';'の行を終端にし同じ行の次の定義を取り込まない |
| 範囲-単位の定義の終端-文字列内括弧取り込み | 70 | extract-code-readings.sh | 文字列中の開き括弧を数え、範囲内で残高が0に戻らない | 範囲-単位の定義の終端 | 異常 | 列の既定値が`'('`という文字列を含み、範囲内で残高が0に戻らないまま除外された行が続く対象 | `--run`付きで実行する | 従来の終端まで伸び、除外されるはずだった隣接行の内容を取り込む（既知の限界の確認。値がid,note,orphan_columnになる） | scripts/extract-code-readings.sh 単位の定義: 文字列中の開き括弧で残高が戻らず従来の終端まで伸びて隣を取り込む（既知の限界） |
| 範囲-単位の定義の終端-関数シグネチャ形 | 71 | extract-code-readings.sh | 丸括弧の対応が0に戻った直後が`{`で、対応する`}`まで数え続ける | 範囲-単位の定義の終端 | 境界 | `function Foo(props) {`のように丸括弧の直後に波括弧の本体が続き、直後に除外した同形の定義が続く対象 | `--run`付きで実行する | 対応する`}`の行を終端にし、除外した定義の内容を取り込まない（値がidだけ） | scripts/extract-code-readings.sh 単位の定義: 関数シグネチャ形は対応する'}'の行を終端にし除外定義を取り込まない |
| 範囲-単位の定義の終端-括弧で囲まない定義 | 72 | extract-code-readings.sh | 本体を括弧で囲まない定義（Pythonのdef）は行末で残高が0 | 範囲-単位の定義の終端 | 境界 | `def f(x):`のように丸括弧の対応が閉じた直後が`:`で数え続けるが行末で残高が0になる対象 | `--run`付きで実行する | 開始行だけが終端になり、本体（次行以降の戻り値）を取り込まない（値が空配列になる） | scripts/extract-code-readings.sh 単位の定義: 本体を括弧で囲まない定義（def）は行末で0に戻り開始行だけになる |
| 範囲-単位の定義の終端-次行波括弧 | 73 | extract-code-readings.sh | 波括弧を次行に置く様式は先読みで本体まで取る | 範囲-単位の定義の終端 | 境界 | `function foo(props)`の次行が`{`で始まり対応する`}`まで本体が続き、直後に同形の除外した定義が続く対象 | `--run`付きで実行する | 先読みにより`{`のある行から数え続け、対応する`}`の行を終端にし、除外した定義の内容を取り込まない（値がidだけ） | scripts/extract-code-readings.sh 単位の定義: 波括弧が次行の定義は先読みで本体まで取り除外定義を取り込まない |
| 範囲-単位の定義の終端-デコレータ開始行 | 74 | extract-code-readings.sh | デコレータ行を開始行にすると先読みでは救えない | 範囲-単位の定義の終端 | 境界 | 検出条件が定義の開始行をデコレータの行（`@Injectable()`）にしており、次の行が`export class Foo {`のように定義の文から始まる対象 | `--run`付きで実行する | 先読みでは救えず開始行（デコレータの行）だけが終端になり、値が空配列になる（既知の限界の確認） | scripts/extract-code-readings.sh 単位の定義: デコレータ行から始まる定義は先読みで救えず開始行だけになる |
| 範囲-単位の定義の終端-次行丸括弧の別文 | 75 | extract-code-readings.sh | 次行が丸括弧で始まる別の文でも先読みで続けない | 範囲-単位の定義の終端 | 境界 | `CREATE TABLE a (…)`（`;`無し）の閉じ括弧の次行が`(orphan_column INT);`のように丸括弧で始まる別の文の対象 | `--run`付きで実行する | 丸括弧では先読みを続けず閉じ括弧の行を終端にし、次行の別の文を取り込まない（値がid,amountだけ） | scripts/extract-code-readings.sh 単位の定義: 次行が丸括弧で始まる別の文は先読みで続けず閉じ括弧の行を終端にする |
| 範囲-単位の定義の終端-defの次行タプル代入 | 76 | extract-code-readings.sh | defの次行が丸括弧で始まるタプル代入でも先読みで続けない | 範囲-単位の定義の終端 | 境界 | `def order_summary(x):`の次行が`(a, b) = (x.id, x.name)`のように丸括弧で始まるタプル代入の対象 | `--run`付きで実行する | 丸括弧では先読みを続けず開始行だけが終端になり、値が空配列になる | scripts/extract-code-readings.sh 単位の定義: defの次行が丸括弧で始まるタプル代入でも続けず開始行だけになる |
| 範囲-単位の定義の終端-波括弧閉じ次行波括弧 | 77 | extract-code-readings.sh | 波括弧で閉じた行の次行が波括弧でも先読みで続けない | 範囲-単位の定義の終端 | 境界 | オブジェクトを並べる様式（`},`の次行が`{`）で、閉じた単位の次に除外した別のオブジェクトが続く対象 | `--run`付きで実行する | 波括弧で残高が0に戻った行を終端にし、次行が波括弧でも続けず除外したオブジェクトの内容を取り込まない | scripts/extract-code-readings.sh 単位の定義: 波括弧で閉じた行の次行が波括弧でも先読みで続けず隣のオブジェクトを取り込まない |
| 範囲-単位の定義の終端-CRLF空行先読み | 78 | extract-code-readings.sh | CRLFの空行を挟んでも先読みが続く | 範囲-単位の定義の終端 | 境界 | CRLF改行のファイルで、丸括弧の署名の次に`\r`だけの空行を挟んで波括弧の本体が続く様式で、直後に除外した同形の定義が続く対象 | `--run`付きで実行する | `\r`だけの行を空行として飛ばして先読みが続き、対応する`}`の行を終端にし、除外した定義の内容を取り込まない | scripts/extract-code-readings.sh 単位の定義: CRLFの空行を挟んでも先読みが続き除外定義を取り込まない |
| 範囲-単位の定義の終端-カンマ終端呼び出し式 | 79 | extract-code-readings.sh | カンマで閉じた呼び出し式は先読みしない | 範囲-単位の定義の終端 | 境界 | `wrapHandler(ordersHandler, "orders-real"),`のように丸括弧の閉じ直後が`,`で終わり、次行に除外したオブジェクトが続く対象 | `--run`付きで実行する | `,`の位置でその行を終端にし、次行の除外したオブジェクトの内容を取り込まない（値が空配列になる） | scripts/extract-code-readings.sh 単位の定義: カンマで閉じた呼び出し式は次行の除外オブジェクトを取り込まない |
| 範囲-単位の定義の終端-CRLF二重CR空行 | 80 | extract-code-readings.sh | CRLFの空行に`\r`が2個続いても先読みが続く | 範囲-単位の定義の終端 | 境界 | CRLF改行のファイルで、丸括弧の署名の次に`\r`が2個続く空行を挟んで波括弧の本体が続く様式で、直後に除外した同形の定義が続く対象 | `--run`付きで実行する | `\r`を1個以上まとめて除いて空行と判定し先読みが続き、対応する`}`の行を終端にし、除外した定義の内容を取り込まない | scripts/extract-code-readings.sh 単位の定義: CRLFの空行に\rが2個続いても先読みが続き除外定義を取り込まない |

## §3 入力条件

**この節の位置づけ: 現場コード**

前提・操作は§2の該当列に記載する。ここでは共通の入力条件だけを示す。

| 区分 | 内容 |
|---|---|
| 現場コード | 検証者が用意する実際の対象リポジトリのコード。文字コードはUTF-8を既定としEUC-JP等は調査と検出条件の定義書.mdの文字コード行で指定する |
| 調査と検出条件の定義書.md | 「4. 単位の見つけ方」章に種別ごとの取り出しの規則表を持つ |
| 一覧 | docs/design/lists/配下の種別ごとのjson。識別子・場所・属するファイル・根拠を持つ |
| run.json | 対象リポジトリ・出力の置き場・実行の識別子・対象のコミットを持つ |

## §4 期待結果

**この節の位置づけ: 現場コード**

期待結果は§2の該当列に記載する。ここでは共通の確認方法だけを示す。

| 区分 | 内容 |
|---|---|
| 確認の方法 | 現場コードの記述と読み取り結果ファイルの値を人とAIが突き合わせる |
| 読み取り結果ファイル | `<出力先>/code-readings/<種別>/<単位のフォルダ名>.json`に読み取り結果・未・取り出した実行を持つ（フォルダ名は表示名から作る。識別子ではない） |
| 集計ファイル | `<出力先>/code-readings/<種別>/集計.json`に単位数・未の項目数・規則の無い項目を持つ |

## §5 異常系

**この節の位置づけ: 現場コード**

| 観点のキー | 機能 | 発生させる条件 | 期待する例外・エラー | 由来する詳細設計書の章 |
|---|---|---|---|---|
| 読み取り結果-規則の不備 | extract-code-readings.sh | 調査と検出条件の定義書の項目の2列目が規定の形でない | 「規則の無い項目」に列挙・警告・終了コードが0以外 | 詳細設計書 §5 エラー処理 |
| 読み取り結果-属するファイル不在検知 | extract-code-readings.sh | 一覧の属するファイルが実在しない | 終了コード1で不合格になる | 詳細設計書 §5 エラー処理 |
| 使い方-誤り | extract-code-readings.sh / compare-code-readings.sh | 必須引数を欠いて実行する | 終了コード2で判定不能になる | 詳細設計書 §5 エラー処理 |
| 読み取り結果-規則解釈不能 | extract-code-readings.sh | 規則を解釈できない項目がある（--verifyの有無を問わない） | 終了コードが0以外になる（改善指示書第1回1-8） | 詳細設計書 §5 エラー処理 |
| 比較-値不一致判定 | compare-code-readings.sh | 2つの読み取り結果の値が不一致 | 終了コード1、値-不一致のメッセージが出る | 詳細設計書 §5 エラー処理 |

## §6 境界値

**この節の位置づけ: 現場コード**

| 観点のキー | 機能 | 対象の引数・状態 | 境界の値 | 境界の直前と直後の扱い |
|---|---|---|---|---|

## §7 網羅基準

**この節の位置づけ: 現場コード**

| 観点 | 基準 |
|---|---|
| 条件の網羅 | 「現場コードで検証するテストケースの案」の「読み取り結果を取り出す」の全ケースを載せる。検査スクリプトの自己テストは別節。 |
| 分岐の網羅 | 詳細設計書に記録された真偽の分岐を網羅する。 |
| エラーの網羅 | 詳細設計書に記録された終了コード・エラーを網羅する。 |

検査スクリプトの自己テストの実測件数は次のとおり。

| スクリプト | 件数 |
|---|---|
| scripts/extract-code-readings.sh | 65 |
| scripts/compare-code-readings.sh | 8 |

## §8 前提条件と終了条件

**この節の位置づけ: 現場コード**

| 区分 | 内容 |
|---|---|
| 実行の前提条件 | 検証者が現場コードと調査と検出条件の定義書・一覧を用意していること。 |
| 終了の条件 | §1の全観点に§2のケースが対応し、現場コードでの実測結果が期待結果と一致していること。 |
