# 基盤文書を書く（reverse-writing-foundation-guides）の単体テスト設計書

**この文書の位置づけ**（別の言語で作り直す場合は、まず「仕様」から読む）

- 仕様: §1 テスト観点、§2 テストケース一覧、§3 入力条件、§4 期待結果、§5 異常系、§6 境界値、§7 網羅基準、§8 前提条件と終了条件
- 現行実装: 該当なし

## 本書が検証するもの

reverse-writing-foundation-guidesが持つ各スクリプトの`--self-test`が確かめる入出力・分岐・終了コードを対象とする。

## テスト対象

| スクリプト | 自己テストの実行 | ケース数 |
|---|---|---|
| scripts/check-foundation-guides.sh | bash scripts/check-foundation-guides.sh --self-test | 17 |

## テストの粒度と自動化の方針

| 項目 | 方針 |
|---|---|
| テストの粒度 | 1本のスクリプトを1単位として、`--self-test`が持つ各ケースの入出力・分岐・終了コードを確認する。 |
| 自動化 | `--self-test`引数を渡す自動テストを優先する。 |

## 本書が扱わない範囲

| 対象外の事項 | 対象外とする理由 | 代わりに確認する資料または試験 |
|---|---|---|
| 複数機能をまたぐ結合 | 単位ごとの文書は結合テストを扱わないため | docs/design/skills/reverse-orchestrating-flow/ |

## §1 テスト観点

**この節の位置づけ: 現行実装**

| キー | 関数・メソッド名 | 観点 | 由来する詳細設計書の章 |
|---|---|---|---|
| 依存の実在確認 | scripts/check-foundation-guides.sh | 技術スタックの名前が依存の定義ファイルに実在する | 詳細設計書 §2 処理の定義 |
| 節構成の充足確認 | scripts/check-foundation-guides.sh | 環境構築手順書の節が揃う | 詳細設計書 §2 処理の定義 |
| 位置づけ行確認 | scripts/check-foundation-guides.sh | §直後に位置づけの行があることを確かめる | 詳細設計書 §2 処理の定義 |
| 記入の完了確認 | scripts/check-foundation-guides.sh | 未記入のプレースホルダが残っていない | 詳細設計書 §2 処理の定義 |
| 位置禁止確認 | scripts/check-foundation-guides.sh | 実装位置（file:line）の記載が無い | 詳細設計書 §2 処理の定義 |
| 秘密の値の不在確認 | scripts/check-foundation-guides.sh | 接続先・秘密の値らしき記述が無い | 詳細設計書 §2 処理の定義 |
| 判定不能の切り分け | scripts/check-foundation-guides.sh | 道標が無い場合を合否と分けて扱う | 詳細設計書 §2 処理の定義 |
| 設計書ルート分離 | scripts/check-foundation-guides.sh | --design-root指定時に依存の定義を対象側から読み、対象側へ書き込まない | 詳細設計書 §2 処理の定義 |
| 使い方エラー | scripts/check-foundation-guides.sh | 引数不足時のエラーを確かめる | 詳細設計書 §2 処理の定義 |

## §2 テストケース一覧

**この節の位置づけ: 現行実装**

| キー | 番号 | 関数・メソッド名 | ケースの名前 | 対応する観点のキー | 区分 | 前提 | 操作 | 期待結果 |
|---|---|---|---|---|---|---|---|---|
| 合格-完成形 | 1 | check-foundation-guides.sh | 技術スタック・環境構築手順書ともに完成形 | 依存の実在確認 | 正常 | package.json・道標.md・技術スタック.md・環境構築手順書.mdを完成形で作る | 対象ディレクトリを引数に実行する | 終了コード0 |
| 不合格-依存不在 | 2 | check-foundation-guides.sh | 技術スタックのフレームワーク名をreactからvueに書き換え | 依存の実在確認 | 異常 | 完成形の技術スタック.mdをsedで書き換える | 対象ディレクトリを引数に実行する | 終了コード1 |
| 不合格-依存不在表示 | 3 | check-foundation-guides.sh | 同上 | 依存の実在確認 | 異常 | 同上 | 同上 | 標準エラーに「依存-不在」を含む |
| 不合格-節の欠落 | 4 | check-foundation-guides.sh | 環境構築手順書から§7以降を削除 | 節構成の充足確認 | 異常 | 完成形の環境構築手順書.mdからawkで§7以降を除去する | 対象ディレクトリを引数に実行する | 終了コード1 |
| 不合格-節の欠落表示 | 5 | check-foundation-guides.sh | 同上 | 節構成の充足確認 | 異常 | 同上 | 同上 | 標準エラーに「節-欠落」を含む |
| 不合格-位置づけ欠落 | 6 | check-foundation-guides.sh | 最初の位置づけの行を別の文言に置き換え | 位置づけ行確認 | 異常 | 完成形の環境構築手順書.mdの最初の位置づけ行をperlで書き換える | 対象ディレクトリを引数に実行する | 終了コード1 |
| 不合格-位置づけ欠落表示 | 7 | check-foundation-guides.sh | 同上 | 位置づけ行確認 | 異常 | 同上 | 同上 | 標準エラーに「位置づけ-欠落」を含む |
| 不合格-未記入残存 | 8 | check-foundation-guides.sh | 末尾に山括弧のプレースホルダを追記 | 記入の完了確認 | 異常 | 完成形の環境構築手順書.mdへ「<書きかけ>」を追記する | 対象ディレクトリを引数に実行する | 終了コード1 |
| 不合格-未記入残存表示 | 9 | check-foundation-guides.sh | 同上 | 記入の完了確認 | 異常 | 同上 | 同上 | 標準エラーに「未記入-残存」を含む |
| 不合格-位置禁止 | 10 | check-foundation-guides.sh | 末尾にsrc/app.ts:120の記述を追記 | 位置禁止確認 | 異常 | 完成形の環境構築手順書.mdへfile:line記述を追記する | 対象ディレクトリを引数に実行する | 終了コード1 |
| 不合格-位置禁止表示 | 11 | check-foundation-guides.sh | 同上 | 位置禁止確認 | 異常 | 同上 | 同上 | 標準エラーに「位置-禁止」を含む |
| 不合格-秘密混入 | 12 | check-foundation-guides.sh | 末尾にAPI_KEY=の行を追記 | 秘密の値の不在確認 | 異常 | 完成形の環境構築手順書.mdへ秘密の値らしき行を追記する | 対象ディレクトリを引数に実行する | 終了コード1 |
| 不合格-秘密混入表示 | 13 | check-foundation-guides.sh | 同上 | 秘密の値の不在確認 | 異常 | 同上 | 同上 | 標準エラーに「秘密-混入」を含む |
| 判定不能-道標不在 | 14 | check-foundation-guides.sh | 道標.mdを削除した対象 | 判定不能の切り分け | 異常 | 完成形一式から道標.mdをrmで削除する | 対象ディレクトリを引数に実行する | 終了コード2 |
| 設計書ルート分離-合格 | 15 | check-foundation-guides.sh | package.jsonのみを持つ対象と、道標一式を持つ別ディレクトリ | 設計書ルート分離 | 正常 | 対象側にpackage.jsonのみ、設計側にpackage.jsonを持たない完成形一式を作る | 対象ディレクトリと--design-rootに設計側を指定して実行する | 終了コード0 |
| 設計書ルート分離-対象に書かない | 16 | check-foundation-guides.sh | 同上 | 設計書ルート分離 | 正常 | 同上 | 同上 | 対象ディレクトリにdocsフォルダが作られていない |
| 使い方-引数不足 | 17 | check-foundation-guides.sh | 引数を渡さない実行 | 使い方エラー | 異常 | なし | 引数無しでスクリプトを実行する | 終了コード2 |

## §3 入力条件

**この節の位置づけ: 現行実装**

| 種類 | 内容 | 該当するケースのキー |
|---|---|---|
| 完成形一式（未編集） | package.json・道標.md・技術スタック.md・環境構築手順書.mdが揃った対象 | 合格-完成形 |
| 完成形一式の単一項目編集 | 技術スタックの名前・節構成・位置づけ行・末尾追記のいずれかを書き換えた対象 | 不合格-依存不在、不合格-依存不在表示、不合格-節の欠落、不合格-節の欠落表示、不合格-位置づけ欠落、不合格-位置づけ欠落表示、不合格-未記入残存、不合格-未記入残存表示、不合格-位置禁止、不合格-位置禁止表示、不合格-秘密混入、不合格-秘密混入表示 |
| 道標を欠いた完成形一式 | 完成形一式から道標.mdを削除した対象 | 判定不能-道標不在 |
| 対象と設計側を分離した一式 | 対象側はpackage.jsonのみ、設計側は道標一式（package.json無し） | 設計書ルート分離-合格、設計書ルート分離-対象に書かない |
| 引数無し | コマンドライン引数を渡さない | 使い方-引数不足 |

## §4 期待結果

**この節の位置づけ: 現行実装**

| 種類 | 終了コード | 標準エラーの内容 | 該当するケースのキー |
|---|---|---|---|
| 合格 | 0 | なし | 合格-完成形、設計書ルート分離-合格、設計書ルート分離-対象に書かない |
| 不合格（理由付き） | 1 | 「依存-不在」「節-欠落」「位置づけ-欠落」「未記入-残存」「位置-禁止」「秘密-混入」のいずれか | 不合格-依存不在、不合格-依存不在表示、不合格-節の欠落、不合格-節の欠落表示、不合格-位置づけ欠落、不合格-位置づけ欠落表示、不合格-未記入残存、不合格-未記入残存表示、不合格-位置禁止、不合格-位置禁止表示、不合格-秘密混入、不合格-秘密混入表示 |
| 判定不能 | 2 | なし | 判定不能-道標不在 |
| 使い方エラー | 2 | なし | 使い方-引数不足 |

## §5 異常系

**この節の位置づけ: 現行実装**

| 観点のキー | 関数・メソッド名 | 発生させる条件 | 期待する例外・エラー | 由来する詳細設計書の章 |
|---|---|---|---|---|
| 依存の実在確認 | check-foundation-guides.sh | 技術スタックの名前が依存の定義に無い | 終了コード1、「依存-不在」を含む | 詳細設計書 §5 エラー処理 |
| 節構成の充足確認 | check-foundation-guides.sh | 環境構築手順書の必須の節が無い | 終了コード1、「節-欠落」を含む | 詳細設計書 §5 エラー処理 |
| 位置づけ行確認 | check-foundation-guides.sh | §直後に位置づけの行が無い | 終了コード1、「位置づけ-欠落」を含む | 詳細設計書 §5 エラー処理 |
| 記入の完了確認 | check-foundation-guides.sh | 未記入のプレースホルダが残る | 終了コード1、「未記入-残存」を含む | 詳細設計書 §5 エラー処理 |
| 位置禁止確認 | check-foundation-guides.sh | 実装位置（file:line）の記載がある | 終了コード1、「位置-禁止」を含む | 詳細設計書 §5 エラー処理 |
| 秘密の値の不在確認 | check-foundation-guides.sh | 秘密の値らしき記述がある | 終了コード1、「秘密-混入」を含む | 詳細設計書 §5 エラー処理 |
| 判定不能の切り分け | check-foundation-guides.sh | 道標.mdが無い | 終了コード2 | 詳細設計書 §5 エラー処理 |
| 使い方エラー | check-foundation-guides.sh | 引数を渡さない | 終了コード2 | 詳細設計書 §5 エラー処理 |

## §6 境界値

**この節の位置づけ: 現行実装**

| 観点のキー | 関数・メソッド名 | 対象の引数・状態 | 境界の値 | 境界の直前と直後の扱い |
|---|---|---|---|---|

自己テストは数値の境界（上限・下限）を持つ判定を扱わないため、境界値のケースは無い。

## §7 網羅基準

**この節の位置づけ: 現行実装**

| 観点 | 基準 |
|---|---|
| 条件の網羅 | 機能設計書・詳細設計書に記録された条件を網羅する。 |
| 分岐の網羅 | 詳細設計書に記録された真偽の分岐を網羅する。 |
| エラーの網羅 | 詳細設計書に記録された終了コード・エラーを網羅する。 |

## §8 前提条件と終了条件

**この節の位置づけ: 現行実装**

| 区分 | 内容 |
|---|---|
| 実行の前提条件 | 対象スクリプトの`--self-test`を直接実行できること。 |
| 終了の条件 | 対象スクリプトの観点とケースがすべて対応していること。 |
