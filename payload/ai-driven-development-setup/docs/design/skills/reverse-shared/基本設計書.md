# reverse の共有部品（reverse-shared）の機能設計書（reverse-shared）の基本設計書

reverse単位の各機能（調査と検出条件の定義書を書く・一覧を作る等）が個別に再実装せず共用する部品をまとめて持つ。範囲の承認の確認・実行フォルダの値の読み取り・一覧の元データの読み取り・単位のフォルダ名の作成・工程の状態の記録、および設計書の書き方の決まり等の写しのlinterを含む。名前の決まり（skill-naming）によりSKILL.mdを持たない共有部品であり、検収はtests/が担う。

## §1 外部仕様

### 要件との対応

| 要件の柱 | 要件の項目 | この機能が満たす内容 |
|---|---|---|
| 柱1 調査を支える | 完了条件: 件数が実測と一致する | list-units-of.shが一覧の元データを機能横断で同じ読み方で提供する |
| 柱2 基盤一式の作成を支える | 完了条件: 未回答の確認事項が無い | check-entry.shが範囲の承認の可否と調査と検出条件の定義書の同一性を確かめてから後続の走査を許す |
| 支援ツール全体の制約 | 柱ごとに取り出して動く。機能を1つ足すとき、統括の手順を直さない | 実行フォルダの値の読み取り（read-run.sh）・単位のフォルダ名（unit-dir-name.sh）・工程の状態（units-status.sh）を共通部品として切り出し、機能追加時の再実装を防ぐ |
| 支援ツール全体の制約 | 全機能がテストと合格基準を持つ | check-doc-heading-addendum.sh・check-unit-test-design-doc-sections.shが設計書の書き方の決まり・単体テスト設計書の決まりを機械検査する |
| 支援ツール全体の制約 | 手順書のコマンドがそのまま動く | tests/test-skill-commands.shが、全機能のSKILL.mdが書くコマンドを実際に走らせ、使い方の誤り・要確認-判定不能・異常終了（終了コード128以上）のいずれの引数不足も無いことを確かめる（第1回改善指示書1-35追記） |

### 入力

| 入力 | 形 | 出所 |
|---|---|---|
| 実行フォルダのrun.json | JSON | 統括の実行の開始スクリプト |
| 対象範囲の承認.md・調査と検出条件の定義書.md | Markdown | reverse-writing-survey-definitionの出力 |
| 一覧の元データ（<種別>.json） | JSON | reverse-listing-unitsの出力 |
| 書き込み対象の文書本文 | Markdown | Write/Editのtool_input |

### 出力

| 出力 | 形 | 置き場 |
|---|---|---|
| 実行フォルダの値（対象リポジトリ等） | 標準出力（1行） | 呼び出し元へ返す |
| 範囲の承認の判定 | 終了コード | 呼び出し元へ返す |
| 一覧の元データの1行1件（タブ区切り） | 標準出力 | 呼び出し元へ返す |
| 単位のフォルダ名 | 標準出力（1行） | 呼び出し元へ返す |
| 単位ごとの工程の状態 | JSON | <実行フォルダ>/logs/units-status.json |
| linterの許可・拒否・通知・対象外の判定 | 終了コード・メッセージ | 呼び出し元（PreToolUse）へ返す |

### 実行の口

Skillを持たない共有部品のため、各スクリプトを他の機能から直接呼び出す。使い方は各スクリプトの先頭コメントに置く。


設計書の節に位置づけの行を置かない（2026-09-08 廃止）。

## §2 業務仕様

### 手順

共有部品のため単一の手順は持たない。各スクリプトの使い方は先頭コメントに置く。

- read-run.sh: `read-run.sh <実行フォルダ> <キー>` で実行フォルダのrun.jsonから値を1行読む
- check-entry.sh: `check-entry.sh <実行フォルダ> <対象リポジトリのルート>` で対象範囲の承認.mdの可否と調査と検出条件の定義書の同一性を確かめる
- unit-dir-name.sh: `unit-dir-name.sh <表示名>` で表示名をフォルダ名に使える形へ置換する。入力は識別子ではなく表示名（工程2-1の完了時の処理「業務名の確定」が組んだ値）。呼び出し元は工程2-1の業務名の確定だけである。record-acceptance.sh・check-acceptance-record.shは現状、識別子からunit-dir-name.shを直接呼んでいる。extract-code-readings.shも同様である。list-units-of.shの5列目（フォルダ名）から得るよう変える対象である
- design-doc-name.sh: `design-doc-name.sh <種別> <basic|test>` で種別ごとの基本設計書・単体テスト設計書のファイル名（実名）を返す
- units-status.sh: `units-status.sh <実行フォルダ> set|get|list <残りの引数>` で工程の状態を読み書きする
- record-acceptance.sh: 単位の記録（`--kind`・`--unit`指定）に`--no-units-status`を付けると、単位の状態のファイル（`<実行フォルダ>/logs/units-status.json`）へ完了判定を書き込まない。共通設計文書の記録（`--common`指定）は元々この状態のファイルを触らないため対象外（第1回改善指示書1-38）
- list-units-of.sh: `list-units-of.sh <対象リポジトリのルート> <種別> [--lists <場所>]` で一覧の元データをタブ区切りで返す。列は識別子・表示名・場所・属するファイルの既存4列の順を変えず、5列目にフォルダ名を持つ5列である。位置で読む呼び出し元は3本である。check-basic-design.sh・check-detail-design.shはそのうち2本である。check-detail-design.shの自己テストは4列スタブを持つ。extract-code-readings.shも位置で読む呼び出し元である。いずれも5列目を読むよう変える対象である。check-test-designs.shはlist-units-of.shの出力の行数だけを数え列を読まないため、列が増えても行数は変わらず変更不要である。2列目の表示名は、表示名がnull、または空白（半角・全角の空白・タブ・改行）だけなら業務名、業務名もnull、または空白（半角・全角の空白・タブ・改行）だけなら識別子へ落ちる。一覧を作る処理は新しく現れた単位の表示名・業務名を空文字列で書くため、空白だけの値をnullと同じに扱う（第1回改善指示書1-29）。一覧.mdの表示（list-units.sh）は表示名が空のとき「名前」鍵を使うが、list-units-of.shの2列目は出力先の名前の元になるため識別子へ落とす。「名前」は元データに由来する表示用の値であり、出力先の名前には使わない。表示名・業務名が文字列でない値（数値・真偽値等）のときは、空白判定の前に文字列にする（`false`は文字列`false`として非空扱いになる）。空白とみなす文字は半角・全角の空白・タブ・改行に加え、ゼロ幅空白（U+200B）とBOM（U+FEFF）も含める（第1回改善指示書1-29・再検証）。読み取りの途中でjqの処理そのものが失敗したときも判定不能（終了コード2）で終える
- check-doc-heading-addendum.sh: PreToolUse(Write|Edit)で設計書の書き方の決まりを検査する
- check-unit-test-design-doc-sections.sh: PreToolUse(Write)で単体テスト設計書の決まりを検査する

### 完了判定の状態

完了判定は流れの設計「完了判定の状態」に従う。判定は合格・不合格の2値である（2026-09-05に単位・共通設計文書の保留を廃止）。record-acceptance.shは観点の値（合・否・要確認）を記録の観点欄にそのまま書く。要確認を含むときは、理由に確認事項一覧のキーが含まれているかを確かめ、無ければ記録を作らない。記録は判定した状態そのものを指す。判定者は判定した時点の文書の同一性の値を`--judged`でrecord-acceptance.shへ渡し、記録を書く時点の値と一致しなければ記録を作らない。食い違った記録は存在しない。

### 判定の規則

| 検査 | スクリプト | 検査キー |
|---|---|---|
| check-acceptance-record.shの合否判定 | scripts/check-acceptance-record.sh | 終了コード0/1/2 |
| check-basic-phase.shの合否判定 | scripts/check-basic-phase.sh | 終了コード0/1/2 |
| design-root.shの合否判定 | scripts/design-root.sh | 終了コード0/1/2 |
| plan-units.shの合否判定 | scripts/plan-units.sh | 終了コード0/1/2 |
| record-acceptance.shの合否判定 | scripts/record-acceptance.sh | 終了コード0/1/2 |
| start-run.shの合否判定 | scripts/start-run.sh | 終了コード0/1/2 |
| 範囲の承認の可否と同一性 | scripts/check-entry.sh | 合格・不合格-否・不合格-同一性不一致・判定不能-承認ファイル不在・判定不能-調査と検出条件の定義書ファイル不在 |
| 実行フォルダの値の読み取り | scripts/read-run.sh | 対象リポジトリ・対象プロジェクト名・出力の置き場・実行の識別子・対象のコミット・キー不在・run.json不在・引数不足 |
| 単位のフォルダ名の作成 | scripts/unit-dir-name.sh | 入力は表示名（識別子ではない）。置換の対象は`/` `{` `}` `:` `\` `?` `*` `"` `<` `>` `\|`と空白。先頭の`_`は除く。引数無し |
| 種別ごとの文書名の作成 | scripts/design-doc-name.sh | 種別×basic/testの14通りの実名・不正な種別/区分・引数不足 |
| 工程の状態の読み書き | scripts/units-status.sh | 未設定は未・set後にget一致・別工程は未のまま・list・不正な工程・引数不足 |
| 一覧の元データの読み取り | scripts/list-units-of.sh | タブ区切り出力・一覧不在・不正なJSON・--listsオプション・読み取り失敗（jqの実行時エラー） |
| 追記章と補足章を作らない | scripts/check-doc-heading-addendum.sh | 追記・補足・その他で始まる見出しの検出 |
| 任意記載と省略記載をしない | scripts/check-doc-heading-addendum.sh | 列挙を打ち切る省略記号の検出 |
| 要件定義書は合意した範囲を確定させる | scripts/check-doc-heading-addendum.sh | 対象範囲・優先度・受入条件の3見出しの実在 |
| 基本設計書は詳細設計を書ける状態を作る | scripts/check-doc-heading-addendum.sh | 様式の見出しと単体テスト設計書の同居フォルダの実在 |
| 記載項目を省かない | scripts/check-unit-test-design-doc-sections.sh | 12節または13節の名前・順序・件数の一致 |
| 単体テスト設計書は基本設計フェーズで作る | scripts/check-unit-test-design-doc-sections.sh | 基本設計フォルダの宣言と単体テスト設計書の実在 |
| 規約が求めるテストを観点へ取り込む | scripts/check-unit-test-design-doc-sections.sh | 検査列に「テスト:」を含む規則名の本文への出現 |
| テスト設計書の節の役割 | scripts/check-unit-test-design-doc-sections.sh | §1・§2・§5・§6のキー集合の整合 |

check-acceptance-record.shは判定が合格以外の記録を不合格に数える。保留は廃止し特別扱いしない（第1回改善指示書1-17は2026-09-05の保留廃止決定に伴い終了）。

## §3 方式設計

機械とAIの分担は、機械検査（`検査`節の各スクリプト）が形式・実在・網羅を確かめ、AIが業務の言葉への翻訳・判断を担う二段構成である。

実行方式はシェルスクリプトと`grep`・正規表現による走査であり、対象コードの言語に依存しない。

### list-units.sh（一覧を作る機能が使う検出条件の実行）に対する共有部品としての位置づけ

reverse-sharedは検出条件の実行そのものは持たず、実行後の元データの読み取り（list-units-of.sh）とフォルダ名の作成（unit-dir-name.sh）を共有する。検出条件の実行はreverse-listing-unitsのlist-units.shが担い、責務を分離している。unit-dir-name.shは一覧化（工程2-1）の完了時の処理「業務名の確定」がフォルダ名を確定するときに呼び、確定した値は一覧の元データjsonの「フォルダ名」フィールドへ書く。後続の各機能はunit-dir-name.shを再度呼ばず、list-units-of.shの出力からこの値を得る。位置で読む3本の呼び出し元は、list-units-of.shの5列目（フォルダ名）を読むよう変える対象である。check-test-designs.shは行数だけを数え列を読まないため対象外である。

unit-dir-name.shの置換対象は、実装の変更対象を含む。現行実装は`/`空白`{``}``:``\``?``*`の8種のみを置換する。本書が示す置換対象（`"``<``>``\|`を加えた12種）はこの拡張を前提にしており、スクリプトと自己テストを同時に直す必要がある。reverse-listing-unitsの単体テスト設計書のケース「一覧-フォルダ名-一意」は、業務名に`\|`を含む例を前提にしており、この拡張が済んでいることを前提にする。

unit-dir-name.shの入力も、実装の変更対象を含む。現行実装は識別子を入力に受け取る。本書が示す入力（表示名）はこの変更を前提にしており、置換対象の拡張と同時にスクリプトと自己テストを直す必要がある。

### check-doc-heading-addendum.sh / check-unit-test-design-doc-sections.sh の写し方式

**必要性**: 設計書の書き方の決まり・単体テスト設計書の決まりはsetup単位の規約が持つ原本であり、reverse単位のPreToolUse(Write|Edit)から同じ検査を使うために写しを置く。原本を直接requireする依存を持たず、テスト（test-self-tests.sh）で写しと原本の一致（改行・通知行を除く）を確かめることで、ずれを検出可能にする。

**代替案を採用しなかった理由**:
- setup側の原本を直接読み込む: reverse単位とsetup単位の間に実行時依存が生まれ、「柱ごとに取り出して動く」という制約に反する
- 写しを持たずreverse独自に再実装する: 同じ検査を2箇所で保守することになり、ずれが機械で検出できない

**保守責任者**: 人手（ユーザー）。原本（setup-scaffolding-rulesのtemplates配下）を変えるときは写しも同時に直し、test-self-tests.shで一致を確かめる。

**廃棄条件**: 写し方式を別の共有機構（symlink等）に置き換えた時。

### 検査の決定性

検査は同じ入力に同じ判定を返す（`docs/design/common/リバースの流れの設計.md#流れに求める条件`の「同じ入力で同じ出力」）。
本文を変数からパイプで早期終了する消費者（`grep -q`・`head`）へ渡さない。
pipefailの下では書き込み側がSIGPIPEで失敗し、パイプ全体が失敗して判定が揺れるため。
本文は一時ファイルへ1回書き出すか、ヒアストリングで渡す。

自己テスト（`--self-test`）のケース名も同じ理由で決定的に保つ。実行ごとに変わる値（`mktemp`が作る一時パス・時刻）をケース名に含めない。単体テスト設計書§2の「自己テストのケース名」列は自己テストの出力と文字列一致で照合するため、ケース名が実行ごとに変わると1対1照合が成立しない。診断のために一時パスを出力に含めたい場合は、実際のパスを`<tmp>`のような固定の置換文字列へ置き換えてから出力する（`check-unit-test-design-doc-sections.sh`の系7・系7b・系7c・系8を参照）。

### design-doc-name.sh の共有部品化

**必要性**: 種別ごとの基本設計書・単体テスト設計書のファイル名（旧様式の実名）は、check-basic-design.sh（様式の検査）とrecord-acceptance.sh（合格の記録）の双方が使う。かつては両スクリプトがそれぞれ独自にこの名前を決め打ちしており、二重定義がずれて合格の記録の文書名が種別ごとの実名と揃わず、記録の同一性の値が空になる不具合（第1回改善指示書1-12）を起こした。名前の決め方を1箇所へ集め、unit-dir-name.shと同じ形の共有部品として持つ必要がある。

**代替案を採用しなかった理由**:
- 現状維持（各スクリプトが個別に決め打ちする）: 名前を変えるときに一方だけ直す事故が再発する。実際に起きた不具合の直接原因である
- check-basic-design.shの関数をrecord-acceptance.shが直接requireする: reverse-writing-basic-designとreverse-sharedの間に実行時依存が生まれ、「柱ごとに取り出して動く」という制約に反する

**保守責任者**: 人手（ユーザー）。文書名を変えるときは、本スクリプトと自己テストと、本スクリプトを使う各機能（check-basic-design.sh・record-acceptance.sh）を同時に確かめる。

**廃棄条件**: 種別ごとの文書名の決め方を構造化データから生成する仕組みに置き換えた時。

## §4 データ仕様

### 読み書きする値の形

| 入力 | 形 | 出所 |
|---|---|---|
| 実行フォルダのrun.json | JSON | 統括の実行の開始スクリプト |
| 対象範囲の承認.md・調査と検出条件の定義書.md | Markdown | reverse-writing-survey-definitionの出力 |
| 一覧の元データ（<種別>.json） | JSON | reverse-listing-unitsの出力 |
| 書き込み対象の文書本文 | Markdown | Write/Editのtool_input |

| 出力 | 形 | 置き場 |
|---|---|---|
| 実行フォルダの値（対象リポジトリ等） | 標準出力（1行） | 呼び出し元へ返す |
| 範囲の承認の判定 | 終了コード | 呼び出し元へ返す |
| 一覧の元データの1行1件（タブ区切り） | 標準出力 | 呼び出し元へ返す |
| 単位のフォルダ名 | 標準出力（1行） | 呼び出し元へ返す |
| 単位ごとの工程の状態 | JSON | <実行フォルダ>/logs/units-status.json |
| linterの許可・拒否・通知・対象外の判定 | 終了コード・メッセージ | 呼び出し元（PreToolUse）へ返す |

出力配置の定義（output-layout.json）は規約提案の項を持たない。工程2-10は2026-09-07に廃止された。

## §5 エラーと例外

check-acceptance-record.shとrecord-acceptance.shは、要確認の観点を持つ記録の照合・記録に`--run`を必須とする。`--run`が無いときは要確認-判定不能として不合格にする。確認事項の記録.mdを参照できないときも同様に不合格にする（2026-09-06改善）。

### 終了コード・差し戻しの一覧

単位・共通設計文書の保留は廃止した（2026-09-05）。record-acceptance.shは`--verdict 保留`を終了コード2で拒否し、check-acceptance-record.shは過去に書かれた保留の記録があっても判定-不合格として扱う。以下の「保留のとき」欄はこの前提での実際の挙動を書く。

| 検査 | スクリプト | 検査キー | 保留のとき |
|---|---|---|---|
| check-acceptance-record.shの合否判定 | scripts/check-acceptance-record.sh | 終了コード0/1/2 | 対象外（判定「保留」は判定-不合格として不合格にする） |
| check-basic-phase.shの合否判定 | scripts/check-basic-phase.sh | 終了コード0/1/2 | 対象外 |
| design-root.shの合否判定 | scripts/design-root.sh | 終了コード0/1/2 | 対象外 |
| plan-units.shの合否判定 | scripts/plan-units.sh | 終了コード0/1/2 | 対象外 |
| record-acceptance.shの合否判定 | scripts/record-acceptance.sh | 終了コード0/1/2 | 対象外（`--verdict 保留`は終了コード2で拒否する） |
| start-run.shの合否判定 | scripts/start-run.sh | 終了コード0/1/2 | 対象外 |
| 範囲の承認の可否と同一性 | scripts/check-entry.sh | 合格・不合格-否・不合格-同一性不一致・判定不能-承認ファイル不在・判定不能-調査と検出条件の定義書ファイル不在 | 対象外 |
| 実行フォルダの値の読み取り | scripts/read-run.sh | 対象リポジトリ・対象プロジェクト名・出力の置き場・実行の識別子・対象のコミット・キー不在・run.json不在・引数不足 | 対象外 |
| 単位のフォルダ名の作成 | scripts/unit-dir-name.sh | 入力は表示名（識別子ではない）。置換の対象は`/` `{` `}` `:` `\` `?` `*` `"` `<` `>` `\|`と空白。先頭の`_`は除く。引数無し | 対象外 |
| 種別ごとの文書名の作成 | scripts/design-doc-name.sh | 種別×basic/testの14通りの実名・不正な種別/区分・引数不足 | 対象外 |
| 工程の状態の読み書き | scripts/units-status.sh | 未設定は未・set後にget一致・別工程は未のまま・list・不正な工程・引数不足 | 対象外 |
| 一覧の元データの読み取り | scripts/list-units-of.sh | タブ区切り出力・一覧不在・不正なJSON・--listsオプション・読み取り失敗（jqの実行時エラー） | 対象外 |
| 追記章と補足章を作らない | scripts/check-doc-heading-addendum.sh | 追記・補足・その他で始まる見出しの検出 | 対象外 |
| 任意記載と省略記載をしない | scripts/check-doc-heading-addendum.sh | 列挙を打ち切る省略記号の検出 | 対象外 |
| 要件定義書は合意した範囲を確定させる | scripts/check-doc-heading-addendum.sh | 対象範囲・優先度・受入条件の3見出しの実在 | 対象外 |
| 基本設計書は詳細設計を書ける状態を作る | scripts/check-doc-heading-addendum.sh | 様式の見出しと単体テスト設計書の同居フォルダの実在 | 対象外 |
| 記載項目を省かない | scripts/check-unit-test-design-doc-sections.sh | 12節または13節の名前・順序・件数の一致 | 対象外 |
| 単体テスト設計書は基本設計フェーズで作る | scripts/check-unit-test-design-doc-sections.sh | 基本設計フォルダの宣言と単体テスト設計書の実在 | 対象外 |
| 規約が求めるテストを観点へ取り込む | scripts/check-unit-test-design-doc-sections.sh | 検査列に「テスト:」を含む規則名の本文への出現 | 対象外 |
| テスト設計書の節の役割 | scripts/check-unit-test-design-doc-sections.sh | §1・§2・§5・§6のキー集合の整合 | 対象外 |

### 判定の再現性

同一入力での判定の揺れは不具合として扱う。
自己テストで同じ入力を20回渡し、判定が変わらないことを確かめる。

### 完了条件

- tests/test-self-tests.shの全件が合格
- tests/test-skill-commands.shが抽出したコマンドすべてで引数不足なし
- 7本の共有スクリプトそれぞれの--self-testが合格
- references/の複製（unit-kinds.json・output-layout.json・code-reading-items.json）がdocs/design/common/の定義と一致
- 2本のlinterの写しが、setup単位の規約が持つ原本と一致

## §6 関連資料

- ../reverse-writing-survey-definition/（read-run.sh・check-doc-heading-addendum.shの利用元）
- ../reverse-listing-units/（check-entry.sh・unit-dir-name.sh・list-units-of.shの利用元）
- ../reverse-writing-basic-design/（check-basic-design.shがdesign-doc-name.sh・unit-dir-name.shの利用元。record-acceptance.shと文書名の定義を共有する）

## 追補: setup統括の畳み込みで加わった検査

統括の一本化（`reverse-orchestrating-flow`新設）により、旧2機能の実装を本共有部品へ移した。実行フォルダ作成・実行順計画・合格記録の判定・記録の書き込みを持つ。

| スクリプト | 役割 |
|---|---|
| check-acceptance-record.sh | 合格記録と現在の文書のsha256一致で合否を判定する |
| check-basic-phase.sh | 共通設計文書6つの実在と合格を機械検査する |
| design-root.sh | 設計書の置き場キーの有無で設計書ルートを解決する |
| plan-units.sh | 入出力の重なりとrequiresから実行順を組み立てる |
| record-acceptance.sh | 単位・共通設計文書ごとに合格・不合格を記録する |
| start-run.sh | 実行フォルダを作りrun.jsonへ初期値を書く |
