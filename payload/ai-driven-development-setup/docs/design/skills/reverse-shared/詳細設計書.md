# reverse の共有部品（reverse-shared）の機能設計書（reverse-shared）の詳細設計書

## §1 構成要素

| スクリプト | 役割 |
|---|---|
| check-acceptance-record.sh | 合格記録と現在の文書のsha256一致で合否を判定する（判定が合格以外の記録は不合格に数える。保留は廃止） |
| check-basic-phase.sh | 共通設計文書6つの実在と合格を機械検査する |
| design-root.sh | 設計書の置き場キーの有無で設計書ルートを解決する |
| plan-units.sh | 入出力の重なりとrequiresから実行順を組み立てる |
| record-acceptance.sh | 単位・共通設計文書ごとに合格・不合格を記録する（観点の要確認は理由のキー実在を検査し、--judgedで判定した時点の同一性の値と記録時の値を突き合わせる。記録の`文書`は基本設計書を常に含み、単体テスト設計書は統括の前提確認「テスト設計書の出力」が`出力する`のときだけ含める。単位の記録（`record_unit`）・共通設計文書の記録（`record_common`）はいずれも`run_dir_readable_or_reason()`という1つの関数で`--run`の実行フォルダのrun.jsonの実在・可読性・「実行の識別子」キーの実在と値の形（空白を除いて空でない文字列）を確かめ、読めない・値が不正なら判定不能で記録を作らない。run.jsonが実在してもOSの権限で読めないときは`run-不可読`として区別する。単位の記録はこれに加えて「テスト設計書の出力」の設定を読み、値が`出力する`／`出力しない`のどちらでもないときも判定不能で記録を作らない。共通設計文書の記録（`--common`）は単体テスト設計書を持たないため「テスト設計書の出力」の値は読まない。値の一致判定は完全一致で行い、末尾に余計な空白・改行が付いた値は不正として拒む。照合側は`文書`のキーを走査する汎用の作りのため変更しない。第1回改善指示書1-31・2026-09-07反証対応・第6版） |
| start-run.sh | 実行フォルダを作りrun.jsonへ初期値を書く |
| scripts/check-doc-heading-addendum.sh | 自己テストと機械検査を持つスクリプト |
| scripts/check-entry.sh | 自己テストと機械検査を持つスクリプト |
| scripts/check-unit-test-design-doc-sections.sh | 自己テストと機械検査を持つスクリプト |
| scripts/design-doc-name.sh | 自己テストと機械検査を持つスクリプト |
| scripts/list-units-of.sh | 自己テストと機械検査を持つスクリプト |
| scripts/read-run.sh | 自己テストと機械検査を持つスクリプト |
| scripts/unit-dir-name.sh | 自己テストと機械検査を持つスクリプト |
| scripts/units-status.sh | 自己テストと機械検査を持つスクリプト |
| tests/test-self-tests.sh（独自ケース） | 自己テストと機械検査を持つスクリプト |
| tests/test-skill-commands.sh | 全機能のSKILL.mdが書くコマンドを実際に走らせ、使い方の誤り・要確認-判定不能・異常終了（終了コード128以上）のいずれの引数不足も無いことを確かめる（第1回改善指示書1-35追記） |
| tests/test-portable-commands.sh | docs/skills 配下の .sh に系に依存する書き方（stat の直書き）が入っていないことを確かめる（第1回改善指示書1-39） |

## §2 処理の定義

共有部品のため単一の手順は持たない。各スクリプトの使い方は先頭コメントに置く。

- read-run.sh: `read-run.sh <実行フォルダ> <キー>` で実行フォルダのrun.jsonから値を1行読む
- check-entry.sh: `check-entry.sh <実行フォルダ> <対象リポジトリのルート>` で対象範囲の承認.mdの可否と調査と検出条件の定義書の同一性を確かめる
- unit-dir-name.sh: `unit-dir-name.sh <表示名>` で表示名をフォルダ名に使える形へ置換する。入力は識別子ではなく表示名（工程2-1の完了時の処理「業務名の確定」が組んだ値）。呼び出し元は工程2-1の業務名の確定だけである。record-acceptance.sh・check-acceptance-record.shは現状、識別子からunit-dir-name.shを直接呼んでいる。extract-code-readings.shも同様である。list-units-of.shの5列目（フォルダ名）から得るよう変える対象である
- design-doc-name.sh: `design-doc-name.sh <種別> <basic|test>` で種別ごとの基本設計書・単体テスト設計書のファイル名（実名）を返す
- units-status.sh: `units-status.sh <実行フォルダ> set|get|list <残りの引数>` で工程の状態を読み書きする
- list-units-of.sh: `list-units-of.sh <対象リポジトリのルート> <種別> [--lists <場所>]` で一覧の元データをタブ区切りで返す。列は識別子・表示名・場所・属するファイルの既存4列の順を変えず、5列目にフォルダ名を持つ5列である。位置で読む呼び出し元は3本である。check-basic-design.sh・check-detail-design.shはそのうち2本である。check-detail-design.shの自己テストは4列スタブを持つ。extract-code-readings.shも位置で読む呼び出し元である。いずれも5列目を読むよう変える対象である。check-test-designs.shはlist-units-of.shの出力の行数だけを数え列を読まないため、列が増えても行数は変わらず変更不要である。2列目の表示名は、表示名がnull、または空白（半角・全角の空白・タブ・改行）だけなら業務名、業務名もnull、または空白（半角・全角の空白・タブ・改行）だけなら識別子へ落ちる。一覧を作る処理は新しく現れた単位の表示名・業務名を空文字列で書くため、空白だけの値をnullと同じに扱う（第1回改善指示書1-29）。一覧.mdの表示（list-units.sh）は表示名が空のとき「名前」鍵を使うが、list-units-of.shの2列目は出力先の名前の元になるため識別子へ落とす。「名前」は元データに由来する表示用の値であり、出力先の名前には使わない。表示名・業務名が文字列でない値（数値・真偽値等）のときは、空白判定の前に文字列にする（`false`は文字列`false`として非空扱いになる）。空白とみなす文字は半角・全角の空白・タブ・改行に加え、ゼロ幅空白（U+200B）とBOM（U+FEFF）も含める（第1回改善指示書1-29・再検証）。読み取りの途中でjqの処理そのものが失敗したときも判定不能（終了コード2）で終える
- check-doc-heading-addendum.sh: PreToolUse(Write|Edit)で設計書の書き方の決まりを検査する
- check-unit-test-design-doc-sections.sh: PreToolUse(Write)で単体テスト設計書の決まりを検査する
- tests/test-self-tests.sh の `setup_copy_enumeration_ok`: 写しの7種別フォルダ（`プロジェクト共通/`配下は対象外）を走査する。フォルダ名から決めた自分の種別以外の名前を含む列挙が無いかを確かめる（第1回改善指示書1-20・再検証）
- tests/test-skill-commands.sh: `test-skill-commands.sh [<docs/skills のルート>]` で、`docs/skills/*/SKILL.md`が書くコマンドを抽出し実際に走らせる（第1回改善指示書1-35追記）。抽出はコードフェンス（言語指定が bash・sh・無指定のもの。インデント許容。末尾`\`の行は次行と連結）とインラインの単一バッククォート`` `...` ``の両方を対象にし、`--self-test`を含む行・`[...]`（省略可の記法）の中身・引数を持たない裸の言及は除外する。抽出した各コマンドの`<...>`引数は、位置ごとの新規ダミーディレクトリへ機械的に置き換える。ただし種別・識別子・sha256・キー・観点値・単位のカンマ区切り・docs/rules又はdocs/skillsのルートは、使い方の誤りとして誤判定される実測（本テスト作成時に検出）を避けるため個別の値へ置き換える（詳細は本スクリプトの先頭コメント「標本」節）。判定は標準出力・標準エラーへの「使い方:」「usage」「引数不正」の出現、または「要確認-判定不能」の出現かつ終了コードが0でないことのいずれかを引数不足として数える
- tests/test-portable-commands.sh: `test-portable-commands.sh [<走査するルート> ...]` で、引数が無ければ `docs/skills` と `docs/rules` の両方（体系の設計が対象と定める範囲）を既定のルートとして走査し、配下の全ての `.sh` に、ファイルの更新時刻を取る書き方が系に依存していないことを確かめる（第1回改善指示書1-39・反証の指摘で走査根を拡張）。検出の対象は `stat` の直後に空白を挟んでオプション `-f` または `-c` が続く形であり、オプションの後ろの空白は条件に入れない（空白を詰めた `stat -f%m` も実測で有効に動くため）。`mtime_of()` を含む行、および `stat-sim-fixture` の印を含む行（自己テストが作る偽の `stat` 実装の内部呼び出し）は除く。対照は5件で、更新時刻の直書き・片方の系にしか無い書き方・空白を詰めた直書きの3種を検出すること、ヘルパー経由の呼び出しを検出しないこと、ヘルパーの定義行を検出しないことを確かめる。走査が0件のときは抽出の仕組みが壊れたとみなして不合格とする
- tests/test-self-tests.sh の工程の担当と出力の対応: 流れの設計の全工程を対象にする（第1回改善指示書1-47）。担当欄にバッククォートで囲んだ機能名があればその機能の実在と outputs の対応を見る。機能名が無い工程は素通りさせず、担当欄に「機能を持たない」の明示があるときだけ通す。担当欄そのものが無い工程は、ブロックに「欠番」の語があるときだけ通す。明示が無ければ不合格にする。あわせて検査した工程の件数（担当欄を持つ工程の件数）を出力し、流れの設計の担当欄の総数（文書冒頭の観点の説明表にある凡例行を除き、工程の見出し以降に限って数える）と一致することを確かめる。件数の出力および個別の不合格メッセージは PASS/FAIL の形にしない。設計文書との整合の検査が末尾の件数分の PASS/FAIL 形式の行を独自ケースとして照合するためである。担当欄の整理そのものは本検査の範囲外である。


## §3 ロジック

### list-units.sh（一覧を作る機能が使う検出条件の実行）に対する共有部品としての位置づけ

reverse-sharedは検出条件の実行そのものは持たず、実行後の元データの読み取り（list-units-of.sh）とフォルダ名の作成（unit-dir-name.sh）を共有する。検出条件の実行はreverse-listing-unitsのlist-units.shが担い、責務を分離している。unit-dir-name.shは一覧化（工程2-1）の完了時の処理「業務名の確定」がフォルダ名を確定するときに1回だけ呼び、確定した値を一覧の元データjsonの「フォルダ名」フィールドへ書く。後続の各機能はunit-dir-name.shを再度呼ばず、list-units-of.shの出力からこの値を得る。位置で読む3本の呼び出し元は、list-units-of.shの5列目（フォルダ名）を読むよう変える対象である。check-test-designs.shは行数だけを数え列を読まないため対象外である。

unit-dir-name.shの置換対象は、実装の変更対象を含む。現行実装は`/`空白`{``}``:``\``?``*`の8種のみを置換する。本書が示す置換対象（`"``<``>``\|`を加えた12種）はこの拡張を前提にしており、スクリプトと自己テストを同時に直す必要がある。reverse-listing-unitsの単体テスト設計書のケース「一覧-フォルダ名-一意」は、業務名に`\|`を含む例を前提にしており、この拡張が済んでいることを前提にする。

unit-dir-name.shの入力も、実装の変更対象を含む。現行実装は識別子を入力に受け取る。本書が示す入力（表示名）はこの変更を前提にしており、置換対象の拡張と同時にスクリプトと自己テストを直す必要がある。

### check-doc-heading-addendum.sh / check-unit-test-design-doc-sections.sh の写し方式

**必要性**: 設計書の書き方の決まり・単体テスト設計書の決まりはsetup単位の規約が持つ原本であり、reverse単位のPreToolUse(Write|Edit)から同じ検査を使うために写しを置く。原本を直接requireする依存を持たず、テスト（test-self-tests.sh）で写しと原本の一致（改行・通知行を除く）を確かめることで、ずれを検出可能にする。

**代替案を採用しなかった理由**:
- setup側の原本を直接読み込む: reverse単位とsetup単位の間に実行時依存が生まれ、「柱ごとに取り出して動く」という制約に反する
- 写しを持たずreverse独自に再実装する: 同じ検査を2箇所で保守することになり、ずれが機械で検出できない

**保守責任者**: 人手（ユーザー）。原本（setup-scaffolding-rulesのtemplates配下）を変えるときは写しも同時に直し、test-self-tests.shで一致を確かめる。

**廃棄条件**: 写し方式を別の共有機構（symlink等）に置き換えた時。

### check-doc-heading-addendum.sh の判定の決定性

**必要性**: `missing_sections`と見出し・省略記載の検査が、本文を変数からパイプで`grep -q`・`head`へ渡していた。pipefailの下でこれらが早期終了すると書き込み側がSIGPIPEで失敗し、パイプ全体が失敗して判定が揺れる不具合があった（第1回改善指示書1-19）。同じ入力を200回渡すと約1.5%の割合で見出しが実在するのに欠落と誤判定した。本文を一時ファイルへ1回書き出し、`grep`にファイルを直接読ませる形に変え、早期終了する消費者への依存を断つ。

**代替案を採用しなかった理由**:
- `grep -q`を`grep -c`等の非早期終了の形へ変えるだけにする: `head -1`を伴う箇所（見出し・省略記載の一致行の取得）まで直せず、同じ形の揺れが残る
- ヒアストリングへ変える: `missing_sections`は本文を複数回走査する。毎回生成し直すより、一時ファイルへ1回書き出す方式のほうが単純である

**保守責任者**: 人手（ユーザー）。本文を扱う検査を新設・変更するときは、変数からパイプで早期終了する消費者へ渡さない（本節を参照する）。

**廃棄条件**: なし（判定の決定性は`docs/design/common/リバースの流れの設計.md`が定める恒久の条件のため）。

### design-doc-name.sh の共有部品化

**必要性**: 種別ごとの基本設計書・単体テスト設計書のファイル名（旧様式の実名）は、check-basic-design.sh（様式の検査）とrecord-acceptance.sh（合格の記録）の双方が使う。かつては両スクリプトがそれぞれ独自にこの名前を決め打ちしており、二重定義がずれて合格の記録の文書名が種別ごとの実名と揃わず、記録の同一性の値が空になる不具合（第1回改善指示書1-12）を起こした。名前の決め方を1箇所へ集め、unit-dir-name.shと同じ形の共有部品として持つ必要がある。

**代替案を採用しなかった理由**:
- 現状維持（各スクリプトが個別に決め打ちする）: 名前を変えるときに一方だけ直す事故が再発する。実際に起きた不具合の直接原因である
- check-basic-design.shの関数をrecord-acceptance.shが直接requireする: reverse-writing-basic-designとreverse-sharedの間に実行時依存が生まれ、「柱ごとに取り出して動く」という制約に反する

**保守責任者**: 人手（ユーザー）。文書名を変えるときは、本スクリプトと自己テストと、本スクリプトを使う各機能（check-basic-design.sh・record-acceptance.sh）を同時に確かめる。

**廃棄条件**: 種別ごとの文書名の決め方を構造化データから生成する仕組みに置き換えた時。

### record-acceptance.sh の判定した時点と記録を書く時点の同一性の突き合わせ

**必要性**: 合格の記録は理由の文と同一性の値（sha256）を持つが、同一性の値は記録を書く時点で計算していた。判定した時点と記録を書く時点がずれると、理由は古い状態を述べたまま同一性の値だけが新しい状態を指す。照合（check-acceptance-record.sh）は同一性の値しか見ないため、この食い違いを検出できなかった（第1回改善指示書1-23、実測78件中33件）。`--judged`で判定した時点の値を必須で受け取り、記録を書く直前に現在値と突き合わせ、1件でも食い違えば記録を作らない。

**代替案を採用しなかった理由**:
- 判定した時点と記録を書いた時点の値を両方とも記録へ残し、照合側で検出する: 記録の鍵が増え、check-acceptance-record.shも同時に直す必要がある。食い違った記録を作らせない方が単純
- 理由を自由記述から構造化する: 理由の文をAIが書く自由度を失い、影響範囲が広い

**保守責任者**: 人手（ユーザー）。判定した時点の値の取得手段（`shasum -a 256`）を変える場合はSKILL.mdの手順も同時に直す。

**廃棄条件**: 判定と記録が単一のスクリプト呼び出しに統合され、時点のずれが構造上起きなくなった時。

### record-acceptance.sh はテスト設計書を出力しない構成で単体テスト設計書を記録に含めない

**必要性**: 統括の前提確認は「テスト設計書の出力」を持ち、既定は「出力しない」である（`docs/design/common/リバースの流れの設計.md`）。この設定のとき単体テスト設計書は作られない。record-acceptance.shの`record_unit()`は設定に関わらず単体テスト設計書のsha256を記録の`文書`へ含めていたため、check-acceptance-record.shの照合が単体テスト設計書の不在を同一性-不一致として拒み、既定の構成で全単位が不合格になっていた（第1回改善指示書1-31）。check-basic-design.shが既に持つ`tests_output_of()`と同じ判定をrecord-acceptance.shへ足し、「出力する」のときだけ単体テスト設計書を`文書`へ含める。

**代替案を採用しなかった理由**:
- check-acceptance-record.sh側で空文字列のsha256を無視する: 照合側の契約（`文書`のキーはすべて現在のファイルと一致する）を緩め、本当に消えた文書を見逃す
- 記録に含めたうえで観点で「単体テスト設計書の実在」を対象外にする: 観点は合否の判定であり、`文書`キーの実在検査とは別の仕組みである。二重の判定になり複雑さが増す

**保守責任者**: 人手（ユーザー）。「テスト設計書の出力」の判定を変えるときは、本スクリプトとcheck-basic-design.shの`tests_output_of()`を同時に直す。

**廃棄条件**: テスト設計書の出力可否の判定を構造化データから生成する仕組みに置き換えた時。

**2026-09-07の変更（反証対応）**: 上記の初版は、設定が読めないとき（`READ_RUN_SH`が無い・read-run.shが失敗する・値が「出力する」以外）を一律「出力しない」とみなしていた。この既定はcheck-basic-design.shの`tests_output_of()`（検査側）と同じ「安全側」のつもりだったが、記録側では逆に働く。「出力する」構成の単位で`--run`の実行フォルダを取り違える・run.jsonが壊れる等の誤りが起きると、黙って「出力しない」へ落ちて単体テスト設計書のsha256が記録の`文書`から抜ける。check-acceptance-record.shの照合は記録済みの`文書`のキーしか走査しない作りのため、以後その単体テスト設計書を消しても記録と一致し続け合格し続ける（fail-open）。`--run`は使い方の誤り検査で常に必須のため、`--run`が指定されているのに設定を読めない・値が不正なときは、記録を作らず判定不能（終了コード2、検査キー`設定-判定不能`）で止める。この区別により、記録側は検査の対象を減らす向きに安全側へ倒すことができない（見えなくなった単体テスト設計書を記録から静かに落とすのではなく、判定不能として人の目に止める）。関数名も検査側の`tests_output_of()`とは既定が異なるため`tests_output_setting_of()`へ改め、複製ではないことを明示する（同一性の検査は不要）。

**2026-09-07の変更（反証対応・第2版）**: 初版は「`--run`が指定されていない（呼び出し元の使い方の誤りで空文字列が渡った）ときだけ「出力しない」とみなす」という防御的な分岐を残していたが、`--run`は使い方の誤り検査（usage_error）で常に必須のため、この分岐は実行時に到達しない死んだコードだった。分岐を削り、`tests_output_setting_of()`は`--run`を常に読み、読めなければ判定不能で止める形に統一した。あわせて、この判定は単位の記録（`record_unit`）だけが行う。共通設計文書の記録（`record_common`）は単体テスト設計書を持たないため、この設定を読まない（元から呼んでいない。§1・§5・§6の説明がこの区別を明示していなかったため補った）。加えて、`read-run.sh`のstderrを一律`2>/dev/null`で捨てていたため判定不能の理由が分からなかった問題を直し、`設定-判定不能`のメッセージへ`read-run.sh`の検査キー（`run-不在`／`jq-不在`／`run-形式`／`キー-不在`）を添えるようにした。

**2026-09-07の変更（反証対応・第3版）**: 第2版は共通設計文書の記録（`record_common`）が単体テスト設計書を持たない点をもって`--run`の実行フォルダの実在自体も確かめない作りだったため、単位の記録と非対称だった。`--run`に不在のフォルダを渡しても記録が書け、`判定した実行`欄が実行フォルダの`basename`へ黙って落ちるだけで判定不能にならなかった（第3版反証対応・所見1）。`record_common()`へ`common_run_dir_readable()`を足し、`--run`のrun.jsonの実在・可読性（read-run.shへ「実行の識別子」を渡した戻り値の可否で判定する。値そのものは捨てる）だけを確かめ、読めなければ`設定-判定不能`で記録を作らない。テスト設計書の出力の値は引き続き読まない。あわせて、`tests_output_setting_of()`が`$(...)`で値を受け取る際に末尾の改行をすべて取り除いてしまい、run.jsonの値そのものに末尾の改行や空白（例: `"出力する\n"`）が混ざっていても正規値「出力する」と誤って一致していた不具合を直した（第3版反証対応・所見2）。改行で終わらない番人文字列と終了コードを値の直後に続けて1回の`$(...)`で受け取ることで、コマンド置換による末尾の取り除きを起こさせず、read-run.sh自身が付ける終端改行1つだけを明示的に取り除いたうえで完全一致で判定する。これにより、値の末尾に余計な空白・改行が付く場合はすべて`値-不正`として拒む。

**2026-09-07の変更（反証対応・第4版）**: 第3版は`common_run_dir_readable()`を共通設計文書の記録専用として足したが、単位の記録（`record_unit`）はこの検査を呼ばず、「テスト設計書の出力」キーの読み取り可否しか確かめていなかった。このため、run.jsonに「実行の識別子」キーが無い（または壊れている）状態でも、単位の記録は「テスト設計書の出力」さえ読めれば記録を書けてしまう非対称が残っていた（第4版反証対応・所見1）。`common_run_dir_readable()`を`run_dir_readable_or_reason()`へ改名し、`record_unit()`と`record_common()`の両方から呼ぶ形へ一本化した。単位の記録は「実行の識別子」キーの実在をまず確かめ、そのうえで従来どおり「テスト設計書の出力」の設定を読む。あわせて、`execution_id_of()`が「実行の識別子」キーを読めないときに実行フォルダの`basename`へ黙って落ちる既定を廃止した。呼び出し元は本関数を呼ぶ前に必ず`run_dir_readable_or_reason()`でキーの実在を確かめているため、`execution_id_of()`はキーの値をそのまま使う（読めない場合の判定不能は呼び出し元が既に検査済みであり、`execution_id_of()`自身に安全側の既定を持たせる必要が無い）。加えて、`tests_output_setting_of()`・`run_dir_readable_or_reason()`がともに関数内で`trap ... RETURN`を使い一時ファイルを掃除していたが、この`trap`は呼び出し元（自己テストの`self_test()`が持つ`trap 'rm -rf "$base"' RETURN`）を上書きしうる潜在欠陥だったため（`RETURN` trapは関数を抜けるたびに実行され、ネストした関数呼び出しの内側で新しい`trap`を設定すると外側の`trap`がその時点で失われる）、両関数とも`trap`を使わず成功・失敗いずれの経路でも読み取り直後に明示的な`rm -f`で後始末する形へ改めた（第4版反証対応・所見2）。

**2026-09-07の変更（反証対応・第5版）**: 第4版は`run_dir_readable_or_reason()`が「実行の識別子」キーの実在だけを確かめ、値そのものの妥当性を問わなかった（所見1）。キーが実在してもnull・空文字列・数値であれば`execution_id_of()`はその値をそのまま使い、記録の`判定した実行`欄に`"null"`／`""`／`"123"`のような値が書かれてしまう。`read-run.sh`を変えずに`run_dir_readable_or_reason()`側で`jq -e '.["実行の識別子"] | type == "string" and length > 0'`を追加し、満たさなければ理由`識別子-不正`で判定不能にする。あわせて、run.jsonがファイルとして実在してもOSの権限で読めないとき、`read-run.sh`の`jq -e . "$run_json"`はファイルを開けずJSON形式不正（`run-形式`）と読み取り不可を区別できなかった（所見2）。`run_dir_readable_or_reason()`が`read-run.sh`を呼ぶ前にファイルの読み取り権限を確かめ、実在するが読めないときは理由`run-不可読`として区別する（ファイルが実在しない場合はこの判定を素通りし、従来どおり`read-run.sh`の`run-不在`に委ねる）。

**2026-09-07の変更（反証対応・第6版）**: 第5版の識別子の値検査`type == "string" and length > 0`は、半角空白・全角空白だけの文字列（`"   "`）を「空でない文字列」として通してしまい、記録の`判定した実行`欄に空白だけの値が書かれる（所見1）。`gsub("[\\s　]";"")`で半角空白類・全角空白を取り除いたうえで`length > 0`を確かめる形に改め、空白を除いて空でない文字列だけを許す。あわせて、`--run`に渡す実行フォルダ自体に実行権限が無い（`chmod 600`等で中に入れない）ときは、run.jsonが実在していても`[ -e "$run_json" ]`がOS側で偽を返すため、既存の読み取り権限確認を素通りしてread-run.shの呼び出しへ進み、run.jsonが見つからず`run-不在`という誤った理由になる（所見2）。`run_dir_readable_or_reason()`の先頭で実行フォルダの実在（`[ -d ]`）と実行権限（`[ -x ]`）を確かめ、フォルダは実在するが入れないときも理由`run-不可読`とする（フォルダ自体が実在しない場合はこの判定を素通りし、従来どおり`read-run.sh`の`run-不在`に委ねる）。

### record-acceptance.sh の単位の状態のファイルへの書き込み抑止

**必要性**: `record_unit()`は記録の書き込み後、単位の状態のファイル（`${run_dir}/logs/units-status.json`）へ`完了判定`を無条件で書き込んでいた。書き出し先は`--run`で渡した実行フォルダ一本で決まり、記録そのものの出力先（`--design-root`）を退避しても止まらない。そのため、検証目的で判定内容を試しに走らせただけでも実データの実行フォルダの状態が書き換わり、副作用なしに試せなかった（第1回改善指示書1-38）。`record_unit()`に第10引数`no_status`（既定0）を足し、1のとき単位の状態のファイルへの書き込みを行わない。`run_main()`は`--no-units-status`を受けて`no_status`を1にし、`record_unit()`へそのまま渡す。共通設計文書の記録（`record_common()`）は元々この状態のファイルを触らないため、`no_status`を持たず対象外のままとする。

**代替案を採用しなかった理由**:
- `--design-root`の指定に連動させる案: `record-acceptance.sh`は`--out`という概念自体を持たず、`--design-root`は記録・基本設計書・単体テスト設計書・共通設計文書の読み書き先を決める値であって、単位の状態のファイルの書き込み可否とは意味が異なる。既存の意味に別の意味を重ねると、`--design-root`だけを見て状態のファイルの挙動を判断できなくなる
- `record_common()`にも同じ引数を足す案: 共通設計文書の記録は元から`UNITS_STATUS_SH`を呼ばないため、引数を増やしても効果を持たない死んだ引数になる

**保守責任者**: 人手（ユーザー）。単位の状態のファイルへの書き込み経路を増やすときは、新しい経路にも同じ抑止の指定を通す。

**廃棄条件**: 単位の状態のファイルの書き込みを、記録の出力先とは独立の設定（実行フォルダ単位のモード切り替え等）へ統合した時。

### tests/test-skill-commands.sh の抽出・置き換え・判定の規則

**必要性**: 第1回改善指示書1-35追記は「全機能のSKILL.mdが書くコマンドを、実際に走らせて確かめること」を求めた。静的な照合（使い方の記述と引数の数の突き合わせ）だけでは、`--run`のような省略可に見える引数が実は必須で、省略すると`要確認-判定不能`として静かに不合格になる型の不具合を見つけられない（実例: check-common-detail-design.shの手順6コマンドに`--run`が無かった1-35原文の指摘）。全機能のSKILL.md（本検査作成時点で14本）から抽出したコマンド（コードフェンス20ブロック（うちコマンドを産むのは15）・インライン多数、計66件（本検査作成時点））を実際に`/bin/bash`で走らせ、使い方の誤り・要確認-判定不能・異常終了（終了コード128以上）のいずれにも該当しないことを繰り返し確かめる検査が必要であり、これは複数段の抽出・置き換え・判定ロジックを持つためスクリプト化以外に実現できない。

**抽出規則**: コードフェンス（言語指定が bash・sh・無指定の ```` ``` ```` から次の ```` ``` ```` まで。言語指定なしのフェンスに置かれたコマンド（reverse-orchestrating-flow の SKILL.md にある check-acceptance-record.sh の例）を落とさないため。インデント許容。末尾`\`の行は次行と連結して1コマンドにする）とインラインの単一バッククォート（`` `...` ``）の両方を対象にする。`--self-test`を含む行は除外する。`[...]`（省略可の記法）は削除する。`scripts/*.sh`を含み、かつスクリプトパスの後に引数を1つ以上持つ行だけを残す（`bash`前置は任意。裸の言及・引数無しの言及は除外する）。言語指定が bash・sh・無指定以外のフェンス（yaml・json・diff 等）も開始と終了は追跡し、内容は捨てる。追跡しないと、その閉じ行を無指定の開始と誤認し、以降の地の文をフェンスの内容として抽出する（反証で実測）。

**置き換え規則**: `<...>`引数は位置ごとに新規のダミーディレクトリへ機械的に置き換えるのが既定である。ただし次の実測（本テスト作成時に発見）により個別の値へ置き換える。
- `<種別>`をダミーディレクトリのパス（英数字とスラッシュを含む文字列）で置き換えると、`extract-code-readings.sh`の`is_valid_kind()`（`screen|api|table|batch|report|external|feature`の英語keyだけを許す）に一致せず、使い方の誤りとして誤判定される。`unit-kinds.json`のkeyの1つ`screen`へ置き換える
- `<docs/rules のルート>`をダミーの空ディレクトリで置き換えると、`validate-rule-definitions.sh`が「渡されたディレクトリの直下にparent.ymlを持たないフォルダがあります」という構造チェックの案内文（「使い方:」を含む）を出し、使い方の誤りとして誤判定される。このリポジトリ自身の実物（`${SKILLS_ROOT}/../rules`。docs/rules を指す）を渡す。`<docs/skills のルート>`も同様に`$SKILLS_ROOT`実物を渡す。いずれも読み取り専用にしか使われないスクリプト、または`build-derived-*.sh`の書き込み先（ダミーディレクトリのまま）とは別の引数であるため、実物を渡しても対象リポジトリ自身を書き換えない
- `<...>`内に空白を含む引数（`<docs/rules のルート>`等）は、空白区切りのトークン化で2トークンに分断されると1トークンとして復元できない。トークン化の前に`_SP_`マーカーへ置き換えて1トークンへ束ねる
置き換えるのは語の全体が`<...>`の引数だけである。語の途中に`<...>`を含む形（`--out <写し>/docs/design/lists`・`--judged <文書名>=<sha256>;...`の2件）は置き換えず字面のまま渡す。いずれも引数の不足ではなく値の形の例示であり、走らせても引数不足には当たらない。

**判定規則**: 標準出力・標準エラーに「使い方:」「usage」「引数不正」（check-business-names.sh・propose 系が使う `check-引数不正:`・`propose-引数不正:` の語彙）「余分な引数」のいずれかを含む場合、あるいは「要確認-判定不能」を含みかつ終了コードが0でない場合を引数不足として数える。「判定不能」という語だけの広い一致にはしない。このリポジトリは`record-acceptance.sh`・`check-acceptance-record.sh`の語彙として「設定-判定不能」（run.json不在・形式不正等の設定不備）を持ち、これは他の「-不在」系の早期終了と同種の正当な既定挙動である（`record-acceptance.sh`の自己テストが多数のケースで意図的に確かめている）。広く一致させると、標本のrun.json未整備という本テスト側の限界をドキュメント側の不具合として誤検出することを実測で確認した。「要確認-判定不能」に絞ることで、1-35追記が示した実例（要確認の観点を持つ合格の記録が`--run`無しで誤って不合格になる型）だけを対象にする。加えて、終了コードが128以上、または127のときは落ちたものとして「異常終了」に数える。127は依存するコマンドが無いときの終了コードである。1-35追記の完了条件「落ちるものが0件」を引数不足の2型だけに読み替えないためである（実例: `validate-rule-definitions.sh` が本文の長い規約で `set -o pipefail` 下の `printf | grep -q` により SIGPIPE を受け rc=141。setup-deriving-rules 側を最後まで読み切る awk に直した）。
合格は既定値である。3つの型のいずれにも当たらない終了はすべて合格に分類する。実測では抽出した66件のうち52件が非0終了であり、その大半は標本の文書・一覧が無いことによる正当な早期終了である。

**陽性・陰性対照**: 対照は6件である。このうち判定に関わる4件は判定の関数`judge_of`を通す。使い方の誤り・要確認-判定不能・異常終了・判定の細目の4件である。配管の対照は`run_and_judge`を通し、標準エラーの捕捉と終了コードの取得を確かめる。本番の走査も同じ`run_and_judge`を通す。配管を1箇所へ集めることで、対照が本番の経路を守る。残る1件（他言語のフェンスの除外）は抽出の対照であり、判定を通さず抽出の結果だけを比べる。対照が各自で判定を再実装すると、判定の条件を壊しても対照が落ちない（反証で実測）。使い方の誤りは`check-entry.sh`へ引数不足で渡し、判定が「不合格-使い方」を返すことで確かめる。要確認-判定不能は`record-acceptance.sh`の自己テストの「要確認を含む合格の記録」ケースと同じ手順で標本を作る。`check-acceptance-record.sh`を`--run`ありで呼ぶと判定が「合格」を、`--run`無しで呼ぶと「不合格-判定不能」を返すことで確かめる。異常終了は`kill -PIPE`で終了コード141を作り判定が「不合格-異常終了」を返すこと、および3型に当たらない非0終了を「合格」に分類することの対で確かめる。この陰性側は既定の分類へ落ちる経路を確かめるものであり、しきい値の退行は終了コード126と128の対が受け持ち、依存するコマンドが無いときの127も異常終了に数える。他言語のフェンスの除外は、yaml のフェンスと地の文と bash のフェンスを持つ合成の手順書から bash のフェンスの1件だけが抽出されることで確かめる。判定の細目の対照は、条件の下位の要素をそれぞれ確かめる。語彙4つ（`使い方:`・`usage`・`引数不正`・`余分な引数`）と、大文字と小文字を区別しないこと、要確認-判定不能が終了コード0のときは合格に落ちることの6点である。条件を丸ごと壊す変異だけでは、これらの要素の退行を検知できない（反証で実測）。配管の対照は、判定の手前で出力と終了コードを取り違える退行を捕える。配管を複製したままでは、対照が本番の通らない側を守ることになる（レビューで実測）。対照が判定の関数だけを見ていると、出力の捕捉から標準エラーを外す変異や終了コードを取り落とす変異を検知できない（反証で実測）。引数を1つずつ別の語として渡すことも確かめる。

**代替案を採用しなかった理由**:
- Bashツール直叩き: 抽出したコマンド（本検査作成時点で66件）を機能追加・変更のたびに手動で走らせ直すのは非現実的であり、確認の省略・見落としを繰り返す
- 引数の数だけを見る静的照合（1-35の対応で既にある）の強化で済ませる: 静的照合は「引数が足りているか」しか見られず、「`--run`はあるが値が実質機能しない」型の不具合（1-35追記が示した型）を検出できない。実際に走らせる検査でなければ埋められない
- 既存Makefileターゲット拡張・package.json scripts追加: このリポジトリはどちらも持たず、新規導入は本検査専用の依存を増やすだけになる

**保守責任者**: 人手（ユーザー）。SKILL.mdのコマンド記法（コードフェンス・インラインバッククォート）を変える場合、または新しい種類の引数不足の型（本節にある使い方の誤り・要確認-判定不能・異常終了の3種以外）を見つけた場合は、抽出規則・置き換え表・判定式を`tests/test-skill-commands.sh`と本節を同時に更新する。

**廃棄条件**: SKILL.mdのコマンド記法を構造化データ（JSON等）に変えた時。

**既知の限界**: `check-skill-design-docs.sh` は `tests/test-self-tests.sh` の出力の末尾から件数分（本検査作成時点で25行）の `[PASS]`/`PASS:` 行を独自ケースとして照合する。この窓の先頭12行は子スクリプト（`check-acceptance-record.sh --self-test`）の出力が漏れ込んだものである。単体テスト設計書 §2 でこれに当たる行は、キーの末尾に `-tail-` を持つ12行である。本検査の対照の印を `[PASS 対照]` にしたのは、この窓へ混入して既存の照合を崩さないためであり、対照の結果は §2 の行「手順書-コマンド-引数不足なし」の期待結果に含めて扱う。窓の脆さ自体は main 由来の構造であり、本検査では直さない。抽出の対象はバッククォート3つのフェンスとインラインの単一バッククォートに限る。チルダ3つ（`~~~`）のフェンスと、4スペース以上のインデントだけで示すコードブロックは抽出しない（反証で実測。本検査作成時点の SKILL.md（14本）にはどちらも無い）。SKILL.md にこれらの記法を使う場合は本検査の抽出規則を同時に広げる。本検査は最上位のプロセスの終了コードだけを見る。パイプの前段がシグナルで死んでも、`set -o pipefail` を持たないスクリプトでは終了コードが後段の値になるため、合格に分類される。今回直した SIGPIPE と同じ型の欠陥が他のスクリプトに残っていても、本検査は合格を返す。省略可の記法 `[...]` は中身ごと削除するため、省略可の引数は一度も渡されない。本検査作成時点では5箇所が該当する。内訳は `--until` が1、`--reason` が2、`--taxonomy` が1、`--unit` が1である。対象は引数を1つ以上持つ言及に限るため、引数を持たない裸の言及は走らせない。本検査作成時点の裸の言及は19件（重複を除くと17本）である。そのうち引数付きの言及を他所に持たず一度も走らないのは2本である。抽出66件のうち終了コード0は14件であり、残りは標本の文書・一覧が無いことによる早期終了である。本来のロジックの奥までは到達しない。抽出が0件のときは不合格とする。抽出の仕組みが壊れたまま合格を返すことを防ぐ。`set -u` の未定義の参照で落ちた実行は合格に分類する。終了コードが1になり、正当な早期終了と見分けられないためである。

### tests/test-portable-commands.sh の検出の規則

**必要性**: 更新時刻を取る書き方が系に依存する欠陥が繰り返し持ち込まれた。1-38 の直しで2箇所、1-37 の直しで4箇所である。実測で16箇所に達した。人が目視で防ぐ運用では同じ欠陥が入り続けることが2度の実例で示されている。走査を機械で繰り返す検査が要る。

**検出の規則**: `stat` の直後に空白を挟んでオプション `-f` または `-c` が続く形を1件でも含む行を不合格とする。オプションの後ろの空白は条件に入れない。入れると `stat -f%m` のように空白を詰めた形を見逃すためである。この形は実測で有効に動く（判定役の指摘で 2026-09-08 に修正）。`mtime_of()` を含む行は除く。ヘルパーの定義そのものは両方の系のオプションを書く必要があるためである。

**対照**: 5件を持つ。更新時刻の直書きを検出すること、片方の系にしか無い書き方を検出すること、空白を詰めた直書きを検出すること、ヘルパー経由の呼び出しを検出しないこと、ヘルパーの定義行を検出しないことである。対照の文字列は `printf` の書式から組み立てる。この検査自身も走査の対象に含まれるため、検出したい文字列をそのまま書くと自分自身を不合格にするからである。

**代替案を採用しなかった理由**: 更新時刻を見るのをやめる案は、状態のファイルが書き換わらないという保証を中身の一致だけに縮める。中身が同じでも書き込みが起きていれば検知すべきであり、この保証は独立に要る。系ごとの分岐を呼び出しのたびに書き下す案は、同じ分岐が16箇所へ散り、次に足す呼び出しで再び忘れられる。

**保守責任者**: 人手（ユーザー）。系に依存する新しい書き方（`stat` 以外の命令）を見つけた場合は、検出の規則と対照を本節と `tests/test-portable-commands.sh` へ同時に足す。

**廃棄条件**: 配布物の実行環境が単一の系に固定された時。

**既知の限界**: 走査の対象は引数で渡したルート（既定は `docs/skills` と `docs/rules` の2つ）に限る。生成物の `.claude/skills` は走査しない。定義と生成物の一致は `check-skill-drift.sh` が別に確かめるため、定義が合格すれば生成物も同じ内容になる。検出は行単位の字面であり、`stat` の呼び出しを変数やコマンド置換で組み立てる形は見つけられない。`mtime_of()` を含む行は無条件に除くため、同じ行にヘルパーの定義と別の直書きを並べた形は見逃す。`stat-sim-fixture` を含む行も除く。GNU を模した系のケース（record-acceptance.sh・extract-code-readings.sh の自己テスト内）が偽の `stat` 実装として書くヒアドキュメントの内部に、実際に `/usr/bin/stat -f` を呼ぶ1行が要る。これは本検査が防ごうとする配布物側の直書きではなく、自己テストが実行時に作る使い捨てのシェルスクリプトの内部でしかない。除外しないと自分自身の対策コードを不合格にする自己矛盾が起きるため、専用の印を付けて除外した。更新時刻の値そのものの正しさは、GNU を模した系のケースが実際の更新時刻との一致を見る1点でだけ確かめる。走っている系の3ケースは字種と前後の大小しか見ないため、値を一定の倍率で変換して返す実装は捕まえられない。

### 工程の担当と出力の対応の検査の範囲

**必要性**: 旧実装は担当欄にバッククォートで囲んだ機能名が1つも無い工程を、何も検査せず成功として返していた。実測では工程の中にある担当欄18件のうち名前を持つのは1件だけで、17件が素通りしていた（旧来の集計は文書冒頭の観点の説明表にある凡例行「| 担当 | 誰が行うか |」を含めて19件と数えており、この凡例行は検査の走査が届かない）。この穴のため、工程2-12と工程2-13の担い手の不在が検出されなかった。終了コード0が合格を意味しない状態だった。

**検出の規則**: 機能名が無い工程は、担当欄に「機能を持たない」の語があるときだけ通す。無ければ不合格にする。担当欄そのものが無い工程（工程2-10、欠番）は、ブロックに「欠番」の語があるときだけ通す。無ければ不合格にする。素通りの条件を明示だけに絞ることで、書き忘れと本当に機能を持たない・欠番の工程を区別する。

**件数の出力**: 検査した工程の件数を出し、流れの設計の担当欄の総数と一致することを確かめる。通った件数が分からなければ、同じ型の穴に気付けない。

**代替案を採用しなかった理由**: 担当欄の書き方を変えずに検査だけを広げる案は、機能名の取り出しが担当欄の自由な文面に依存し続けるため、次の書き方で再び素通りする。工程の定義の側で機能名をバッククォートで囲むことを必須にし、持たない工程は明示させる形にした。

**独立ケースとしての登録**: 網羅の判定（`process_all_covered_ok`）と件数一致の判定（`process_count_matches_ok`）を、既存の統合判定（`process_function_mapping_ok`）と同じ `run_case` に相乗りさせず、それぞれ独立した名前で登録した。統合判定1件だけでは、単体テスト設計書 §2 に追加した2行のケース名が自己テストの出力へ現れず、設計先行の検査（取り込み後の出力に新しく現れるケース名との一致を求める）を満たせない。3つの判定は同じ走査（`process_mapping_compute`）の結果をキャッシュして参照するため、走査そのものは1回だけで済む。

**保守責任者**: 人手（ユーザー）。工程を増減する場合は流れの設計の担当欄と本節を同時に更新する。

**既知の限界**: 本検査を全工程へ広げた時点で、工程の中の担当欄18件のうち17件が落ちる。落ちる理由はすべて「担当欄にバッククォートで囲んだ機能名が無い」の1つである。担当欄にバッククォートで囲んだ機能名を持つのは1件（工程2-11）だけであり、担当欄そのものを持たない工程2-10は「欠番」の明示で通る。これは検査の誤りではなく、検出すべき状態が検出された結果である。担当欄へ機能名を書き足すと、出力欄の文言と宣言のファイル名が字面で一致しない工程が新たに落ちることを実測で確かめている。担当欄の整理と出力欄の突合の直しは本検査の範囲外であり、別に起票して扱う。素通りの受け口として「機能を持たない」の明示を用意したが、いまこの語を持つ工程は1件も無い。総数の集計は工程の見出し以降に限る。文書の冒頭の凡例にも担当の行があり、これを数えると実際に検査した件数と一致しない。

## §4 入出力の値

| 入力 | 形 | 出所 |
|---|---|---|
| 実行フォルダのrun.json | JSON | 統括の実行の開始スクリプト |
| 対象範囲の承認.md・調査と検出条件の定義書.md | Markdown | reverse-writing-survey-definitionの出力 |
| 一覧の元データ（<種別>.json） | JSON | reverse-listing-unitsの出力 |
| 書き込み対象の文書本文 | Markdown | Write/Editのtool_input |

| 出力 | 形 | 置き場 |
|---|---|---|
| 実行フォルダの値（対象リポジトリ等） | 標準出力（1行） | 呼び出し元へ返す |
| 範囲の承認の判定 | 終了コード | 呼び出し元へ返す |
| 一覧の元データの1行1件（タブ区切り） | 標準出力 | 呼び出し元へ返す |
| 単位のフォルダ名 | 標準出力（1行） | 呼び出し元へ返す |
| 単位ごとの工程の状態 | JSON | <実行フォルダ>/logs/units-status.json |
| linterの許可・拒否・通知・対象外の判定 | 終了コード・メッセージ | 呼び出し元（PreToolUse）へ返す |

## §5 エラー処理

check-acceptance-record.shは、要確認の観点を持つ記録の照合に`--run`を必須とする。`--run`が無いときは要確認-判定不能として不合格にする。確認事項の記録.mdを参照できないときも同様に不合格にする（2026-09-06改善、後方互換より正しさを取る）。record-acceptance.shも同じ区別を持つ。記録を作れない理由（`設定-判定不能`）には、`read-run.sh`が返す検査キー（`run-不在`／`jq-不在`／`run-形式`／`キー-不在`）、または`record-acceptance.sh`自身が返す理由（`識別子-不正`／`run-不可読`／`値-不正`／`共有部品-不在`／`一時領域-不可`／`理由不明`）を添える（2026-09-07反証対応・第5版）。単位の記録（`record_unit`）・共通設計文書の記録（`record_common`）はいずれも`run_dir_readable_or_reason()`で`--run`の実行フォルダのrun.jsonの実在・可読性・「実行の識別子」キーの実在と値の形（空白を除いて空でない文字列）を確かめる。単位の記録はこれに加えて「テスト設計書の出力」の値まで確かめる（共通設計文書の記録はこの値を読まない）。

| 検査 | スクリプト | 検査キー  |
| --- | --- | --- |
| 範囲の承認の可否と同一性 | scripts/check-entry.sh | 合格・不合格-否・不合格-同一性不一致・判定不能-承認ファイル不在・判定不能-調査と検出条件の定義書ファイル不在 |
| 実行フォルダの値の読み取り | scripts/read-run.sh | 対象リポジトリ・対象プロジェクト名・出力の置き場・実行の識別子・対象のコミット・キー不在・run.json不在・引数不足 |
| 単位のフォルダ名の作成 | scripts/unit-dir-name.sh | 入力は表示名（識別子ではない）。置換の対象は`/` `{` `}` `:` `\` `?` `*` `"` `<` `>` `\|`と空白。先頭の`_`は除く。引数無し |
| 種別ごとの文書名の作成 | scripts/design-doc-name.sh | 種別×basic/testの14通りの実名・不正な種別/区分・引数不足 |
| 工程の状態の読み書き | scripts/units-status.sh | 未設定は未・set後にget一致・別工程は未のまま・list・不正な工程・引数不足 |
| 一覧の元データの読み取り | scripts/list-units-of.sh | タブ区切り出力・一覧不在・不正なJSON・--listsオプション・読み取り失敗（jqの実行時エラー） |
| 追記章と補足章を作らない | scripts/check-doc-heading-addendum.sh | 追記・補足・その他で始まる見出しの検出 |
| 任意記載と省略記載をしない | scripts/check-doc-heading-addendum.sh | 列挙を打ち切る省略記号の検出 |
| 要件定義書は合意した範囲を確定させる | scripts/check-doc-heading-addendum.sh | 対象範囲・優先度・受入条件の3見出しの実在 |
| 基本設計書は詳細設計を書ける状態を作る | scripts/check-doc-heading-addendum.sh | 様式の見出しと単体テスト設計書の同居フォルダの実在 |
| 記載項目を省かない | scripts/check-unit-test-design-doc-sections.sh | 12節または13節の名前・順序・件数の一致 |
| 単体テスト設計書は基本設計フェーズで作る | scripts/check-unit-test-design-doc-sections.sh | 基本設計フォルダの宣言と単体テスト設計書の実在 |
| 規約が求めるテストを観点へ取り込む | scripts/check-unit-test-design-doc-sections.sh | 検査列に「テスト:」を含む規則名の本文への出現 |
| テスト設計書の節の役割 | scripts/check-unit-test-design-doc-sections.sh | §1・§2・§5・§6のキー集合の整合 |
| 判定が合格以外の記録を不合格に数える | scripts/check-acceptance-record.sh | 判定-不合格（不合格・不明） |
| 観点がJSONのオブジェクトでない記録を不合格に数える | scripts/check-acceptance-record.sh | 観点-形式不正 |
| 観点のキーが空文字列の記録を不合格に数える | scripts/check-acceptance-record.sh | 観点の値-不正（キー空） |
| 要確認の観点があるのに--runが無いか確認事項の記録.mdを参照できないか確かめる | scripts/check-acceptance-record.sh・scripts/record-acceptance.sh | 要確認-判定不能 |
| 要確認の理由に確認事項一覧のキーがあるか確かめる | scripts/record-acceptance.sh | 要確認-キー不在 |
| 観点のキーが空文字列の指定を拒む | scripts/record-acceptance.sh | 観点-キー空 |
| 判定した時点と記録を書く時点の同一性の値を突き合わせる | scripts/record-acceptance.sh | 同一性-判定後変更 |
| 単位の記録（`record_unit`）・共通設計文書の記録（`record_common`）がいずれも`run_dir_readable_or_reason()`で`--run`の実行フォルダのrun.jsonの実在・可読性・「実行の識別子」キーの実在と値の形（空白を除いて空でない文字列）を確かめられなければ記録を作らず止める | scripts/record-acceptance.sh | 設定-判定不能（理由: run-不在／jq-不在／run-形式／キー-不在／識別子-不正／run-不可読／共有部品-不在／一時領域-不可／理由不明） |
| 単位の記録（`record_unit`）が上記に加えてテスト設計書の出力の設定を読めない・値が不正なら記録を作らず止める（共通設計文書の記録はこの値を読まない） | scripts/record-acceptance.sh | 設定-判定不能（理由: 上記に加え値-不正） |

## §6 関連資料

- ../reverse-writing-survey-definition/（read-run.sh・check-doc-heading-addendum.shの利用元）
- ../reverse-listing-units/（check-entry.sh・unit-dir-name.sh・list-units-of.shの利用元）
- ../reverse-writing-basic-design/（check-basic-design.shがdesign-doc-name.sh・unit-dir-name.shの利用元。record-acceptance.shと文書名の定義を共有する）

## 追補: setup統括の畳み込みで加わった構成要素

旧`setup-orchestrating-units`・旧`reverse-checking-basic-phase`の実装は本共有部品へ移した。

| スクリプト | 役割 |
|---|---|
| check-acceptance-record.sh | 合格記録と現在の文書のsha256一致で合否を判定する（判定が合格以外の記録は不合格に数える。保留は廃止） |
| check-basic-phase.sh | 共通設計文書6つの実在と合格を機械検査する |
| design-root.sh | 設計書の置き場キーの有無で設計書ルートを解決する |
| plan-units.sh | 入出力の重なりとrequiresから実行順を組み立てる |
| record-acceptance.sh | 単位・共通設計文書ごとに合格・不合格を記録する（観点の要確認は理由のキー実在を検査し、--judgedで判定した時点の同一性の値と記録時の値を突き合わせる。記録の`文書`は基本設計書を常に含み、単体テスト設計書は統括の前提確認「テスト設計書の出力」が`出力する`のときだけ含める。単位の記録（`record_unit`）・共通設計文書の記録（`record_common`）はいずれも`run_dir_readable_or_reason()`という1つの関数で`--run`の実行フォルダのrun.jsonの実在・可読性・「実行の識別子」キーの実在と値の形（空白を除いて空でない文字列）を確かめ、読めない・値が不正なら判定不能で記録を作らない。run.jsonが実在してもOSの権限で読めないときは`run-不可読`として区別する。単位の記録はこれに加えて「テスト設計書の出力」の設定を読み、値が`出力する`／`出力しない`のどちらでもないときも判定不能で記録を作らない。共通設計文書の記録（`--common`）は単体テスト設計書を持たないため「テスト設計書の出力」の値は読まない。値の一致判定は完全一致で行い、末尾に余計な空白・改行が付いた値は不正として拒む。照合側は`文書`のキーを走査する汎用の作りのため変更しない。第1回改善指示書1-31・2026-09-07反証対応・第6版） |
| start-run.sh | 実行フォルダを作りrun.jsonへ初期値を書く |
