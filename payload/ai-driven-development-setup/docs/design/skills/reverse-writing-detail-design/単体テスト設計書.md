# 詳細設計書を書く（reverse-writing-detail-design）の単体テスト設計書

**この文書の位置づけ**（別の言語で作り直す場合は、まず「仕様」から読む）

- 仕様: §1 テスト観点、§2 テストケース一覧、§3 入力条件、§4 期待結果、§5 異常系、§6 境界値、§7 網羅基準、§8 前提条件と終了条件
- 現行実装: 該当なし

## 本書が検証するもの

reverse-writing-detail-designが持つ各スクリプトの`--self-test`が確かめる入出力・分岐・終了コードを対象とする。

## テスト対象

| スクリプト | 自己テストの実行 | ケース数 |
|---|---|---|
| check-detail-design.sh | `bash scripts/check-detail-design.sh --self-test` | 19 |

## テストの粒度と自動化の方針

| 項目 | 方針 |
|---|---|
| テストの粒度 | 1本のスクリプトを1単位として、`--self-test`が持つ各ケースの入出力・分岐・終了コードを確認する。 |
| 自動化 | `--self-test`引数を渡す自動テストを優先する。 |

## 本書が扱わない範囲

| 対象外の事項 | 対象外とする理由 | 代わりに確認する資料または試験 |
|---|---|---|
| 複数機能をまたぐ結合 | 単位ごとの文書は結合テストを扱わないため | docs/design/skills/reverse-orchestrating-flow/ |

## §1 テスト観点

**この節の位置づけ: 現行実装**

| キー | 関数・メソッド名 | 観点 | 由来する詳細設計書の章 |
|---|---|---|---|
| 合格の判定 | check-detail-design.sh | 節・読み取り結果の網羅が揃った見本を合格とする | 詳細設計書 §2 処理の定義 |
| 型制約表記 | check-detail-design.sh | 表の列ごとの型・制約が§4に書かれていれば合格とする | 詳細設計書 §4 入出力の値 |
| 設計書ルート分離 | check-detail-design.sh | --design-root指定時に設計書を対象側へ書かない | 詳細設計書 §2 処理の定義 |
| 合格記録の入口 | check-detail-design.sh | 単位の合格の記録が無いときに不合格とする | 詳細設計書 §2 処理の定義 |
| 保留の除外 | check-detail-design.sh | 保留の単位を不合格にせず飛ばす | 詳細設計書 §2 処理の定義 |
| 複合不合格 | check-detail-design.sh | 節・未記入・file:line・読み取り結果の未網羅の複数不備を一度に検出する | 詳細設計書 §2 処理の定義 |
| 文書の実在 | check-detail-design.sh | 対応する詳細設計書が無いときに不合格とする | 詳細設計書 §2 処理の定義 |
| 判定不能-前提不在 | check-detail-design.sh | 一覧・検査基盤が無い、種別が不正、使い方を誤ったときに判定不能を返す | 詳細設計書 §2 処理の定義 |

## §2 テストケース一覧

**この節の位置づけ: 現行実装**

| キー | 番号 | 関数・メソッド名 | ケースの名前 | 対応する観点のキー | 区分 | 前提 | 操作 | 期待結果 |
|---|---|---|---|---|---|---|---|---|
| 合格 | 1 | check-detail-design.sh | 節・読み取り結果の網羅が揃った見本で実行する | 合格の判定 | 正常 | テーブル定義書.mdが全節を含み読み取り結果jsonと合格記録が揃っている | --kind tableで実行する | 終了コード0 |
| 型制約表記-合格 | 2 | check-detail-design.sh | 表の列ごとの型・制約を§4に書いて実行する | 型制約表記 | 正常 | 読み取り結果jsonに型・制約を持たせ§4に型・制約の列を書く | --kind tableで実行する | 終了コード0 |
| 設計書ルート分離-合格 | 3 | check-detail-design.sh | --design-rootで設計書を分離して実行する | 設計書ルート分離 | 正常 | 対象ディレクトリはコードのみで設計書は別ルートに置く | --design-rootを指定して実行する | 終了コード0 |
| 設計書ルート分離-対象に書かない | 4 | check-detail-design.sh | 実行後に対象側の副作用を確認する | 設計書ルート分離 | 正常 | 設計書ルート分離-合格を実行済み | 対象ディレクトリにdocsが無いか確認する | 対象側にdocsディレクトリが存在しない |
| 不合格-合格記録なし | 5 | check-detail-design.sh | 合格記録が無い状態で実行する | 合格記録の入口 | 異常 | 合格記録チェックの終了コードを1にする | --kind tableで実行する | 終了コード1 |
| 不合格-合格記録なし-理由 | 6 | check-detail-design.sh | 出力に不在の理由が出るか確認する | 合格記録の入口 | 正常 | 不合格-合格記録なしを実行済み | 標準エラーを確認する | 「合格記録-不在」を含む |
| 保留は不合格にしない | 7 | check-detail-design.sh | 単位を保留として記録して実行する | 保留の除外 | 正常 | 単位の合格記録に判定「保留」を書く | --kind tableで実行する | 終了コード0 |
| 保留-skip表示 | 8 | check-detail-design.sh | 保留の単位がskip表示になるか確認する | 保留の除外 | 正常 | 保留は不合格にしないを実行済み | 標準エラーを確認する | 「保留-対象外」のskip表示を含む |
| 不合格-複合 | 9 | check-detail-design.sh | 複数の不備を持つ見本で実行する | 複合不合格 | 異常 | 必須節を欠き未記入・file:line表記を残す設計書にする | --kind tableで実行する | 終了コード1 |
| 複合-節欠落 | 10 | check-detail-design.sh | 節の欠落が指摘に出るか確認する | 複合不合格 | 正常 | 不合格-複合を実行済み | 標準エラーを確認する | 「節-欠落」を含む |
| 複合-未記入残存 | 11 | check-detail-design.sh | 未記入のプレースホルダーが指摘に出るか確認する | 複合不合格 | 正常 | 不合格-複合を実行済み | 標準エラーを確認する | 「未記入-残存」を含む |
| 複合-位置禁止 | 12 | check-detail-design.sh | file:line表記が指摘に出るか確認する | 複合不合格 | 正常 | 不合格-複合を実行済み | 標準エラーを確認する | 「位置-禁止」を含む |
| 複合-読み取り結果未網羅 | 13 | check-detail-design.sh | 読み取り結果の未網羅が指摘に出るか確認する | 複合不合格 | 正常 | 不合格-複合を実行済み | 標準エラーを確認する | 「読み取り結果-未網羅」を含む |
| 不合格-文書不在 | 14 | check-detail-design.sh | 詳細設計書を置かずに実行する | 文書の実在 | 異常 | テーブル定義書.mdを削除する | --kind tableで実行する | 終了コード1 |
| 文書不在-理由 | 15 | check-detail-design.sh | 出力に不在の理由が出るか確認する | 文書の実在 | 正常 | 不合格-文書不在を実行済み | 標準エラーを確認する | 「文書-不在」を含む |
| 判定不能-一覧不在 | 16 | check-detail-design.sh | 単位の一覧を置かずに実行する | 判定不能-前提不在 | 異常 | 単位一覧のtsvを退避する | --kind tableで実行する | 終了コード2 |
| 判定不能-種別不正 | 17 | check-detail-design.sh | 一覧データに無い種別を指定して実行する | 判定不能-前提不在 | 異常 | 一覧データはtableのみで種別featureを渡す | --kind featureで実行する | 終了コード2 |
| 判定不能-使い方の誤り | 18 | check-detail-design.sh | 引数を渡さずに実行する | 判定不能-前提不在 | 異常 | 対象・--run等の引数を渡さない | 引数なしで実行する | 終了コード2 |
| 判定不能-検査基盤不在 | 19 | check-detail-design.sh | 依存スクリプトを欠いて実行する | 判定不能-前提不在 | 異常 | reverse-sharedのlist-units-of.shを削除する | --kind tableで実行する | 終了コード2 |

## §3 入力条件

**この節の位置づけ: 現行実装**

各ケースの前提・操作は§2の該当列を参照する。

## §4 期待結果

**この節の位置づけ: 現行実装**

各ケースの期待結果は§2の該当列を参照する。

## §5 異常系

**この節の位置づけ: 現行実装**

| 観点のキー | 関数・メソッド名 | 発生させる条件 | 期待する例外・エラー | 由来する詳細設計書の章 |
|---|---|---|---|---|
| 合格記録の入口 | check-detail-design.sh | 単位の合格の記録が無い | 終了コード1・「合格記録-不在」 | 詳細設計書 §5 エラー処理 |
| 複合不合格 | check-detail-design.sh | 節・未記入・file:line・読み取り結果未網羅を複合させる | 終了コード1・各指摘文言 | 詳細設計書 §5 エラー処理 |
| 文書の実在 | check-detail-design.sh | 詳細設計書を置かない | 終了コード1・「文書-不在」 | 詳細設計書 §5 エラー処理 |
| 判定不能-前提不在 | check-detail-design.sh | 単位一覧・検査基盤が無い、種別不正、引数誤り | 終了コード2 | 詳細設計書 §5 エラー処理 |

## §6 境界値

**この節の位置づけ: 現行実装**

| 観点のキー | 関数・メソッド名 | 対象の引数・状態 | 境界の値 | 境界の直前と直後の扱い |
|---|---|---|---|---|
| 保留の除外 | check-detail-design.sh | 単位の判定 | 「保留」 | 保留は不合格にせずskip、それ以外の判定は合否判定の対象にする |

## §7 網羅基準

**この節の位置づけ: 現行実装**

| 観点 | 基準 |
|---|---|
| 条件の網羅 | 機能設計書・詳細設計書に記録された条件を網羅する。 |
| 分岐の網羅 | 詳細設計書に記録された真偽の分岐を網羅する。 |
| エラーの網羅 | 詳細設計書に記録された終了コード・エラーを網羅する。 |

## §8 前提条件と終了条件

**この節の位置づけ: 現行実装**

| 区分 | 内容 |
|---|---|
| 実行の前提条件 | 対象スクリプトの`--self-test`を直接実行できること。 |
| 終了の条件 | 対象スクリプトの観点とケースがすべて対応していること。 |
