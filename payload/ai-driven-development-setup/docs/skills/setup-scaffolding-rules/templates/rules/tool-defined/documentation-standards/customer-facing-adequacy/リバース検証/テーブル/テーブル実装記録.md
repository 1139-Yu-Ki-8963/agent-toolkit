---
# テーブル定義書テンプレート（unit_kind=table・1テーブル1枚）
table_key: TABLEKEY
table_id: TABLEID
table_name: TABLENAME
# table_subkind の値域: table（物理テーブル）/ view（ビュー）/ migration（マイグレーション）
table_subkind: TABLESUBKIND
source_ref: SOURCEREF
unit_kind: table
status: draft
---
# <TABLENAME> テーブル実装記録

<!-- INTRODUCTION_GUIDANCE: 冒頭案内と本文の文体は reverse-writing-basic-design/SKILL.md の「記入規則（文体）」に従う。生成する本文の自由記述はである調で書く。読み手への語りかけ（です・ます）を混ぜない。 -->

**この文書の位置づけ**（別の言語で作り直す場合は、まず「仕様」から読む）

- 仕様: 該当なし
- 現行実装: §1 疑似コード、§2 データ定義、§3 実装契約
- 仕様／現行実装（詳細は各節の太字1行を見る）: 該当なし

本書は、このテーブルの今の作りをそのまま記録した文書です。作り直す際に引き継がない情報だけを置きます。業務の決まり・外部との契約・満たすべき性質は テーブル定義書 を参照してください。

## §1 疑似コード

**この節の位置づけ: 現行実装。本節は現在の実装の作りをそのまま記録した節です。作り直す前に、この節に書いた作りをどう扱うかを決める必要がある**

<!-- 分岐と繰り返しの入れ子だけを日本語で書く。書き方は実装契約定義「疑似コード」の 7 点（関数名はそのまま・もし〜ならば・分岐ごとに空行・条件と本体を改行して字下げ・繰り返しの終わりを明示・字下げは全角の空白 2 つ・変数名は書かず役割で述べる）に従う。関数名は §3 実装契約（8.1 ファイルと関数）
     が定めるものを使ってよい。実装言語の記法（メソッド呼び出しの矢印・ハッシュの区切り等）
     と実装の変数名は使わない。変数の役割を述べる必要がある場合は役割を表す日本語
     （実行コンテキストの利用者情報 の類）を用いる。
     実際のコードを貼ることを禁じる。原本の行を指したい場合は §3 実装契約の「ファイルと関数」から辿らせる。 -->

`PSEUDOCODE`

## §2 データ定義

**この節の位置づけ: 現行実装。本節は現在の実装の作りをそのまま記録した節です。作り直す前に、この節に書いた作りをどう扱うかを決める必要がある**

### 2.1 カラム定義

| 物理名 | 論理名 | 型 | 長さ | 必須 | 既定値 | 制約 |
|---|---|---|---|---|---|---|

### 2.2 索引

| 索引名 | 対象カラム | 一意性 |
|---|---|---|

### 2.3 外部キー

| カラム | 参照先のテーブル | 参照先のカラム | 削除時の動作 | 関連の種別 |
|---|---|---|---|---|
| `<実測: 外部キーカラム名>` | `<実測: 参照先テーブル名>` | `<実測: 参照先カラム名>` | `<実測: CASCADE/RESTRICT/SET NULL等>` | `<実測: 一対一 または 一対多 または 多対多>` |

### 2.4 整合性のチェック点

| 対象 | チェックの内容 | 実施の箇所 |
|---|---|---|

## §3 実装契約

**この節の位置づけ: 現行実装。本節は現在の実装の作りをそのまま記録した節です。作り直す前に、この節に書いた作りをどう扱うかを決める必要がある**

### 3.1 ファイルと関数

| ファイル | 関数 | 役割 | 参照先 |
|---|---|---|---|

### 3.2 型定義

| 型名 | フィールド | 型 | 参照先 |
|---|---|---|---|

### 3.3 依存

| 依存先 | 用途 | 参照先 |
|---|---|---|

