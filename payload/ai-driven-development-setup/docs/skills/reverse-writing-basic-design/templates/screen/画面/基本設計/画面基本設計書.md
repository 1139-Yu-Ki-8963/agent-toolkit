---
doc_id: screen-<画面ID>
type: screen-basic-design
screen_name: <画面名>
created: <YYYY-MM-DD>
status: draft
---

# <画面名> 画面基本設計書

**この文書の位置づけ**（別の言語で作り直す場合は、まず「仕様」から読む）

- 仕様: §1 画面の目的、§2 画面構成、§3 機能仕様（業務機能の一覧）、§4 業務ルール、§5 入出力の業務的意味、§6 画面遷移の業務文脈、§7 関連資料
- 現行実装: 該当なし
- 仕様／現行実装（詳細は各節の太字1行を見る）: 該当なし

<!-- INTRODUCTION_GUIDANCE: 冒頭案内と本文の文体は reverse-writing-basic-design/SKILL.md の「記入規則（文体）」に従う。生成する本文の自由記述はである調で書く。読み手への語りかけ（です・ます）を混ぜない。 -->

<!-- 禁止事項: 本書にはコード識別子（変数名・関数名・コンポーネント名等）・フレームワーク用語（React/Vue/Hooks 等）・
     型構文（TypeScript の interface/type 等）・ファイルパス・ライブラリ名を一切含めない。
     本書は業務担当者が読める業務レベルの記述に限定する。実装寄りの契約は画面詳細設計書（../詳細設計/画面詳細設計書.md）が担う。 -->

## §1 画面の目的

**この節の位置づけ: 仕様**

<!-- この画面が業務上なぜ必要か、何を達成するためのものかを 1〜2 文で記述する -->

`<この画面が業務上果たす役割>`

## §2 画面構成

**この節の位置づけ: 仕様**

### 画面キャプチャ

<!-- 画像実体が取得できた場合は、この placeholder を ![元コードの画面](../詳細設計/original.png) に置き換える。 -->
<div class="screen-capture-placeholder" role="img" aria-label="画面キャプチャは未取得である。後続の画面確認で追加する。" style="box-sizing:border-box;display:flex;align-items:center;justify-content:center;min-height:180px;padding:24px;color:#64748b;background:#f8fafc;border:2px dashed #94a3b8;border-radius:0;text-align:center;">画面キャプチャは未取得である。後続の画面確認で追加する。</div>

### 部品構成

<!-- 画面の視覚的な領域配置を、業務用語で示す。
     コード識別子（コンポーネント名・関数名等）は禁止。
     ユーザーから見た配置（ヘッダー・入力欄・ボタン等）を業務名で記述する。 -->

```
┌────────────────────────────────┐
│ <領域名（業務用語）>             │
├────────────────────────────────┤
│  ┌──────────────────────────┐  │
│  │ <部品名（業務用語）>      │  │
│  └──────────────────────────┘  │
└────────────────────────────────┘
```

## §3 機能仕様（業務機能の一覧）

**この節の位置づけ: 仕様**

<!-- コード上の実装単位ではなく、業務上の機能単位で列挙する -->

| キー | 機能名 | 業務上の目的 | 利用者 |
|---|---|---|---|
| `<機能名-要約>` | `<機能名>` | `<この機能で利用者が達成できること>` | `<想定利用者・役割>` |

## §4 業務ルール

**この節の位置づけ: 仕様**

<!-- 業務上の制約・条件分岐・権限による違いを記述する。実装の条件式ではなく業務の言葉で書く -->

| キー | ルール内容 | 適用条件 | 例外 |
|---|---|---|---|
| `<ルール-要約>` | `<ルール内容>` | `<適用条件>` | `<例外または「なし」>` |

## §5 入出力の業務的意味

**この節の位置づけ: 仕様**

<!-- 画面が受け取る情報・提示する情報を、業務上の意味で記述する。データ型ではなく「何を表す値か」を書く -->

### 5.1 入力（利用者が入力・選択する情報）

| キー | 項目名 | 業務上の意味 | 必須性 |
|---|---|---|---|
| `<項目-要約>` | `<項目名>` | `<この項目が業務上何を表すか>` | 必須 / 任意 |

### 5.2 出力（画面が提示する情報）

| キー | 項目名 | 業務上の意味 | 提示形式の業務要件 |
|---|---|---|---|
| `<項目-要約>` | `<項目名>` | `<この項目が業務上何を表すか>` | `<一覧/詳細/集計 等>` |

## §6 画面遷移の業務文脈

**この節の位置づけ: 仕様**

<!-- 業務フロー上、この画面がどの段階に位置し、前後の画面と何を引き継ぐかを記述する -->

| 遷移元画面 | 引き継ぐ業務情報 | 遷移先画面 | 引き渡す業務情報 | 遷移する業務上の契機 |
|---|---|---|---|---|
| `<画面名または「なし（起点画面）」>` | `<情報>` | `<画面名>` | `<情報>` | `<契機>` |

## §7 関連資料

**この節の位置づけ: 仕様**

他文書の所在はこの節へまとめる。確定できなかった事項は推測で埋めず、空欄のままとする。

| 資料 | パス | 本書との関係 |
|---|---|---|
| 対応する設計書・テスト設計書 | `<対応する文書のパス>` | 本書と対応する他文書 |
